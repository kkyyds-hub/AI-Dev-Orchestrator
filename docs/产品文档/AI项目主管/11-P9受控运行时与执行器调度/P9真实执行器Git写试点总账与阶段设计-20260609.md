# P9 真实执行器 Git 写试点总账与阶段设计

> **文档类型**: 高风险阶段 ledger / current-state readback / safety boundary
> **生成日期**: 2026-06-09
> **远端基准**: `origin/main` = `00c356700f2682ef3495e9eb43bd388fae4fec06`（feat: add real Git write pilot readiness readback）
> **前置阶段**: P1-P9 scoped closure 均 Pass；GitWrite scoped closure: Scoped Pass to fake-only
> **相关文档**: `../11-P9受控运行时与执行器调度/P9受控运行时与执行器调度总账与阶段设计-20260608.md`、`../../12-产品运行时Git写闭环/Git写闭环总账与阶段设计-20260608.md`
> **参考项目**: Agent Orchestrator（`/Users/kk/project explore/agent-orchestrator`），仅机制参考
> **状态**: P9-RGWP-A: Pass（真实执行器 Git 写试点 ledger + current-state readback + safety boundary）；P9-RGWP-B: Pass（pure domain contract + targeted tests）；P9-RGWP-C: Pass（preview service + preview-only API + targeted tests）；P9-RGWP-D: Pass（executor readiness + workspace binding readback + targeted tests）；P9-RGWP-E: Pass（dry-run command plan service + read-only API + targeted tests）；P9-RGWP-F: Not started；P9 real executor launch: Not started；Real Git write pilot actual execution: Not started；产品运行时 Git 写操作: Not started；AI Project Director 总闭环: Partial

---

## 1. 当前状态 readback

| 状态项 | 结论 | 证据 |
|--------|------|------|
| P1-P8 scoped closure | **Pass** | 各阶段 final gate |
| P9 scoped closure | **Pass** | `61665cf` docs: close P9 controlled runtime scoped gate |
| P9 real executor launch | **Not started** | Step 8 RealExecutorAdapter 未实现 |
| P9-H real executor pilot gate design | **Pass** | 21-item checklist + 11-step execution sequence；still default off |
| GitWrite scoped closure | **Scoped Pass to fake-only closure** | GitWrite-A/B/C/D/E/F/G/Final 全部 Pass；no real adapter |
| P9-RGWP-A ledger | **Pass** | 本文档 — 真实执行器 Git 写试点 ledger + current-state readback + safety boundary |
| P9-RGWP-B pure domain contract | **Pass** | `runtime/orchestrator/app/domain/real_git_write_pilot.py` + `runtime/orchestrator/tests/test_real_git_write_pilot_domain.py`；targeted tests 164 passed |
| P9-RGWP-C preview API | **Pass** | `runtime/orchestrator/app/services/real_git_write_pilot_preview_service.py` + `runtime/orchestrator/app/api/routes/real_git_write_pilot.py`；targeted tests 113 passed |
| P9-RGWP-D executor readiness + workspace binding readback | **Pass** | `runtime/orchestrator/app/services/real_git_write_pilot_readiness_service.py` + `POST /real-git-write-pilot/readiness`；targeted tests 148 passed |
| P9-RGWP-E dry-run command plan | **Pass** | `runtime/orchestrator/app/services/real_git_write_pilot_dry_run_plan_service.py` + `POST /real-git-write-pilot/dry-run-plan`；targeted tests 159 passed |
| P9-RGWP-F | **Not started** | 未进入真实 preflight / approval / launch 后续切片 |
| Real Git write pilot actual execution | **Not started** | 所有 feature flag 默认关闭 |
| 产品运行时 Git 写操作 | **Not started** | 全线禁止 |
| AI Project Director 总闭环 | **Partial** | — |

---

## 2. 试点范围定义

### 2.1 本试点是什么

本试点属于 **P9 real executor launch 的高风险后续轨道**，目标是让外部执行器（Codex / Claude Code / DeepSeek）在严格 gate 下执行一次**受控、可回滚、可审计的最小 Git 写**。

### 2.2 本试点不是什么

| # | 不是 | 说明 |
|---|------|------|
| 1 | **不是**开启自由写权限 | 仅限一次一条明确的试点操作 |
| 2 | **不是** main 直写 | main/master 一律禁止 |
| 3 | **不是**自动 PR / merge | PR 创建和 merge 决策必须人工触发 |
| 4 | **不是**默认打开真实写能力 | 所有 feature flag 默认关闭 |
| 5 | **不是** CI 自动触发 | 试点提交不应触发 CI pipeline |
| 6 | **不是**对已有安全门的降级 | P4/P8/P9/GitWrite 所有 gate 必须通过 |
| 7 | **不是**真实执行器接管 Git | 执行器只写试点分支上的试点文件 |
| 8 | **不是**无限预算 | 必须在 budget gate 内 |

### 2.3 试点最小范围

```text
Pilot scope = ONE executor
            + ONE branch（ai/gitwrite-pilot/<date>-doc-only）
            + ONE file type（docs/*.md only）
            + ONE commit
            + NO push（unless user explicitly enables for this pilot）
            + NO PR
            + NO merge
            + NO force
```

---

## 3. 参考项目机制映射与治理边界

**参考项目信息**：
- 名称：ComposioHQ Agent Orchestrator 镜像
- 本地路径：`/Users/kk/project explore/agent-orchestrator`
- GitHub 镜像：`https://github.com/kkyyds-hub/project-explore-one.git`
- 本轮读取：6 个文件（workspace-worktree L1-560、lifecycle-manager L1-260、lifecycle-state L1-260、activity-events L1-260、session-manager L1-260）

**P9-RGWP 可借鉴机制**：

| 参考机制 | 参考文件 | 可借鉴点 | P9-RGWP 映射 | 不复制/不引入/不实现 |
|---------|---------|---------|-------------|-------------------|
| git worktree create/destroy | `workspace-worktree/src/index.ts` | `assertSafePathSegment`、`resolveBaseRef`、`isRegisteredWorktree`、`clearStaleWorktreePath`、destroy 保留分支不删除 | RGWP workspace gate：试点分支必须在安全 worktree 中创建，destroy 不删分支 | 不引入 TypeScript 实现、不引入 tmux、不引入 execFile |
| lifecycle state machine | `lifecycle-manager.ts` | 状态轮询→转换检测→emit 事件→触发反应→升级人工通知 | RGWP execution sequence 的状态推进模式 | 不引入 polling loop、不引入 reaction engine |
| canonical session lifecycle | `lifecycle-state.ts` | session/pr/runtime 分层生命周期，每层独立 state+reason | RGWP pilot request 的分层 gate snapshot | 不引入 Zod schema、不引入 worker/orchestrator 分类 |
| activity event + secret redaction | `activity-events.ts` | 结构化 event kind/source + TOKEN_PATTERNS 脱敏 + credential URL 脱敏 | RGWP audit event 的安全摘要 + secret scan gate | 不引入 SQLite event DB、不引入 FTS5 index |
| session manager spawn→kill | `session-manager.ts` | workspace→runtime→agent 顺序创建、kill 顺序清理、CleanupStack | RGWP pilot 的 create→execute→verify→cleanup 顺序 | 不引入 OpenCode session、不引入 issue tracking |

**明确不引入**：TypeScript / Node.js / tmux runtime / plugin hot reload / Zod / YAML / CleanupStack / Notifier / Tracker / OpenCode session management。

**治理声明**：
- 本轮只吸收机制为 ledger / safety boundary / 子阶段拆分，不写任何代码。
- 不修改参考项目任何文件。
- 不把参考项目路径、包名、类型名写入主项目代码。

---

## 4. 强制 Gate 清单

以下 17 个 gate 构成 P9 真实执行器 Git 写试点的完整准入门槛。每个 gate 必须显式通过，任一失败 = **Blocked**。

| # | Gate | 目的 | 不满足时 | P9/GitWrite 前置状态 | Pilot 前需补齐 |
|---|------|------|---------|--------------------|-------------|
| 1 | **Feature Flag Gate** | 三个 flag 必须同时为 True | **Blocked** | P9-C RuntimeFeatureFlagPolicy + GitWrite-B 已有 domain 定义 | 需要真实 feature flag 配置源 |
| 2 | **Executor Readiness Gate** | executor status == AVAILABLE | **Blocked** | P8-C discovery service + P8-D readiness API | 需要真实 probe（非 noop） |
| 3 | **Workspace/Worktree Gate** | workspace 必须存在、合法、已绑定 | **Blocked** | P9-B ExecutorRuntimeWorkspaceBinding + P4 RepositoryWorkspace | 需要真实 worktree 创建验证 |
| 4 | **Target Branch Allowlist Gate** | 仅允许白名单分支 | **Blocked** | GitWrite-B GitWriteSafetyGate 已有 TARGET_BRANCH_ALLOWLIST | 需要真实 branch allowlist 配置 |
| 5 | **Diff Preview Gate** | 写前必须展示完整 diff | **Blocked** | GitWrite-C preview service + P4 Git Diff Dry-run | GitWrite-C 已有 |
| 6 | **Secret Scan Gate** | diff 中不得含疑似 secret | **Blocked** | GitWrite-C SecretScanner + domain _SUSPICIOUS_TEXT_PATTERN | GitWrite-C 已有 |
| 7 | **Human Approval Gate** | 用户当轮独立确认 | **Blocked** | GitWrite-B GitWriteApproval domain + P4 HumanApprovalGate | 需要真实 UI 确认入口 |
| 8 | **One-Shot Token Gate** | 一次性 token，用完即废 | **Blocked** | GitWrite-B OneShotApprovalToken domain | 需要真实 token 生成器 |
| 9 | **Budget/Cost Gate** | 在 daily/session budget 范围内 | **Blocked** | P9-C RuntimeBudgetGateInput + BudgetGuardService | 需要真实 cost estimation |
| 10 | **Timeout/Kill Switch Gate** | session 必须可超时、可终止 | **Blocked** | P9-D timeout/cancel/kill + P9-C timeout_configured | P9-D 已有（fake） |
| 11 | **Rollback Plan Gate** | 写前必须准备回滚方案 | **Blocked** | GitWrite-C RollbackPlanGenerator | GitWrite-C 已有 |
| 12 | **No Direct Main Write Gate** | 禁止写 main/master | **Blocked** | GitWrite-B target_branch validation（排除 HEAD） | GitWrite-B 已有 |
| 13 | **No Force Push Gate** | 禁止 --force push | **Blocked** | GitWrite-C ForcePushDetector | GitWrite-C 已有 |
| 14 | **No Auto PR / Merge Gate** | 禁止自动创建 PR 或 merge | **Blocked** | GitWrite operation_kinds 不含 merge_pr | GitWrite-B 已有 |
| 15 | **Append-Only Audit Gate** | 每一步记录不可篡改 audit event | **Blocked** | GitWrite-B GitWriteAuditEvent + P9-B RuntimeEvent | 需要持久化 event storage |
| 16 | **Post-Write Verify Gate** | 写后验证 commit 存在、diff 匹配 | **Blocked** | 不适用（P9-RGWP 新增） | 需设计 |
| 17 | **Manual Final Confirmation Gate** | 写后人工检查通过才完成试点 | **Blocked** | 不适用（P9-RGWP 新增） | 需设计 |

---

## 5. Feature Flag 策略

### 5.1 三层开关（默认全部关闭）

```text
# Layer 1: P9 real executor launch 总开关
P9_REAL_EXECUTOR_LAUNCH_ENABLED = false

# Layer 2: 产品运行时 Git 写总开关
PRODUCT_RUNTIME_GIT_WRITE_ENABLED = false

# Layer 3: Real Git Write Pilot 专用开关
REAL_GIT_WRITE_PILOT_ENABLED = false
REAL_GIT_WRITE_ALLOWED_BRANCHES = []
REAL_GIT_WRITE_ALLOW_PUSH = false
REAL_GIT_WRITE_ALLOW_PR = false
REAL_GIT_WRITE_ALLOW_MERGE = false
```

### 5.2 开启条件

| Flag | 变为 True 的条件 |
|------|----------------|
| `P9_REAL_EXECUTOR_LAUNCH_ENABLED` | 用户显式在 feature flag 配置中开启 |
| `PRODUCT_RUNTIME_GIT_WRITE_ENABLED` | 用户显式在 feature flag 配置中开启 |
| `REAL_GIT_WRITE_PILOT_ENABLED` | 用户显式在 feature flag 配置中开启 + pilot request approved |
| `REAL_GIT_WRITE_ALLOWED_BRANCHES` | 用户配置白名单，初始建议 `ai/gitwrite-pilot/<date>-doc-only` |
| `REAL_GIT_WRITE_ALLOW_PUSH` | 用户显式开启（首次试点建议 False） |
| `REAL_GIT_WRITE_ALLOW_PR` | 用户显式开启（首次试点建议 False） |
| `REAL_GIT_WRITE_ALLOW_MERGE` | 用户显式开启（首次试点建议 False） |

### 5.3 关键约束

- **任一 flag 为 False 时，对应操作拒绝执行。**
- **flag 变更必须记录 audit event。**
- **flag 不能通过 API 在不经用户确认的情况下修改。**
- **P9-RGWP-A 不实现任何 flag 读写代码。**

---

## 6. 目标分支策略

### 6.1 禁止分支

- `main` / `master` / `release/*` / `production` / `staging`
- 任何以 `gh-pages` 开头的分支
- 任何 CI/CD workflow 自动创建的分支

### 6.2 允许分支（初次试点）

第一轮试点只允许：

```text
ai/gitwrite-pilot/<yyyy-mm-dd>-doc-only
```

示例：`ai/gitwrite-pilot/2026-06-09-doc-only`

### 6.3 允许文件范围（初次试点）

| 允许 | 禁止 |
|------|------|
| `docs/**/*.md` | `*.py`、`*.ts`、`*.tsx`、`*.js`、`*.json`、`*.yaml`、`*.yml`、`*.toml`、`*.env` |
| 新建文件 | 修改现有文件 |
| 单文件试点 | 批量文件修改 |
| 纯文档内容 | 代码/配置/密钥/CI 脚本 |

### 6.4 禁止操作

- 删除文件
- 修改 CI / workflow / package scripts / secrets / config
- 修改 `.gitignore` / `.gitattributes`
- 修改 `pyproject.toml` / `package.json` / lock 文件
- 修改 `alembic/` / `migrations/` 下的任何文件

---

## 7. 回滚策略

### 7.1 写前必须记录

| 必须记录 | 说明 |
|---------|------|
| `base_commit` | 试点写之前的 HEAD commit SHA |
| `target_branch` | 试点写入的目标分支全名 |
| `rollback_plan_id` | 回滚方案唯一 ID（GitWrite-C 已有 contract） |
| `restore_branch_hint` | 回滚时恢复到哪个分支状态 |
| `restore_commit_hint` | 回滚时恢复到哪个 commit |

### 7.2 允许的回滚操作（第一轮试点）

| # | 允许 | 禁止 |
|---|------|------|
| 1 | `git revert <pilot-commit>`（创建 revert commit） | `git reset --hard` |
| 2 | 删除未合并的试点分支（`git branch -D <pilot-branch>`） | `git push --force` |
| 3 | 人工手动回滚 | 自动回滚脚本 |

### 7.3 回滚验证

- 回滚后必须确认 base_commit 状态恢复。
- 回滚操作也必须记录 audit event。
- 回滚后试点分支可以保留用于参考，但不应留在远程。

---

## 8. 用户显式确认策略

### 8.1 确认原则

| # | 原则 | 说明 |
|---|------|------|
| 1 | **当轮独立确认** | 每次试点需要用户当轮显式确认，不能复用历史确认 |
| 2 | **完整信息展示** | 确认前必须展示所有关键信息 |
| 3 | **风险明确告知** | 确认界面必须告知试点风险 |
| 4 | **One-shot token** | 确认后生成一次性 token，用完即废 |
| 5 | **限时有效** | token 有效期 5 分钟，过期需重新确认 |

### 8.2 确认界面必须展示的内容

| 展示项 | 说明 |
|--------|------|
| executor | 哪个执行器执行试点（如 Codex） |
| branch | 目标试点分支 |
| file scope | 将修改的文件列表 |
| operation | 将要执行的操作（create_commit） |
| diff preview | 将要写入的 diff 摘要 |
| rollback plan | 回滚方案 |
| safety statements | "不含 main 写入 / 不含 push / 不含 PR / 不含 merge" |
| one-shot token hint | 一次性 token 提示 |
| expiry time | token 过期时间 |

### 8.3 确认流程

```text
1. System → 展示 PilotRequest（含所有 §8.2 信息）
2. User → 审查所有信息
3. User → 点击 "我确认此次试点写入"（明确措辞，不含"approve all"语义）
4. System → 生成 OneShotApprovalToken
5. System → 执行器在 token 有效期内执行写入
6. System → token 标记为 consumed
7. System → 记录 audit event
```

---

## 9. 推荐子阶段拆分

### 9.1 子阶段总览

| 子阶段 | 内容 | 负责人建议 | 说明 |
|--------|------|-----------|------|
| **P9-RGWP-A** | ledger + current-state readback + safety boundary | DeepSeek | **本文档** |
| **P9-RGWP-B** | RealGitWritePilotRequest / Approval / GateSnapshot 纯 domain contract | Codex | 不执行任何 Git |
| **P9-RGWP-C** | pilot preview API only（dry-run command plan generate） | Codex | 只生成命令预览 |
| **P9-RGWP-D** | executor readiness + workspace binding readback | Codex | 只读就绪检查 |
| **P9-RGWP-E** | dry-run command plan only（no execute） | Codex | 只生成命令计划 |
| **P9-RGWP-F** | manual approval UI/readback | Codex | 只读展示 + 确认入口 |
| **P9-RGWP-G** | one-shot token contract + audit readback | Codex | token 生成/验证 |
| **P9-RGWP-H** | single doc-only branch write pilot design | Codex | 仅设计，不实现真实写入 |
| **P9-RGWP-I** | final preflight evidence | DeepSeek | full gate chain dry-run |
| **P9-RGWP-Pilot** | 真实试点执行 | — | **只有用户未来单独确认后才允许** |

### 9.2 各子阶段详细说明

#### P9-RGWP-A（本文档）
- **输入**: P9 scoped closure Pass、GitWrite scoped closure Pass
- **产出**: ledger、current-state readback、17-gate checklist、feature flag 策略、分支策略、回滚策略、确认策略、子阶段拆分
- **不产出**: 代码、API、frontend、真实执行
- **Gate**: 本文档完成即 P9-RGWP-A Pass

#### P9-RGWP-B：Pure domain contract
- **输入**: P9-RGWP-A ledger
- **产出**: `RealGitWritePilotRequest`、`RealGitWritePilotApproval`、`RealGitWritePilotGateSnapshot` 域模型
- **不产出**: service、API、execution

#### P9-RGWP-C：Pilot preview API
- **输入**: P9-RGWP-B domain
- **产出**: `POST /pilot-git-write/preview` — 生成 pilot preview（command plan only）
- **不产出**: 真实执行

#### P9-RGWP-D：Executor readiness + workspace binding readback
- **输入**: P8 discovery service + P4 workspace
- **产出**: executor readiness check + workspace binding 验证 API
- **不产出**: 真实进程启动

#### P9-RGWP-E：Dry-run command plan
- **输入**: P9-RGWP-C preview + GitWrite preview
- **产出**: 完整的 dry-run command plan
- **不产出**: 真实执行

#### P9-RGWP-F：Manual approval UI/readback
- **输入**: P9-RGWP-E plan
- **产出**: 前端审批面板 + 确认入口
- **不产出**: 真实写入触发

#### P9-RGWP-G：One-shot token contract + audit readback
- **输入**: GitWrite-B OneShotApprovalToken
- **产出**: token 生成/验证/消费逻辑
- **不产出**: 真实 Git 操作

#### P9-RGWP-H：Single doc-only branch write pilot design
- **输入**: P9-RGWP-A 至 P9-RGWP-G 全部完成
- **产出**: 试点写入完整设计文档（不实现代码）
- **不产出**: 真实代码

#### P9-RGWP-I：Final preflight evidence
- **输入**: P9-RGWP-A 至 P9-RGWP-H
- **产出**: full gate chain dry-run evidence aggregation
- **不产出**: 真实写入

#### P9-RGWP-Pilot：真实试点执行
- **仅当用户未来单独确认后才进入。**
- **不在 P9-RGWP 子阶段拆分中承诺。**
- **需要独立的 pilot gate checklist 验证。**

---

## 10. 明确禁止项

| # | 禁止项 | 原因 |
|---|--------|------|
| 1 | 本轮不得真实启动外部执行器 | P9 real executor launch Not started |
| 2 | 本轮不得真实执行 Git 写 | 产品运行时 Git 写 Not started |
| 3 | 本轮不得新增 execute / commit / push / PR / merge endpoint | 安全边界 |
| 4 | 本轮不得写 main | 目标分支策略 |
| 5 | 本轮不得 PR / merge | 人工决策 |
| 6 | 本轮不得 force push | 不可逆操作 |
| 7 | 本轮不得读取 secret / token / API key / 环境变量 | 安全边界 |
| 8 | 本轮不得默认打开任何 feature flag | 所有 flag 默认关闭 |
| 9 | 本轮不得把试点设计写成真实试点完成 | P9-RGWP-Pilot 独立于 P9-RGWP-A 至 I |
| 10 | 本轮不得修改 runtime/orchestrator / apps/web / tests / migrations | 文档阶段 |
| 11 | 本轮不得篡改 P9 scoped closure / GitWrite scoped closure | 已 Pass 的 gate 不得降级 |

---

## 11. P9-RGWP-A Gate

| # | Gate 条件 | 结论 |
|---|----------|------|
| 1 | origin/main HEAD 确认 | Pass — `b0ab0e8d3f7b3088c887da11f27dd95428f9f737` |
| 2 | P9 scoped closure Pass | Pass |
| 3 | GitWrite scoped closure Scoped Pass | Pass |
| 4 | current-state readback 完整 | Pass |
| 5 | 17-gate checklist 完整 | Pass |
| 6 | feature flag 策略完整（7 flags，全部默认关闭） | Pass |
| 7 | 目标分支策略完整（禁止/允许清单） | Pass |
| 8 | 回滚策略完整（允许/禁止 + 验证） | Pass |
| 9 | 用户确认策略完整（5 原则 + 7 展示项 + 7 步流程） | Pass |
| 10 | 子阶段拆分到 P9-RGWP-Pilot | Pass |
| 11 | 10 项明确禁止项 | Pass |
| 12 | 参考项目只读读取记录完整 | Pass |
| 13 | 未修改 runtime/orchestrator | Pass |
| 14 | 未修改 apps/web | Pass |
| 15 | 未修改 tests | Pass |
| 16 | 未修改 migrations | Pass |
| 17 | 未读取 secret/token/API key/env | Pass |
| 18 | 未启动外部执行器 | Pass |
| 19 | 未执行任何 Git 写 | Pass |
| 20 | P9 real executor launch 仍 Not started | Pass |
| 21 | Real Git write pilot actual execution 仍 Not started | Pass |
| 22 | 产品运行时 Git 写操作仍 Not started | Pass |
| 23 | AI Project Director 总闭环仍 Partial | Pass |
| **P9-RGWP-A Gate** | **Pass** | — |

---

## 12. P9-RGWP-A 结论

| 结论项 | 状态 |
|--------|------|
| **P9-RGWP-A ledger** | **Pass** — 本文档完成 |
| **P9-RGWP-A Gate** | **Pass** — 23 项条件全部通过 |
| P9 scoped closure | **Pass**（不变） |
| P9 real executor launch | **Not started**（不变） |
| Real Git write pilot actual execution | **Not started**（不变） |
| GitWrite scoped closure | **Scoped Pass to fake-only closure**（不变） |
| 产品运行时 Git 写操作 | **Not started**（不变） |
| AI Project Director 总闭环 | **Partial**（不变） |

---

## 13. 本轮收口声明

### 13.1 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `11-P9受控运行时与执行器调度/P9真实执行器Git写试点总账与阶段设计-20260609.md` | **新增** | P9-RGWP-A ledger | 否 |
| `00-总览与索引/AI项目主管文档索引.md` | **修改** | 追加 P9-RGWP-A 状态行和入口 | 否 |
| `11-P9受控运行时与执行器调度/P9受控运行时与执行器调度总账与阶段设计-20260608.md` | **修改** | 追加 cross-reference | 否 |
| `12-产品运行时Git写闭环/Git写闭环总账与阶段设计-20260608.md` | **修改** | 追加 cross-reference | 否 |

### 13.2 边界声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否实现真实 Git adapter | 否 |
| 是否执行 git add/commit/push/PR/merge | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否使用 subprocess / shell / os.popen | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否把 P9 real executor launch 判 Pass | 否 |
| 是否把 Real Git write pilot actual execution 判 Pass | 否 |
| 是否把产品运行时 Git 写判 Pass | 否 |
| 是否把 AI Project Director 总闭环判 Pass | 否 |
| 是否只做文档 ledger | 是 |

### 13.3 参考项目只读读取记录

| 参考来源 | 读取文件 | 只借鉴机制 | 结论 |
|----------|---------|-----------|------|
| AO workspace-worktree | `packages/plugins/workspace-worktree/src/index.ts`（L1-560） | `assertSafePathSegment`、`resolveBaseRef`、`isRegisteredWorktree`、`clearStaleWorktreePath`、destroy 保留分支 | **未照搬代码** |
| AO lifecycle-manager | `packages/core/src/lifecycle-manager.ts`（L1-260） | 状态机轮询 + event 发射 + 人工通知 | **未照搬代码** |
| AO lifecycle-state | `packages/core/src/lifecycle-state.ts`（L1-260） | session/pr/runtime 分层生命周期 | **未照搬代码** |
| AO activity-events | `packages/core/src/activity-events.ts`（L1-260） | 结构化 event kind + TOKEN_PATTERNS 脱敏 | **未照搬代码** |
| AO session-manager | `packages/core/src/session-manager.ts`（L1-260） | spawn→kill→cleanup + CleanupStack | **未照搬代码** |

---

## 14. P9-RGWP-B Pure Domain Contract

### 14.1 新增文件

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/domain/real_git_write_pilot.py` | 新增 | Real Git Write Pilot 纯 domain contract | 否 |
| `runtime/orchestrator/tests/test_real_git_write_pilot_domain.py` | 新增 | P9-RGWP-B targeted domain tests + 静态边界检查 | 否 |

本轮未修改 `runtime/orchestrator/app/services/**`、`runtime/orchestrator/app/api/**`、`apps/web/**`、`migrations/**`、GitHub Actions / CI 配置、package / dependency 配置。

### 14.2 Domain contract 列表

| Contract | 说明 |
|----------|------|
| `RealGitWritePilotStatus` | pending / preview_ready / approval_required / approved / token_issued / preflight_ready / blocked / expired / cancelled；不含 executed / completed |
| `RealGitWritePilotOperationKind` | create_doc_file / create_commit_candidate / local_branch_candidate；不含 push / pr / merge 默认操作 |
| `RealGitWritePilotGateName` | 覆盖 P9-RGWP-A 17 个 gate |
| `RealGitWritePilotGateStatus` | pending / passed / blocked / not_applicable |
| `RealGitWritePilotBlockReason` | 覆盖 feature flag、executor、workspace、branch、file scope、secret、approval、token、budget、timeout、rollback、force、auto PR/merge、audit、post-write verify、manual confirmation、未启动真实执行等阻塞原因 |
| `RealGitWritePilotApprovalDecision` | pending / approved / rejected / expired / cancelled |
| `RealGitWritePilotGateCheck` | 单个 gate 的状态一致性与安全摘要 contract |
| `RealGitWritePilotGateSnapshot` | required gates 覆盖、`all_passed` / `blocking_reasons` 派生、helper：`get_gate` / `failed_gates` / `pilot_preflight_gates_passed` |
| `RealGitWritePilotRequest` | 试点 request contract；保留 product runtime write / real executor started 双 False 边界 |
| `RealGitWritePilotApproval` | 审批 contract；只保存 token id / hint，不保存真实 token value |
| `RealGitWritePilotRollbackPlan` | 回滚方案 contract；只描述允许/禁止动作，不执行 |
| `RealGitWritePilotAuditEvent` | append-only audit event contract；只保存 safe summary 与 metadata_count |

### 14.3 Validation 规则

| 规则 | 结论 |
|------|------|
| Required gates | `RealGitWritePilotGateSnapshot` 必须覆盖全部 17 个 gate，缺任意 gate validation 失败 |
| GateCheck 一致性 | passed gate 必须 `passed=True` 且无 block reason；blocked gate 必须 `passed=False` 且有 block reason；pending / not_applicable 必须 `passed=False` |
| 派生状态 | `all_required_gates_present`、`all_passed`、`blocking_reasons` 均由 snapshot 派生，调用方不可伪造 |
| Preflight helper | `pilot_preflight_gates_passed()` 只覆盖写前 gate；post_write_verify / manual_final_confirmation 不被写成执行完成证据 |
| Human/token/final confirmation | human approval / one-shot token / manual final confirmation 任一未通过时，`all_passed=False` |
| Target branch | 禁止 main / master / release / production / staging / gh-pages；第一轮仅允许 `ai/gitwrite-pilot/<yyyy-mm-dd>-doc-only` |
| File scope | 第一轮只允许 `docs/**/*.md` 风格的相对 markdown 文档路径 |
| Product runtime write | `product_runtime_git_write_executed=True` validation 失败 |
| Real executor start | `real_executor_started=True` validation 失败 |
| Approval | approved decision 必须有 approver、approved_at、one_shot_token_id、token_hint、expires_at；expires_at 必须晚于 approved_at |
| Rollback | allowed actions 禁止 reset --hard / force push / 自动回滚脚本；forbidden actions 必须显式覆盖三类危险动作 |
| Audit | `append_only` 必须为 True；safe_summary 拒绝疑似 secret / token / key 文本 |
| 静态边界 | domain 文件不导入 subprocess / os.popen / asyncio.subprocess / app.api / app.services / app.workers，不读取 `os.environ`，不包含产品运行时 Git 写命令字面量，不包含参考项目路径、包名、类型名 |

### 14.4 Targeted tests 结果

```bash
cd runtime/orchestrator && .venv/bin/pytest tests/test_executor_runtime_domain.py tests/test_executor_runtime_safety_domain.py tests/test_git_write_domain.py tests/test_real_git_write_pilot_domain.py -q
```

结果：

```text
164 passed in 0.14s
```

### 14.5 P9-RGWP-B Gate

| Gate 条件 | 结论 |
|----------|------|
| origin/main 基准确认 | Pass — `29d5ac233032c704f862968993b661abe79feef1` |
| 参考项目 6 个指定文件只读读取 | Pass |
| 未修改参考项目 | Pass |
| 未复制参考项目代码 | Pass |
| 未引入参考项目技术栈 | Pass |
| 未把参考项目路径、包名、类型名写入主项目代码 | Pass |
| 新增 pure domain contract | Pass |
| 新增 targeted tests | Pass |
| Targeted tests | Pass — 164 passed |
| 未实现 service / API / frontend | Pass |
| 未启动外部执行器 | Pass |
| 未实现真实 Git adapter | Pass |
| 未新增 execute / commit / push / PR / merge endpoint | Pass |
| 未新增前端写入入口 | Pass |
| 未执行或触发产品运行时 Git 写 | Pass |
| P9-RGWP-C | Not started |
| P9 real executor launch | Not started |
| Real Git write pilot actual execution | Not started |
| 产品运行时 Git 写操作 | Not started |
| AI Project Director 总闭环 | Partial |
| **P9-RGWP-B Gate** | **Pass** |

### 14.6 P9-RGWP-B 收口结论

| 结论项 | 状态 |
|--------|------|
| P9-RGWP-A | **Pass**（不变） |
| **P9-RGWP-B** | **Pass** — pure domain contract + targeted tests |
| P9-RGWP-C | **Not started** |
| P9 real executor launch | **Not started** |
| Real Git write pilot actual execution | **Not started** |
| GitWrite scoped closure | **Scoped Pass to fake-only closure**（不变） |
| 产品运行时 Git 写操作 | **Not started** |
| AI Project Director 总闭环 | **Partial** |

---

## 17. P9-RGWP-E Dry-run Command Plan Service + Read-only API

### 17.1 新增/修改文件

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/services/real_git_write_pilot_dry_run_plan_service.py` | 新增 | dry-run semantic plan service；只基于 preview/readiness readback 与显式 caller input | 否 |
| `runtime/orchestrator/app/api/routes/real_git_write_pilot.py` | 修改 | 新增 `POST /real-git-write-pilot/dry-run-plan` read-only API | 否 |
| `runtime/orchestrator/tests/test_real_git_write_pilot_dry_run_plan_service.py` | 新增 | service targeted tests + 静态边界检查 | 否 |
| `runtime/orchestrator/tests/test_real_git_write_pilot_api.py` | 修改 | dry-run-plan API tests + 写入 endpoint 不存在检查 | 否 |
| `docs/产品文档/AI项目主管/11-P9受控运行时与执行器调度/P9真实执行器Git写试点总账与阶段设计-20260609.md` | 修改 | 追加 P9-RGWP-E 状态、范围、证据、Gate | 否 |
| `docs/产品文档/AI项目主管/00-总览与索引/AI项目主管文档索引.md` | 修改 | 同步 P9-RGWP-E 状态一句 | 否 |

本轮未修改 `apps/web/**`、migrations、CI / package / dependency 配置，未修改 `runtime/orchestrator/app/services/git_write_adapter.py`。

### 17.2 Dry-run command plan service 范围

| Contract | 说明 |
|----------|------|
| `RealGitWritePilotDryRunPlanRequest` | pilot_id + preview readback + readiness readback + requested_by + requested_at |
| `RealGitWritePilotDryRunStep` | step_id / step_order / step_kind / safe_summary / requires_human_confirmation / produces_repository_side_effect=False |
| `RealGitWritePilotGateSnapshotSummary` | preview gate snapshot 的安全汇总；只包含计数、all_passed、blocking_reasons |
| `RealGitWritePilotDryRunPlan` | dry-run readback；包含 semantic_steps、forbidden_operations、rollback_plan_summary、audit_event_summaries、dry_run_ready、三项 no-execution 标记 |
| `RealGitWritePilotDryRunPlanService.build_plan()` | 仅从 preview + readiness readback 推导语义计划；不读真实仓库，不扫描真实工作区，不调用 Git |

Semantic steps 仅为流程语义：

```text
verify executor readiness readback
verify workspace binding readback
verify pilot branch allowlist
verify docs-only file scope
verify preview gate snapshot
prepare local branch candidate description
prepare doc-only file candidate description
prepare local commit candidate description
prepare rollback plan description
wait for manual approval
wait for one-shot approval grant
```

Forbidden operations 固定包含：

```text
raw shell execution
direct main write
git force push
automatic PR creation
automatic merge
branch delete
reset hard
tag creation
stash operation
```

`readiness.ready_for_preview=True` 且 preview 未 blocked 时，`dry_run_ready=True`。human approval / one-shot grant / manual final confirmation 未完成时，`ready_for_execution` 仍恒为 False。

### 17.3 Dry-run-plan API 范围

| Endpoint | 结论 |
|----------|------|
| `POST /real-git-write-pilot/dry-run-plan` | 已新增；只返回 dry-run semantic plan readback |
| `/execute` / `/commit` / `/push` / `/pr` / `/merge` / `/checkout` / `/reset` / `/rebase` / `/stash` / `/tag` | 未新增；API tests 逐项确认 404 |

API response 包含 `pilot_id`、`readiness_ready_for_preview`、`preview_status`、`gate_snapshot_summary`、`semantic_steps`、`forbidden_operations`、`rollback_plan_summary`、`audit_event_summaries`、`dry_run_ready`、`ready_for_execution=false`、`product_runtime_git_write_executed=false`、`real_executor_started=false`。

API request / response 不包含 raw command、raw args、cwd、env、token value、secret、raw diff、subprocess output、error output。

### 17.4 No-execution / no-write boundary

| 边界 | 结论 |
|------|------|
| 不启动 Codex / Claude Code / DeepSeek 外部执行器 | Pass |
| 不实现真实 Git adapter | Pass |
| 不执行 Git 写 / branch / PR / merge / tag / stash 等产品运行时操作 | Pass |
| 不读取真实密钥、环境变量值、API key、token、secret | Pass |
| 不扫描真实工作区 | Pass |
| 不创建 worktree、branch、文件、commit | Pass |
| dry-run plan 与 execution | 分层；plan 只从 readiness / preview readback 推导 |
| rollback / cleanup | 只表达计划，不执行真实 cleanup |
| audit | 只返回 safe summary |
| `ready_for_execution` | 恒为 False |
| `product_runtime_git_write_executed` | 恒为 False |
| `real_executor_started` | 恒为 False |

### 17.5 Targeted tests 结果

```bash
cd runtime/orchestrator && .venv/bin/pytest tests/test_real_git_write_pilot_domain.py tests/test_real_git_write_pilot_preview_service.py tests/test_real_git_write_pilot_readiness_service.py tests/test_real_git_write_pilot_dry_run_plan_service.py tests/test_real_git_write_pilot_api.py tests/test_runtime_api.py tests/test_git_write_api.py -q
```

结果：

```text
159 passed
```

### 17.6 P9-RGWP-E Gate

| Gate 条件 | 结论 |
|----------|------|
| origin/main 基准确认 | Pass — `00c356700f2682ef3495e9eb43bd388fae4fec06` |
| 参考项目 6 个指定文件只读读取 | Pass |
| 未修改参考项目 | Pass |
| 未复制参考项目代码 | Pass |
| 未引入参考项目技术栈 | Pass |
| 未把参考项目路径、包名、类型名写入主项目代码 | Pass |
| Dry-run command plan service | Pass |
| Read-only API | Pass — `POST /real-git-write-pilot/dry-run-plan` |
| Targeted tests | Pass — 159 passed |
| 未新增 execute / commit / push / PR / merge endpoint | Pass |
| 未新增前端写入入口 | Pass |
| 未启动外部执行器 | Pass |
| 未实现真实 Git adapter | Pass |
| 未执行或触发产品运行时 Git 写 | Pass |
| P9-RGWP-F | Not started |
| P9 real executor launch | Not started |
| Real Git write pilot actual execution | Not started |
| 产品运行时 Git 写操作 | Not started |
| AI Project Director 总闭环 | Partial |
| **P9-RGWP-E Gate** | **Pass** |

### 17.7 P9-RGWP-E 收口结论

| 结论项 | 状态 |
|--------|------|
| P9-RGWP-A | **Pass**（不变） |
| P9-RGWP-B | **Pass**（不变） |
| P9-RGWP-C | **Pass**（不变） |
| P9-RGWP-D | **Pass**（不变） |
| **P9-RGWP-E** | **Pass** — dry-run command plan service + read-only API + targeted tests |
| P9-RGWP-F | **Not started** |
| P9 real executor launch | **Not started** |
| Real Git write pilot actual execution | **Not started** |
| GitWrite scoped closure | **Scoped Pass to fake-only closure**（不变） |
| 产品运行时 Git 写操作 | **Not started** |
| AI Project Director 总闭环 | **Partial** |

---

## 16. P9-RGWP-D Executor Readiness + Workspace Binding Readback

### 16.1 新增/修改文件

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/services/real_git_write_pilot_readiness_service.py` | 新增 | executor readiness + workspace binding readback service；只基于显式 caller input | 否 |
| `runtime/orchestrator/app/api/routes/real_git_write_pilot.py` | 修改 | 新增 `POST /real-git-write-pilot/readiness` read-only API | 否 |
| `runtime/orchestrator/tests/test_real_git_write_pilot_readiness_service.py` | 新增 | service targeted tests + 静态边界检查 | 否 |
| `runtime/orchestrator/tests/test_real_git_write_pilot_api.py` | 修改 | API readback tests + 写入 endpoint 不存在检查 | 否 |
| `docs/产品文档/AI项目主管/11-P9受控运行时与执行器调度/P9真实执行器Git写试点总账与阶段设计-20260609.md` | 修改 | 追加 P9-RGWP-D 状态、范围、证据、Gate | 否 |
| `docs/产品文档/AI项目主管/00-总览与索引/AI项目主管文档索引.md` | 修改 | 同步 P9-RGWP-D 状态一句 | 否 |

本轮未修改 `apps/web/**`、migrations、CI / package / dependency 配置，未修改 `runtime/orchestrator/app/services/git_write_adapter.py`。

### 16.2 Readiness service 范围

| Contract | 说明 |
|----------|------|
| `RealGitWritePilotExecutorReadinessInput` | caller 显式提供 executor_id / executor_kind / configured / authenticated / available / model_or_profile / safe_summary / checked_at |
| `RealGitWritePilotWorkspaceBindingInput` | caller 显式提供 workspace_id / repository_id / base_commit / target_branch / file_paths / workspace_bound / worktree_registered / stale_workspace_detected / safe_path_confirmed / safe_summary / checked_at |
| `RealGitWritePilotReadinessRequest` | pilot_id + executor + workspace + requested_by + requested_at |
| `RealGitWritePilotReadinessGateCheck` | D 专属 gate readback；支持 executor_readiness / workspace_binding / target_branch_allowlist / file_scope |
| `RealGitWritePilotReadinessReadback` | readiness readback；包含 executor_readiness、workspace_binding、gate_checks、ready_for_preview、三项 no-execution 标记、safe_summary、audit_event_summaries |
| `RealGitWritePilotReadinessService.build_readiness()` | 仅从 request input 推导 readback；不读真实仓库，不扫描真实工作区，不调用 Git |

Gate 语义：

| Gate | Passed 条件 | Blocked 条件 |
|------|-------------|--------------|
| `executor_readiness` | configured / authenticated / available 全为 True | 任一为 False，`executor_not_ready` |
| `workspace_binding` | workspace_bound / worktree_registered / safe_path_confirmed 全为 True，且 stale_workspace_detected 为 False | 任一不满足，`workspace_not_bound` |
| `target_branch_allowlist` | `ai/gitwrite-pilot/<yyyy-mm-dd>-doc-only` | protected branch 或非法 pilot branch |
| `file_scope` | 全部 `docs/**/*.md` | 任一非 docs markdown scope |

`ready_for_preview` 可在上述四个 gate 均通过时为 True。`ready_for_execution`、`product_runtime_git_write_executed`、`real_executor_started` 恒为 False。

### 16.3 Read-only API 范围

| Endpoint | 结论 |
|----------|------|
| `POST /real-git-write-pilot/readiness` | 已新增；只返回 readiness readback |
| `/execute` / `/commit` / `/push` / `/pr` / `/merge` / `/checkout` / `/reset` / `/rebase` / `/stash` / `/tag` | 未新增；API tests 逐项确认 404 |

API request / response 不包含 raw command、raw args、cwd、env、token value、secret、subprocess output、error output。

### 16.4 Read-only / no-execution boundary

| 边界 | 结论 |
|------|------|
| 不启动 Codex / Claude Code / DeepSeek 外部执行器 | Pass |
| 不实现真实 Git adapter | Pass |
| 不执行 Git 写 / branch / PR / merge / tag / stash 等产品运行时操作 | Pass |
| 不读取真实密钥、环境变量值、API key、token、secret | Pass |
| 不扫描真实工作区 | Pass |
| 不创建 worktree、branch、文件、commit | Pass |
| stale workspace / unsafe path | 只形成 blocked readback，不做真实 cleanup |
| lifecycle state | 不跳到 running / executed / completed |
| audit | 只返回 safe summary |

### 16.5 Targeted tests 结果

```bash
cd runtime/orchestrator && .venv/bin/pytest tests/test_real_git_write_pilot_domain.py tests/test_real_git_write_pilot_preview_service.py tests/test_real_git_write_pilot_readiness_service.py tests/test_real_git_write_pilot_api.py tests/test_runtime_api.py tests/test_git_write_api.py -q
```

结果：

```text
148 passed
```

### 16.6 P9-RGWP-D Gate

| Gate 条件 | 结论 |
|----------|------|
| origin/main 基准确认 | Pass — `febc0f57200d573c69b9912cc4dc1a41808635b4` |
| 参考项目 6 个指定文件只读读取 | Pass |
| 未修改参考项目 | Pass |
| 未复制参考项目代码 | Pass |
| 未引入参考项目技术栈 | Pass |
| 未把参考项目路径、包名、类型名写入主项目代码 | Pass |
| Readiness service | Pass |
| Read-only API | Pass — `POST /real-git-write-pilot/readiness` |
| Targeted tests | Pass — 148 passed |
| 未新增 execute / commit / push / PR / merge endpoint | Pass |
| 未新增前端写入入口 | Pass |
| 未启动外部执行器 | Pass |
| 未实现真实 Git adapter | Pass |
| 未执行或触发产品运行时 Git 写 | Pass |
| P9-RGWP-E | Not started |
| P9 real executor launch | Not started |
| Real Git write pilot actual execution | Not started |
| 产品运行时 Git 写操作 | Not started |
| AI Project Director 总闭环 | Partial |
| **P9-RGWP-D Gate** | **Pass** |

### 16.7 P9-RGWP-D 收口结论

| 结论项 | 状态 |
|--------|------|
| P9-RGWP-A | **Pass**（不变） |
| P9-RGWP-B | **Pass**（不变） |
| P9-RGWP-C | **Pass**（不变） |
| **P9-RGWP-D** | **Pass** — executor readiness + workspace binding readback + targeted tests |
| P9-RGWP-E | **Not started** |
| P9 real executor launch | **Not started** |
| Real Git write pilot actual execution | **Not started** |
| GitWrite scoped closure | **Scoped Pass to fake-only closure**（不变） |
| 产品运行时 Git 写操作 | **Not started** |
| AI Project Director 总闭环 | **Partial** |

---

## 15. P9-RGWP-C Preview Service + Preview-Only API

### 15.1 新增/修改文件

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/services/real_git_write_pilot_preview_service.py` | 新增 | preview-only service；只基于显式 caller input 生成 gate readback、command plan、rollback plan | 否 |
| `runtime/orchestrator/app/api/routes/real_git_write_pilot.py` | 新增 | `POST /real-git-write-pilot/preview` preview-only API | 否 |
| `runtime/orchestrator/app/api/router.py` | 修改 | 注册 preview-only route | 否 |
| `runtime/orchestrator/tests/test_real_git_write_pilot_preview_service.py` | 新增 | service targeted tests + 静态边界检查 | 否 |
| `runtime/orchestrator/tests/test_real_git_write_pilot_api.py` | 新增 | API targeted tests + 不存在写入 endpoint 检查 | 否 |
| `docs/产品文档/AI项目主管/11-P9受控运行时与执行器调度/P9真实执行器Git写试点总账与阶段设计-20260609.md` | 修改 | 追加 P9-RGWP-C 状态、范围、证据、Gate | 否 |
| `docs/产品文档/AI项目主管/00-总览与索引/AI项目主管文档索引.md` | 修改 | 同步 P9-RGWP-C 状态一句 | 否 |

本轮未修改 `apps/web/**`、migrations、CI / package / dependency 配置，未修改 `runtime/orchestrator/app/services/git_write_adapter.py`。

### 15.2 Preview service 范围

| Contract | 说明 |
|----------|------|
| `RealGitWritePilotPreviewFeatureFlags` | 三层 flag 输入；默认全部 False |
| `RealGitWritePilotPreviewGateInputs` | executor/workspace/branch/diff/budget/timeout/rollback/audit/approval/token 等显式 gate 输入 |
| `RealGitWritePilotPreviewRequest` | caller 提供 pilot/project/run/executor/workspace/repository/base_commit/target_branch/file_paths/requested_at/expires_at/diff_summary/rollback_summary |
| `RealGitWritePilotCommandPlan` | 语义 command plan；只含 safe_steps / forbidden_operations，不保存 raw command / raw args / cwd / env |
| `RealGitWritePilotPreview` | preview readback；包含 status、gate_snapshot、command_plan、rollback_plan、safe_summary、audit_event_summaries、双 False no-write 标记 |
| `RealGitWritePilotPreviewService.build_preview()` | 构造 `RealGitWritePilotRequest`、gate snapshot、command plan、rollback plan；不读真实仓库，不扫描真实工作区，不调用 Git |

Command plan 语义步骤仅包含：

```text
validate pilot branch
prepare doc-only file candidate
prepare local commit candidate
prepare rollback plan
```

Forbidden operations 固定包含：

```text
main write
force push
auto PR
auto merge
raw shell execution
```

### 15.3 Preview-only API 范围

| Endpoint | 结论 |
|----------|------|
| `POST /real-git-write-pilot/preview` | 已新增；只返回 preview-only readback |
| `/execute` / `/commit` / `/push` / `/pr` / `/merge` / `/checkout` / `/reset` / `/rebase` / `/stash` / `/tag` | 未新增；API tests 逐项确认 404 |

API request / response 不包含 raw command、raw args、cwd、env、token value、secret、raw diff、subprocess output、error output。

### 15.4 No-write boundary

| 边界 | 结论 |
|------|------|
| 不启动 Codex / Claude Code / DeepSeek 外部执行器 | Pass |
| 不实现真实 Git adapter | Pass |
| 不执行 Git 写 / branch / PR / merge / tag / stash 等产品运行时操作 | Pass |
| 不读取真实密钥、环境变量值、API key、token、secret | Pass |
| 不扫描真实工作区执行写入 | Pass |
| `product_runtime_git_write_executed` | 恒为 False |
| `real_executor_started` | 恒为 False |
| human approval / one-shot token / manual final confirmation 未过 | `all_passed=False` |

### 15.5 Targeted tests 结果

```bash
cd runtime/orchestrator && .venv/bin/pytest tests/test_real_git_write_pilot_domain.py tests/test_real_git_write_pilot_preview_service.py tests/test_real_git_write_pilot_api.py tests/test_runtime_api.py tests/test_git_write_api.py -q
```

结果：

```text
113 passed
```

### 15.6 P9-RGWP-C Gate

| Gate 条件 | 结论 |
|----------|------|
| origin/main 基准确认 | Pass — `60193910875933d8582737a7d8991cd3bf4c38e1` |
| 参考项目 6 个指定文件只读读取 | Pass |
| 未修改参考项目 | Pass |
| 未复制参考项目代码 | Pass |
| 未引入参考项目技术栈 | Pass |
| 未把参考项目路径、包名、类型名写入主项目代码 | Pass |
| Preview service | Pass |
| Preview-only API | Pass — `POST /real-git-write-pilot/preview` |
| Targeted tests | Pass — 113 passed |
| 未新增 execute / commit / push / PR / merge endpoint | Pass |
| 未新增前端写入入口 | Pass |
| 未启动外部执行器 | Pass |
| 未实现真实 Git adapter | Pass |
| 未执行或触发产品运行时 Git 写 | Pass |
| P9-RGWP-D | Not started |
| P9 real executor launch | Not started |
| Real Git write pilot actual execution | Not started |
| 产品运行时 Git 写操作 | Not started |
| AI Project Director 总闭环 | Partial |
| **P9-RGWP-C Gate** | **Pass** |

### 15.7 P9-RGWP-C 收口结论

| 结论项 | 状态 |
|--------|------|
| P9-RGWP-A | **Pass**（不变） |
| P9-RGWP-B | **Pass**（不变） |
| **P9-RGWP-C** | **Pass** — preview service + preview-only API + targeted tests |
| P9-RGWP-D | **Not started** |
| P9 real executor launch | **Not started** |
| Real Git write pilot actual execution | **Not started** |
| GitWrite scoped closure | **Scoped Pass to fake-only closure**（不变） |
| 产品运行时 Git 写操作 | **Not started** |
| AI Project Director 总闭环 | **Partial** |
