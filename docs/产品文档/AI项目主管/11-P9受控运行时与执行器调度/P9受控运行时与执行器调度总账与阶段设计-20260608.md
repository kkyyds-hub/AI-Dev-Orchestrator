# P9 受控运行时与执行器调度总账与阶段设计

> **文档类型**: 阶段 ledger / 现状审计 / 安全边界 / 子阶段拆分
> **生成日期**: 2026-06-08
> **远端基准**: `origin/main` = `61ad2538574a3783cf7f7c57598d54979d393e1f`（feat: add P9 runtime fake API）
> **前置阶段**: P1-P8 均 Pass；P8 Executor Config Discovery: Pass
> **路线文档**: `../01-总规划与路线/P8-P9外部执行器配置与运行时规划-20260607.md`
> **主产品基线**: `../02-页面与产品基线/页面信息架构-20260518.md`
> **参考项目**: Agent Orchestrator（`/Users/kk/project explore/agent-orchestrator`），仅机制参考
> **状态**: P9-A: Pass（Controlled Runtime ledger + current-state audit + safety boundary）；P9-B: Pass（ExecutorRuntimeSession / RuntimeState / RuntimeEvent 纯 domain contract）；P9-C: Pass（RuntimeSafetyGate / LaunchRequest / LaunchApproval 纯 domain contract）；P9-D: Pass（ControlledRuntimeService skeleton + FakeExecutorAdapter + in-memory event recorder）；P9-E: Pass（read-only runtime API + fake launch request preview API）；P9-F: Pass（frontend read-only fake runtime readback）；P9-G: Pass（fake-only dry-run E2E evidence）；P9-H: Pass（real executor pilot gate design；still default off）；P9-Final: Pass（targeted evidence + P9 scoped gate）；P9 Controlled Runtime scoped closure: Pass（fake dry-run + pilot gate design）；P9 real executor launch: Not started；产品运行时 Git 写操作: Not started；AI Project Director 总闭环: Partial

---

## 1. 当前状态

| 状态项 | 结论 | 证据 |
|--------|------|------|
| P8 Executor Config Discovery | **Pass** | `91c33ed` docs: close P8 executor config discovery gate |
| P9-A ledger | **Pass** | 本文档 — Controlled Runtime ledger + current-state audit + safety boundary |
| P9-B domain contract | **Pass** | `runtime/orchestrator/app/domain/executor_runtime.py` + `tests/test_executor_runtime_domain.py` |
| P9-C domain contract | **Pass** | `runtime/orchestrator/app/domain/executor_runtime_safety.py` + `tests/test_executor_runtime_safety_domain.py` |
| P9-D ControlledRuntimeService skeleton | **Pass** | `runtime/orchestrator/app/services/controlled_runtime_service.py` + `tests/test_controlled_runtime_service.py`；fake adapter only |
| P9-E read-only runtime API / launch request preview API | **Pass** | `runtime/orchestrator/app/api/routes/runtime.py` + `tests/test_runtime_api.py`；fake runtime only |
| P9-F frontend readback | **Pass** | `apps/web/src/pages/workbench/components/RuntimeReadbackPanel.tsx`；只读读取 fake runtime sessions/events |
| P9-G dry-run E2E evidence | **Pass** | 本文档 §14；fake-only dry-run E2E evidence + 115 backend + build + 6 frontend tests |
| P9-H real executor pilot gate design | **Pass** | 本文档 §15；21-item pilot gate checklist + execution sequence + P9-H/P9-Final boundary；still default off |
| P9-Final | **Pass** | 本文档 §16；targeted evidence + P9 scoped gate |
| P9 Controlled Runtime scoped closure | **Pass** | fake dry-run + pilot gate design；P9-A/B/C/D/E/F/G/H/Final 全部 Pass |
| P9 Controlled Runtime implementation | **Scoped Pass** | P9-A～H domain/service/API/frontend/evidence/design + P9-Final evidence；no real runtime |
| P9 real executor launch | **Not started** | 未启动任何 Codex / Claude Code / DeepSeek 进程 |
| 产品运行时 Git 写操作 | **Not started** | 全线禁止，gate_allows_write 永不为 True |
| AI Project Director 总闭环 | **Partial** | P9 未完成前不得写 Pass |

---

## 2. P9 范围定义

### 2.1 P9 是什么

P9 目标是 **controlled runtime / executor orchestration** — 在安全 gate 全过的前提下，受控启动和管理外部执行器。

P9 回答的问题：
- 如何在不暴露 secret / 不绕过审批的前提下，受控启动 Codex / Claude Code / DeepSeek？
- 如何追踪执行器 session 生命周期（spawn → running → idle → done → killed）？
- 如何检测执行器 activity 并回填 cost / token usage？
- 如何在执行器活动期间保持 workspace 绑定安全？
- 如何提供用户中断 / 超时 / 清理能力？

### 2.2 P9 不是什么

| # | 不是 | 说明 |
|---|------|------|
| 1 | **不是**直接启动 Codex / Claude Code / DeepSeek | 必须先有 domain contract、safety gate、approval gate、workspace gate、cost/budget gate |
| 2 | **不是**产品运行时 Git 写闭环 | P9 initial runtime 不触发 git add/commit/push/PR/merge |
| 3 | **不是**自动 PR / merge | 产品运行时 Git 写是独立的安全门，不与 P9 runtime 捆绑 |
| 4 | **不是**AI 主管偷偷启动执行器 | 必须先 feature flag + human confirmation |
| 5 | **不是**无限并发 / 无限预算 | 必须有 concurrency limit + cost/budget guard |
| 6 | **不是**无超时运行 | 必须可配置 timeout + kill switch |

### 2.3 P9 必须先建立的硬 Gate（在真实启动之前）

| # | Gate | 说明 |
|---|------|------|
| 1 | **Feature Flag Gate** | feature flag default off，永不为 True 除非用户显式开启 |
| 2 | **Human Confirmation Gate** | 用户显式确认后才允许进入 runtime |
| 3 | **Executor Readiness Gate** | executor status == AVAILABLE，launch preview ready == True |
| 4 | **Workspace/Worktree Gate** | workspace 必须存在、合法、已绑定，不能任意目录启动 |
| 5 | **Cost/Budget Gate** | 必须在 daily/session budget 范围内，超预算拒绝启动 |
| 6 | **Concurrency Gate** | 同时运行的 executor session 不能超过上限 |
| 7 | **No Secret Exposure Gate** | 任何 response/log/event 中不得出现 API key/token/secret 明文 |
| 8 | **No Product Runtime Git Write Gate** | gate_allows_write 永不为 True |
| 9 | **Event/Audit Gate** | 所有 runtime 事件必须 append-only 记录 |
| 10 | **Timeout/Cancellation Gate** | 每个 session 必须有可配置 timeout，必须可被用户取消 |
| 11 | **Cleanup Gate** | session 结束后必须清理 workspace 绑定 |

---

## 3. P8 → P9 前置能力映射

| P8 能力 | P8 状态 | P9 可复用方式 | P9 不能误判为什么 |
|---------|---------|-------------|-----------------|
| ExecutorProfile | Pass | P9 runtime session 引用 executor_id → profile 获取 capabilities/permission_model | 不是 runtime session state；profile 是静态配置，不是动态状态 |
| ExecutorConfigDiscovery | Pass | P9 启动前检查 discovery.token_configured / login_status | 不是真实配置读取；discovery 是 noop/fake probe，P9 需要更真实的就绪检查 |
| ExecutorConfigDiscoveryService | Pass | P9 复用 registry snapshot 获取 profile 列表 | 不是 runtime service；discovery service 不管理 session |
| GET /executors | Pass | P9 前端展示 executor 列表，用户选择执行器 | 不是启动入口；只读 API 不能触发 runtime |
| GET /executors/{id}/readiness | Pass | P9 启动前 readiness 检查 | 不是 launch gate；readiness 是静态快照，P9 需要动态 runtime gate |
| GET /executors/available | Pass | P9 过滤可用的 executor | 不是可用性保证；available 是 metadata 判断，不等于可以安全启动 |
| POST /executors/{id}/launch-preview | Pass | P9 launch request 必须以 preview 为前置步骤 | 不是 launch；preview 是只读命令字符串，P9 才是受控执行 |
| ExecutorLaunchSafetyFlags | Pass | P9 继承所有 safety flags 作为 runtime gate 检查清单 | 不是 runtime enforcement；safety flags 是静态声明，P9 需要运行时强制执行 |
| noop/fake probe seam | Pass | P9 可用类似 seam 模式先做 fake adapter，再逐步替换为真实 adapter | 不是真实 executor adapter；fake probe 不启动进程 |
| no secret / no subprocess / no runtime session 边界 | Pass | P9 必须继承并严格维持此边界，直到用户显式授权 | 不是 P9 就可以打破边界；P9 每个子阶段都要重新验证边界 |

---

## 4. 当前项目 runtime / worker / approval / event 能力审计矩阵

### 4.1 ExecutorService（现有 provider/shell/simulate 执行能力）

| 维度 | 内容 |
|------|------|
| **当前已有能力** | `ExecutorService` 提供三种模式：shell（subprocess 执行本地命令）、simulate（模拟执行）、provider（调用 OpenAI-compatible API）。shell mode 用 `/bin/sh -lc` 或 `powershell.exe -Command`。provider mode 有完整 fallback 链：provider → provider_mock → simulate。 |
| **证据文件** | `runtime/orchestrator/app/services/executor_service.py`（698 行） |
| **与 P9 的关系** | ExecutorService 的 shell mode 证明系统已有 subprocess 能力。P9 的 ControlledRuntimeService 需要的是 **受控** subprocess：有 feature flag、有 human confirmation、有 timeout、有 kill switch、有 output capture——不是 ExecutorService 当前的无状态 shell 执行。 |
| **风险点** | ExecutorService 的 shell mode 直接执行任意 shell 命令，无 runtime session 追踪，无 activity detection，无 cost tracking。如果 P9 直接用 ExecutorService 启动 codex/claude，会缺失所有安全 gate。 |
| **是否可直接用于真实 executor runtime** | **否**。P9 需要全新的 ControlledRuntimeService，不能复用 ExecutorService 的 shell mode。 |
| **后续 P9 子阶段建议** | P9-D 的 ControlledRuntimeService skeleton 应设计独立的 subprocess 管理层，不与 ExecutorService 耦合。 |

### 4.2 Worker run path（task_worker.py）

| 维度 | 内容 |
|------|------|
| **当前已有能力** | `task_worker.py` 实现完整单周期 worker：context building → budget guard → strategy engine → model routing → executor service → result → evidence → failure recovery → human approval → delivery gate。Worker 通过 `ExecutorService` 执行任务，通过 `RepositoryWorkspaceRepository` 管理 workspace 绑定。 |
| **证据文件** | `runtime/orchestrator/app/workers/task_worker.py` |
| **与 P9 的关系** | Worker 的 context → budget → strategy → execute → evidence → approval 链路是 P9 runtime orchestration 的参考模式。P9 的 ControlledRuntimeService 需要类似的编排能力，但面向外部执行器进程而非 provider API。 |
| **风险点** | Worker 当前不管理外部进程 session、不追踪 activity、不回填 token usage、不支持 subprocess 超时/kill。 |
| **是否可直接用于真实 executor runtime** | **否**。Worker 是 task 级别的编排，P9 需要 executor 级别的 runtime 编排。 |
| **后续 P9 子阶段建议** | P9-D 的 runtime service 可参考 Worker 的编排模式，但 domain 和生命周期完全不同。 |

### 4.3 Provider execution path

| 维度 | 内容 |
|------|------|
| **当前已有能力** | `OpenAIProviderExecutorService` 调用 OpenAI-compatible API（含 DeepSeek）。`ProviderConfigService` 管理 saved_config + env fallback。`ModelRoutingService` 将 strategy decision 转为 routing contract。 |
| **证据文件** | `runtime/orchestrator/app/services/openai_provider_executor_service.py`、`runtime/orchestrator/app/services/provider_config_service.py`、`runtime/orchestrator/app/services/model_routing_service.py` |
| **与 P9 的关系** | Provider execution path 是 API 调用链，P9 需要的是外部 CLI 进程管理链。两者技术路径完全不同：provider 走 HTTP，executor 走 subprocess。 |
| **风险点** | 不能把 provider execution 误判为 executor runtime。Provider 的 key/token 管理属于 API 调用层，P9 executor 的 auth/token 管理属于 CLI 原生配置层。 |
| **是否可直接用于真实 executor runtime** | **否**。Provider path 和 executor path 是不同的执行抽象。 |
| **后续 P9 子阶段建议** | P9-C 的 RuntimeSafetyGate 应与 provider routing 解耦，独立设计 executor 级别的安全 gate。 |

### 4.4 Shell / simulate / provider route

| 维度 | 内容 |
|------|------|
| **当前已有能力** | `ExecutorRouteMode`（shell/simulate/provider）+ `ExecutorModelRoutingContract`。shell mode 可执行任意命令（30 秒超时），simulate 是纯虚构返回，provider 走 API。 |
| **证据文件** | `runtime/orchestrator/app/domain/model_policy.py` |
| **与 P9 的关系** | P9 需要新的 ExecutorRuntimeMode 或 ExecutorSessionMode，不同于现有的三种模式。P9 的 "executor runtime" 模式是全新的：受控 subprocess + session 管理 + activity 追踪。 |
| **风险点** | 现有 shell mode 的 30 秒硬超时、无 session 管理、无 activity detection 都不能满足 P9 需求。 |
| **是否可直接用于真实 executor runtime** | **否**。需要新的 runtime mode。 |
| **后续 P9 子阶段建议** | P9-B 的 ExecutorRuntimeSession domain 需定义新的 runtime mode 枚举。 |

### 4.5 Approvals / Human Gate

| 维度 | 内容 |
|------|------|
| **当前已有能力** | P4 完成 Human Approval Gate 全链路：`HumanApprovalGateBuilder`、`DeliveryHumanApprovalResult`、`ApprovalService`（含 ApprovalQueueItem、ApprovalHistory、ApprovalReworkCycle、ProjectApprovalInbox）。支持 APPROVE_GIT_ADD_COMMIT_PREVIEW scope。 |
| **证据文件** | `runtime/orchestrator/app/api/routes/approvals.py`、`runtime/orchestrator/app/services/approval_service.py`、`runtime/orchestrator/app/domain/human_approval_gate.py` |
| **与 P9 的关系** | P9 需要新增 executor launch approval scope（如 `APPROVE_EXECUTOR_LAUNCH`），确认用户知晓"即将启动外部执行器进程、将消耗预算、将绑定 workspace"。现有的 Git add/commit preview approval 可作为参考模式。 |
| **风险点** | 现有 approval scope 聚焦 Git 交付审批，不覆盖 executor launch。P9 的 launch approval 需要不同的 scope、expiry、确认文案。 |
| **是否可直接用于真实 executor runtime** | **部分可复用**。ApprovalService 的架构可复用，但需要新增 scope 和流程。 |
| **后续 P9 子阶段建议** | P9-C 的 LaunchApproval domain 应扩展现有 ApprovalService，新增 executor launch scope。 |

### 4.6 Events / Audit Timeline

| 维度 | 内容 |
|------|------|
| **当前已有能力** | `event_stream_service` 提供 SSE 事件流（含 heartbeat、connected 等事件类型）。console events endpoint（`GET /events/console`）。P4 delivery_event_audit_service。P6 agent_dispatch_audit_service。 |
| **证据文件** | `runtime/orchestrator/app/api/routes/events.py`、`runtime/orchestrator/app/services/event_stream_service.py` |
| **与 P9 的关系** | P9 runtime event 应扩展事件类型（如 `executor.session.spawned`、`executor.session.running`、`executor.session.completed`、`executor.session.killed`、`executor.activity.detected`、`executor.cost.updated`）。SSE 传输层可复用。 |
| **风险点** | 当前事件粒度偏粗（console-level），P9 需要 executor 级别的事件分类和 audit trail。 |
| **是否可直接用于真实 executor runtime** | **部分可复用**。event_stream_service 的 SSE 管道可复用，但需要新增 executor event types 和 audit storage。 |
| **后续 P9 子阶段建议** | P9-B 的 RuntimeEvent domain 应定义 executor-level event types。P9-D/E 的 runtime service 应通过 event_stream_service 发射 runtime event。 |

### 4.7 Repository / Worktree / Workspace

| 维度 | 内容 |
|------|------|
| **当前已有能力** | P1 worktree lifecycle。P4 Git Diff Dry-run + Git Operation Dry-run Preview。`RepositoryWorkspaceRepository` 管理 workspace CRUD。`RepositoryAccessMode`（read_only/read_write）。`ChangeSession` 管理 workspace dirty file scope。 |
| **证据文件** | `runtime/orchestrator/app/api/routes/repositories.py`、`runtime/orchestrator/app/domain/repository_workspace.py` |
| **与 P9 的关系** | P9 executor session 必须绑定合法的 workspace/worktree。不能在没有 workspace 的情况下启动执行器。`RepositoryWorkspace` 的 access_mode 可限制 executor 的文件访问范围。 |
| **风险点** | P9 executor 启动后可能修改文件，需要 workspace dirty file tracking。当前 workspace 的 read_only mode 是逻辑限制，不是 OS 级别强制。 |
| **是否可直接用于真实 executor runtime** | **部分可复用**。workspace 绑定模式可复用，但需要 P9 级别的 workspace gate 验证（workspace 存在、合法、未被其他 session 占用）。 |
| **后续 P9 子阶段建议** | P9-C 的 RuntimeSafetyGate 应包含 workspace validation，复用 `RepositoryWorkspaceRepository`。 |

### 4.8 Cost / Budget / Provider Settings

| 维度 | 内容 |
|------|------|
| **当前已有能力** | `BudgetGuardService`（daily/session budget）。`CostEstimatorService`。`ProviderConfigService`（API key mask、provider type detection、model tier selection）。`TokenAccountingService`。 |
| **证据文件** | `runtime/orchestrator/app/services/budget_guard_service.py`、`runtime/orchestrator/app/services/cost_estimator_service.py`、`runtime/orchestrator/app/services/provider_config_service.py` |
| **与 P9 的关系** | P9 executor launch 前必须通过 budget guard。P9 executor session 结束后必须回填 cost/token usage。 |
| **风险点** | 当前 budget guard 基于 provider API 调用成本估算，不覆盖 executor CLI 的 cost 追踪。P9 需要从 executor 原生 JSONL/API response 回填真实 cost。 |
| **是否可直接用于真实 executor runtime** | **部分可复用**。BudgetGuardService 架构可复用，但需要 executor 级别的 cost estimation（非 provider API 调用）。 |
| **后续 P9 子阶段建议** | P9-C 的 RuntimeSafetyGate 应集成 BudgetGuardService。P9 后续子阶段需要 executor cost 回填。 |

### 4.9 P7 Governance Readback

| 维度 | 内容 |
|------|------|
| **当前已有能力** | P7 Project Director Conversation Hub + Governance total closure。含 ProjectRole、Skill、BudgetGuard、DirectorContextAssembler、UserChallenge/Intervention、PlanRevision/DirectorActionProposal/ProposalApproval。 |
| **与 P9 的关系** | P7 governance 决定"AI 主管能不能建议启动执行器"。P9 runtime gate 决定"当前条件是否允许启动执行器"。两者互补：governance 是决策层，runtime gate 是执行层。 |
| **风险点** | P7 的 DirectorActionProposal 可以"建议启动 Codex 修 bug"，但不能直接触发 P9 launch。必须经过 P9 的 runtime gate。 |
| **是否可直接用于真实 executor runtime** | **部分可复用**。P7 conversation 可用于呈现 launch preview、收集用户确认，但不能替代 P9 runtime gate。 |
| **后续 P9 子阶段建议** | P9-F frontend readback 应与 P7 conversation hub 集成，通过 DirectorActionProposal 呈现 executor launch 选项。 |

---

## 5. P9 安全模型草案

### 5.1 安全原则总表

| # | 原则 | 规则 |
|---|------|------|
| 1 | **Feature Flag Default Off** | `EXECUTOR_RUNTIME_ENABLED` 默认 False，永不为 True 除非用户显式开启 |
| 2 | **User Explicit Confirmation** | 每次 executor launch 前必须用户显式确认，确认 scope 明确 |
| 3 | **Executor Readiness Must Be Available** | executor status == AVAILABLE + launch preview ready == True |
| 4 | **Launch Preview Must Exist Before Runtime** | 每次 runtime launch 前必须先生成 launch preview |
| 5 | **Workspace/Worktree Must Be Explicit and Safe** | 必须绑定合法 workspace，不能任意目录启动 |
| 6 | **No Secret Exposure** | 任何 response/log/event/DTO 中无 API key/token/secret 明文 |
| 7 | **No Env Dump** | 不 dump 环境变量到任何输出 |
| 8 | **No Native Config Path Dump** | 不在 response 中暴露原生配置文件路径 |
| 9 | **Timeout / Cancellation / Kill Switch** | 每个 session 必须有可配置 timeout，必须可被用户取消 |
| 10 | **Concurrency Limit** | 同时运行的 executor session 不超过可配置上限 |
| 11 | **Cost/Budget Guard** | 必须在 daily/session budget 范围内 |
| 12 | **Event/Audit Append-Only** | 所有 runtime 事件必须记录，不可篡改 |
| 13 | **Human Approval Before Any Product Runtime Git Write** | Git 写操作独立审批，不与 executor launch 捆绑 |
| 14 | **Product Runtime Git Write Remains Separate** | P9 initial runtime 不触发 Git 写 |

### 5.2 Feature Flag 设计

```text
EXECUTOR_RUNTIME_ENABLED: bool = False  # 默认关闭
EXECUTOR_RUNTIME_MAX_CONCURRENT: int = 1  # 默认最多 1 个并发
EXECUTOR_RUNTIME_DEFAULT_TIMEOUT_SECONDS: int = 600  # 默认 10 分钟
EXECUTOR_RUNTIME_MAX_TIMEOUT_SECONDS: int = 3600  # 最大 1 小时
EXECUTOR_RUNTIME_ALLOWED_EXECUTORS: list[str] = []  # 空白名单 = 全禁止
EXECUTOR_RUNTIME_ALLOWED_WORKSPACE_ROOTS: list[str] = []  # 空白名单 = 全禁止
```

### 5.3 Launch 安全流程

```text
UserAction → DirectorActionProposal（P7）
  → LaunchPreview 生成（P8-E，复用）
  → HumanConfirmation（用户显式确认）
  → FeatureFlagGate（EXECUTOR_RUNTIME_ENABLED == True）
  → ExecutorReadinessGate（status == AVAILABLE）
  → WorkspaceGate（workspace 合法、存在、未占用）
  → BudgetGate（daily/session budget 未超）
  → ConcurrencyGate（未超并发上限）
  → RuntimeSafetyGate（全部 gates passed）
  → ControlledRuntimeService.launch（受控启动）
  → EventStream（emit executor.session.spawned）
  → ActivityDetection（轮询进程 + JSONL）
  → Cost/Token Readback（从原生 JSONL 回填）
  → Completion / Timeout / UserCancellation
  → Cleanup（解绑 workspace，清理临时文件）
  → EventStream（emit executor.session.completed）
```

### 5.4 安全边界红线

| # | 红线 | 后果 |
|---|------|------|
| 1 | 不能跳过 human confirmation 直接启动 | 测试/验证阶段也必须 mock confirmation |
| 2 | 不能在非 workspace 目录启动 executor | WorkspaceGate 必须拒绝 |
| 3 | 不能暴露 API key / token / secret | 任何代码路径都不能 |
| 4 | 不能超过并发上限 | ConcurrencyGate 必须拒绝 |
| 5 | 不能超预算启动 | BudgetGate 必须拒绝 |
| 6 | 不能不记录 audit event | 每个 runtime 事件必须 append-only |
| 7 | 不能没有 timeout | 每个 session 必须有超时配置 |
| 8 | 不能没有 kill switch | 用户必须可以取消运行中的 session |
| 9 | 不能自动触发 Git 写 | 产品运行时 Git 写是独立 gate |

---

## 6. P9 子阶段拆分建议

### 6.1 子阶段总览

| 子阶段 | 内容 | 负责人建议 | 说明 |
|--------|------|-----------|------|
| **P9-A** | ledger + current-state audit + safety boundary | DeepSeek | **本文档** |
| **P9-B** | ExecutorRuntimeSession / RuntimeState / RuntimeEvent 纯 domain contract | Codex | 不启动任何进程，不读配置 |
| **P9-C** | RuntimeSafetyGate / LaunchRequest / LaunchApproval 纯 domain + tests | Codex | 不启动任何进程 |
| **P9-D** | ControlledRuntimeService skeleton with fake adapter | Codex | fake adapter 模拟 subprocess 行为，不启动真实进程 |
| **P9-E** | read-only runtime API / launch request preview API | Codex | 不启动真实外部进程 |
| **P9-F** | frontend readback / governance inbox linkage | Codex | 只读展示，不触发任何 runtime |
| **P9-G** | dry-run end-to-end evidence（fake adapter only） | Codex | 全链路 fake adapter，验证 gate 链 |
| **P9-H** | real executor pilot gate design | Codex | **只设计 pilot gate，仍默认关闭**；不在此阶段启动真实执行器 |
| **P9-Final** | targeted evidence + P9 scoped gate | DeepSeek | P9 evidence 收口 |

### 6.2 各子阶段详细说明

#### P9-A（本文档）

- **输入**: P8 Final Gate Pass、P8/P9 路线文档
- **产出**: P9-A ledger、现状审计矩阵、安全模型草案、子阶段拆分、参考项目机制映射、P9 Gate 草案
- **不产出**: 任何代码、domain、service、API、frontend
- **Gate**: 本文档完成即 P9-A Pass

#### P9-B：ExecutorRuntimeSession / RuntimeState / RuntimeEvent 纯 domain contract

- **输入**: P9-A ledger、P8 executor_config domain
- **产出**:
  - `ExecutorRuntimeSession` 域模型（session_id / executor_id / run_id / task_id / workspace_path / branch_name / status / started_at / finished_at / pid / exit_code / result_summary / error_message / total_tokens / estimated_cost / created_by）
  - `ExecutorRuntimeState` 枚举（pending / launching / running / waiting_input / blocked / completed / failed / cancelled / timed_out）
  - `RuntimeEvent` 域模型（event_id / session_id / event_type / timestamp / payload）
  - `RuntimeEventType` 枚举（session.spawned / session.running / session.completed / session.killed 等）
- **不产出**: service 实现、API、subprocess 调用、真实进程管理
- **Gate**: domain contract freeze 完成，无安全字段泄露

#### P9-C：RuntimeSafetyGate / LaunchRequest / LaunchApproval 纯 domain + tests

- **输入**: P9-B domain contracts
- **产出**:
  - `RuntimeSafetyGate` 域模型（feature_flag_gate / human_confirmation_gate / readiness_gate / workspace_gate / budget_gate / concurrency_gate）
  - `ExecutorLaunchRequest` 域模型（executor_id / task_id / run_id / workspace_id / launch_preview_id / user_confirmed / confirmation_token）
  - `ExecutorLaunchApproval` 域模型（扩展 ApprovalService，新增 APPROVE_EXECUTOR_LAUNCH scope）
  - targeted tests（domain validation only）
- **不产出**: service 实现、API、真实 gate 执行
- **Gate**: domain contract freeze 完成

#### P9-D：ControlledRuntimeService skeleton with fake adapter

- **输入**: P9-B/C domain contracts
- **产出**:
  - `ControlledRuntimeService` skeleton（launch / get_session / poll / cancel / kill / cleanup / events_for_session）
  - `FakeExecutorAdapter`（不启动真实进程，模拟 session 生命周期）
  - `InMemoryRuntimeEventRecorder`（append-only 内存事件记录，不接数据库 / 文件 / event_stream_service）
  - targeted tests（fake adapter only）
- **不产出**: API、frontend、DB persistence、真实 subprocess 调用、真实 executor adapter（codex/claude/deepseek）
- **Gate**: skeleton 通过 fake adapter 测试，无真实进程启动

#### P9-E：read-only runtime API / launch request preview API

- **输入**: P9-D skeleton
- **产出**:
  - `GET /runtime/sessions` — 列出 runtime session 历史（只读）
  - `GET /runtime/sessions/{session_id}` — 获取单个 session 状态
  - `GET /runtime/sessions/{session_id}/events` — 获取 session 事件流
  - `POST /runtime/launch-request` — 生成 launch request（不执行）
  - `POST /runtime/launch-request/{request_id}/confirm` — 确认 launch request（fake only）
  - `POST /runtime/sessions/{session_id}/cancel` — 取消 session（fake only）
- **不产出**: 真实 subprocess launch、真实 executor command 执行
- **Gate**: API 只读 + fake confirm/cancel，不触发真实进程

#### P9-F：frontend readback / governance inbox linkage

- **输入**: P9-E API
- **产出**:
  - 前端 runtime session 列表（只读）
  - 前端 launch request 确认卡片
  - 与 P7 conversation hub 集成（DirectorActionProposal 呈现 executor launch 选项）
- **不产出**: 真实 launch 按钮、真实 executor 交互
- **Gate**: 前端只读，不触发 runtime

#### P9-G：dry-run end-to-end evidence（fake adapter only）

- **输入**: P9-D/E/F 产出
- **产出**:
  - 全链路 fake adapter E2E 测试
  - runtime event stream 验证
  - safety gate 链验证
  - targeted evidence 汇总
- **不产出**: 真实 executor adapter
- **Gate**: dry-run E2E 通过，fake adapter 全链路验证

#### P9-H：real executor pilot gate design

- **输入**: P9-G dry-run evidence
- **产出**:
  - Real Executor Pilot Gate 设计文档
  - 明确 pilot 条件（需用户独立确认，不在 P9-H 阶段内启动）
  - Codex adapter design（只设计，不实现）
  - Claude Code adapter design（只设计，不实现）
  - DeepSeek adapter design（只设计，不实现）
  - 真实 subprocess 管理方案设计
  - Activity detection 方案设计
  - Cost/Token readback 方案设计
- **不产出**: 真实 adapter 实现、真实进程启动
- **Gate**: 设计完成，pilot gate 仍默认关闭

#### P9-Final：targeted evidence + P9 scoped gate

- **输入**: P9-B 至 P9-H 所有产出
- **产出**: P9 阶段 targeted evidence + P9 Gate 结论
- **Gate**: P9 Final Gate Pass 或 Blocked

### 6.3 明确声明：real executor pilot 启动不在 P9-A 至 P9-H 内

| 声明 | 说明 |
|------|------|
| P9-A 至 P9-H 全程不启动真实 Codex / Claude Code / DeepSeek 进程 | 只用 fake adapter |
| P9-H 只做 pilot gate 设计，不进入真实 pilot | pilot 启动需用户后续独立确认 |
| real executor pilot 是 P9-H 后的独立决策点 | 不在 P9 子阶段拆分中承诺 |

---

## 7. 参考项目机制映射

### 7.1 借鉴映射表

| 参考机制 | 参考文件 | 可借鉴点 | 在 AI-Dev-Orchestrator 的 P9 映射 | 不复制 / 不引入 / 不实现 |
|---------|---------|---------|----------------------------------|------------------------|
| LifecycleManager 状态机 | `packages/core/src/lifecycle-manager.ts` | 定期轮询 session → 检测状态转换 → emit 事件 → 触发反应 → 升级到人工通知 | P9-D ControlledRuntimeService 的 session 状态管理 + activity 轮询 | 不引入 TypeScript/Node.js 实现、不引入 polling loop 架构 |
| CanonicalSessionLifecycle | `packages/core/src/lifecycle-state.ts` | 规范化 session 状态（not_started/working/idle/needs_input/stuck/detecting/done/terminated）+ reason + timestamps | P9-B ExecutorRuntimeState 枚举 + ExecutorRuntimeSession domain | 不引入 Zod schema、不引入 worker/orchestrator 二分类 |
| CanonicalRuntimeState | `packages/core/src/types.ts`（L73-76） | runtime 状态：unknown/alive/exited/missing/probe_failed | P9-B ExecutorRuntimeState 的 runtime 维度（process probe 结果） | 不引入 tmux_missing reason、不引入 tmux runtime |
| Session state transitions | `packages/core/src/lifecycle-manager.ts`（L1-100） | activity signal → state classification → transition decision → event emission | P9-D ControlledRuntimeService 的 state transition 逻辑 | 不引入 CI failure / PR review / review comment reaction 机制 |
| Plugin registry | `packages/core/src/plugin-registry.ts` | 8-slot plugin architecture（runtime/agent/workspace/tracker/SCM/notifier/terminal/lifecycle），built-in + external plugins | P8 ExecutorRegistry（3 built-in manifests）→ P9 可能扩展为 runtime adapter registry | 不引入 plugin 热加载、不引入 npm package 架构、不引入 8-slot 设计 |
| Config discovery | `packages/core/src/config.ts` | 从 agent-orchestrator.yaml 加载配置，Zod 校验，session prefix 生成 | P8 ConfigDiscovery → P9 RuntimeConfig（feature flag、executor allowlist、timeout、concurrency） | 不引入 YAML 配置格式、不引入 Zod 校验 |
| Activity event logging | `packages/core/src/activity-events.ts` | 结构化 event kind + source 分类，append-only | P9-B RuntimeEvent domain + P9-D InMemoryRuntimeEventRecorder | 不引入 SQLite event DB、不引入 event drop counter |
| Session manager CRUD | `packages/core/src/session-manager.ts` | spawn → create workspace → create runtime → launch agent → kill → cleanup | P9-D ControlledRuntimeService 的 session 生命周期管理 | 不引入 tmux runtime、不引入 workspace plugin 架构 |

### 7.2 明确不引入的能力

| 参考项目能力 | 原因 |
|------------|------|
| TypeScript / Node.js 全栈 | AI-Dev 技术栈为 Python + FastAPI + SQLite + React |
| tmux runtime 管理 | AI-Dev 走 subprocess 直启，不需要 tmux |
| Plugin 热加载 / 8-slot 架构 | AI-Dev executor 数量有限（3-5 个），不需要动态插件架构 |
| PR / CI / review 自动反应 | 产品运行时 Git 写全线禁止 |
| CleanupStack / LIFO undo | 无副作用操作，不需要 undo stack |
| Zod schema 校验 | AI-Dev 使用 Pydantic |
| YAML 配置 | AI-Dev 使用环境变量 + JSON |
| 独立 SQLite event DB | AI-Dev 使用现有 SQLite + event_stream_service |
| Notifier 插件（desktop/slack/webhook） | AI-Dev 当前无通知需求 |
| Tracker 插件（GitHub/Linear/Jira issues） | AI-Dev 无 issue tracking 集成计划 |

---

## 8. P9-B ExecutorRuntimeSession / RuntimeState / RuntimeEvent 纯 domain contract

### 8.1 本轮产出

| 文件 | 动作 | 内容 |
|------|------|------|
| `runtime/orchestrator/app/domain/executor_runtime.py` | 新增 | P9 controlled runtime 纯领域合约：state/event/exit/source 枚举、process/usage/workspace/session/event/event stream 模型、纯状态方法 |
| `runtime/orchestrator/tests/test_executor_runtime_domain.py` | 新增 | P9-B targeted domain tests，覆盖默认 source、安全校验、状态转换、事件流一致性、禁止字段与导入边界 |

### 8.2 合同冻结范围

| 合同项 | 冻结内容 |
|--------|----------|
| Runtime state | `pending` / `launch_requested` / `awaiting_human_confirmation` / `launch_approved` / `launching` / `running` / `waiting_input` / `idle` / `blocked` / `completed` / `failed` / `cancelled` / `timed_out` / `killed` / `cleanup_required` / `cleaned_up` |
| Runtime event | `session.created` 至 `session.audit_note` 的 executor-level append-only event type |
| Exit reason | `completed` / `failed` / `cancelled_by_user` / `timeout` / `killed_by_user` / safety/workspace/budget gate blocked / `unknown` |
| Source | `fake_adapter` / `dry_run` / `real_executor_pilot` / `unknown`；默认 `fake_adapter`，不默认 real executor |
| Process snapshot | 仅 contract 字段：`process_id`、`exit_code`、timestamps；不读取真实 pid，不含 handle/log/config/cli path |
| Usage snapshot | token 与 estimated cost 合同；不含 invoice/payment/billing secret |
| Workspace binding | workspace/repository/branch/worktree 标识与 path hint；绝对路径、home、Windows 盘符、UNC path 自动脱敏 |
| Session | session/executor 标识、状态、source、workspace/process/usage、exit、summary、blocking reasons、timestamps |
| Event payload | 安全 payload contract，不允许 arbitrary dict；message/reason/state/metadata_count only |
| Event stream | 校验 session_id 一致、total 与事件数一致、timestamp 非递减 |

### 8.3 参考项目只读记录

| 参考路径 | 本轮是否读取 | 只借鉴机制 |
|----------|--------------|------------|
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-state.ts` | 是 | 规范化 lifecycle 状态、reason、timestamp 校验思路 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/types.ts` | 是 | session/runtime/activity 状态分类口径 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` | 是 | event kind/source 分类、append-only audit 思路、敏感信息保护意识 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-manager.ts` | 是 | 状态转换、event emission、activity→state 机制 |

明确声明：
- 未修改参考项目任何文件。
- 未复制 TypeScript 代码、类型名或包路径。
- 未引入 TypeScript / Node.js / tmux / plugin / Zod / YAML。
- 仅抽象为 Python Pydantic domain contract。

### 8.4 Targeted tests

| 验证命令 | 结果 |
|----------|------|
| `cd runtime/orchestrator && .venv/bin/pytest tests/test_executor_runtime_domain.py -q` | Pass — 41 passed |

覆盖点：
- 默认 source 是 `fake_adapter`，不是 `real_executor_pilot`。
- `session_id` / `executor_id` trim 且空字符串拒绝。
- `finished_at` 不得早于 `started_at`，`updated_at` 不得早于 `created_at`。
- `running` / `launching` 不允许 `finished_at`。
- terminal / cleanup-required 状态纯方法正确。
- 状态转换合法/非法路径校验。
- `blocking_reasons` trim + 去重。
- workspace path hint 脱敏。
- process_id、tokens、total_tokens、estimated_cost 校验。
- session summary / event payload message 拒绝疑似 secret/token/API key 明文。
- RuntimeEvent id/session_id、append_only、RuntimeEventStreamSnapshot 一致性和 timestamp 顺序校验。
- domain 文件不导入 service/api/worker，不导入 subprocess/os.popen，不包含禁止字段。
- 参考项目路径未写入 `executor_runtime.py`。

### 8.5 边界声明

| 声明 | 结论 |
|------|------|
| 是否只实现纯 domain contract | 是 |
| 是否实现 runtime service | 否 |
| 是否实现 executor adapter / fake adapter | 否 |
| 是否实现 API | 否 |
| 是否实现 frontend | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否启动真实外部执行器或创建真实进程 | 否 |
| 是否创建真实 ExecutorSession | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否实现或触发产品运行时 Git 写操作 | 否 |
| 是否修改 apps/web / services / api / workers / migrations / lock 文件 | 否 |

### 8.6 P9-B Gate

| Gate 条件 | 结论 |
|----------|------|
| 新增 ExecutorRuntime domain contract | Pass |
| 新增 targeted tests | Pass |
| 测试通过 | Pass — 41 passed |
| 无 service/API/worker 依赖 | Pass |
| 无 subprocess / shell / os.popen runtime 实现 | Pass |
| 无 forbidden sensitive/runtime fields | Pass |
| workspace_path_hint 脱敏 | Pass |
| event payload 不使用裸 dict | Pass |
| event stream append-only / ordered contract | Pass |
| 未复制参考项目代码、未引入参考项目技术栈 | Pass |
| **P9-B Gate** | **Pass** |

### 8.7 后续阶段状态

| 阶段/能力 | 状态 | 说明 |
|-----------|------|------|
| P9-C RuntimeSafetyGate / LaunchRequest / LaunchApproval | Pass | 已在第 9 节完成纯 domain contract |
| P9-D ControlledRuntimeService skeleton / fake adapter | Pass | 已在第 10 节完成 fake service skeleton |
| P9-E read-only runtime API | Not started | 本轮未实现 |
| P9-F frontend readback | Not started | 本轮未实现 |
| P9-G dry-run E2E evidence | Not started | 本轮未实现 |
| P9-H real executor pilot gate design | Not started | 本轮未实现 |
| P9-Final targeted evidence + scoped gate | Not started | 本轮未进入 |
| P9 implementation | Partial | P9-B/C domain + P9-D fake service skeleton；no API/frontend/real runtime |
| P9 real executor launch | Not started | 未启动任何真实执行器 |
| 产品运行时 Git 写操作 | Not started | 产品内部仍严禁 git add/commit/push/PR/merge/branch/delete/reset/checkout/rebase/stash/tag/CI 自动触发 |
| AI Project Director 总闭环 | Partial | P9 未完成前不得写 Pass |

---

## 9. P9-C RuntimeSafetyGate / LaunchRequest / LaunchApproval 纯 domain contract

### 9.1 本轮产出

| 文件 | 动作 | 内容 |
|------|------|------|
| `runtime/orchestrator/app/domain/executor_runtime_safety.py` | 新增 | P9 runtime safety gate 纯领域合约：gate name/status、block reason、feature flag policy、workspace/budget/concurrency gate input、safety evaluation input、launch request、launch approval |
| `runtime/orchestrator/tests/test_executor_runtime_safety_domain.py` | 新增 | P9-C targeted domain tests，覆盖默认关闭、gate consistency、required gate coverage、预算/并发/workspace、安全聚合、launch request/approval 校验、源码边界 |

### 9.2 合同冻结范围

| 合同项 | 冻结内容 |
|--------|----------|
| RuntimeSafetyGateName | feature flag、human confirmation、executor readiness、launch preview、workspace、cost/budget、concurrency、no secret/env/git write、timeout、cancellation、audit event |
| RuntimeSafetyGateStatus | `passed` / `blocked` / `pending` / `not_applicable` / `unknown` |
| RuntimeLaunchRequestStatus | `draft` / `awaiting_confirmation` / `approved` / `rejected` / `expired` / `blocked` / `consumed` / `cancelled`；默认不是 approved |
| RuntimeApprovalDecision | `pending` / `approved` / `rejected` / `expired` / `cancelled`；默认 pending |
| RuntimeLaunchBlockReason | feature flag disabled、人审 required、executor/preview/workspace/budget/concurrency/secret/env/git/timeout/cancel/audit 等阻断原因 |
| RuntimeApprovalScope | 仅 `executor_launch`；只是 P9-C domain enum，不代表 ApprovalService 已接入 |
| RuntimeSafetyGateCheck | 单个 gate 的状态、passed、block reason、安全摘要和检查时间一致性 |
| RuntimeSafetyGateSnapshot | 必须覆盖全部 required gates；派生 `all_passed` 和去重 `blocking_reasons`；提供 `failed_gates()` / `get_gate()` 纯方法 |
| RuntimeFeatureFlagPolicy | 三个 feature flags 默认 False；`gate_check()` 默认 blocked / `feature_flag_disabled` |
| RuntimeWorkspaceGateInput | workspace 绑定输入；path hint 按 P9-B 同口径脱敏；workspace_bound=False 时 blocked |
| RuntimeBudgetGateInput | estimated/session/daily cost 合同；非负校验；超预算 blocked |
| RuntimeConcurrencyGateInput | active/max 并发合同；active 非负、max 正整数；达到上限 blocked |
| RuntimeSafetyEvaluationInput | 聚合输入；`evaluate()` 只做纯内存 gate 合成，不做 IO |
| ExecutorLaunchRequest | request/executor/preview id、状态、安全快照、人审要求和时间校验；合并 blocked reasons |
| ExecutorLaunchApproval | approval/request id、scope、decision、decided_by/at、confirmation/safe summary 安全校验 |

### 9.3 参考项目只读记录

| 参考路径 | 本轮是否读取 | 只借鉴机制 |
|----------|--------------|------------|
| 本地参考项目 `packages/core/src/lifecycle-manager.ts` | 是 | 状态转换、人工升级、event emission、优先级推断机制 |
| 本地参考项目 `packages/core/src/lifecycle-state.ts` | 是 | lifecycle reason、timestamp 规范化、状态一致性思路 |
| 本地参考项目 `packages/core/src/activity-events.ts` | 是 | event kind/source 分类、append-only audit、敏感信息保护/脱敏意识 |
| 本地参考项目 `packages/core/src/types.ts` | 是 | session/runtime/activity 状态与 reason 分类 |

明确声明：
- 未修改参考项目任何文件。
- 未复制 TypeScript 代码、类型名或包路径。
- 未引入 TypeScript / Node.js / tmux / plugin / Zod / YAML。
- 仅抽象为 Python Pydantic domain contract。

### 9.4 Targeted tests

| 验证命令 | 结果 |
|----------|------|
| `cd runtime/orchestrator && .venv/bin/pytest tests/test_executor_runtime_domain.py tests/test_executor_runtime_safety_domain.py -q` | Pass — 81 passed |

覆盖点：
- feature flags 默认全 False，默认 feature flag gate blocked。
- gate check 的 `status` / `passed` / `block_reason` 一致性。
- safe summary / approval text 拒绝疑似 credential 明文。
- snapshot 必须覆盖全部 required gates，且不能伪造 `all_passed`。
- blocking reasons 自动汇总并去重。
- workspace path hint 对绝对路径、home、Windows 盘符、UNC path 脱敏。
- budget 金额非负，超 session/daily budget blocked。
- concurrency active/max 校验，到达上限 blocked。
- 默认 safety evaluation 不通过；全条件满足时通过。
- no product git write / no secret exposure / no env dump gate 强制阻断。
- launch request 默认不是 approved；failed safety snapshot 不能 approved；id trim 且空值拒绝；时间顺序校验。
- launch approval 默认 pending；approved/rejected/cancelled 需要 decided_by + decided_at。
- domain 文件不导入 service/api/worker，不导入 subprocess/os.popen，不包含禁止字段。
- 参考项目路径未写入 `executor_runtime_safety.py`。

### 9.5 边界声明

| 声明 | 结论 |
|------|------|
| 是否只实现纯 domain contract | 是 |
| 是否实现 RuntimeSafetyGate service | 否 |
| 是否实现 ControlledRuntimeService | 否 |
| 是否实现 executor adapter / fake adapter | 否 |
| 是否实现 API | 否 |
| 是否实现 frontend | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否启动真实外部执行器或创建真实进程 | 否 |
| 是否创建真实 ExecutorSession | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否修改 ApprovalService / approvals route / DB | 否 |
| 是否实现或触发产品运行时 Git 写操作 | 否 |
| 是否修改 apps/web / services / api / workers / migrations / lock 文件 | 否 |

### 9.6 P9-C Gate

| Gate 条件 | 结论 |
|----------|------|
| 新增 runtime safety domain contract | Pass |
| 新增 targeted tests | Pass |
| 测试通过 | Pass — 81 passed |
| feature flag 默认关闭且 blocked | Pass |
| required gates 覆盖完整 | Pass |
| launch request / approval 默认不 approved | Pass |
| no product runtime git write 仍 forbidden | Pass |
| 无 service/API/worker/DB/ApprovalService 接入 | Pass |
| 无 subprocess / shell / os.popen runtime 实现 | Pass |
| 无 forbidden sensitive/runtime fields | Pass |
| 未复制参考项目代码、未引入参考项目技术栈 | Pass |
| **P9-C Gate** | **Pass** |

### 9.7 后续阶段状态

| 阶段/能力 | 状态 | 说明 |
|-----------|------|------|
| P9-D ControlledRuntimeService skeleton / fake adapter | Pass | 已在第 10 节完成 fake service skeleton |
| P9-E read-only runtime API | Not started | 本轮未实现 |
| P9-F frontend readback | Not started | 本轮未实现 |
| P9-G dry-run E2E evidence | Not started | 本轮未实现 |
| P9-H real executor pilot gate design | Not started | 本轮未实现 |
| P9-Final targeted evidence + scoped gate | Not started | 本轮未进入 |
| P9 implementation | Partial | P9-B/C domain + P9-D fake service skeleton；no API/frontend/real runtime |
| P9 real executor launch | Not started | 未启动任何真实执行器 |
| 产品运行时 Git 写操作 | Not started | 产品内部仍严禁 git add/commit/push/PR/merge/branch/delete/reset/checkout/rebase/stash/tag/CI 自动触发 |
| AI Project Director 总闭环 | Partial | P9 未完成前不得写 Pass |

---

## 10. P9-D ControlledRuntimeService skeleton with fake adapter

### 10.1 本轮产出

| 文件 | 动作 | 内容 |
|------|------|------|
| `runtime/orchestrator/app/services/controlled_runtime_service.py` | 新增 | P9-D controlled runtime service skeleton；包含 `ExecutorRuntimeAdapter` protocol、`FakeExecutorAdapter`、`InMemoryRuntimeEventRecorder`、`ControlledRuntimeService` 和安全异常 |
| `runtime/orchestrator/tests/test_controlled_runtime_service.py` | 新增 | P9-D targeted tests，覆盖 fake launch、blocked launch、poll/cancel/kill/cleanup、append-only events、源码边界 |

### 10.2 skeleton 范围

| 能力 | 本轮实现 |
|------|----------|
| `ControlledRuntimeService.launch()` | 接收 P9-C `ExecutorLaunchRequest`；安全快照未全过时返回 blocked fake session 并记录 blocked events；approved + all_passed 时创建 fake session 并交给 fake adapter |
| `get_session()` | 只读返回内存 session copy；不存在返回 `None` |
| `poll()` | fake adapter 将 `running` 推进到 `completed` 或配置为 `idle`，并记录事件 |
| `cancel()` | fake adapter 将可取消 session 推进到 `cancelled`，`exit_reason=cancelled_by_user`，并记录事件 |
| `kill()` | fake adapter 将 running session 推进到 `killed`，`exit_reason=killed_by_user`，并记录事件 |
| `cleanup()` | fake adapter 将 terminal session 推进到 `cleaned_up`，并记录事件 |
| `events_for_session()` | 返回 `RuntimeEventStreamSnapshot`，事件来自内存 append-only recorder |
| `FakeExecutorAdapter` | 只更新 P9-B domain object；使用 fake `process_id=1`；不启动真实外部进程 |
| `InMemoryRuntimeEventRecorder` | append-only 内存事件列表；返回 deep copy；不写 DB、不写文件、不接 SSE/event_stream_service |
| 安全异常 | `ControlledRuntimeError`、`ControlledRuntimeBlockedError`、`ControlledRuntimeNotFoundError`、`ControlledRuntimeInvalidTransitionError` |

### 10.3 参考项目只读记录

| 参考路径 | 本轮是否读取 | 只借鉴机制 |
|----------|--------------|------------|
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-manager.ts` | 是 | lifecycle transition、poll→event emission、cleanup 分离思路 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/session-manager.ts` | 是 | session CRUD、launch/kill/cleanup 生命周期编排思路 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` | 是 | append-only activity event、敏感字段保护意识 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-state.ts` | 是 | lifecycle state/reason/timestamp 规范化思路 |

明确声明：
- 未修改参考项目任何文件。
- 未复制 TypeScript 代码、类型名或包路径。
- 未引入 TypeScript / Node.js / tmux / plugin / Zod / YAML。
- 未把参考项目路径写入 `controlled_runtime_service.py`。

### 10.4 Targeted tests

| 验证命令 | 结果 |
|----------|------|
| `cd runtime/orchestrator && .venv/bin/pytest tests/test_executor_runtime_domain.py tests/test_executor_runtime_safety_domain.py tests/test_controlled_runtime_service.py -q` | Pass — 95 passed |
| `git diff --check` | Pass |

覆盖点：
- service 默认 adapter 是 `FakeExecutorAdapter`。
- safety snapshot failed 时返回 blocked session，不调用 fake adapter launch，并记录 `session.safety_gate_blocked` / `session.blocked`。
- request status 非 approved 时拒绝 launch。
- approved + all gates passed 时创建 `state=running`、`source=fake_adapter` 的 fake session。
- launch 记录 `session.created` / `session.launching` / `session.running`。
- poll 可推进 `running → completed` 或 `running → idle`，并记录事件。
- cancel / kill / cleanup 分别记录 lifecycle event。
- missing session 对 get/poll/cancel/kill/cleanup 返回 `None`。
- in-memory recorder 返回 copy，外部不能删除或改写历史事件。
- event payload message 继续拒绝疑似 secret/token/API key。
- service 文件不导入 `app.api`、`app.workers`、`executor_service`、`openai_provider_executor_service`、`provider_config_service`。
- service 文件不导入或调用 subprocess、os.popen、shell helper、asyncio subprocess。
- service 文件不读取 `~/.codex`、`~/.claude`、`os.environ`。
- service 文件不包含 command/raw args/env/api key/token/native config/process handle/log path 等 forbidden runtime fields。

### 10.5 边界声明

| 声明 | 结论 |
|------|------|
| 是否只实现 ControlledRuntimeService skeleton + FakeExecutorAdapter + in-memory event recorder | 是 |
| 是否实现 API | 否 |
| 是否实现 frontend | 否 |
| 是否接入 DB persistence | 否 |
| 是否接入 ApprovalService / approvals API / DB | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否启动真实外部执行器或创建真实进程 | 否 |
| 是否创建真实 executor session | 否；仅创建内存 fake session |
| 是否使用 subprocess / shell / os.popen / asyncio subprocess | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否调用 `ExecutorService.run_shell_command` | 否 |
| 是否实现或触发产品运行时 Git 写操作 | 否 |
| 是否修改 apps/web / API / workers / migrations / lock 文件 | 否 |
| 是否修改参考项目 | 否 |
| 是否复制参考项目代码或引入参考项目技术栈 | 否 |

### 10.6 P9-D Gate

| Gate 条件 | 结论 |
|----------|------|
| 新增 ControlledRuntimeService skeleton | Pass |
| 新增 FakeExecutorAdapter | Pass |
| 新增 InMemoryRuntimeEventRecorder | Pass |
| blocked launch 不启动 fake adapter | Pass |
| fake launch lifecycle events 完整 | Pass |
| poll/cancel/kill/cleanup 生命周期可验证 | Pass |
| targeted tests 通过 | Pass — 95 passed |
| `git diff --check` 通过 | Pass |
| 无真实 subprocess / real executor launch / API / frontend / DB | Pass |
| 未复制参考项目代码、未引入参考项目技术栈 | Pass |
| **P9-D Gate** | **Pass** |

### 10.7 后续阶段状态

| 阶段/能力 | 状态 | 说明 |
|-----------|------|------|
| P9-E read-only runtime API | Not started | 本轮未实现 |
| P9-F frontend readback | Not started | 本轮未实现 |
| P9-G dry-run E2E evidence | Not started | 本轮未实现 |
| P9-H real executor pilot gate design | Not started | 本轮未实现 |
| P9-Final targeted evidence + scoped gate | Not started | 本轮未进入 |
| P9 implementation | Partial | P9-B/C domain + P9-D fake service skeleton；no API/frontend/real runtime |
| P9 real executor launch | Not started | 未启动任何真实执行器 |
| 产品运行时 Git 写操作 | Not started | 产品内部仍严禁 git add/commit/push/PR/merge/branch/delete/reset/checkout/rebase/stash/tag/CI 自动触发 |
| AI Project Director 总闭环 | Partial | P9 未完成前不得写 Pass |

### 10.8 P9-E read-only runtime API / launch request preview API

本轮新增 P9-E 后端 API，只把 P9-C/P9-D 的纯 domain contract 与 fake runtime service 暴露为安全 DTO：

| 文件 | 动作 | 说明 |
|------|------|------|
| `runtime/orchestrator/app/api/routes/runtime.py` | 新增 | `/runtime` route；safe response DTO；safe request DTO；module-level fake service dependency；in-memory launch request registry |
| `runtime/orchestrator/app/api/router.py` | 最小修改 | include `runtime_router` |
| `runtime/orchestrator/tests/test_runtime_api.py` | 新增 | P9-E targeted API tests + static safety boundary tests |

新增 API endpoints：

| Method | Path | 行为 | 边界 |
|--------|------|------|------|
| GET | `/runtime/sessions` | list fake runtime sessions | 默认空；不启动进程 |
| GET | `/runtime/sessions/{session_id}` | 读取单个 fake session | 不存在返回 404 |
| GET | `/runtime/sessions/{session_id}/events` | 读取 fake event stream | 不存在返回 404 |
| POST | `/runtime/launch-requests` | 创建 launch request 并执行 `RuntimeSafetyEvaluationInput.evaluate()` | 不自动 launch；不 approved |
| GET | `/runtime/launch-requests/{request_id}` | 读取 in-memory launch request | 不存在返回 404 |
| POST | `/runtime/launch-requests/{request_id}/confirm` | fake confirm；all gates passed 后调用 P9-D fake service launch | safety failed 返回 409；confirmation missing 返回 400 |
| POST | `/runtime/sessions/{session_id}/cancel` | cancel fake in-memory session | 不存在返回 404 |

本轮 in-memory registry：

- `InMemoryLaunchRequestRegistry` 只存在于 API route 文件中。
- create/get/approve/session_ids 全部为进程内 dict/list，不写文件、不写数据库。
- confirm 后只调用 `ControlledRuntimeService.launch(request)` 的 fake adapter 路径。
- registry 记录 launched fake session id，用于 API 层 list sessions；不修改 P9-D service 文件。

本轮参考项目只读读取记录：

| 参考来源 | 是否存在并读取 | 只借鉴机制 | 结论 |
|----------|----------------|------------|------|
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/session-manager.ts` | 是 | session create/list/readback 生命周期入口 | 未复制代码 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-manager.ts` | 是 | lifecycle transition / polling / event emission 思路 | 未复制代码 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` | 是 | activity event append + safe event payload 思路 | 未复制代码 |

参考项目使用边界：

- 只学习 session API/readback/event stream 机制。
- 未复制 TypeScript 代码。
- 未引入 TypeScript / Node.js / tmux / plugin / Zod / YAML。
- 未修改参考项目任何文件。
- 未把参考项目路径、包名、类型名搬进主项目实现。

测试证据：

| 命令 | 结果 |
|------|------|
| `cd runtime/orchestrator && .venv/bin/pytest tests/test_executor_runtime_domain.py tests/test_executor_runtime_safety_domain.py tests/test_controlled_runtime_service.py tests/test_runtime_api.py -q` | Pass — 115 passed, 1 warning |

覆盖点：

- GET `/runtime/sessions` 默认返回 200 和空列表。
- POST `/runtime/launch-requests` 默认 feature flag off，返回 blocked / non-approved，blocking reasons 包含 `feature_flag_disabled`。
- all gates passed + human confirmed 时仍不自动创建 session。
- launch request 可按 id 读取。
- safety failed request confirm 返回 409，不创建 session。
- safety passed request confirm 创建 fake session，`state=running`，`source=fake_adapter`。
- confirm fake launch 记录 `session.created` / `session.launching` / `session.running`。
- session readback、event stream、cancel 均可验证。
- missing session/request 返回 404。
- response keys 不包含 command/raw args/env/API key/secret/native config/process handle/log/raw output 等 forbidden fields。
- runtime route 文件未注册 `/execute` / `/run` / `/start` 真实执行 endpoint。
- runtime route 文件不导入 subprocess / os.popen / asyncio subprocess。
- runtime route 文件不导入 `executor_service` / `openai_provider_executor_service` / `provider_config_service`。
- runtime route 文件不读取 `~/.codex` / `~/.claude` / `os.environ`。
- runtime route 文件不接入 ApprovalService / approvals API / DB session。
- router 已 include runtime router。
- P9-B/P9-C/P9-D targeted tests 仍通过。

边界声明：

| 声明 | 结论 |
|------|------|
| 是否只实现 runtime fake API / in-memory launch request registry | 是 |
| 是否实现 frontend | 否 |
| 是否接入 DB persistence | 否 |
| 是否接入真实 ApprovalService | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否导入或使用 subprocess / os.popen / asyncio subprocess | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否创建真实进程或真实 executor session | 否；仅 fake in-memory session |
| 是否调用 `ExecutorService.run_shell_command` | 否 |
| 是否触发产品运行时 Git 写操作 | 否 |
| 是否修改参考项目 | 否 |
| 是否复制参考项目代码、引入参考项目技术栈 | 否 |

P9-E Gate：

| Gate 条件 | 结论 |
|----------|------|
| 新增 read-only runtime API route | Pass |
| 新增 fake-only launch request preview API | Pass |
| 新增 in-memory launch request registry | Pass |
| create request 调用 safety evaluation 且不自动 launch | Pass |
| confirm 仅 fake launch，且 safety failed 时拒绝 | Pass |
| session/events/cancel readback 可验证 | Pass |
| targeted tests 通过 | Pass — 115 passed |
| 无 frontend / DB / ApprovalService / real executor launch | Pass |
| 无真实 subprocess / 本机配置读取 / secret 暴露 | Pass |
| 未复制参考项目代码、未引入参考项目技术栈 | Pass |
| **P9-E Gate** | **Pass** |

后续阶段状态：

| 阶段/能力 | 状态 | 说明 |
|-----------|------|------|
| P9-F frontend readback | Pass | 工作台右栏新增只读 fake runtime readback 面板 |
| P9-G dry-run E2E evidence | Not started | 本轮未实现 |
| P9-H real executor pilot gate design | Not started | 本轮未进入 |
| P9-Final targeted evidence + scoped gate | Not started | 本轮未进入 |
| P9 implementation | Partial | P9-B/C domain + P9-D fake service + P9-E fake API + P9-F read-only frontend；no real runtime |
| P9 real executor launch | Not started | 未启动任何真实执行器 |
| 产品运行时 Git 写操作 | Not started | 产品内部仍严禁 git add/commit/push/PR/merge/branch/delete/reset/checkout/rebase/stash/tag/CI 自动触发 |
| AI Project Director 总闭环 | Partial | P9 未完成前不得写 Pass |

---

### 10.9 P9-F frontend read-only runtime readback

本轮新增 P9-F 前端只读展示，只读取 P9-E fake runtime API 的 GET endpoint，不实现真实 executor launch，不实现 launch / confirm / cancel / kill / cleanup 控制入口。

新增/修改前端文件：

| 文件 | 动作 | 说明 |
|------|------|------|
| `apps/web/src/features/runtime/types.ts` | 新增 | safe runtime readback DTO；只覆盖 session、workspace、process、usage、event payload 的只读字段 |
| `apps/web/src/features/runtime/api.ts` | 新增 | 只读 GET client：`/runtime/sessions`、`/runtime/sessions/{session_id}`、`/runtime/sessions/{session_id}/events` |
| `apps/web/src/features/runtime/hooks.ts` | 新增 | React Query hooks；无 mutation、无 POST |
| `apps/web/src/pages/workbench/components/RuntimeReadbackPanel.tsx` | 新增 | 工作台右栏 P9 fake runtime readback 面板 |
| `apps/web/src/pages/workbench/WorkbenchPage.tsx` | 修改 | 挂载 `RuntimeReadbackPanel` |
| `apps/web/src/pages/workbench/components/ProjectDirectorInboxPanel.tsx` | 修改 | 增加只读提醒：fake session 观察不代表真实执行器已启动 |
| `apps/web/src/pages/workbench/components/__tests__/RuntimeReadbackPanel.test.mjs` | 新增 | targeted static contract spec |

展示位置：

- 工作台 `/workbench` 右栏：`P9 受控运行时只读观察`。
- 主管收件箱：仅增加只读提醒，未强行重构 inbox。

只读 API endpoint 列表：

- `GET /runtime/sessions`
- `GET /runtime/sessions/{session_id}`
- `GET /runtime/sessions/{session_id}/events`

明确未调用的 endpoint：

- 未调用 `POST /runtime/launch-requests`
- 未调用 `POST /runtime/launch-requests/{id}/confirm`
- 未调用 `POST /runtime/sessions/{id}/cancel`
- 未调用任何 `/execute` / `/run` / `/start` / 真实 `/launch` endpoint

面板展示内容：

- sessions 列表：`session_id` 短显示 + copy、`executor_id`、`state`、`source`、`project_id` / `task_id` / `run_id`、`created_at` / `updated_at`。
- selected session detail：安全状态、source、绑定信息、fake process snapshot、usage snapshot、blocking reasons、workspace binding；`workspace_path_hint` 只显示尾部脱敏提示。
- event stream：`event_type`、`timestamp`、`payload.message`、`append_only` 标识。
- 空态：`暂无 fake runtime session`。
- 安全说明：当前仅展示 fake runtime / read-only runtime sessions；不启动 Codex / Claude Code / DeepSeek；不执行 subprocess / shell；不触发产品运行时 Git 写。

参考项目实际读取路径：

| 参考路径 | 读取结果 | 本轮只借鉴机制 |
|----------|----------|----------------|
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/session-manager.ts` | 已只读读取 | session readback 思路：只读查看 session，不提供 runtime control |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-manager.ts` | 已只读读取 | lifecycle 状态变化可观察，但 P9-F 不执行状态变化 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-state.ts` | 已只读读取 | 将内部生命周期状态转成人能理解的展示文案 |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` | 已只读读取 | activity event timeline 按时间展示、强调 append-only |
| `/Users/kk/project explore/agent-orchestrator/packages/core/src/types.ts` | 已只读读取 | session / runtime / activity 状态边界分层 |

参考项目边界：

- 未修改参考项目任何文件。
- 未复制参考项目代码。
- 未引入参考项目技术栈。
- 未引入 tmux / terminal runtime / plugin hot reload / YAML / Zod / real executor adapter。
- 新增前端源码中不出现参考项目路径或包名。

验证命令：

| 命令 | 结果 |
|------|------|
| `cd apps/web && node --test src/pages/workbench/components/__tests__/RuntimeReadbackPanel.test.mjs` | Pass — 6 tests passed |
| `cd apps/web && npm run build` | Pass — `tsc -b && vite build` 通过；Vite 仅提示 chunk size warning |
| `git diff --check` | Pass — 无 whitespace error |

targeted spec 覆盖：

- 默认空 sessions 显示 `暂无 fake runtime session`。
- 有 fake session 字段时展示 `executor_id` / `state` / `source`。
- `source=fake_adapter` 时明确标注 `fake runtime only`。
- event stream 展示动态 `event_type`，代表性事件覆盖 `session.created` / `session.running`。
- `RuntimeReadbackPanel` 不渲染启动 / 执行 / 确认 / 取消 / kill / cleanup 控制按钮。
- readback client 不调用 `POST /runtime/launch-requests`。
- readback client 不调用 `POST /runtime/launch-requests/{id}/confirm`。
- readback client 不调用 `POST /runtime/sessions/{id}/cancel`。
- 前端 readback 源码不包含 command / raw args / env vars / API key / token / secret / native config path / process handle / log / raw output / raw error 等不安全 DTO 字段。
- 前端不展示产品运行时 Git 写入口。
- 参考项目路径没有出现在新增前端源码中。

边界声明：

| 声明 | 结论 |
|------|------|
| 是否只实现 frontend read-only runtime readback | 是 |
| 是否修改 `runtime/orchestrator` 后端代码 | 否 |
| 是否新增后端 API / service / schema / migration | 否 |
| 是否调用 POST launch / confirm / cancel API | 否 |
| 是否实现启动 / 执行 / 确认 / 取消 / kill / cleanup 按钮 | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否调用 subprocess / shell | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否触发产品运行时 Git 写操作 | 否 |
| 是否修改参考项目 | 否 |
| 是否复制参考项目代码、引入参考项目技术栈 | 否 |

P9-F Gate：

| Gate 条件 | 结论 |
|----------|------|
| 前端只读取 P9-E fake runtime GET endpoint | Pass |
| DTO 不包含不安全字段 | Pass |
| loading / error / empty states | Pass |
| fake runtime / no real executor launch 安全说明 | Pass |
| selected session detail + event stream readback | Pass |
| governance inbox linkage 最小提醒 | Pass |
| 无 POST launch / confirm / cancel 调用 | Pass |
| 无真实执行器启动 / subprocess / 产品运行时 Git 写 | Pass |
| targeted frontend spec + build 通过 | Pass |
| **P9-F Gate** | **Pass** |

后续阶段状态：

| 阶段/能力 | 状态 | 说明 |
|-----------|------|------|
| P9-G dry-run E2E evidence | Not started | 本轮未实现 |
| P9-H real executor pilot gate design | Not started | 本轮未进入 |
| P9-Final targeted evidence + scoped gate | Not started | 本轮未进入 |
| P9 implementation | Partial | P9-B/C domain + P9-D fake service + P9-E fake API + P9-F read-only frontend；no real runtime |
| P9 real executor launch | Not started | 未启动任何真实执行器 |
| 产品运行时 Git 写操作 | Not started | 产品内部仍严禁 git add/commit/push/PR/merge/branch/delete/reset/checkout/rebase/stash/tag/CI 自动触发 |
| AI Project Director 总闭环 | Partial | P9 未完成前不得写 Pass |

---

## 11. P9 Gate 草案

### 11.1 P9-A Gate（本轮）

| # | Gate 条件 | 结论 |
|---|----------|------|
| 1 | origin/main HEAD 确认 | Pass — `91c33ed1924c6052d3c7e075cb682e96887cafb2` |
| 2 | P8 Executor Config Discovery 前置 | Pass |
| 3 | P9-A ledger 完成 | Pass — 本文档 |
| 4 | P8→P9 前置能力映射 ≥10 项 | Pass — 10 项全覆盖 |
| 5 | 现状审计矩阵 ≥9 个维度 | Pass — 9 个维度全覆盖 |
| 6 | P9 安全模型草案完整 | Pass — 14 条安全原则 + feature flag + launch 流程 + 红线 |
| 7 | P9 子阶段拆分到 P9-Final | Pass — 第 6 节 |
| 8 | 明确 real executor pilot 不在 P9-A 至 P9-H | Pass — 6.3 明确声明 |
| 9 | 参考项目机制映射完成 | Pass — 第 7 节 |
| 10 | 未修改 runtime/orchestrator | Pass |
| 11 | 未修改 apps/web | Pass |
| 12 | 未修改 tests | Pass |
| 13 | 未修改 migrations | Pass |
| 14 | 未读取或记录 secret/token/API key/env 明文 | Pass |
| 15 | 未实现任何代码 | Pass |
| 16 | 未启动外部执行器 | Pass |
| 17 | 未使用 subprocess / shell / os.popen | Pass |
| 18 | 未创建 ExecutorSession | Pass |
| 19 | P9 implementation 仍 Partial | Pass — P9-B/C domain + P9-D fake service skeleton；no API/frontend/real runtime |
| 20 | P9 real executor launch 仍 Not started | Pass |
| 21 | 产品运行时 Git 写操作仍 Not started | Pass |
| 22 | AI Project Director 总闭环仍 Partial | Pass |
| **P9-A Gate** | **Pass** | — |

### 11.2 P9 Final Gate 草案

| # | Gate 条件 | 状态 |
|---|----------|------|
| 1 | no secret exposure（response/log/event 中无 API key/token 明文） | 待 P9-D/E 验证 |
| 2 | no env value exposure（不 dump 环境变量） | 待 P9-D/E 验证 |
| 3 | no uncontrolled subprocess（所有进程启动经过 ControlledRuntimeService） | 待 P9-D/G 验证 |
| 4 | no runtime without explicit human confirmation | 待 P9-C/E 验证 |
| 5 | no runtime without workspace gate | 待 P9-C 验证 |
| 6 | no runtime without cost/budget gate | 待 P9-C 验证 |
| 7 | no product runtime Git write | 持续验证 |
| 8 | no PR / merge automation | 持续验证 |
| 9 | events/audit required（所有 runtime 事件 append-only） | 待 P9-B/D 验证 |
| 10 | cancellation/timeout required（每个 session 可取消/超时） | 待 P9-D 验证 |
| 11 | feature flag default off（EXECUTOR_RUNTIME_ENABLED == False） | 待 P9-C 验证 |
| 12 | AI Project Director 总闭环仍 Partial（P9 完成 ≠ 总闭环完成） | 持续成立 |
| 13 | real executor pilot 未经用户独立确认不得启动 | 待 P9-H 验证 |
| **P9 Final Gate** | **Not started** | — |

---

## 12. P9-A 历史结论

| 结论项 | 状态 |
|--------|------|
| **P9-A ledger** | **Pass** — 本文档完成 |
| **P9-A Gate** | **Pass** — 22 项条件全部通过 |
| P9 Controlled Runtime implementation | **Not started** — P9-B 至 P9-H 未开始 |
| P9 real executor launch | **Not started** — 无真实 Codex / Claude Code / DeepSeek 进程启动 |
| 产品运行时 Git 写操作 | **Not started** — 全线禁止 |
| AI Project Director 总闭环 | **Partial** — P9 未完成前不得写 Pass |

---

## 13. P9-A 历史收口声明

### 13.1 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `11-P9受控运行时与执行器调度/P9受控运行时与执行器调度总账与阶段设计-20260608.md` | **新增** | P9-A ledger + 现状审计 + 安全模型 + 子阶段拆分 | 否 |
| `00-总览与索引/AI项目主管文档索引.md` | **修改** | 追加 P9-A ledger 链接和状态行 | 否 |

### 13.2 边界声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否实现 ExecutorRuntimeSession domain | 否 |
| 是否实现 ControlledRuntimeService | 否 |
| 是否实现 RuntimeSafetyGate | 否 |
| 是否实现 executor adapter（codex/claude/deepseek） | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否使用 subprocess / shell / os.popen | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否创建 ExecutorSession | 否 |
| 是否产品运行时 Git 写操作 | 否 |
| 是否修改参考项目 | 否 |
| 是否复制参考项目代码 | 否 |
| 是否引入 TypeScript / Node.js 架构 | 否 |
| 是否引入 tmux / plugin 热加载 / Zod / YAML / 8-slot | 否 |
| 是否进入 P9-B | 否 |
| 是否承诺 real executor pilot 启动时间 | 否 |
| 是否只新增 ledger 文档 | 是 |

### 13.3 参考项目只读检查记录

| 参考来源 | 读取文件 | 只借鉴机制 | 结论 |
|----------|---------|-----------|------|
| AO core lifecycle-manager | `packages/core/src/lifecycle-manager.ts`（L1-80） | 状态机轮询 + 状态转换检测 + 事件发射 + 反应引擎 + 人工通知升级 | **未照搬代码** |
| AO core lifecycle-state | `packages/core/src/lifecycle-state.ts` | CanonicalSessionLifecycle 规范化状态 + reason + timestamps | **未照搬代码** |
| AO core types | `packages/core/src/types.ts`（L1-80） | SessionId/Kind、CanonicalSessionState/Reason、CanonicalRuntimeState、PRState/Reason | **未照搬代码** |
| AO core plugin-registry | `packages/core/src/plugin-registry.ts`（L1-60） | 8-slot plugin architecture、built-in + external plugins | **未照搬代码** |
| AO core config | `packages/core/src/config.ts`（L1-60） | YAML 配置加载、session prefix 生成、全局配置管理 | **未照搬代码** |

---

## 14. P9-G：fake-only dry-run end-to-end evidence

> **审计日期**: 2026-06-08
> **当前 HEAD**: `c7faf54836fa7e0a2167fbfd2c47e20848c409d3`（feat: add P9 runtime readback panel）
> **审计执行器**: DeepSeek

### 14.1 P9-G 范围定义

| # | P9-G 做什么 | P9-G 不做什么 |
|---|-----------|-------------|
| 1 | fake-only dry-run E2E evidence | 不实现新代码 |
| 2 | 验证 P9-B/C/D/E/F 链路可被 targeted tests + build 覆盖 | 不启动真实 Codex / Claude Code / DeepSeek |
| 3 | 审计全链路安全边界 | 不创建真实 executor process |
| 4 | 聚合 backend + frontend evidence | 不做 DB persistence |
| 5 | 确认 fake adapter only 闭环 | 不接真实 ApprovalService |
| 6 | 确认 frontend read-only | 不做产品运行时 Git 写 |

### 14.2 P9 dry-run E2E 链路映射

| 子阶段 | 已完成能力 | 证据文件 | 测试覆盖 | 仍不是哪些真实能力 |
|--------|----------|---------|---------|-----------------|
| P9-B domain | ExecutorRuntimeSession / RuntimeState / RuntimeEvent 纯 domain contract | `executor_runtime.py`（561 行） | `test_executor_runtime_domain.py` | 不是运行时数据库记录；不是真实 session |
| P9-C safety | RuntimeSafetyGate / LaunchRequest / LaunchApproval + 13 required gates | `executor_runtime_safety.py`（640 行） | `test_executor_runtime_safety_domain.py` | 不是真实 feature flag 检查；不是真实 budget/concurrency enforcement |
| P9-D fake service | ControlledRuntimeService + FakeExecutorAdapter + InMemoryRuntimeEventRecorder | `controlled_runtime_service.py`（375 行） | `test_controlled_runtime_service.py` | 不是真实 subprocess 管理；不是真实 executor adapter（codex/claude/deepseek） |
| P9-E fake API | GET sessions/events + POST fake launch-request/confirm/cancel | `runtime.py`（632 行） | `test_runtime_api.py` | 不是真实 runtime launch；confirm/cancel 是 fake-only |
| P9-F frontend | GET-only RuntimeReadbackPanel + useQuery hooks | `RuntimeReadbackPanel.tsx`（489 行）、`api.ts`（25 行） | `RuntimeReadbackPanel.test.mjs`（6 tests） | 不是真实 runtime 控制面板；无 launch/confirm/cancel 按钮 |

### 14.3 Targeted evidence commands

| # | 命令 | 结果 |
|---|------|------|
| 1 | `cd runtime/orchestrator && .venv/bin/pytest tests/test_executor_runtime_domain.py tests/test_executor_runtime_safety_domain.py tests/test_controlled_runtime_service.py tests/test_runtime_api.py -q` | **115 passed, 1 warning**（Starlette deprecation，与 P9 无关） |
| 2 | `cd apps/web && npm run build` | **✓ built in 1.28s**（511 modules） |
| 3 | `cd apps/web && node --test src/pages/workbench/components/__tests__/RuntimeReadbackPanel.test.mjs` | **6 passed, 0 fail** |
| 4 | `git diff --check` | **Pass** |

### 14.4 静态边界审计：backend rg 命中摘要

| 搜索词 | app 代码命中 | 判定 |
|--------|-------------|------|
| `subprocess` | 0（仅 test 否定断言） | **Pass** |
| `os.popen` | 0（仅 test 否定断言） | **Pass** |
| `shell=True` | 0（仅 test 否定断言） | **Pass** |
| `asyncio.create_subprocess` | 0（仅 test 否定断言） | **Pass** |
| `ExecutorService` | 0 | **Pass** |
| `run_shell_command` | 0（仅 test 否定断言） | **Pass** |
| `api_key` | 0 DTO 字段（仅 domain regex pattern + test forbidden list） | **Pass — 安全机制** |
| `token_value` | 0（仅 test forbidden list） | **Pass** |
| `auth_token` | 0（仅 test forbidden list） | **Pass** |
| `secret` | 仅 domain regex pattern + enum values + field names | **Pass — 安全机制** |
| `native_config_path` | 0（仅 test forbidden list） | **Pass** |
| `cli_path` | 0（仅 test forbidden list） | **Pass** |
| `process_handle` | 0（仅 test forbidden list） | **Pass** |
| `log_path` | 0（仅 test forbidden list） | **Pass** |
| `raw_output` | 0（仅 test forbidden list） | **Pass** |
| `raw_error` | 0（仅 test forbidden list） | **Pass** |

**app 代码中的合法命中**：
- `_SECRET_TEXT_PATTERN` — domain 层的 secret text 检测正则，用于拒绝含敏感文本的 summary/message
- `no_secret_exposure: bool` / `NO_SECRET_EXPOSURE` / `SECRET_EXPOSURE_RISK` — safety gate 定义，非敏感值
- `POST /runtime/launch-requests/{id}/confirm` / `POST /runtime/sessions/{id}/cancel` — P9-E fake-only API endpoints

**tests 中的命中**：所有 forbidden 字段命中均为安全断言集合，验证 DTO 不包含这些字段。POST confirm/cancel 调用是 fake API 的 targeted test。

### 14.5 静态边界审计：frontend rg 命中摘要

| 搜索词 | app 代码命中 | 判定 |
|--------|-------------|------|
| `subprocess` / `os.popen` / `shell=True` | 0 | **Pass** |
| `Codex` / `Claude Code` / `DeepSeek` | 仅 safety disclaimers 中显示 "不启动 Codex / Claude Code / DeepSeek" | **Pass — 安全声明** |
| `api_key` / `token_value` / `auth_token` / `secret` | 0 | **Pass** |
| `POST /runtime` | 0（api.ts 仅 3 个 GET 函数） | **Pass** |
| `/launch-requests` | 仅 test 中做否定 RegExp 匹配 | **Pass** |
| `/confirm` / `/cancel` | 仅 test 中做否定 RegExp 匹配 | **Pass** |
| `git add` / `git commit` / `git push` / `merge` | 仅 test 中做否定 label 检查 | **Pass** |
| `useMutation` | 0（hooks.ts 仅 3 个 useQuery） | **Pass** |
| `requestJson.*POST` | 0（api.ts 仅 requestJson with GET paths） | **Pass** |

**关键结论**：
- 前端 api.ts 只调用 3 个 GET endpoints（`/runtime/sessions`、`/runtime/sessions/{id}`、`/runtime/sessions/{id}/events`）
- 前端 hooks.ts 只使用 `useQuery`（只读），无 `useMutation`
- 前端 RuntimeReadbackPanel 无任何 POST/launch/confirm/cancel/kill 控制按钮
- 前端 types.ts 只定义 readback 接口，无 input/control 类型
- 安全声明显示 "当前仅展示 fake runtime / 不启动执行器 / 不执行 subprocess / 不触发 Git 写"

### 14.6 P9-G Gate

| # | Gate 条件 | 结论 |
|---|----------|------|
| 1 | backend targeted tests pass（115 passed） | **Pass** |
| 2 | frontend build pass | **Pass** |
| 3 | frontend targeted source test pass（6 passed） | **Pass** |
| 4 | git diff --check pass | **Pass** |
| 5 | no real executor launch | **Pass** |
| 6 | no subprocess / shell / os.popen / asyncio subprocess | **Pass** |
| 7 | no env/secret exposure in DTO | **Pass** |
| 8 | no DB persistence | **Pass**（in-memory only） |
| 9 | no ApprovalService coupling | **Pass** |
| 10 | no frontend runtime control buttons（launch/confirm/cancel/kill） | **Pass** |
| 11 | no product runtime Git write | **Pass** |
| 12 | P9 real executor launch remains Not started | **Pass** |
| 13 | AI Project Director 总闭环仍 Partial | **Pass** |
| **P9-G Gate** | **Pass** | — |

### 14.7 P9-G 结论

| 结论项 | 状态 |
|--------|------|
| **P9-G dry-run E2E evidence** | **Pass** — fake-only full-stack evidence complete |
| **P9-G Gate** | **Pass** — 13 项条件全部通过 |
| P9 Controlled Runtime implementation | **Partial** — fake dry-run E2E evidence only；no real runtime |
| P9-H real executor pilot gate design | **Not started** |
| P9-Final targeted evidence + P9 Gate | **Not started** |
| P9 real executor launch | **Not started** |
| 产品运行时 Git 写操作 | **Not started** |
| AI Project Director 总闭环 | **Partial** |

### 14.8 P9-G 收口声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否（仅为审计现有 P9-B/C/D/E/F 产物） |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否使用 subprocess / shell / os.popen | 否 |
| 是否创建真实 ExecutorRuntimeSession | 否 |
| 是否产品运行时 Git 写操作 | 否 |
| 是否进入 P9-H | 否 |
| 是否修改参考项目 | 否 |
| 是否只做文档审计 | 是 |

---

## 15. P9-H：real executor pilot gate design（still default off）

> **设计日期**: 2026-06-08
> **当前 HEAD**: `bf236fd0023e4282930be751c8468b48ec436774`（docs: add P9 fake runtime dry-run evidence）
> **设计执行器**: DeepSeek

### 15.1 P9-H 范围定义

| # | P9-H 做什么 | P9-H 不做什么 |
|---|-----------|-------------|
| 1 | 定义未来真实执行器 pilot 的准入门槛（gate checklist） | 不启动真实执行器 |
| 2 | 设计 pilot execution sequence（从用户确认到 session 管理的完整流程） | 不实现 Codex / Claude Code / DeepSeek adapter |
| 3 | 定义 pilot 的红线（hard blockers） | 不读取用户本机真实配置 |
| 4 | 明确 P9-H 与 P9-Final 的边界 | 不读取 API key/token/secret |
| 5 | 确认所有 gate 在 P9-B/C/D/E/F/G 中的 fake 前置状态 | 不实现产品运行时 Git 写 |
| 6 | 给出 pilot 前仍需补齐的工作清单 | 不修改任何代码 |

### 15.2 Real executor pilot 红线

以下红线必须在任何真实 executor pilot 启动前满足。每一项不满足 = **Blocked**。

| # | 红线 | 说明 |
|---|------|------|
| 1 | 没有用户当轮明确确认，不得启动 | 每次 pilot 都需要用户独立、当轮的显式确认，不能复用历史确认 |
| 2 | 没有 feature flag，不得启动 | `EXECUTOR_RUNTIME_ENABLED` 和 `REAL_EXECUTOR_PILOT_ENABLED` 必须同时为 True |
| 3 | 没有 executor readiness available，不得启动 | executor status == AVAILABLE，不得用 fake-ready 冒充 |
| 4 | 没有 launch preview，不得启动 | 每次启动前必须生成并展示 launch preview |
| 5 | 没有 workspace gate，不得启动 | workspace 必须存在、合法、已绑定 |
| 6 | 没有 human approval，不得启动 | 必须通过 RuntimeLaunchRequest→HumanConfirmation→LaunchApproval 流程 |
| 7 | 没有 timeout/cancel/kill/cleanup，不得启动 | 每个 session 必须有完整的生命周期管理 |
| 8 | 没有 event/audit，不得启动 | 每个 runtime 事件必须 append-only 记录 |
| 9 | 没有 budget/cost guard，不得启动 | 必须在 daily/session budget 范围内 |
| 10 | 没有 secret redaction，不得启动 | 任何输出/日志/事件中不得出现 API key/token/secret 明文 |
| 11 | 不允许产品运行时 Git 写 | gate_allows_write 永不为 True |
| 12 | 不允许 PR / merge 自动化 | P9 不触发 GitHub PR 或 merge 操作 |
| 13 | 不允许默认读取 ~/.codex、~/.claude、环境变量值、API key/token/secret | 配置读取必须是用户显式授权的白名单模式 |
| 14 | 不允许把 P8/P9 fake-ready 误判为 real-ready | fake adapter 的 Pass 不等于真实执行器可用 |

### 15.3 Pilot gate checklist

以下 21 项 gate 构成 real executor pilot 的完整准入门槛。每项标注 P9 前置状态和 pilot 前是否需要补齐。

| # | Gate | 目的 | 不满足时 | P9 fake 前置状态 | Pilot 前需补齐 |
|---|------|------|---------|-----------------|-------------|
| 1 | `user_explicit_pilot_confirmation` | 用户当轮独立确认 | **Blocked** | P9-C LaunchApproval domain 已有 | 需要真实 UI 确认卡片（非 fake） |
| 2 | `feature_flag_executor_runtime_enabled` | runtime 总开关 | **Blocked** | P9-C RuntimeFeatureFlagPolicy domain | 需要真实 feature flag 配置源 |
| 3 | `feature_flag_real_executor_pilot_enabled` | pilot 二级开关 | **Blocked** | P9-C domain 已有字段 | 需要真实配置源 |
| 4 | `executor_profile_available` | executor 存在且可用 | **Blocked** | P8-C discovery service + P8-D API | 需要真实 probe（非 noop） |
| 5 | `executor_config_discovery_safe` | 配置安全可读 | **Blocked** | P8-C discovery service（noop） | 需要真实 safe probe |
| 6 | `launch_preview_reviewed` | 用户已查看预览 | **Blocked** | P8-E launch preview service + API | P8 已有，可复用 |
| 7 | `workspace_bound_and_safe` | workspace 合法 | **Blocked** | P9-B ExecutorRuntimeWorkspaceBinding + P9-C RuntimeWorkspaceGateInput | 需要真实 workspace 验证 |
| 8 | `task_scope_readback` | 任务范围可读 | **Blocked** | P9-E RuntimeSessionResponse 含 task/project/run 绑定 | P9-E 已有，可复用 |
| 9 | `cost_budget_limit_confirmed` | 预算限制已确认 | **Blocked** | P9-C RuntimeBudgetGateInput domain + gate_check() | 需要真实 BudgetGuardService 集成 |
| 10 | `concurrency_limit_confirmed` | 并发限制已确认 | **Blocked** | P9-C RuntimeConcurrencyGateInput domain + gate_check() | 需要真实 active session 计数 |
| 11 | `timeout_configured` | 超时已配置 | **Blocked** | P9-C safety gate timeout_configured 字段 | 需要真实 timeout 配置源 |
| 12 | `cancellation_supported` | 用户可取消 | **Blocked** | P9-D cancel() + P9-E POST /cancel（fake only） | 需要真实进程 kill |
| 13 | `kill_switch_supported` | 用户可强制终止 | **Blocked** | P9-D kill()（fake only） | 需要真实进程 SIGKILL |
| 14 | `cleanup_supported` | session 可清理 | **Blocked** | P9-D cleanup()（fake only） | 需要真实 workspace 清理 |
| 15 | `event_audit_enabled` | 事件审计开启 | **Blocked** | P9-B RuntimeEvent + P9-D InMemoryRuntimeEventRecorder | 需要持久化 event storage |
| 16 | `no_secret_exposure` | 无密钥泄露 | **Blocked** | P9-C safety gate + domain _SECRET_TEXT_PATTERN | P9 已有，持续验证 |
| 17 | `no_env_dump` | 无环境变量泄露 | **Blocked** | P9-C safety gate no_env_dump | P9 已有，持续验证 |
| 18 | `no_product_runtime_git_write` | 无产品 Git 写 | **Blocked** | P9-C safety gate no_product_git_write | P9 已有，持续验证 |
| 19 | `no_pr_merge_automation` | 无 PR/merge 自动化 | **Blocked** | P9 safety model §5 红线 | P9 已有，持续验证 |
| 20 | `rollback_plan_exists` | 回滚方案就绪 | **Blocked** | 不适用（P9 新增 gate） | 需设计 |
| 21 | `dry_run_evidence_passed` | dry-run 测试通过 | **Blocked** | P9-G fake dry-run E2E 已完成 | 需要 real-adapter dry-run（fake adapter → real adapter 转换验证） |

### 15.4 Pilot execution sequence design

以下为 real executor pilot 的完整执行序列设计。**仅设计，不实现。** 步骤 8 是真实执行器启动点，**本轮不实现**。

```text
[Pilot Execution Sequence — Design Only]

Step  1: User asks for one explicit pilot
         Input: User types "I want to run a pilot with Codex to fix bug #42"
         Gate:  user_explicit_pilot_confirmation (preliminary)

Step  2: System reads current executor readiness
         Action: Call P8 ExecutorConfigDiscoveryService with a real probe
         Gate:  executor_profile_available + executor_config_discovery_safe

Step  3: System renders launch preview
         Action: Call P8 POST /executors/{id}/launch-preview
         Gate:  launch_preview_reviewed

Step  4: System renders pilot risk readback
         Action: Display estimated cost, workspace path (redacted), timeout, safety flags
         Gate:  cost_budget_limit_confirmed + concurrency_limit_confirmed

Step  5: User gives explicit confirmation
         Action: User clicks "I confirm this pilot launch" in UI
         Gate:  user_explicit_pilot_confirmation (final)

Step  6: RuntimeSafetyGate evaluates
         Action: 13 required gates checked against RuntimeSafetyEvaluationInput
         Result: RuntimeSafetyGateSnapshot.all_passed == True (or Blocked)
         Gate:   ALL 21 pilot gates from §15.3

Step  7: ControlledRuntimeService creates runtime session
         Action: session_id generated, state = LAUNCHING, event stream started
         Gate:   event_audit_enabled + timeout_configured + cancellation_supported

Step  8: 【NOT IMPLEMENTED IN P9-H】
         RealExecutorAdapter pilot would start only if enabled
         Action: Real subprocess launch of Codex CLI / Claude Code CLI / DeepSeek API
         Requires: REAL_EXECUTOR_PILOT_ENABLED == True (user explicitly toggled)
         Requires: RealExecutorAdapter implemented (not FakeExecutorAdapter)
         Requires: Process handle tracked, stdout/stderr captured, activity detected

Step  9: Event/audit records every transition
         Action: Every state change → RuntimeEvent emitted → persisted
         Gate:   append_only audit trail

Step 10: Timeout/cancel/kill/cleanup guard remains active
         Action: Background watchdog monitors timeout, user can cancel/kill at any time
         Action: On terminal state → cleanup workspace binding, emit final event

Step 11: No product runtime Git write unless later independent Git gate is created
         Action: gate_allows_write remains False throughout the session
         Gate:   no_product_runtime_git_write + no_pr_merge_automation
```

**关键声明**：步骤 8（RealExecutorAdapter pilot start）**不在 P9-H 范围内**。它是一个独立决策点，需要用户在未来单独确认。

### 15.5 P9-H 与 P9-Final 边界

| 边界 | P9-H 状态 | P9-Final 状态 |
|------|---------|-------------|
| Real executor pilot gate design 完成 | **Pass** | — |
| 21-item pilot gate checklist 完成 | **Pass** | — |
| Pilot execution sequence 设计完成 | **Pass**（design only） | — |
| Real executor launch | **Not started**（Step 8 未实现） | **Not started** |
| RealExecutorAdapter 实现 | **Not started** | **Not started** |
| Real pilot execution | **Not started** | **Not started** |
| Targeted evidence + P9 scoped gate | — | **Not started** |

**澄清**：
- P9-H 把 real executor pilot gate design 判 Pass，不把 real executor launch 判 Pass。
- P9-Final 收口时只能收口 scoped P9：fake dry-run + pilot gate design。
- 真实 pilot 启动需要未来独立阶段或用户单独授权。

### 15.6 P9-H Gate

| # | Gate 条件 | 结论 |
|---|----------|------|
| 1 | ledger chapter numbering fixed（12→14, no duplicates） | **Pass** |
| 2 | real executor pilot gate checklist complete（21 items） | **Pass** |
| 3 | pilot execution sequence designed（11 steps, Step 8 marked as not implemented） | **Pass** |
| 4 | P9-H/P9-Final boundary clearly defined | **Pass** |
| 5 | no real executor launch | **Pass** |
| 6 | no subprocess / shell / os.popen | **Pass** |
| 7 | no env/secret read | **Pass** |
| 8 | no runtime/orchestrator code changes | **Pass** |
| 9 | no apps/web code changes | **Pass** |
| 10 | no tests changes | **Pass** |
| 11 | no migrations changes | **Pass** |
| 12 | no product runtime Git write | **Pass** |
| 13 | no reference project modification | **Pass** |
| 14 | no reference project code copied | **Pass** |
| 15 | P9-Final Not started | **Pass** |
| 16 | P9 real executor launch Not started | **Pass** |
| 17 | AI Project Director total closure Partial | **Pass** |
| **P9-H Gate** | **Pass** | — |

### 15.7 P9-H 结论

| 结论项 | 状态 |
|--------|------|
| **P9-H real executor pilot gate design** | **Pass** — design only；still default off |
| **P9-H Gate** | **Pass** — 17 项条件全部通过 |
| P9-Final targeted evidence + P9 Gate | **Not started** |
| P9 Controlled Runtime implementation | **Partial** — fake dry-run + pilot gate design；no real runtime |
| P9 real executor launch | **Not started** — Step 8 not implemented |
| 产品运行时 Git 写操作 | **Not started** |
| AI Project Director 总闭环 | **Partial** |

### 15.8 P9-H 收口声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否实现 RealExecutorAdapter | 否 |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否使用 subprocess / shell / os.popen | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否创建真实 ExecutorRuntimeSession | 否 |
| 是否产品运行时 Git 写操作 | 否 |
| 是否修改参考项目 | 否 |
| 是否复制参考项目代码 | 否 |
| 是否进入 P9-Final | 否 |
| 是否承诺 real executor pilot 启动时间 | 否 |
| 是否只做文档设计 | 是 |

---

## 16. P9-Final：targeted evidence + P9 scoped gate

> **审计日期**: 2026-06-08
> **当前 HEAD**: `c85442d5dc0cbb6e7c0bcbb102737d0bd8e4d0c1`（docs: add P9 real executor pilot gate design）
> **审计执行器**: DeepSeek

### 16.1 P9 子阶段总览

| 子阶段 | 内容 | Gate |
|--------|------|------|
| P9-A | Controlled Runtime ledger + current-state audit + safety boundary | **Pass** |
| P9-B | ExecutorRuntimeSession / RuntimeState / RuntimeEvent 纯 domain contract | **Pass** |
| P9-C | RuntimeSafetyGate / LaunchRequest / LaunchApproval 纯 domain contract | **Pass** |
| P9-D | ControlledRuntimeService skeleton + FakeExecutorAdapter + InMemoryRuntimeEventRecorder | **Pass** |
| P9-E | read-only runtime API + fake launch request preview API | **Pass** |
| P9-F | frontend read-only fake runtime readback | **Pass** |
| P9-G | fake-only dry-run E2E evidence | **Pass** |
| P9-H | real executor pilot gate design（still default off） | **Pass** |
| **P9-Final** | targeted evidence + P9 scoped gate | **Pass** |

### 16.2 P9 scoped completed capabilities

| # | 能力 | 状态 | 证据 |
|---|------|------|------|
| 1 | ExecutorRuntimeSession / RuntimeState / RuntimeEvent domain | **完成** | `executor_runtime.py`（561 行） |
| 2 | RuntimeSafetyGate / LaunchRequest / LaunchApproval domain | **完成** | `executor_runtime_safety.py`（640 行） |
| 3 | ControlledRuntimeService skeleton | **完成** | `controlled_runtime_service.py`（375 行） |
| 4 | FakeExecutorAdapter（no real process） | **完成** | `FakeExecutorAdapter` class，in-memory state transitions only |
| 5 | InMemoryRuntimeEventRecorder（no DB） | **完成** | `InMemoryRuntimeEventRecorder` class，in-memory dict store |
| 6 | fake runtime API（GET sessions/events + POST fake confirm/cancel） | **完成** | `runtime.py`（632 行），5 GET + 3 POST endpoints |
| 7 | in-memory launch request registry | **完成** | `InMemoryLaunchRequestRegistry` class |
| 8 | frontend runtime readback panel（GET-only） | **完成** | `RuntimeReadbackPanel.tsx`（489 行），3 useQuery hooks |
| 9 | dry-run E2E evidence（115 backend + build + 6 frontend） | **完成** | P9-G §14，全量通过 |
| 10 | real executor pilot gate design（21-item checklist） | **完成** | P9-H §15，11-step execution sequence |

### 16.3 P9 hard boundary / not completed capabilities

| # | 未完成能力 | 状态 | 说明 |
|---|----------|------|------|
| 1 | real executor launch | **Not started** | 未启动任何 Codex / Claude Code / DeepSeek 进程 |
| 2 | RealExecutorAdapter | **Not started** | 仅有 FakeExecutorAdapter，无真实 adapter |
| 3 | subprocess / shell / os.popen | **未使用** | app 代码中无任何 subprocess 调用 |
| 4 | real ~/.codex / ~/.claude / env value / token / API key read | **未实现** | 无任何真实配置读取 |
| 5 | DB persistence | **未实现** | 全部使用 in-memory store（dict） |
| 6 | real ApprovalService coupling | **未实现** | P9-B/C domain 定义 LaunchApproval 模型，但未接入 ApprovalService |
| 7 | product runtime Git write | **Not started** | gate_allows_write 永不为 True |
| 8 | PR / merge automation | **Not started** | 全线禁止 |
| 9 | AI Project Director total closure | **Partial** | P9 完成 ≠ 总闭环完成 |

### 16.4 Targeted evidence commands

| # | 命令 | 结果 |
|---|------|------|
| 1 | `cd runtime/orchestrator && .venv/bin/pytest tests/test_executor_runtime_domain.py tests/test_executor_runtime_safety_domain.py tests/test_controlled_runtime_service.py tests/test_runtime_api.py -q` | **115 passed, 1 warning**（Starlette deprecation，无关） |
| 2 | `cd apps/web && npm run build` | **✓ built in 1.30s**（511 modules） |
| 3 | `cd apps/web && node --test src/pages/workbench/components/__tests__/RuntimeReadbackPanel.test.mjs` | **6 passed, 0 fail** |
| 4 | `git diff --check` | **Pass** |

### 16.5 Static boundary audit summary

| 搜索词 | app 代码命中 | 判定 |
|--------|-------------|------|
| `subprocess` / `os.popen` / `shell=True` / `asyncio.create_subprocess` | 0（仅 test 否定断言） | **Pass** |
| `ExecutorService` / `run_shell_command` | 0（仅 test 否定断言） | **Pass** |
| `Codex` / `Claude Code` / `DeepSeek` | 仅 frontend safety disclaimers + docs design text | **Pass — 安全声明/设计说明** |
| `api_key` / `token_value` / `auth_token` / `secret` | 0 DTO 字段（仅 domain regex + safety enum + test forbidden lists） | **Pass — 安全机制** |
| `native_config_path` / `cli_path` / `process_handle` / `log_path` | 0（仅 test forbidden lists） | **Pass** |
| `raw_output` / `raw_error` | 0（仅 test forbidden lists） | **Pass** |
| `POST /runtime` / `/launch-requests` / `/confirm` / `/cancel` | backend P9-E fake-only API endpoints + tests | **Pass — fake-only** |
| `git add` / `git commit` / `git push` / `merge` | 仅 frontend test 否定 label 检查 + docs 红线说明 | **Pass — 安全断言/设计说明** |
| `RealExecutorAdapter` / `REAL_EXECUTOR_PILOT_ENABLED` | 仅 docs P9-H 设计文本 | **Pass — 设计说明** |

**关键结论**：
- Backend app 代码：0 真实 subprocess、0 secret 字段输出、0 DB persistence、0 ApprovalService 耦合
- Backend POST endpoints：fake-only confirm/cancel，无真实进程操作
- Frontend api.ts：仅 3 个 GET 函数，无 POST
- Frontend component：0 控制按钮（无 launch/confirm/cancel/kill）

### 16.6 P9 Final Gate

| # | Gate 条件 | 结论 |
|---|----------|------|
| 1 | P9-A through P9-H all Pass | **Pass** |
| 2 | backend targeted tests pass（115 passed） | **Pass** |
| 3 | frontend build pass | **Pass** |
| 4 | frontend targeted source test pass（6 passed） | **Pass** |
| 5 | git diff --check pass | **Pass** |
| 6 | no real executor launch | **Pass** |
| 7 | no RealExecutorAdapter | **Pass** |
| 8 | no subprocess / shell / os.popen | **Pass** |
| 9 | no env/secret exposure in DTO | **Pass** |
| 10 | no DB persistence（in-memory only） | **Pass** |
| 11 | no ApprovalService coupling | **Pass** |
| 12 | no frontend runtime control buttons | **Pass** |
| 13 | real executor pilot gate design complete（21-item checklist） | **Pass** |
| 14 | no product runtime Git write | **Pass** |
| 15 | P9 scoped gate Pass | **Pass** |
| 16 | P9 real executor launch remains Not started | **Pass** |
| 17 | AI Project Director total closure remains Partial | **Pass** |
| **P9 Final Gate** | **Pass** | — |

### 16.7 P9-Final conclusion

| 结论项 | 状态 |
|--------|------|
| **P9-Final** | **Pass** — targeted evidence + scoped gate complete |
| **P9 Final Gate** | **Pass** — 17 项条件全部通过 |
| **P9 scoped closure** | **Pass** — fake dry-run + pilot gate design |
| **P9 Controlled Runtime implementation** | **Scoped Pass** — P9-A～H domain/service/API/frontend/evidence/design + P9-Final evidence；no real runtime |
| P9 real executor launch | **Not started** — Step 8（RealExecutorAdapter pilot start）未实现 |
| 产品运行时 Git 写操作 | **Not started** — 全线禁止 |
| AI Project Director 总闭环 | **Partial** — P9 未完成 real runtime + Git 写未启动 |

### 16.8 P9-Final 收口声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否（仅为审计现有 P9-B/C/D/E/F 产物） |
| 是否启动 Codex / Claude Code / DeepSeek | 否 |
| 是否使用 subprocess / shell / os.popen | 否 |
| 是否读取真实本机配置、环境变量值、API key、token、secret | 否 |
| 是否修改参考项目 | 否 |
| 是否复制参考项目代码 | 否 |
| 是否把 real executor launch 判 Pass | 否 |
| 是否把产品运行时 Git 写判 Pass | 否 |
| 是否把 AI Project Director 总闭环判 Pass | 否 |
| 是否只做文档审计 | 是 |

---

**Cross-reference**: P9-RGWP 试点轨道，详见 [P9真实执行器Git写试点总账与阶段设计-20260609](./P9真实执行器Git写试点总账与阶段设计-20260609.md)。P9-REL 真实执行器接入轨道，详见 [P9-REL真实执行器接入总账与阶段设计-20260610](./P9-REL真实执行器接入总账与阶段设计-20260610.md)。P9-REL-A: Pass；P9 real executor launch: Not started。
