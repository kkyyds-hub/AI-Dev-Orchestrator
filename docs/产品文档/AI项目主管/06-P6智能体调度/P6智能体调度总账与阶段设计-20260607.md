# P6 智能体调度总账与阶段设计

> **文档类型**: P6 阶段 Ledger + Stage Design（合一）
> **生成日期**: 2026-06-07
> **远端基准**: `origin/main` = `12730284e72f98e488922c8f94cc7f7105379eaf`（P6-A ledger + stage design 初版）
> **路线文档**: `../01-总规划与路线/P1-P7参考规划复用说明书.md`
> **主产品基线**: `../02-页面与产品基线/页面信息架构-20260518.md`
> **前置阶段**: P1 Coding Session + Worktree Lifecycle: Pass；P2 Worker Context + Runtime Safe Execution: Pass；P3 Runtime Lifecycle Evidence + Event Audit: Pass；P4 Git Delivery + Human Approval Gate: Pass；P5 Failure Recovery: Pass
> **状态**: P6-A Design/Ledger: Pass；P6-B AgentDispatchDecision 纯 domain model: Pass；P6-B R1 测试补证: Pass；P6-B R2 domain contract 小修正: Pass；P6-B R3 audit_event_type contract 小修正: Pass；P6-C FailureRecoveryDecision → AgentDispatchDecision builder: Pass；P6-C R1 审查补证: Pass；P6-D AgentMessage / audit timeline 记录调度建议: Pass；P6-D R1 审查补证: Pass；P6-E WorkerRunOnceResponse 只读透传 dispatch decision: Pass；P6-F 至 P6-G: Not started；P6 Agent Orchestration 总 Gate: Not started；AI Project Director 总闭环: Partial

---

## 1. P6 当前基准

| 项目 | 状态 | 证据 |
|------|------|------|
| `origin/main` HEAD（P6-D R1 开始前） | `e644fe3aa6dafe754036eb949d93a5f6ef0b9ea6` | feat: add P6 dispatch audit timeline |
| P1 Coding Session + Worktree Lifecycle | **Pass** | P1 closure |
| P2 Worker Context + Runtime Safe Execution | **Pass** | P2 closure |
| P3 Runtime Lifecycle Evidence + Event Audit | **Pass** | P3 closure |
| P4 Git Delivery + Human Approval Gate | **Pass** | P4 ledger final gate |
| P5 Failure Recovery | **Pass** | P5 ledger final gate |
| P6-A Design/Ledger | **Pass**（本文档） | — |
| P6-B AgentDispatchDecision 纯 domain model | **Pass** | `runtime/orchestrator/app/domain/agent_dispatch_decision.py`；`runtime/orchestrator/tests/test_agent_dispatch_decision.py`；`pytest tests/test_agent_dispatch_decision.py -q` = 43 passed |
| P6-B R1 测试补证 | **Pass** | `pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` = 99 passed；`git diff --check` = Pass |
| P6-B R2 domain contract 小修正 | **Pass** | P6-B 版本提升到 `p6_b.r2`；必填文本 trim 后不可为空；P6-B safety flags 禁止 `api_response_exposed` / `agent_message_written` 提前置真；组合 targeted tests = 106 passed |
| P6-B R3 audit_event_type contract 小修正 | **Pass** | P6-B 版本提升到 `p6_b.r3`；补齐 `audit_event_type=agent_dispatch_decision` 纯合约；组合 targeted tests = 108 passed |
| P6-C FailureRecoveryDecision → AgentDispatchDecision builder | **Pass** | `runtime/orchestrator/app/domain/agent_dispatch_decision.py`；`runtime/orchestrator/tests/test_agent_dispatch_decision.py`；`pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` = 117 passed；`git diff --check` = Pass |
| P6-C R1 审查补证 | **Pass** | Builder 关键代码位置已复核；API/Worker/services/repositories/apps 接入 grep 无命中；组合 targeted tests = 117 passed；`git diff --check` = Pass |
| P6-D AgentMessage / audit timeline 记录调度建议 | **Pass** | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py`；`runtime/orchestrator/app/workers/task_worker.py`；`runtime/orchestrator/tests/test_agent_dispatch_audit_service.py`；组合 targeted tests = 132 passed；未接 API response/前端/自动派发/retry |
| P6-D R1 审查补证 | **Pass** | P6-D audit service 独立于 P5 audit service 的最小修正；组合 targeted tests = 133 passed；`git diff --check` = Pass |
| P6-E WorkerRunOnceResponse 只读透传 dispatch decision | **Pass** | `WorkerRunResult.agent_dispatch_decision` 内部只读字段；`WorkerRunOnceResponse.agent_dispatch_decision` 只读 DTO；组合 targeted tests = 133 passed；未接前端/自动派发/retry/Git 写 |
| P6-F 前端只读展示调度建议 | **Not started** | — |
| P6-G P6 ledger 总 Gate 回填 | **Not started** | — |
| **P6 Agent Orchestration 总 Gate** | **Not started** | — |
| P7 Project Director Conversation Hub | **Not started** | — |
| 产品运行时 git add / commit / push / PR | **Not started** | — |
| **AI Project Director 总闭环** | **Partial** | P7 完成前不得写 Pass |

---

## 2. P6 核心定位

### 2.1 P6 是什么

P6 Agent Orchestration 的目标是让 AI Project Director 具备调度决策能力——不是自动执行调度，而是生成结构化调度建议。

P6 的核心工作是：在 P5 FailureRecoveryDecision 的基础上，生成 `AgentDispatchDecision`，回答"这个任务下一步应该由谁来做"，并把决策建议透传到前端供用户只读查看。

P6 不自动派发 Codex / DeepSeek，不自动创建任务，不自动启动 Worker。调度建议是只读的、可审计的、需要用户确认的。

### 2.2 P6 与 P5 的边界

| P5（已完成） | P6（本阶段） |
|-------------|-----------|
| 生成 FailureRecoveryDecision | 基于 FailureRecoveryDecision 生成 AgentDispatchDecision |
| 判断 recommended_owner | 转化为 dispatch recommended_agent |
| 生成 next_instruction_kind | 转化为 instruction_kind + instruction_draft |
| 判断 retry_allowed | 作为 dispatch_status 的输入信号 |
| 写入 failure recovery audit event | 写入 dispatch audit event |
| 前端只读展示 recovery decision | 前端只读展示 dispatch decision |

### 2.3 P6 与 P7 的边界

| P6（本阶段） | P7（后续） |
|-------------|-----------|
| 生成 dispatch decision | Project Director Conversation Hub 中讨论调度决策 |
| 推荐 agent 分工 | 用户通过 Conversation Hub 质疑/确认调度建议 |
| 前端只读展示 dispatch suggestion | Conversation Hub 中用户与 AI 主管协作调整调度 |
| 写入 dispatch audit event | 主管根据 conversation 修改调度 |
| 调度建议可审计 | 用户质疑/修改调度建议 |

**重要澄清**：

- P6 不做自动派发。自动派发 Codex / DeepSeek、自动创建任务、自动启动 Worker 均不在 P6 范围。
- P7 当前定义是 Project Director Conversation Hub + Governance，**不等于自动派发**。
- 未来如需进入真实自动调度阶段，必须另行设计 feature flag、guardrail、human confirmation、安全 Gate 和 evidence 链。不得把自动派发写成 P7 的既定范围。
- 不得把 AI Project Director 总闭环写成 Pass。

### 2.4 参考机制

参考 Agent Orchestrator 的以下机制（不照搬代码）：

| AO 机制 | P6 映射 |
|---------|--------|
| Session manager：spawn/list/kill/cleanup CRUD | P6 对 AgentSession 状态变化触发 dispatch 重新评估 |
| Lifecycle manager：轮询循环 + status detection + reaction engine | P6 对 run.failure_category / task.status 变更触发 dispatch decision |
| Routing / dispatch decision：谁应该接手下一步 | P6 的 AgentDispatchDecision.recommended_agent |
| Activity event / audit event：结构化审计事件 | P6-D dispatch audit event |
| Agent handoff：角色交接机制 | P6 的 dispatch_reason_code + instruction_kind |

参考项目只读检查（`/Users/kk/project explore/agent-orchestrator`）：

| 参考来源 | 读取文件 | 只借鉴机制 | 结论 |
|----------|----------|------------|------|
| AO session-manager.ts | `packages/core/src/session-manager.ts`（L1-100，入口类型） | spawn/list/kill/cleanup CRUD 理念 | **未照搬代码** |
| AO lifecycle-manager.ts | `packages/core/src/lifecycle-manager.ts`（L1-100，入口类型） | 轮询循环 + status detection + reaction engine | **未照搬代码** |
| AO feedback-routing-and-followup-design.md | `docs/design/feedback-routing-and-followup-design.md` | routing dispatch、governance hooks、human approval gate、session spawning contract | **未照搬代码** |

---

## 3. P6 阶段目标

| # | 目标 | 说明 |
|---|------|------|
| 1 | 建立 AgentDispatchDecision 纯域模型 | 结构化调度建议 contract |
| 2 | 从 FailureRecoveryDecision 构建 dispatch decision | 复用 P5 的 recommended_owner / next_instruction_kind / safety / evidence |
| 3 | 记录 dispatch decision 到 AgentMessage timeline | 可审计的调度建议记录 |
| 4 | WorkerRunOnceResponse 只读透传 dispatch decision | API response 中只读暴露调度建议 |
| 5 | 前端只读展示 dispatch decision | 工作台展示调度建议，不新增操作按钮 |
| 6 | P6 总 Gate 证据收口 | 阶段内全链证据统一追加到本文档 |

---

## 4. P6 不做清单

| # | 不做 | 原因 |
|---|------|------|
| 1 | 真实 Git 写操作 | 全线禁止 |
| 2 | git add / commit / push / PR / merge | 全线禁止 |
| 3 | 自动派发 Codex / DeepSeek | 调度建议只读，不自动派发 |
| 4 | 自动创建任务 | 不属于 P6 |
| 5 | 自动启动 Worker | 不属于 P6 |
| 6 | 实现 retry | P5 已做 retry_allowed 判断，P6 不做真实 retry |
| 7 | 绕过 P1-P5 evidence / gate / dry-run / human approval / failure recovery decision 链 | P6 是证据链的下游消费者，不是替代者 |
| 8 | P7 Conversation Hub | 不属于 P6 |
| 9 | 把 AI Project Director 总闭环写成 Pass | P7 完成前不得 |
| 10 | 修改 P5 FailureRecoveryDecision 的域模型或 builder 逻辑 | P5 已完成，P6 只读取复用 |
| 11 | 修改 Worker 成功路径 | 只扩展 dispatch decision 生成路径 |
| 12 | 重构 Task / Run / AgentSession 模型 | 新模型在现有之上建设 |
| 13 | 前端大改版 | 最小只读展示在 P6-F |

---

## 5. P6 对 P5 FailureRecoveryDecision 的复用关系

### 5.1 复用字段

P6 AgentDispatchDecision 从 P5 FailureRecoveryDecision 复用以下字段：

| P5 字段 | P6 复用方式 | 说明 |
|---------|-----------|------|
| `recommended_owner` | 映射为 `recommended_agent` | codex / deepseek / user / blocked |
| `next_instruction_kind` | 复用为 `instruction_kind` | code_fix / test_fix / config_fix / evidence_fix / replay / pause / replan / human_question |
| `next_instruction_draft` | 复用为 `instruction_draft` | 只读透传已有中文指令草案 |
| `requires_human_decision` | 映射为 `dispatch_status` 的 needs_user_decision | 需要人工决策的调度不自动建议 |
| `retry_allowed` | 映射为 dispatch safety 信号 | 自动 retry 不在当前任何阶段范围，P6 只标记不可执行 |
| `recoverable` | 映射为 dispatch safety 信号 | 不可恢复的调度建议 blocked |
| `safety_flags` | 合并到 P6 dispatch safety_flags | 保留 P5 安全标记，叠加 P6 调度 safety |
| `rule_codes` | 作为内部判断依据 | 不直接变成用户误导文案 |
| `evidence_refs`（P5 中 rule_codes 承载） | 转为 `evidence_refs` | 调度建议可追溯到 P5 recovery 证据 |

### 5.2 不直接暴露的字段

以下 P5 内部字段在 P6 前端展示中**不直接暴露**给用户（与 P5-F R1 保持一致）：

| 字段 | 处理方式 |
|------|---------|
| `recommended_owner`（enum 原值） | 映射为中文 label 后展示 |
| `next_action`（enum 原值） | 映射为中文 label 后展示 |
| `failure_category`（enum 原值） | 不展示 |
| `reason_code`（enum 原值） | 不展示 |
| `rule_codes`（内部码） | 不展示，仅用于内部判断 |
| `version` | 不展示 |
| `audit_event_type` | 不展示 |

---

## 6. AgentDispatchDecision 设计草案

### 6.1 建议域模型

```text
AgentDispatchDecision
├── dispatch_decision_id: str
├── source_failure_recovery_decision_id: str | None
├── source_run_id: UUID | None
├── source_task_id: UUID | None
├── recommended_agent: DispatchAgent
├── dispatch_status: DispatchStatus
├── dispatch_reason_code: str
├── dispatch_reason_cn: str
├── instruction_kind: InstructionKind
├── instruction_draft: str | None
├── evidence_refs: list[str]
├── safety_flags: AgentDispatchDecisionSafetyFlags
├── created_at: datetime
├── created_by: str
└── api_response_exposed: bool
```

### 6.2 DispatchAgent 枚举草案

```text
DispatchAgent
├── codex: 代码实现、后端 domain、API、Worker、前端实现、复杂测试修正
├── deepseek: 文档、ledger、证据整理、测试结果回填、路线梳理、设计说明
├── user: 需要真实业务判断、授权、确认、缺少关键信息、成本/风险选择
└── blocked: 违反安全 gate、缺证据、缺前置状态、产品运行时 Git 写风险、总闭环误判风险
```

### 6.3 DispatchStatus 枚举草案

```text
DispatchStatus
├── suggested: 调度建议已生成，AI 主管推荐此 agent
├── needs_user_decision: 调度无法自动判断，需要用户决策
├── blocked: 违反安全 gate 或缺少前置条件，不可调度
└── not_applicable: 当前不适用调度（如成功路径）
```

### 6.4 AgentDispatchDecisionSafetyFlags 草案

```text
AgentDispatchDecisionSafetyFlags
├── runs_git: bool = False
├── runs_write_git: bool = False
├── git_add_triggered: bool = False
├── git_commit_triggered: bool = False
├── git_push_triggered: bool = False
├── pr_opened: bool = False
├── merge_triggered: bool = False
├── branch_deleted: bool = False
├── git_reset_triggered: bool = False
├── git_checkout_triggered: bool = False
├── git_switch_triggered: bool = False
├── git_stash_triggered: bool = False
├── git_rebase_triggered: bool = False
├── git_tag_triggered: bool = False
├── ci_triggered: bool = False
├── execution_enabled: bool = False
├── worker_dispatch_triggered: bool = False
├── api_response_exposed: bool = False
├── agent_message_written: bool = False
├── task_created: bool = False
├── retry_triggered: bool = False
└── auto_dispatch_triggered: bool = False
```

所有 safety_flags 在 P6 纯域模型中默认为 `False`。P6-E 透传时 `api_response_exposed` 设为 `True`。

### 6.5 Codex / DeepSeek / user / blocked 判定边界

#### Codex 判定

| 触发条件 | 说明 |
|---------|------|
| 代码实现缺陷 | 后端 / 前端 / API / Worker 逻辑错误 |
| 测试失败 | 单元测试 / targeted test 失败 |
| 类型错误 | Python type hint / TypeScript type 错误 |
| 输出格式问题 | Provider 返回格式不合法 |
| 复杂测试修正 | 需要修改测试代码本身 |
| 域模型实现 | domain model 代码实现 |
| Provider 调用失败（可重试型） | 临时性网络/服务错误 |

#### DeepSeek 判定

| 触发条件 | 说明 |
|---------|------|
| 文档不一致 | 设计文档与实现偏离 |
| 证据链断裂 | evidence 缺失或不完整 |
| Ledger 过期 | 阶段总账状态未更新 |
| Gate 状态错误 | Gate 结论与证据不匹配 |
| 测试结果回填 | 将测试结果整理为证据写入 ledger |
| 路线梳理 | 规划文档更新 |
| 设计说明 | 设计方案文档化 |
| 配置修正 | 验证模板/规则配置错误 |

#### user 判定

| 触发条件 | 说明 |
|---------|------|
| 需求不清 | 任务需求不明确，需要补充验收标准 |
| 预算变更 | 需要确认是否增加预算或更换模型 |
| 权限决策 | 涉及密钥、权限变更 |
| 安全边界决策 | 高风险方向改变 |
| 超过预设重试阈值 | 连续失败超过 N 次 |
| 缺少关键信息 | AI 主管无法根据现有 evidence 给出可信判断 |
| 成本/风险选择 | 涉及成本或风险 trade-off |
| 项目方向改变 | 当前计划方向可能需要调整 |

#### blocked 判定

| 触发条件 | 说明 |
|---------|------|
| 违反安全 gate | runtime_launch_gate / delivery_gate 未通过 |
| 缺证据 | 缺少必要的 evidence 链 |
| 缺前置状态 | P5 failure recovery decision 未生成 |
| 产品运行时 Git 写风险 | 操作涉及 git write（当前全线禁止） |
| 总闭环误判风险 | 可能把 Partial 误判为 Pass |
| 依赖缺失 | 前置任务未完成 |
| Feature flag 未开启 | 所需功能未启用 |
| 当前阶段不允许继续 | 阶段 Gate 未通过 |

---

## 7. P6 子阶段规划

| 子阶段 | 内容 | 负责人 | 说明 |
|--------|------|--------|------|
| **P6-A** | Ledger + Stage Design | DeepSeek | 本文档 |
| **P6-B** | AgentDispatchDecision 纯 domain model + targeted tests | Codex | 域模型实现 |
| **P6-C** | FailureRecoveryDecision → AgentDispatchDecision builder | Codex | 从 P5 决策构建 P6 调度决策 |
| **P6-D** | AgentMessage / audit timeline 记录调度建议 | Codex | 将 dispatch decision 写入可审计事件 |
| **P6-E** | WorkerRunOnceResponse 只读透传 dispatch decision | Codex | API response 中透传 dispatch decision 字段 |
| **P6-F** | 前端只读展示调度建议 | Codex → DeepSeek | 前端展示 dispatch decision；Gate 证据收口 |
| **P6-G** | P6 ledger 总 Gate 回填 | DeepSeek | P6 全链证据收口与最终 Gate 更新 |

### 7.1 P6 子阶段顺序

```text
P6-A（本文档）
  ↓
P6-B AgentDispatchDecision 纯域模型
  ↓
P6-C FailureRecoveryDecision → AgentDispatchDecision builder
  ↓
P6-D AgentMessage / audit timeline 记录调度建议
  ↓
P6-E WorkerRunOnceResponse 只读透传 dispatch decision
  ↓
P6-F 前端只读展示调度建议
  ↓
P6-G P6 ledger 总 Gate 回填
```

### 7.2 各阶段允许范围、禁止范围、targeted tests 建议

#### P6-A：Ledger + Stage Design（本轮）

| 项目 | 说明 |
|------|------|
| **允许** | 新增 P6 ledger + stage design 合一文档；修改总索引 |
| **禁止** | 修改 Python 业务代码、测试代码、前端、DB / migration、API、AgentMessage |
| **禁止** | 实现 AgentDispatchDecision 代码 |
| **禁止** | 新增 API / 路由 / schema |
| **禁止** | 启动 Worker / 自动派发 / retry |
| **测试** | `git diff --check` |

#### P6-B：AgentDispatchDecision 纯 domain model

| 项目 | 说明 |
|------|------|
| **允许** | 新增 `runtime/orchestrator/app/domain/agent_dispatch_decision.py` |
| **允许** | 新增 `runtime/orchestrator/tests/test_agent_dispatch_decision.py` |
| **允许** | 定义 DispatchAgent / DispatchStatus / AgentDispatchDecisionSafetyFlags / AgentDispatchDecision 域模型 |
| **禁止** | 接 Worker / API / DB / AgentMessage |
| **禁止** | 修改 P5 FailureRecoveryDecision |
| **禁止** | 实现 builder（P6-C） |
| **禁止** | 实现自动调度 / 自动派发 / retry |
| **禁止** | 实现产品运行时 Git 写操作 |
| **测试** | `pytest tests/test_agent_dispatch_decision.py -q`（预计 15-25 条） |

#### P6-C：FailureRecoveryDecision → AgentDispatchDecision builder

| 项目 | 说明 |
|------|------|
| **允许** | 在 `agent_dispatch_decision.py` 中新增 `AgentDispatchDecisionBuilder` |
| **允许** | 读取 FailureRecoveryDecision、RunFailureCategory、TaskBlockingReasonCode |
| **允许** | 从 P5 recommended_owner 映射到 P6 recommended_agent |
| **允许** | 从 P5 rule_codes / evidence 构建 evidence_refs |
| **禁止** | 修改 P5 FailureRecoveryDecisionBuilder |
| **禁止** | 修改 Worker 成功路径 |
| **禁止** | 接 API / DB / AgentMessage |
| **禁止** | 实现自动调度 / 自动派发 / retry |
| **禁止** | 实现产品运行时 Git 写操作 |
| **测试** | `pytest tests/test_agent_dispatch_decision.py -q`（扩展至 35-50 条） |

#### P6-D：AgentMessage / audit timeline 记录调度建议

| 项目 | 说明 |
|------|------|
| **允许** | 新增 `runtime/orchestrator/app/services/dispatch_audit_service.py` |
| **允许** | 在 Worker failed/cancelled 路径 best-effort 记录 dispatch decision timeline |
| **允许** | 复用既有 AgentMessage repository/service 模式 |
| **禁止** | 修改 API response schema |
| **禁止** | 修改前端 |
| **禁止** | 实现自动调度 / 自动派发 / retry |
| **禁止** | 实现产品运行时 Git 写操作 |
| **测试** | `pytest tests/test_agent_dispatch_decision.py tests/test_worker_failed_recovery_decision.py -q` |

#### P6-E：WorkerRunOnceResponse 只读透传 dispatch decision

| 项目 | 说明 |
|------|------|
| **允许** | 在 WorkerRunOnceResponse schema 中新增只读 dispatch_decision DTO |
| **允许** | 新增中文 label 字段 |
| **允许** | 设置 response-level `api_response_exposed=True` |
| **禁止** | 修改 WorkerRunResult 内部 dispatch 构建逻辑（由 P6-C 提供） |
| **禁止** | 修改前端 |
| **禁止** | 实现自动调度 / 自动派发 / retry |
| **禁止** | 实现产品运行时 Git 写操作 |
| **测试** | `pytest tests/test_agent_dispatch_decision.py tests/test_worker_failed_recovery_decision.py -q` |

#### P6-F：前端只读展示调度建议

| 项目 | 说明 |
|------|------|
| **允许** | 新增 `WorkerAgentDispatchDecisionCard.tsx` 只读展示组件 |
| **允许** | 在工作台 run-once 结果卡片中展示 dispatch decision |
| **允许** | 仅展示中文 label、中文摘要、dispatch 中文原因 |
| **禁止** | 展示内部 enum 原值（recommended_agent / dispatch_status / dispatch_reason_code），仅展示中文 label、中文摘要、dispatch 中文原因 |
| **禁止** | 新增 retry / dispatch / apply / git 操作按钮 |
| **禁止** | 调用写入型 API |
| **禁止** | 实现自动调度 / 自动派发 / retry |
| **禁止** | 实现产品运行时 Git 写操作 |
| **测试** | `cd apps/web && npm run build` |

#### P6-G：P6 ledger 总 Gate 回填

| 项目 | 说明 |
|------|------|
| **允许** | 更新本文档第 9 节 Gate 表 |
| **允许** | 追加各子阶段证据与测试结果 |
| **允许** | 回填 P6 Agent Orchestration 总 Gate |
| **禁止** | 修改 P1-P5 Gate 结论 |
| **禁止** | 把 AI Project Director 总闭环写成 Pass |
| **禁止** | 实现产品运行时 Git 写操作 |
| **测试** | `git diff --check` |

---

## 8. 文档治理规则

P6 阶段统一遵守以下规则（与 P4/P5 ledger 一致）：

| 场景 | 策略 |
|------|------|
| P6 子阶段实现（B/C/D/E/F） | 证据追加到本文档 |
| 小修正 / R1 / R2 / R3 | 追加到本文档，不新建独立文件 |
| 测试结果记录 | 追加到本文档 |
| 阶段内 Gate 变更 | 更新本文档第 9 节 |
| P6 大阶段完成 | 在本文档内做 Final Gate（P6-G），不新建独立 closure |
| P6 高风险新设计（需对齐多个模块） | 可新建独立设计文档 |
| 后续给 Codex / DeepSeek 的任务指令 | 必须遵守本规则，不得要求新建零散 closure 文档 |

---

## 9. Gate 结论

| Gate | 结论 | 证据 |
|------|------|------|
| `origin/main` HEAD 确认 | Pass | `12730284e72f98e488922c8f94cc7f7105379eaf` |
| P1 Coding Session + Worktree Lifecycle | **Pass** | P1 closure |
| P2 Worker Context + Runtime Safe Execution | **Pass** | P2 closure |
| P3 Runtime Lifecycle Evidence + Event Audit | **Pass** | P3 closure |
| P4 Git Delivery + Human Approval Gate | **Pass** | P4 ledger final gate |
| P5 Failure Recovery | **Pass** | P5 ledger final gate |
| **P6-A Ledger + Stage Design** | **Pass** | 本文档 |
| **P6-A R1 安全文案修正** | **Pass** | 本文档 §11 |
| P6-B AgentDispatchDecision 纯 domain model | **Pass** | `runtime/orchestrator/app/domain/agent_dispatch_decision.py`；`runtime/orchestrator/tests/test_agent_dispatch_decision.py`；`pytest tests/test_agent_dispatch_decision.py -q` = 43 passed |
| **P6-B R1 测试补证** | **Pass** | `pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` = 99 passed；`git diff --check` = Pass |
| **P6-B R2 domain contract 小修正** | **Pass** | P6-B 版本提升到 `p6_b.r2`；必填文本 trim 后不可为空；P6-B safety flags 禁止 `api_response_exposed` / `agent_message_written` 提前置真；`pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` = 106 passed |
| **P6-B R3 audit_event_type contract 小修正** | **Pass** | P6-B 版本提升到 `p6_b.r3`；补齐 `audit_event_type=agent_dispatch_decision` 纯合约；`pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` = 108 passed |
| P6-C FailureRecoveryDecision → AgentDispatchDecision builder | **Pass** | `AgentDispatchDecisionBuilder.build_from_failure_recovery_decision(...)` 纯转换 P5 recovery decision；组合 targeted tests = 117 passed；未接 Worker/API/DB/AgentMessage/前端 |
| P6-C R1 审查补证 | **Pass** | 仅回填审查证据；无代码变更；禁止越界 grep 已记录 |
| P6-D AgentMessage / audit timeline 记录调度建议 | **Pass** | P6 dispatch decision best-effort 写入 AgentMessage timeline；组合 targeted tests = 132 passed；未接 P6-E API response |
| P6-D R1 审查补证 | **Pass** | 仅审查补证 + 最小修正；P6-D audit 不再被 P5 audit service 缺失短路；组合 targeted tests = 133 passed |
| P6-E WorkerRunOnceResponse 只读透传 dispatch decision | **Pass** | `WorkerRunResult.agent_dispatch_decision` 内部只读字段；`WorkerRunOnceResponse.agent_dispatch_decision` 只读 DTO；组合 targeted tests = 133 passed；未接前端/自动派发/retry/Git 写 |
| P6-F 前端只读展示调度建议 | **Not started** | — |
| P6-G P6 ledger 总 Gate 回填 | **Not started** | — |
| **P6 Agent Orchestration 总 Gate** | **Not started** | 需 P6-B/C/D/E/F/G 全部完成后评估 |
| P7 Project Director Conversation Hub | **Not started** | — |
| 产品运行时 Git 写操作 | **Not started** | — |
| **AI Project Director 总闭环** | **Partial** | P7 完成前不得写 Pass |

---

## 10. 本轮收口声明

### 10.1 P6-A 实际文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **新增** | P6 ledger + stage design 合一文档 | 否 |
| `00-总览与索引/AI项目主管文档索引.md` | **修改** | 补充 P6 入口与状态 | 否 |

### 10.2 边界声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否写 AgentMessage | 否 |
| 是否实现 AgentDispatchDecision 代码 | 否 |
| 是否实现 retry / 自动派发 / 创建任务 | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否把总闭环写成 Pass | 否 |
| 是否只新增设计文档 | 是 |
| 是否删除任何历史证据文档 | 否 |

---

## 附录 A：P6 依赖的现有代码信号参考

以下清单来自 `origin/main` = `a8cf5f3420f0bc41e7d03ccc9f70108134a2a791` 的代码审计。

### P5 FailureRecoveryDecision（P6 复用源）

| 字段 | 位置 | 说明 |
|------|------|------|
| `FailureRecoveryDecision` | `runtime/orchestrator/app/domain/failure_recovery_decision.py` | P5 决策域模型 |
| `RecoveryOwner` | 同上 | codex / deepseek / user / blocked |
| `RecoveryNextAction` | 同上 | retry / fix_and_retry / pause_and_wait / replan / escalate_to_human / block_permanently / archive |
| `InstructionKind` | 同上 | code_fix / test_fix / config_fix / evidence_fix / replay / pause / replan / human_question |
| `FailureRecoveryDecisionBuilder` | 同上 | 基于 failure_category + reason_code 构建决策 |
| `FailureRecoveryDecisionSafetyFlags` | 同上 | P5-B 安全标记 |

### Worker / API 层（P6 透传目标）

| 字段 | 位置 | 说明 |
|------|------|------|
| `WorkerRunResult.failure_recovery_decision` | `runtime/orchestrator/app/workers/task_worker.py:369` | Worker 内部携带 P5 decision |
| `WorkerRunResult.dispatch_status` | 同上 L142 | 现有 dispatch_status 字段（来自 routing） |
| `WorkerRunResult.owner_role_code` | 同上 L138 | 现有 owner_role_code 字段 |
| `WorkerRunResult.handoff_reason` | 同上 L141 | 现有 handoff_reason 字段 |
| `WorkerRunResult.agent_type` | 同上 L161 | 现有 agent_type 字段 |

### Run 层（P6 调度来源信号）

| 字段 | 位置 | 说明 |
|------|------|------|
| `Run.dispatch_status` | `runtime/orchestrator/app/domain/run.py:135` | 现有 dispatch_status 字段 |
| `Run.handoff_reason` | 同上 L134 | 现有 handoff_reason 字段 |
| `Run.owner_role_code` | 同上 L131 | 现有 owner_role_code 字段 |

---

## 附录 B：Agent Orchestrator 参考机制映射总表（P6 相关）

| AO 机制 | AO 证据路径 | 学习要点 | P6 对应阶段 |
|---------|-----------|---------|---------------|
| Session manager | `packages/core/src/session-manager.ts` | spawn/list/kill/cleanup CRUD | P6-B/C（调度决策参考 AgentSession 状态） |
| Lifecycle manager | `packages/core/src/lifecycle-manager.ts` | 轮询循环 + status detection + reaction engine | P6-C（Builder 参考 lifecycle state 触发 dispatch） |
| Routing / dispatch decision | `docs/design/feedback-routing-and-followup-design.md` | FeedbackRouter、FollowUpPlanner | P6-B/C（dispatch decision 构建模式） |
| Agent handoff | `docs/design/feedback-routing-and-followup-design.md` §2 Session Spawning Contract | agent 交接 contract | P6-C（Codex / DeepSeek / user / blocked 分工） |
| Activity event / audit event | `packages/core/src/activity-events.ts` | 结构化事件类型、source 分类 | P6-D（dispatch audit event） |
| Governance hooks | `docs/design/feedback-routing-and-followup-design.md` §6 | canCreateIssue / canCreateFork / canCreatePR | P6-B（safety_flags 设计参考） |
| Human approval gate | `docs/design/feedback-routing-and-followup-design.md` §Consent Gates | 人类确认 gate | P6-C（dispatch_status.needs_user_decision） |

---

## 11. P6-A R1 安全文案修正（2026-06-07）

### 11.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-A R1** | P6 ledger 安全文案修正 |
| 代码边界 | **未越过** | 只修正文档文案，不写代码 |
| 修正范围 | P6-F 禁止项反向文案、P6-P7 边界误导表达、retry 归属误导表达 | — |

### 11.2 修正清单

| # | 修正前 | 修正后 | 位置 |
|---|--------|--------|------|
| 1 | P6-F 禁止项写"隐藏内部 enum 原值"（语义反向：禁止隐藏 = 允许展示） | 改为"展示内部 enum 原值"（明确禁止展示内部 enum，仅展示中文 label / 摘要 / 中文原因） | §7.2 P6-F |
| 2 | P6-P7 边界表写"自动派发执行 / 自动路由到正确角色" + "自动派发属于 P7" | 移除表格中的自动派发列；P7 列改为 Conversation Hub 讨论调度；新增重要澄清段落：P7 不等于自动派发，未来自动调度需另行设计 feature flag / guardrail / human confirmation / safety Gate / evidence 链 | §2.3 |
| 3 | "retry 属于 P7 自动调度" | 改为"自动 retry 不在当前任何阶段范围，P6 只标记不可执行" | §5.1 |

### 11.3 边界声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否写 AgentMessage | 否 |
| 是否实现 AgentDispatchDecision 代码 | 否 |
| 是否实现 retry / 自动派发 / 创建任务 | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否把总闭环写成 Pass | 否 |

### 11.4 P6-A R1 Gate

| Gate | 结论 |
|------|------|
| P6-F 前端只读展示安全文案修正 | **Pass** |
| P6-P7 边界误导表达修正 | **Pass** |
| retry 归属误导表达修正 | **Pass** |
| 未改代码 / 未改前端 / 未改 API / 未改 DB | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| P6-B/C/D/E/F/G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-A R1 Gate 结论** | **Pass** |

---

## 12. P6-B AgentDispatchDecision 纯 domain model（2026-06-07）

### 12.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-B** | AgentDispatchDecision 纯 domain model + targeted tests |
| 代码边界 | **未越过** | 只新增纯 domain model 与 targeted tests |
| 是否实现 builder | 否 | P6-C 范围仍 Not started |
| 是否接 Worker / API / DB / AgentMessage | 否 | P6-D/E 范围仍 Not started |
| 是否修改前端 | 否 | P6-F 范围仍 Not started |
| 是否实现自动调度 / 自动派发 / retry | 否 | 仅结构化只读调度建议 contract |
| 是否实现产品运行时 Git 写操作 | 否 | safety flags 禁止 Git 写、CI、执行、自动派发等副作用 |

### 12.2 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/domain/agent_dispatch_decision.py` | **新增** | 定义 `DispatchAgent`、`DispatchStatus`、`AgentDispatchDecisionSafetyFlags`、`AgentDispatchDecision` 纯域模型 | 否 |
| `runtime/orchestrator/tests/test_agent_dispatch_decision.py` | **新增** | 覆盖 P6-B 纯域模型 targeted tests | 否 |
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **修改** | 回填 P6-B Gate 与本轮证据 | 否 |

### 12.3 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_decision.py -q` | **Pass**：43 passed |

### 12.4 边界声明

| 声明 | 结论 |
|------|------|
| 是否新增 `AgentDispatchDecision` 纯 domain model | 是 |
| 是否新增 P6-B targeted tests | 是 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否实现 `AgentDispatchDecisionBuilder` | 否 |
| 是否修改 Worker / API / DB / AgentMessage | 否 |
| 是否修改前端 | 否 |
| 是否实现 retry / 自动派发 / 创建任务 | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 12.5 P6-B Gate

| Gate | 结论 |
|------|------|
| AgentDispatchDecision 纯域模型已定义 | **Pass** |
| DispatchAgent / DispatchStatus 枚举稳定 | **Pass** |
| safety_flags 默认全 false | **Pass** |
| safety_flags 拒绝 Git 写、CI、执行、Worker dispatch、task creation、retry、auto dispatch 副作用 | **Pass** |
| 未接 Worker / API / DB / AgentMessage / 前端 | **Pass** |
| 未实现 builder（P6-C） | **Pass** |
| targeted tests 通过 | **Pass** |
| P6-C/P6-D/P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-B Gate 结论** | **Pass** |

---

## 13. P6-B R1 测试补证（2026-06-07）

### 13.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-B R1** | 测试补证 + ledger 回填 |
| 代码边界 | **未越过** | 未修改 domain model、测试代码、Worker、API、DB、AgentMessage、前端 |
| 补证目标 | **完成** | 补跑 P6-B 与 P5 FailureRecoveryDecision 组合 targeted tests，确认 P6-B 未破坏 P5 |
| 是否进入 P6-C | 否 | 未实现 builder，未做 FailureRecoveryDecision → AgentDispatchDecision 映射 |
| 是否实现自动调度 / 自动派发 / retry | 否 | 仅补充测试证据 |
| 是否实现产品运行时 Git 写操作 | 否 | 本轮开发提交不代表产品运行时具备 Git 写能力 |

### 13.2 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **修改** | 回填 P6-B R1 测试补证与 Gate 结论 | 否 |

### 13.3 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：99 passed |
| `git diff --check` | **Pass** |

### 13.4 边界声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python domain model | 否 |
| 是否修改测试代码 | 否 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否实现 `AgentDispatchDecisionBuilder` | 否 |
| 是否接 Worker / API / DB / AgentMessage | 否 |
| 是否修改前端 | 否 |
| 是否读取或照搬参考项目代码 | 否 |
| 是否实现 retry / 自动派发 / 创建任务 | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 13.5 P6-B R1 Gate

| Gate | 结论 |
|------|------|
| P6-B targeted tests 仍通过 | **Pass** |
| P5 FailureRecoveryDecision targeted tests 仍通过 | **Pass** |
| P6-B 未破坏 P5 recovery decision contract | **Pass** |
| `git diff --check` 通过 | **Pass** |
| 未改 domain/test/Worker/API/DB/AgentMessage/前端 | **Pass** |
| 未实现 builder（P6-C） | **Pass** |
| P6-C/P6-D/P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-B R1 Gate 结论** | **Pass** |

---

## 14. P6-B R2 domain contract 小修正（2026-06-07）

### 14.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-B R2** | P6-B domain contract 小修正 + targeted tests |
| 代码边界 | **未越过** | 仅修改 P6-B 纯 domain model、对应 targeted tests、P6 ledger |
| 修正目标 | **完成** | 收紧必填文本字段与 P6-B safety flags 纯域边界 |
| 是否进入 P6-C | 否 | 未实现 builder，未做 FailureRecoveryDecision → AgentDispatchDecision 映射 |
| 是否接 Worker / API / DB / AgentMessage / 前端 | 否 | 未接任何运行时链路 |
| 是否实现自动调度 / 自动派发 / retry | 否 | 仍只是结构化只读调度建议 contract |
| 是否实现产品运行时 Git 写操作 | 否 | 本轮开发提交不代表产品运行时具备 Git 写能力 |

### 14.2 修正清单

| # | 修正 | 说明 |
|---|------|------|
| 1 | `P6_AGENT_DISPATCH_DECISION_VERSION` 从 `p6_b.r1` 提升到 `p6_b.r2` | 标记本轮 contract 收紧 |
| 2 | 必填文本字段 trim 后不可为空 | `source`、`version`、`dispatch_decision_id`、`dispatch_reason_code`、`dispatch_reason_cn`、`created_by` 若为空白会被拒绝 |
| 3 | P6-B safety flags 禁止 `api_response_exposed=True` | API response 暴露属于 P6-E，不属于 P6-B |
| 4 | P6-B safety flags 禁止 `agent_message_written=True` | AgentMessage 写入属于 P6-D，不属于 P6-B |
| 5 | 更新 targeted tests | 覆盖必填文本空白值拒绝、未来阶段只读标记提前置真拒绝、P5 组合回归 |

### 14.3 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/domain/agent_dispatch_decision.py` | **修改** | 收紧 P6-B 纯域模型 contract | 否 |
| `runtime/orchestrator/tests/test_agent_dispatch_decision.py` | **修改** | 更新 P6-B R2 targeted tests | 否 |
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **修改** | 回填 P6-B R2 证据与 Gate 结论 | 否 |

### 14.4 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：106 passed |
| `git diff --check` | **Pass** |

### 14.5 边界声明

| 声明 | 结论 |
|------|------|
| 是否只修改 P6-B 纯 domain model | 是 |
| 是否只修改对应 P6-B targeted tests | 是 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否实现 `AgentDispatchDecisionBuilder` | 否 |
| 是否接 Worker / API / DB / AgentMessage | 否 |
| 是否修改前端 | 否 |
| 是否读取或照搬参考项目代码 | 否 |
| 是否实现 retry / 自动派发 / 创建任务 | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 14.6 P6-B R2 Gate

| Gate | 结论 |
|------|------|
| 必填文本字段空白值被拒绝 | **Pass** |
| P6-B safety flags 默认全 false | **Pass** |
| P6-B safety flags 拒绝 Git 写、CI、API 暴露、AgentMessage 写入、Worker dispatch、task creation、retry、auto dispatch 副作用 | **Pass** |
| P6-B targeted tests 通过 | **Pass** |
| P5 FailureRecoveryDecision targeted tests 仍通过 | **Pass** |
| 未改 P5 builder / 未实现 P6-C builder | **Pass** |
| 未接 Worker / API / DB / AgentMessage / 前端 | **Pass** |
| P6-C/P6-D/P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-B R2 Gate 结论** | **Pass** |

---

## 15. P6-B R3 audit_event_type contract 小修正（2026-06-07）

### 15.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-B R3** | P6-B `audit_event_type` 纯 domain contract 小修正 + targeted tests |
| 代码边界 | **未越过** | 仅修改 P6-B 纯 domain model、对应 targeted tests、P6 ledger |
| 修正目标 | **完成** | 补齐 `AgentDispatchDecision.audit_event_type` 稳定合约 |
| 是否写 AgentMessage | 否 | `audit_event_type` 只是未来审计事件类型 contract，不触发写入 |
| 是否进入 P6-C | 否 | 未实现 builder，未做 FailureRecoveryDecision → AgentDispatchDecision 映射 |
| 是否接 Worker / API / DB / AgentMessage / 前端 | 否 | 未接任何运行时链路 |
| 是否实现自动调度 / 自动派发 / retry | 否 | 仍只是结构化只读调度建议 contract |
| 是否实现产品运行时 Git 写操作 | 否 | 本轮开发提交不代表产品运行时具备 Git 写能力 |

### 15.2 修正清单

| # | 修正 | 说明 |
|---|------|------|
| 1 | `P6_AGENT_DISPATCH_DECISION_VERSION` 从 `p6_b.r2` 提升到 `p6_b.r3` | 标记本轮 contract 收紧 |
| 2 | 新增 `P6_AGENT_DISPATCH_DECISION_AUDIT_EVENT_TYPE` 常量 | 固定值为 `agent_dispatch_decision` |
| 3 | 新增 `AgentDispatchDecision.audit_event_type` 字段 | 默认值固定为 `agent_dispatch_decision` |
| 4 | 校验 `audit_event_type` 固定值与空白值 | 错误值或 trim 后为空会被拒绝 |
| 5 | 更新 targeted tests | 覆盖默认值、JSON 序列化、错误值拒绝、空白值拒绝、P5 组合回归 |

### 15.3 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/domain/agent_dispatch_decision.py` | **修改** | 补齐 P6-B `audit_event_type` 纯合约 | 否 |
| `runtime/orchestrator/tests/test_agent_dispatch_decision.py` | **修改** | 更新 P6-B R3 targeted tests | 否 |
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **修改** | 回填 P6-B R3 证据与 Gate 结论 | 否 |

### 15.4 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：108 passed |
| `git diff --check` | **Pass** |

### 15.5 边界声明

| 声明 | 结论 |
|------|------|
| 是否只修改 P6-B 纯 domain model | 是 |
| 是否只修改对应 P6-B targeted tests | 是 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否实现 `AgentDispatchDecisionBuilder` | 否 |
| 是否接 Worker / API / DB / AgentMessage | 否 |
| 是否修改前端 | 否 |
| 是否读取或照搬参考项目代码 | 否 |
| 是否实现 retry / 自动派发 / 创建任务 | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 15.6 P6-B R3 Gate

| Gate | 结论 |
|------|------|
| `audit_event_type` 默认值固定为 `agent_dispatch_decision` | **Pass** |
| `audit_event_type` 错误值或空白值被拒绝 | **Pass** |
| `audit_event_type` JSON 序列化稳定 | **Pass** |
| P6-B targeted tests 通过 | **Pass** |
| P5 FailureRecoveryDecision targeted tests 仍通过 | **Pass** |
| 未改 P5 builder / 未实现 P6-C builder | **Pass** |
| 未接 Worker / API / DB / AgentMessage / 前端 | **Pass** |
| P6-C/P6-D/P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-B R3 Gate 结论** | **Pass** |

---

## 16. P6-C FailureRecoveryDecision → AgentDispatchDecision builder（2026-06-07）

### 16.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-C** | FailureRecoveryDecision → AgentDispatchDecision 纯 builder + targeted tests |
| 代码边界 | **未越过** | 只在 P6 domain model 中新增纯 builder，并扩展对应 targeted tests |
| 输入 | **P5 FailureRecoveryDecision 实例** | 只读取 `recommended_owner`、`next_instruction_kind`、`next_instruction_draft`、`requires_human_decision`、`rule_codes`、`failure_category`、`reason_code` 等既有字段 |
| 输出 | **P6 AgentDispatchDecision 实例** | 只读调度建议，不执行、不派发、不重试 |
| 是否修改 P5 builder | 否 | 未修改 `FailureRecoveryDecisionBuilder` |
| 是否接 Worker / API / DB / AgentMessage | 否 | P6-D/E 仍 Not started |
| 是否修改前端 | 否 | P6-F 仍 Not started |
| 是否实现自动调度 / 自动派发 / retry | 否 | builder 只做确定性映射 |
| 是否实现产品运行时 Git 写操作 | 否 | 产品内部仍无 git add / commit / push / PR / merge / reset / checkout / switch / stash / rebase / tag 能力 |

### 16.2 实现清单

| # | 实现 | 说明 |
|---|------|------|
| 1 | 新增 `AgentDispatchDecisionBuilder.build_from_failure_recovery_decision(...)` | 从 P5 recovery decision 构建 P6 dispatch decision |
| 2 | P5 `RecoveryOwner.CODEX` → P6 `DispatchAgent.CODEX` / `DispatchStatus.SUGGESTED` | 透传中文指令草案，生成 `p5_owner_codex` reason code |
| 3 | P5 `RecoveryOwner.DEEPSEEK` → P6 `DispatchAgent.DEEPSEEK` / `DispatchStatus.SUGGESTED` | 透传中文指令草案，生成 `p5_owner_deepseek` reason code |
| 4 | P5 `requires_human_decision=True` 或 owner=user → P6 `DispatchAgent.USER` / `DispatchStatus.NEEDS_USER_DECISION` | 不带可执行 instruction draft |
| 5 | P5 owner=blocked → P6 `DispatchAgent.BLOCKED` / `DispatchStatus.BLOCKED` | 不带可执行 instruction draft |
| 6 | evidence refs 构建 | `rule_codes` 转为 `p5:<rule_code>`，并追加 `p5:failure_category:<value>` 与可选 `p5:reason_code:<value>` |
| 7 | safety flags 保持全 false | 未触发 Worker dispatch、retry、AgentMessage、API response、Git/CI、task creation、auto dispatch |

### 16.3 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/domain/agent_dispatch_decision.py` | **修改** | 新增 P6-C 纯 `AgentDispatchDecisionBuilder`，只做 P5 → P6 决策映射 | 否 |
| `runtime/orchestrator/tests/test_agent_dispatch_decision.py` | **修改** | 增加 P6-C builder targeted tests；保留 P6-B domain contract 回归 | 否 |
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **修改** | 回填 P6-C 证据与 Gate 结论 | 否 |

### 16.4 参考项目只读读取与命中点

参考项目路径：`/Users/kk/project explore/agent-orchestrator`。本轮只读参考机制，未复制代码。

| 命令 / 读取点 | 命中 / 读取内容 | 本项目采用方式 |
|---------------|----------------|----------------|
| `grep -RIn "dispatch\|recovery\|decision\|handoff\|agent" ... \| head -120` | 命中 `pnpm-lock.yaml` 中 agent 插件、`docs/design/feedback-routing-and-followup-design.md` 中 routing / dispatch / handoff / session / approval 等条目 | 仅确认参考项目存在插件化 agent 与 routing/handoff 概念 |
| `find .../packages -maxdepth 5 ... agent/orchestr/task/session/workflow/handoff/dispatch/decision/recovery` | 命中 CLI tests、commands、core/package 等路径 | 仅用于定位参考文件 |
| `packages/core/src/session-manager.ts` L1-L100 | Session Manager 说明：spawn/list/kill/cleanup/send messages；入口类型和插件编排边界 | 借鉴“状态/会话管理与调度建议分离”的机制，不照搬代码 |
| `packages/core/src/lifecycle-manager.ts` L1-L100 | Lifecycle Manager 说明：polling loop、state transitions、events、reactions、human notification | 借鉴“状态变化触发决策”的机制，本轮仅实现纯 builder，不接轮询或 reaction engine |
| `docs/design/feedback-routing-and-followup-design.md` grep 命中 L8/L13/L25/L42/L46/L58/L64/L118/L193/L222/L226/L273/L278/L279/L290 | routing mode、agent-session path、spawn policy、session contract、human approval、FeedbackRouter、FollowUpExecutor、governance hooks 等 | 借鉴 routing / handoff / human approval gate 概念；本轮只做 P5 → P6 只读 dispatch decision 映射 |

### 16.5 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：117 passed |
| `git diff --check` | **Pass** |

### 16.6 边界声明

| 声明 | 结论 |
|------|------|
| 是否只实现 P6-C 纯 builder | 是 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否接 Worker / API / DB / AgentMessage | 否 |
| 是否修改前端 | 否 |
| 是否新增运行时 dispatch / retry / task creation | 否 |
| 是否新增 API response DTO 或 schema | 否 |
| 是否写 AgentMessage / audit timeline | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否照搬参考项目代码 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 16.7 P6-C Gate

| Gate | 结论 |
|------|------|
| FailureRecoveryDecision → AgentDispatchDecision 纯 builder 已实现 | **Pass** |
| Codex / DeepSeek / user / blocked 映射覆盖 | **Pass** |
| evidence refs 可追溯到 P5 rule/failure/reason 信号 | **Pass** |
| safety flags 默认全 false 且保持只读 | **Pass** |
| P6-C targeted tests 通过 | **Pass** |
| P5 FailureRecoveryDecision targeted tests 仍通过 | **Pass** |
| 未改 P5 builder / 未接 Worker / API / DB / AgentMessage / 前端 | **Pass** |
| 未实现自动调度 / 自动派发 / retry / 创建任务 | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| P6-D/P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-C Gate 结论** | **Pass** |

---

## 17. P6-C R1 审查补证（2026-06-07）

### 17.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-C R1** | 审查补证 + ledger 回填 |
| 代码边界 | **未越过** | 未修改 Python domain model、测试代码、Worker、API、DB、AgentMessage、前端 |
| 是否做新功能 | 否 | 未新增 builder 行为，未进入 P6-D |
| 是否修改 P5 builder | 否 | 未修改 `FailureRecoveryDecisionBuilder` |
| 是否实现 retry / 自动派发 / 创建任务 | 否 | 仅审查 P6-C 纯 builder 证据 |
| 是否实现产品运行时 Git 写操作 | 否 | 产品内部仍无 git add / commit / push / PR / merge / reset / checkout / switch / stash / rebase / tag 能力 |

### 17.2 AgentDispatchDecisionBuilder 关键代码证据位置

| 证据 | 位置 | 结论 |
|------|------|------|
| Builder class | `runtime/orchestrator/app/domain/agent_dispatch_decision.py:304` | 仅位于 P6 domain model |
| 入口方法 | `runtime/orchestrator/app/domain/agent_dispatch_decision.py:315` | `build_from_failure_recovery_decision(...)` 只读构建 P6 decision |
| status 映射 | `runtime/orchestrator/app/domain/agent_dispatch_decision.py:361` | P5 human/user/blocked/codex/deepseek 映射为 P6 dispatch status |
| agent 映射 | `runtime/orchestrator/app/domain/agent_dispatch_decision.py:378` | P5 owner 映射为 P6 recommended_agent |
| evidence refs | `runtime/orchestrator/app/domain/agent_dispatch_decision.py:444` | P5 rule/failure/reason 信号转为 evidence refs |
| 运行时入口防线测试 | `runtime/orchestrator/tests/test_agent_dispatch_decision.py:413` | 断言 domain module 不存在 dispatch/retry/git/API 写入口 |
| Codex 映射测试 | `runtime/orchestrator/tests/test_agent_dispatch_decision.py:430` | execution failure → Codex suggested |
| DeepSeek 映射测试 | `runtime/orchestrator/tests/test_agent_dispatch_decision.py:464` | verification config failure → DeepSeek suggested |
| user 映射测试 | `runtime/orchestrator/tests/test_agent_dispatch_decision.py:485` | budget/retry-limit/human decision → user decision，无 draft |
| blocked 映射测试 | `runtime/orchestrator/tests/test_agent_dispatch_decision.py:534` | dependency/pause/status blocked → blocked，无 draft |
| safety flags 测试 | `runtime/orchestrator/tests/test_agent_dispatch_decision.py:567` | P6 safety flags 全 false，只读 |

### 17.3 禁止越界 grep 结果

| 命令 | 结果 | 结论 |
|------|------|------|
| `grep -RIn "AgentDispatchDecisionBuilder\|build_from_failure_recovery_decision" runtime/orchestrator/app runtime/orchestrator/tests docs --exclude-dir=__pycache__ --exclude='*.pyc' \| head -200` | 仅命中 `agent_dispatch_decision.py`、`test_agent_dispatch_decision.py`、P6 ledger | Builder 仍局限于 domain/tests/docs |
| `grep -RIn "AgentDispatchDecisionBuilder\|build_from_failure_recovery_decision" runtime/orchestrator/app/api runtime/orchestrator/app/workers runtime/orchestrator/app/services runtime/orchestrator/app/repositories apps 2>/dev/null \| head -200 \|\| true` | 无输出 | 未接 API / Worker / services / repositories / 前端 apps |
| `grep -RIn "def dispatch_worker\|def create_task\|def write_agent_message\|def retry_run\|def expose_api_response\|git add\|git commit\|git push\|create PR\|merge PR\|checkout\|switch\|stash\|rebase\|tag" runtime/orchestrator/app/domain/agent_dispatch_decision.py runtime/orchestrator/tests/test_agent_dispatch_decision.py \| head -200 \|\| true` | 仅命中模块禁止文案、safety flags、测试 forbidden copy/forbidden symbol 断言 | 未发现运行时 Git 写、dispatch、retry、API 暴露、AgentMessage 写入口 |

### 17.4 参考项目只读读取记录

参考项目路径：`/Users/kk/project explore/agent-orchestrator`。本轮只读复核机制，未复制代码。

| 读取点 | 读取范围 / 命中 | 审查结论 |
|--------|----------------|----------|
| `packages/core/src/session-manager.ts` | L1-L70：Session Manager CRUD、spawn/list/kill/cleanup/send messages 说明 | 仅用于确认参考项目把会话生命周期与 agent 插件编排分层；P6-C 未接 session/runtime |
| `packages/core/src/lifecycle-manager.ts` | L1-L80：state machine、polling loop、transition、reaction、human notification 说明 | 仅用于确认状态变化触发反应机制；P6-C 未接 polling/reaction engine |
| `docs/design/feedback-routing-and-followup-design.md` | grep 命中 L8/L13/L25/L42/L46/L58/L64/L118/L193/L222/L226/L273/L278/L279/L290 | 仅用于确认 routing / handoff / human approval / executor side-effect 边界；P6-C 保持只读 builder |

### 17.5 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：117 passed |
| `git diff --check` | **Pass** |

### 17.6 边界声明

| 声明 | 结论 |
|------|------|
| 是否只做 P6-C 审查补证和 ledger 回填 | 是 |
| 是否修改 P6-C builder 功能 | 否 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否接 Worker / API / DB / AgentMessage | 否 |
| 是否修改前端 | 否 |
| 是否新增运行时 dispatch / retry / task creation | 否 |
| 是否新增 API response DTO 或 schema | 否 |
| 是否写 AgentMessage / audit timeline | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否照搬参考项目代码 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 17.7 P6-C R1 Gate

| Gate | 结论 |
|------|------|
| P6-C builder 关键代码位置已复核 | **Pass** |
| 禁止越界 grep 已完成 | **Pass** |
| API / Worker / services / repositories / 前端 apps 未接入 builder | **Pass** |
| 未发现运行时 Git 写、dispatch、retry、API 暴露、AgentMessage 写入口 | **Pass** |
| 参考项目只读读取记录已回填 | **Pass** |
| P6-C/P5 targeted tests 通过 | **Pass** |
| `git diff --check` 通过 | **Pass** |
| P6-D/P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-C R1 Gate 结论** | **Pass** |

---

## 18. P6-D AgentMessage / audit timeline 记录调度建议（2026-06-07）

### 18.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-D** | AgentMessage / audit timeline 记录调度建议 |
| 代码边界 | **未越过** | 只新增 P6 dispatch audit service、worker failed/cancelled 路径 best-effort 接入、targeted tests |
| 输入 | **P6-C AgentDispatchDecision** | 由 `AgentDispatchDecisionBuilder.build_from_failure_recovery_decision(...)` 从 P5 decision 生成 |
| 输出 | **AgentMessage timeline row** | 只记录 audit/timeline，不对 API response 透传 dispatch decision |
| 是否进入 P6-E | 否 | 未新增 WorkerRunOnceResponse dispatch DTO 或 schema 字段 |
| 是否修改前端 | 否 | P6-F 仍 Not started |
| 是否实现 retry / 自动派发 / 创建任务 | 否 | 只记录只读审计事件 |
| 是否实现产品运行时 Git 写操作 | 否 | 产品内部仍无 git add / commit / push / PR / merge / reset / checkout / switch / stash / rebase / tag 能力 |

### 18.2 实现清单

| # | 实现 | 说明 |
|---|------|------|
| 1 | 新增 `AgentDispatchAuditService` | 将 `AgentDispatchDecision` 持久化为 `AgentMessageType.TIMELINE` |
| 2 | AgentMessage 幂等记录 | 同一 run + `agent_dispatch_decision` event_type 重复记录返回既有 message |
| 3 | structured detail | `content_detail` 写入 `p6_stage=P6-D`、run/task 状态、dispatch decision JSON、P6-D safety flags |
| 4 | user-facing summary | `content_summary` 仅中文摘要，不暴露内部 reason code / enum 原值 |
| 5 | TaskWorker best-effort 接入 | 在已有 P5 failure recovery audit helper 中追加 P6 dispatch audit 记录 |
| 6 | build_task_worker wiring | 使用既有 `AgentMessageRepository` 构建 `AgentDispatchAuditService` |
| 7 | P6-E 边界断言 | targeted test 明确 `WorkerRunOnceResponse` 仍不包含 `agent_dispatch_decision` / `dispatch_decision` |

### 18.3 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/services/agent_dispatch_audit_service.py` | **新增** | P6-D dispatch audit timeline service | 否 |
| `runtime/orchestrator/app/workers/task_worker.py` | **修改** | failed/cancelled 路径 best-effort 记录 P6 dispatch audit message | 否 |
| `runtime/orchestrator/tests/test_agent_dispatch_audit_service.py` | **新增** | P6-D service targeted tests | 否 |
| `runtime/orchestrator/tests/test_worker_failed_recovery_decision.py` | **修改** | 扩展 failed worker timeline 测试，覆盖 P6-D audit message 与 P6-E 未启动边界 | 否 |
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **修改** | 回填 P6-D 证据与 Gate 结论 | 否 |

### 18.4 AgentDispatchAuditService 关键代码位置

| 证据 | 位置 | 结论 |
|------|------|------|
| Service class | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:16` | P6-D audit timeline service |
| `record_decision(...)` | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:28` | 记录 dispatch decision 为 AgentMessage timeline |
| 幂等检查 | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:39` | 同一 run + event_type 返回既有 message |
| summary | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:84` | 中文只读摘要，不暴露内部 reason code / enum 原值 |
| detail JSON | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:106` | 写入 `p6_stage`、decision JSON、P6-D safety flags |

### 18.5 task_worker 接入点位置

| 证据 | 位置 | 结论 |
|------|------|------|
| import builder | `runtime/orchestrator/app/workers/task_worker.py:18` | 只使用 P6-C builder 构建 audit 输入 |
| import service | `runtime/orchestrator/app/workers/task_worker.py:63` | 引入 P6-D audit service |
| constructor 注入 | `runtime/orchestrator/app/workers/task_worker.py:1393` | 可选依赖，保持 best-effort |
| 实例字段 | `runtime/orchestrator/app/workers/task_worker.py:1417` | 保存可选 audit service |
| failed/cancelled hook | `runtime/orchestrator/app/workers/task_worker.py:3752` | 在 P5 recovery audit 后记录 P6 dispatch audit |
| worker factory | `runtime/orchestrator/app/workers/task_worker.py:4587` | 基于既有 `AgentMessageRepository` 构建 service |
| factory wiring | `runtime/orchestrator/app/workers/task_worker.py:4670` | 注入 `TaskWorker` |

### 18.6 参考项目只读读取与 grep 命中点

参考项目路径：`/Users/kk/project explore/agent-orchestrator`。本轮只读参考机制，未复制代码。

| 读取点 / 命中 | 内容 | 本项目采用方式 |
|---------------|------|----------------|
| `packages/core/src/activity-events.ts` L1-L120 | `recordActivityEvent()` best-effort、event source/kind/level、结构化 data | 借鉴 best-effort audit event 思路；本项目写 AgentMessage timeline |
| `grep -RIn "recordActivityEvent\|activity event\|event_type\|source" packages/core/src ...` | 命中 `activity-events.ts`、`events-db.ts`、`session-manager.ts`、`recovery/manager.ts`、`recovery/actions.ts`、`agent-report.ts` 等 | 仅确认 AO 使用结构化 activity event 与 source/kind 分类；未照搬实现 |
| `packages/core/src/session-manager.ts` L1-L70 | Session Manager CRUD / plugin orchestration 边界 | 仅用于确认会话生命周期与审计分层；P6-D 未接自动 session spawn |
| `packages/core/src/lifecycle-manager.ts` L1-L80 | polling loop、transition、reaction、human notification | 仅用于确认状态变化可产生审计事件；P6-D 未接 reaction engine |
| `docs/design/feedback-routing-and-followup-design.md` grep 命中 routing/dispatch/handoff/approval/session | routing、handoff、人类审批、executor side-effect 边界 | 仅用于校验 P6-D 保持只读 audit，不自动派发 |

### 18.7 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_audit_service.py tests/test_worker_failed_recovery_decision.py tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：132 passed |
| `git diff --check` | **Pass** |

### 18.8 边界声明

| 声明 | 结论 |
|------|------|
| 是否只实现 P6-D audit timeline 记录 | 是 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否修改 P6-C builder 合约 | 否 |
| 是否新增 API response DTO / WorkerRunOnceResponse 字段 | 否 |
| 是否接前端展示 | 否 |
| 是否自动派发 Codex / DeepSeek | 否 |
| 是否实现 retry / 创建任务 | 否 |
| 是否触发 CI | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否照搬参考项目代码 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 18.9 P6-D Gate

| Gate | 结论 |
|------|------|
| AgentDispatchAuditService 已实现 | **Pass** |
| TaskWorker failed/cancelled 路径已 best-effort 记录 dispatch audit | **Pass** |
| AgentMessage timeline 幂等记录已覆盖 | **Pass** |
| P6-D safety flags 保持无 API 暴露、无 dispatch、无 retry、无 task creation、无 Git/CI | **Pass** |
| P6-E API response 透传仍 Not started | **Pass** |
| P6-F 前端展示仍 Not started | **Pass** |
| targeted tests 通过 | **Pass** |
| `git diff --check` 通过 | **Pass** |
| P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-D Gate 结论** | **Pass** |

---

## 19. P6-D R1 审查补证 + 必要最小修正（2026-06-07）

### 19.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-D R1** | 审查补证 + 必要最小修正 |
| 代码边界 | **未越过** | 只修正 P6-D audit helper 的可选依赖短路问题，并补 targeted test |
| 必要小修 | **完成** | P6-D dispatch audit 记录不再依赖 P5 failure recovery audit service 必须存在 |
| 是否进入 P6-E | 否 | 未改 API response，未改 `WorkerRunOnceResponse` DTO/schema |
| 是否修改前端 | 否 | P6-F 仍 Not started |
| 是否实现 retry / 自动派发 / 创建任务 | 否 | 仍只记录只读审计事件 |
| 是否实现产品运行时 Git 写操作 | 否 | 产品内部仍无 git add / commit / push / PR / merge / reset / checkout / switch / stash / rebase / tag 能力 |

### 19.2 必要最小修正说明

| 修正点 | 修正前 | 修正后 | 结论 |
|--------|--------|--------|------|
| P6-D audit 可选依赖 | `_record_failure_recovery_audit_if_needed` 在 `failure_recovery_audit_service is None` 时直接 return，导致 P6-D audit service 也被短路 | 只有 P5 audit service 与 P6-D audit service 均为 `None` 时才 return；两者分别独立判断并 best-effort 记录 | P6-D audit 不再依赖 P5 audit service 存在 |
| targeted test | 仅覆盖缺少 session/decision 时跳过 | 新增 `test_worker_dispatch_audit_helper_does_not_require_p5_audit_service` | 覆盖 P6-D 独立可选服务边界 |

### 19.3 AgentDispatchAuditService 关键代码证据行号

| 证据 | 位置 | 结论 |
|------|------|------|
| Service class | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:16` | P6-D audit timeline service |
| `record_decision(...)` | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:28` | 记录 dispatch decision 为 AgentMessage timeline |
| 幂等检查 | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:39` | 同一 run + event_type 返回既有 message |
| summary | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:84` | 中文只读摘要，不暴露内部 reason code / enum 原值 |
| detail JSON | `runtime/orchestrator/app/services/agent_dispatch_audit_service.py:106` | 写入 `p6_stage`、decision JSON、P6-D safety flags |

### 19.4 task_worker 接入点行号

| 证据 | 位置 | 结论 |
|------|------|------|
| import builder | `runtime/orchestrator/app/workers/task_worker.py:18` | 只使用 P6-C builder 构建 audit 输入 |
| import service | `runtime/orchestrator/app/workers/task_worker.py:63` | 引入 P6-D audit service |
| constructor 注入 | `runtime/orchestrator/app/workers/task_worker.py:1393` | 可选依赖，保持 best-effort |
| 实例字段 | `runtime/orchestrator/app/workers/task_worker.py:1417` | 保存可选 audit service |
| 独立可选服务判断 | `runtime/orchestrator/app/workers/task_worker.py:3728` | P5 audit 与 P6-D audit 均为空才跳过 |
| P5 audit 独立判断 | `runtime/orchestrator/app/workers/task_worker.py:3744` | P5 audit service 存在时记录 P5 timeline |
| P6-D audit 独立判断 | `runtime/orchestrator/app/workers/task_worker.py:3752` | P6-D audit service 存在时记录 dispatch timeline |
| worker factory | `runtime/orchestrator/app/workers/task_worker.py:4587` | 基于既有 `AgentMessageRepository` 构建 service |
| factory wiring | `runtime/orchestrator/app/workers/task_worker.py:4670` | 注入 `TaskWorker` |

### 19.5 禁止越界 grep 结果

| 命令 | 结果 | 结论 |
|------|------|------|
| `grep -RIn "agent_dispatch_decision\|dispatch_decision\|AgentDispatchAuditService\|agent_dispatch_audit_service" runtime/orchestrator/app/api apps runtime/orchestrator/app/workers runtime/orchestrator/app/services runtime/orchestrator/tests --exclude-dir=__pycache__ --exclude='*.pyc' \| head -300` | 仅命中 P6-D service、task_worker、P6-D/P6-C tests；API 命中仅为既有 P5 response DTO，不含 P6 dispatch response 字段 | 未接 P6-E API response / apps 前端 |
| `grep -RIn "def git_add\|def git_commit\|def git_push\|subprocess.*git\|git add\|git commit\|git push\|create PR\|merge PR\|retry_run\|dispatch_worker\|auto_dispatch" runtime/orchestrator/app/services/agent_dispatch_audit_service.py runtime/orchestrator/app/workers/task_worker.py runtime/orchestrator/tests/test_agent_dispatch_audit_service.py runtime/orchestrator/tests/test_worker_failed_recovery_decision.py \| head -200 \|\| true` | 仅命中安全字段 / 禁止文案 / 测试断言中的 `auto_dispatch_triggered=False` 与既有只读说明 | 未新增运行时 Git 写、retry、自动派发、CI 触发入口 |

### 19.6 参考项目只读读取记录

参考项目路径：`/Users/kk/project explore/agent-orchestrator`。本轮只读复核机制，未复制代码。

| 读取点 | 读取范围 / 命中 | 审查结论 |
|--------|----------------|----------|
| `packages/core/src/activity-events.ts` | L1-L140：`recordActivityEvent()` best-effort，不抛出异常；source/kind/level/data 结构 | 仅用于复核 P6-D best-effort audit 思路；未照搬代码 |
| `grep -RIn "recordActivityEvent\|source: \"recovery\"\|kind: \"recovery" ...` | 命中 `recovery/manager.ts`、`recovery/actions.ts`、`session-manager.ts`、`lifecycle-manager.ts` 等 recordActivityEvent 调用点 | 仅确认结构化 audit event 可作为 side-effect 边界内的记录机制 |
| `packages/core/src/session-manager.ts` | 多处 `recordActivityEvent` 调用与 session lifecycle 操作分离 | P6-D 仍只写 AgentMessage，不创建/启动 session |
| `packages/core/src/lifecycle-manager.ts` | 多处 lifecycle transition / reaction 事件记录 | P6-D 未接 polling / reaction engine |

### 19.7 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_audit_service.py tests/test_worker_failed_recovery_decision.py tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：133 passed，38 warnings（既有 datetime.utcnow deprecation warning） |
| `git diff --check` | **Pass** |

### 19.8 边界声明

| 声明 | 结论 |
|------|------|
| 是否只做 P6-D 审查补证和必要最小修正 | 是 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否修改 P6-C builder 合约 | 否 |
| 是否新增 API response DTO / WorkerRunOnceResponse 字段 | 否 |
| 是否接前端展示 | 否 |
| 是否自动派发 Codex / DeepSeek | 否 |
| 是否实现 retry / 创建任务 | 否 |
| 是否触发 CI | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否照搬参考项目代码 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 19.9 P6-D R1 Gate

| Gate | 结论 |
|------|------|
| P6-D audit service 关键代码已复核 | **Pass** |
| P6-D audit helper 不再依赖 P5 audit service 存在 | **Pass** |
| 禁止越界 grep 已完成 | **Pass** |
| 未接 P6-E API response / WorkerRunOnceResponse DTO / 前端 apps | **Pass** |
| 未新增运行时 Git 写、retry、自动派发、CI 触发入口 | **Pass** |
| 参考项目只读读取记录已回填 | **Pass** |
| targeted tests 通过 | **Pass** |
| `git diff --check` 通过 | **Pass** |
| P6-E/P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-D R1 Gate 结论** | **Pass** |

---

## 20. P6-E WorkerRunOnceResponse 只读透传 dispatch decision（2026-06-07）

### 20.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P6-E** | WorkerRunOnceResponse 只读透传 dispatch decision |
| 代码边界 | **未越过** | 只新增 WorkerRunResult 内部只读字段、WorkerRunOnceResponse DTO、targeted tests 与本 ledger 证据 |
| 输入 | **P6-D 已生成 / P6-C builder 生成的 AgentDispatchDecision** | WorkerResult 初始化生成内部只读决策；P6-D audit helper 复用同一个对象 |
| 输出 | **API response `agent_dispatch_decision`** | 只读 DTO，包含中文 label 与 response-level `api_response_exposed=True` |
| 是否修改 P6-C builder 合约 | 否 | 未改 `AgentDispatchDecisionBuilder` 映射规则 |
| 是否修改 P5 FailureRecoveryDecision / builder | 否 | 未改 P5 domain/builder |
| 是否修改前端 | 否 | P6-F 仍 Not started |
| 是否实现 retry / 自动派发 / 创建任务 | 否 | 只读透传，不触发 worker dispatch / retry / task creation |
| 是否实现产品运行时 Git 写操作 | 否 | 产品内部仍无 git add / commit / push / PR / merge / reset / checkout / switch / stash / rebase / tag 能力 |

### 20.2 实现清单

| # | 实现 | 说明 |
|---|------|------|
| 1 | `WorkerRunResult.agent_dispatch_decision` | 新增内部只读字段，持有 P6 dispatch decision |
| 2 | `_build_worker_agent_dispatch_decision(...)` | 复用 P6-C builder 从既有 P5 recovery decision 构建 P6 decision；不派发、不写 Git、不 retry |
| 3 | P6-D audit helper 复用 result 字段 | P6-D AgentMessage audit 与 P6-E API response 读取同一个 `AgentDispatchDecision` 对象 |
| 4 | `WorkerRunOnceResponse.AgentDispatchDecisionResponse` | 新增只读 DTO，包含中文 agent/status/instruction label、dispatch reason、evidence refs、safety |
| 5 | response-level exposure flag | DTO 顶层 `api_response_exposed=True`，DTO safety 中 `api_response_exposed=True`；domain 对象仍保持 false |
| 6 | targeted tests | 覆盖 success 无 dispatch、failed/cancelled 透传、Codex/DeepSeek/User/Blocked 映射、safety flags、P6-D audit 复用对象 |

### 20.3 文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `runtime/orchestrator/app/workers/task_worker.py` | **修改** | 新增 `WorkerRunResult.agent_dispatch_decision` 与只读构建/复用逻辑 | 否 |
| `runtime/orchestrator/app/api/routes/workers.py` | **修改** | 新增 `AgentDispatchDecisionResponse` DTO 与 `agent_dispatch_decision` response 字段 | 否 |
| `runtime/orchestrator/tests/test_worker_failed_recovery_decision.py` | **修改** | 扩展 P6-E targeted tests，替换 P6-D 阶段“不透传”边界断言 | 否 |
| `06-P6智能体调度/P6智能体调度总账与阶段设计-20260607.md` | **修改** | 回填 P6-E 证据与 Gate 结论 | 否 |

### 20.4 关键代码位置

| 证据 | 位置 | 结论 |
|------|------|------|
| WorkerRunResult 新增字段 | `runtime/orchestrator/app/workers/task_worker.py:375` | `agent_dispatch_decision: AgentDispatchDecision \| None = None` |
| WorkerRunResult 初始化赋值 | `runtime/orchestrator/app/workers/task_worker.py:393` | `__post_init__` 中仅在字段为空时构建只读 P6 decision |
| P6-E 构建 helper | `runtime/orchestrator/app/workers/task_worker.py:418` | `_build_worker_agent_dispatch_decision(...)` 复用 P6-C builder |
| P6-D audit 复用字段 | `runtime/orchestrator/app/workers/task_worker.py:3782` | audit helper 先补齐 `result.agent_dispatch_decision`，再传给 audit service |
| WorkerRunOnceResponse DTO | `runtime/orchestrator/app/api/routes/workers.py:280` | `AgentDispatchDecisionResponse` 只读 DTO |
| WorkerRunOnceResponse 新增字段 | `runtime/orchestrator/app/api/routes/workers.py:690` | `agent_dispatch_decision: AgentDispatchDecisionResponse \| None = None` |
| API response 序列化 | `runtime/orchestrator/app/api/routes/workers.py:1300` | `WorkerRunOnceResponse.from_result(...)` 序列化 `result.agent_dispatch_decision` |

### 20.5 参考项目只读读取与 grep 命中点

参考项目路径：`/Users/kk/project explore/agent-orchestrator`。本轮只读参考机制，未复制代码。

| 读取点 / 命中 | 内容 | 本项目采用方式 |
|---------------|------|----------------|
| `packages/core/src/activity-events.ts` L1-L120 | `recordActivityEvent()` best-effort 写入、source/kind/level/data 结构、失败不抛出 | 仅复核 audit/response 应保持只读边界；未照搬代码 |
| `packages/core/src/recovery/manager.ts` grep 命中 `recordActivityEvent` | recovery failure 事件以结构化 data 记录 recovery/action/classification | 仅用于确认 recovery/dispatch 证据可结构化表达；本轮只做 API DTO 透传 |
| `packages/core/src/session-manager.ts` grep 命中 `recordActivityEvent` 与 dispatch-hash 注释 | session send failure 与 duplicate dispatch 防线分离 | 本项目 P6-D audit 与 P6-E response 复用同一个 result decision，避免同一路径重复生成不同对象 |
| `packages/core/src/lifecycle-manager.ts` grep 命中 `dispatch` / `recordActivityEvent` | lifecycle reaction/notification 中存在 dispatch 去重与事件记录 | 仅用于确认 P6-E 不应接 reaction engine 或自动派发 |
| `grep -RIn "run-once\|response\|dispatch\|routing\|recordActivityEvent\|activity event" ...` | 命中 `activity-events.ts`、`events-db.ts`、`recovery/manager.ts`、`recovery/actions.ts`、`session-manager.ts`、`lifecycle-manager.ts` 等 | 只读复核，未复制实现 |

### 20.6 禁止越界 grep 结果

| 命令 | 结果 | 结论 |
|------|------|------|
| `grep -RIn "agent_dispatch_decision\|dispatch_decision\|AgentDispatchDecisionResponse" runtime/orchestrator/app/api runtime/orchestrator/app/workers runtime/orchestrator/tests apps --exclude-dir=__pycache__ --exclude='*.pyc' \| head -300` | 命中 API DTO/序列化、WorkerRunResult 字段/helper、P6-C/P6-D/P6-E tests；未命中 `apps` 前端实现 | P6-E 后端透传已接入，P6-F 前端仍 Not started |
| `grep -RIn "def git_add\|def git_commit\|def git_push\|subprocess.*git\|git add\|git commit\|git push\|create PR\|merge PR\|retry_run\|dispatch_worker\|auto_dispatch" runtime/orchestrator/app/api/routes/workers.py runtime/orchestrator/app/workers/task_worker.py runtime/orchestrator/tests/test_worker_failed_recovery_decision.py --exclude-dir=__pycache__ --exclude='*.pyc' \| head -200 \|\| true` | 仅命中 `auto_dispatch_triggered=False` safety 字段/断言与既有只读说明 | 未新增运行时 Git 写、retry、自动派发、CI 触发入口 |

### 20.7 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_agent_dispatch_decision.py tests/test_worker_failed_recovery_decision.py -q` | **Pass**：75 passed，34 warnings（既有 datetime.utcnow deprecation warning） |
| `pytest tests/test_agent_dispatch_audit_service.py tests/test_worker_failed_recovery_decision.py tests/test_agent_dispatch_decision.py tests/test_failure_recovery_decision.py -q` | **Pass**：133 passed，38 warnings（既有 datetime.utcnow deprecation warning） |
| `git diff --check` | **Pass** |

### 20.8 边界声明

| 声明 | 结论 |
|------|------|
| 是否只做 P6-E WorkerRunOnceResponse 只读透传 | 是 |
| 是否修改 P5 `FailureRecoveryDecision` 或 builder | 否 |
| 是否修改 P6-C builder 合约 | 否 |
| 是否新增 AgentMessage 写入逻辑 | 否 |
| 是否接前端展示 | 否 |
| 是否自动派发 Codex / DeepSeek | 否 |
| 是否实现 retry / 创建任务 | 否 |
| 是否触发 CI | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否照搬参考项目代码 | 否 |
| 是否把 P6 Agent Orchestration 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |

### 20.9 P6-E Gate

| Gate | 结论 |
|------|------|
| WorkerRunResult 已新增只读 dispatch decision 字段 | **Pass** |
| WorkerRunOnceResponse 已新增只读 `agent_dispatch_decision` DTO | **Pass** |
| response-level `api_response_exposed=True` 已只在 DTO 层设置 | **Pass** |
| domain safety flags 仍不允许提前置真 API/AgentMessage/dispatch/retry/Git 写副作用 | **Pass** |
| P6-D audit 与 P6-E response 复用同一个 result decision | **Pass** |
| targeted tests 通过 | **Pass** |
| `git diff --check` 通过 | **Pass** |
| P6-F/P6-G 仍 Not started | **Pass** |
| P7 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P6-E Gate 结论** | **Pass** |
