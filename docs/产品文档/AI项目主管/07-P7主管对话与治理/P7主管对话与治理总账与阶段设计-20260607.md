# P7 主管对话中枢与治理总账与阶段设计

> **文档类型**: P7 阶段 Ledger + Stage Design（合一）
> **生成日期**: 2026-06-07
> **远端基准**: `origin/main` = `3c72e3943451c52539b4c1d7f47ad580378277c3`（P8/P9 外部执行器规划）
> **路线文档**: `../01-总规划与路线/P1-P7参考规划复用说明书.md`
> **后续路线文档**: `../01-总规划与路线/P8-P9外部执行器配置与运行时规划-20260607.md`
> **主产品基线**: `../02-页面与产品基线/页面信息架构-20260518.md`
> **前置阶段**: P1 Coding Session + Worktree Lifecycle: Pass；P2 Worker Context + Runtime Safe Execution: Pass；P3 Runtime Lifecycle Evidence + Event Audit: Pass；P4 Git Delivery + Human Approval Gate: Pass；P5 Failure Recovery: Pass；P6 Agent Orchestration: Pass（scoped gate）
> **历史输入文档**: `P7对话中枢设计-20260603.md`、`P7对话中枢审计-20260603.md`（Stage 7-B0/7-B 历史基线，不删除不覆盖）
> **状态**: P7-A Design/Ledger: Pass；P7-B Current-state audit / contract freeze: Pass；P7-C Conversation/ConversationList contract: Pass；P7-C1 initial implementation: Partial，需要 R1；P7-C1-R1 ConversationList read-only API contract correction: Pass；P7-C2 ConversationList frontend API/types/hooks: Pass；P7-C3 Workbench ConversationList read-only UI: Pass；P7-D DirectorInbox contract: Pass；P7-D1 DirectorInbox synthetic read-only API: Pass；P7-E 至 P7 Final: Not started；P7 总 Gate: Not started；P8/P9 implementation: Not started；AI Project Director 总闭环: Partial

---

## 1. P7 当前基准

| 项目 | 状态 | 证据 |
|------|------|------|
| `origin/main` HEAD | `f7736b97bc3a84d5c1e1bb9d89bd5aabbe11befd` | docs: start P7 conversation hub governance ledger |
| P7-B 审计基线 | `f7736b9` | 本轮审计 11 个代码文件 + 两套 targeted test 文件 |
| P1 Coding Session + Worktree Lifecycle | **Pass** | P1 closure |
| P2 Worker Context + Runtime Safe Execution | **Pass** | P2 closure |
| P3 Runtime Lifecycle Evidence + Event Audit | **Pass** | P3 closure |
| P4 Git Delivery + Human Approval Gate | **Pass** | P4 ledger final gate |
| P5 Failure Recovery | **Pass** | P5 ledger final gate |
| P6 Agent Orchestration | **Pass**（scoped gate） | P6 ledger final gate |
| P8/P9 roadmap | **Pass**（仅路线规划） | P8-P9 外部执行器配置与运行时规划 |
| P7-A Design/Ledger | **Pass**（本文档） | — |
| P7-B Current-state audit / contract freeze | **Pass** | 本文档 §10 |
| P7-C Conversation/ConversationList contract | **Pass** | 本文档 §11 |
| P7-C1 initial implementation | **Partial，需要 R1** | 本文档 §13；初版只读后端实现存在 contract 缺口 |
| P7-C1-R1 ConversationList read-only API contract correction | **Pass** | 本文档 §14；before cursor / limit / kind 修正 + targeted tests |
| P7-C2 ConversationList frontend API/types/hooks | **Pass** | 本文档 §15；前端 read-only 类型/API/hooks 接线 + build |
| P7-C3 Workbench ConversationList read-only UI | **Pass** | 本文档 §16；Workbench 只读列表 + 选择恢复 + session null 输入修正 |
| P7-D DirectorInbox contract | **Pass** | 本文档 §12 |
| P7-D1 DirectorInbox synthetic read-only API | **Pass** | 本文档 §17；synthetic A/B/C sources + targeted tests |
| P7-E 至 P7-J | **Not started** | — |
| P7 Final Gate | **Not started** | — |
| **P7 总 Gate** | **Not started** | 需 P7-A 至 P7-J 全部完成后评估 |
| P8/P9 implementation | **Not started** | — |
| 产品运行时 git add / commit / push / PR | **Not started** | — |
| **AI Project Director 总闭环** | **Partial** | P7 完成前不得写 Pass |

---

## 2. P7 核心定位

### 2.1 P7 是什么

P7 Project Director Conversation Hub + Governance 的目标是：

**让系统从"安全 Agent 编排器"升级为"可讨论、可质疑、可影响计划的 AI 项目主管"。**

引用 P1-P7 参考规划复用说明书第 5 节的核心判断：

> P1-P6 是理想 AI 主管的必要条件，但不是充分条件。
> 如果没有 P7 Conversation Hub，系统最终会更接近一个
> 能执行代码任务、能做工作区隔离、能做审批与交付治理的安全 Agent 编排器，
> 而不是理想中的 AI 项目主管。

### 2.2 P7 不是什么

| # | P7 不是 | 说明 |
|---|--------|------|
| 1 | 不是外部执行器接入 | P8/P9 才处理 executor config / runtime adapter |
| 2 | 不等于自动派发 | 不自动派发 Codex / Claude Code / DeepSeek |
| 3 | 不是 Git 写操作 | 产品运行时全线禁止 |
| 4 | 不是 P6 调度建议的自动执行 | P6 只出建议，P7 可以讨论建议，但不执行 |
| 5 | 不是 P8/P9 的实现 | P8/P9 是 P7 后的独立阶段 |

### 2.3 P7 与 P8/P9 的关系

| P7（本阶段） | P8/P9（后续） |
|-------------|-------------|
| 产出对话、质疑、建议、提案、审批治理 | 产出 executor config discovery + controlled launch |
| 设计 DirectorActionProposal | 设计 ExecutorLaunchPreview |
| 讨论"应该让 Codex 修这个 bug 吗？" | 展示"Codex CLI 可用，launch command 已预览" |
| 用户确认提案 | feature flag + 用户确认 + workspace 验证后真实启动 |
| 对话和提案可审计 | 执行器 session 可追踪 |

**P7 可以设计 DirectorActionProposal，但不能启动 Codex / Claude Code / DeepSeek。**

如果后续要把 Proposal 转成 Executor Launch Preview，必须进入 P8/P9，不能在 P7 偷做。

### 2.4 参考机制

参考 Agent Orchestrator 的以下机制（不照搬代码）：

| AO 机制 | P7 映射 |
|---------|--------|
| Activity event：结构化事件类型、source 分类、best-effort 写入 | P7 conversation / challenge / proposal / audit event |
| Audit trail：完整事件记录，按 session / project 可追踪 | P7 对话与提案全链路回放 |
| Lifecycle state：只表达真实状态，不把建议写成执行 | P7 对话 session 状态、提案状态、审批状态 |
| Front-end observability：dashboard / session list / activity timeline | 工作台多会话、inbox、proposal 状态可见 |
| Feedback routing：report → issue → session → PR 链路 | P7 conversation → proposal → plan revision 链路 |
| Governance hooks：canCreateIssue / canCreateFork / canCreatePR | P7 proposal approval governance hooks |
| Human approval gate：关键操作需人类确认 | P7 提案审批必须用户显式确认 |

参考项目只读检查（`/Users/kk/project explore/agent-orchestrator`）：

| 参考来源 | 读取文件 | 只借鉴机制 | 结论 |
|----------|----------|------------|------|
| AO activity-events.ts | `packages/core/src/activity-events.ts`（L1-60） | 结构化 event source/kind/level + best-effort 写入 | **未照搬代码** |
| AO lifecycle-state.ts | `packages/core/src/lifecycle-state.ts` | CanonicalSessionLifecycle 只表达真实状态 | **未照搬代码** |
| AO session-manager.ts | `packages/core/src/session-manager.ts`（L1-100） | session lifecycle CRUD + 事件记录 | **未照搬代码** |
| AO lifecycle-manager.ts | `packages/core/src/lifecycle-manager.ts`（L1-100） | state machine + polling + reaction engine | **未照搬代码** |
| AO feedback-routing-and-followup-design.md | `docs/design/feedback-routing-and-followup-design.md` | routing / follow-up / consent gates / governance hooks | **未照搬代码** |

---

## 3. 当前已有能力盘点（可复用基础，不是 P7 已完成）

### 3.1 已有 Project Director 状态机与 API

以下能力来自当前代码（`origin/main` = `3c72e39`），**可作为 P7 设计输入，但不能直接判 P7 Pass**：

| 现有能力 | 代码位置 | P7 可复用程度 |
|---------|---------|-------------|
| Project Director session 状态机 | `runtime/orchestrator/app/api/routes/project_director.py` | 高 — DRAFT → CLARIFYING → READY_TO_CONFIRM → CONFIRMED 状态链 |
| ProjectDirectorMessage 域模型 | `runtime/orchestrator/app/domain/project_director_message.py` | 高 — User/Assistant/System 角色、session 绑定、sequence_no |
| ProjectDirectorMessageService | `runtime/orchestrator/app/services/project_director_message_service.py` | 高 — 已实现 provider-first chat + rule fallback |
| ProjectDirectorContextBuilderService | `runtime/orchestrator/app/services/project_director_context_builder_service.py` | 高 — 只读上下文拼装 |
| Session messages API | `runtime/orchestrator/app/api/routes/project_director.py` | 高 — GET/POST sessions/{id}/messages |
| Plan version / task creation | 同上 | 中 — 已实现 plan draft → review → create formal project |
| Frontend DirectorChatEntry | `apps/web/src/pages/workbench/components/DirectorChatEntry.tsx` | 高 — 已有聊天 UI 入口 |
| Frontend hooks | `apps/web/src/features/project-director/hooks.ts` | 高 — 已有 session / plan / task creation hooks |
| P6 dispatch decision | `runtime/orchestrator/app/domain/agent_dispatch_decision.py` | 中 — P7 可以读取 dispatch suggestion 并在对话中讨论 |

### 3.2 现有缺口（P7 需要填补的核心差异）

| 缺口 | 当前状态 | P7 目标 |
|------|---------|--------|
| 自由对话能力 | 固定流程状态机；任何自由文本被当成新 goal_text | session 内任意文本作为对话消息，不重置 session |
| Conversation List | 只有单 session 恢复端点 | 多会话列表、切换、归档 |
| DirectorInbox | 不存在 | 跨页面积压消息汇总 |
| ConversationRouter | 不存在 | 消息归属与控制 |
| UserChallenge / Intervention | 不存在 | 结构化质疑类型 + 接收处理 |
| PlanRevision | 不存在 | 对话结果生成计划修订提案 |
| DirectorActionProposal | 不存在 | AI 主管行动建议（只建议不执行） |
| ProposalApproval | 不存在 | 提案审批工作流 |
| Conversation-to-Task 链路 | 不存在 | 对话结论转成真实任务影响 |
| 多会话工作台 | 只有单会话 chat entry | 多会话并行、历史回放 |

### 3.3 旧 P7 文档处理方式

| 文档 | 处理 |
|------|------|
| `P7对话中枢设计-20260603.md` | Stage 7-B0 历史设计基线，**保留不删除不覆盖**，作为当前设计输入参考 |
| `P7对话中枢审计-20260603.md` | Stage 7-B 历史审计基线，**保留不删除不覆盖**，其识别缺口仍有效 |
| `P7主管对话与治理说明.md` | 目录说明文件，保留 |
| **本文档** | **P7 阶段唯一总账**，后续 P7-B/P7-C 等证据统一追加至此，不再新建零散 closure |

---

## 4. P7 阶段目标

| # | 目标 | 说明 |
|---|------|------|
| 1 | 建立 ProjectDirectorConversation 自由对话能力 | session-scoped 任意消息，不重置状态机 |
| 2 | 建立 ConversationList 多会话管理 | 多会话列表、切换、历史回放 |
| 3 | 建立 DirectorInbox 跨页面消息汇总 | 其他页面质疑/note 统一汇总 |
| 4 | 建立 ConversationRouter 消息路由 | 消息归属控制、跨页面自动归并 |
| 5 | 建立 DirectorContextAssembler | AgentSession/Task/Run/Approval/Deliverable 状态拼装为对话上下文 |
| 6 | 建立 UserChallenge / Intervention 机制 | 结构化质疑类型 + AI 主管接收处理 + 工作台归档 |
| 7 | 建立 PlanRevision 计划变更机制 | 对话结果变成计划修订提案并审批应用 |
| 8 | 建立 DirectorActionProposal + ProposalApproval | AI 主管行动建议 → 用户审批 → 应用 |
| 9 | 建立 Conversation-to-Task / Conversation-to-Plan 影响链路 | 讨论结果作用到真实编排 |
| 10 | 工作台多会话前端展示 | 手动开新对话、自动追加相关对话、历史查看 |

---

## 5. P7 不做清单

| # | 不做 | 原因 |
|---|------|------|
| 1 | 真实 Git 写操作 | 全线禁止 |
| 2 | git add / commit / push / PR / merge | 全线禁止 |
| 3 | 自动创建任务 | 提案审批后用户显式确认才允许 |
| 4 | 自动启动 Worker | 不属于 P7 |
| 5 | 自动派发 Codex / Claude Code / DeepSeek | P7 只出提案，启动属于 P9 |
| 6 | 实现 retry | 不属于 P7 |
| 7 | P8/P9 executor config / runtime adapter | 不属于 P7 |
| 8 | 调用 planning/apply | 已在前端禁止，P7 也不在后端自动执行 |
| 9 | 调用 apply-local | 全线禁止 |
| 10 | 写仓库 | 全线禁止 |
| 11 | 把用户确认写成授权写入 | 确认范围严格限定 |
| 12 | 把建议写成已执行 | suggested ≠ executed |
| 13 | 把 P7 阶段启动写成总闭环 Pass | P7 Final Gate 通过前不得 |
| 14 | 绕过 P1-P6 evidence / gate / dry-run / human approval / failure recovery / dispatch decision 链 | P7 是证据链的下游消费者 |

---

## 6. P7 子阶段规划

### 6.1 子阶段总览

| 子阶段 | 内容 | 负责人 | 说明 |
|--------|------|--------|------|
| **P7-A** | Ledger + Stage Design | DeepSeek | 本文档 |
| **P7-B** | Current-state audit / contract freeze | Codex | 审计代码现状，冻结 P7 契约边界 |
| **P7-C** | ProjectDirectorConversation / ConversationList contract | Codex | 域模型 + API 设计 |
| **P7-D** | DirectorInbox / cross-page note & challenge intake | Codex | 跨页面消息汇总域模型 |
| **P7-E** | ConversationRouter + DirectorContextAssembler governance | Codex | 路由规则 + 上下文拼装 |
| **P7-F** | UserChallenge / Intervention domain contract | Codex | 结构化质疑类型 |
| **P7-G** | PlanRevision / DirectorActionProposal / ProposalApproval 设计 | Codex → DeepSeek | 提案与审批设计 + 只读骨架 |
| **P7-H** | Conversation-to-Task / Conversation-to-Plan 影响链路 | Codex | 对话结论 → 编排影响 |
| **P7-I** | 前端工作台多会话最低可用展示 | Codex | 前端多会话 UI |
| **P7-J** | P7 targeted evidence + UAT seed dataset | DeepSeek | 测试证据收口 |
| **P7 Final** | Conversation Hub + Governance scoped closure | DeepSeek | P7 总 Gate 回填 |

### 6.2 P7 子阶段顺序

```text
P7-A（本文档）
  ↓
P7-B Current-state audit / contract freeze
  ↓
P7-C ProjectDirectorConversation / ConversationList contract
  ↓
P7-D DirectorInbox / cross-page note & challenge intake
  ↓
P7-E ConversationRouter + DirectorContextAssembler governance
  ↓
P7-F UserChallenge / Intervention domain contract
  ↓
P7-G PlanRevision / DirectorActionProposal / ProposalApproval
  ↓
P7-H Conversation-to-Task / Conversation-to-Plan
  ↓
P7-I 前端工作台多会话展示
  ↓
P7-J targeted evidence + UAT seed
  ↓
P7 Final Gate
```

### 6.3 各阶段允许范围、禁止范围、targeted tests 建议

#### P7-A：Ledger + Stage Design（本轮）

| 项目 | 说明 |
|------|------|
| **允许** | 新增 P7 ledger + stage design 合一文档；修改总索引 |
| **禁止** | 修改 Python 业务代码、测试代码、前端、DB / migration、API、AgentMessage |
| **禁止** | 实现 Conversation Hub 代码 |
| **禁止** | 新增 API / 路由 / schema |
| **禁止** | 启动 Worker / 自动派发 / retry |
| **推荐模型** | DeepSeek |
| **测试** | `git diff --check` |

#### P7-B：Current-state audit / contract freeze

| 项目 | 说明 |
|------|------|
| **允许** | 只读审计现有 `project_director.py`、`project_director_message_service.py`、`project_director_context_builder_service.py`、`project_director_message.py`、`DirectorChatEntry.tsx`、`hooks.ts` |
| **允许** | 记录当前 session / message / plan / task creation 状态机完整 contract |
| **允许** | 更新 P7 ledger 回填审计证据 |
| **禁止** | 修改任何代码 |
| **禁止** | 新增 API / domain / service / component |
| **禁止** | 把当前状态判为 P7 Pass |
| **推荐模型** | Codex（审计）→ DeepSeek（证据回填） |
| **测试** | 已有 tests `test_project_director_messages.py` 应仍通过 |
| **Gate 标准** | 审计 contract 文档完整；已有测试仍通过；P7-B 至 P7-J 仍 Not started |

#### P7-C：ProjectDirectorConversation / ConversationList contract

| 项目 | 说明 |
|------|------|
| **允许** | 设计 `ProjectDirectorConversation` 域模型（不替换现有 session，叠加对话层） |
| **允许** | 设计 `ConversationList` 域模型 + API |
| **允许** | 设计自由对话 session-scoped message 路由（不再把任意文本当新 goal_text） |
| **禁止** | 替换或删除现有 Project Director session 状态机 |
| **禁止** | 修改 `DirectorChatEntry.tsx` 的 `handleSubmit` 逻辑（P7-I 才允许前端实现） |
| **禁止** | 实现 DirectorInbox / ConversationRouter |
| **推荐模型** | Codex |
| **测试** | 新增 `test_project_director_conversation.py` targeted tests |
| **Gate 标准** | Contract 设计完整；targeted tests 通过；P7-D 至 P7-J 仍 Not started |

#### P7-D：DirectorInbox / cross-page note & challenge intake

| 项目 | 说明 |
|------|------|
| **允许** | 设计 `DirectorInbox` 域模型 |
| **允许** | 设计跨页面消息收口 API |
| **禁止** | 实现前端 inbox UI |
| **禁止** | 修改后端路由逻辑 |
| **推荐模型** | Codex |
| **测试** | 新增 `test_director_inbox.py` targeted tests |
| **Gate 标准** | Contract 设计完整；targeted tests 通过 |

#### P7-E：ConversationRouter + DirectorContextAssembler governance

| 项目 | 说明 |
|------|------|
| **允许** | 设计 `ConversationRouter` 消息路由规则 |
| **允许** | 设计 `DirectorContextAssembler` 上下文拼装 contract |
| **允许** | 设计 governance hooks（conversation 归属、敏感操作拒绝） |
| **禁止** | 自动执行 routing（P7 只出路由建议，不自动移动消息） |
| **禁止** | 修改前端 |
| **推荐模型** | Codex |
| **测试** | 新增 `test_conversation_router.py` + `test_director_context_assembler.py` targeted tests |
| **Gate 标准** | Contract 设计完整；governance rules 明确 |

#### P7-F：UserChallenge / Intervention domain contract

| 项目 | 说明 |
|------|------|
| **允许** | 设计 `UserChallenge` 结构化质疑类型枚举 |
| **允许** | 设计 `Intervention` 域模型 |
| **允许** | 设计 challenge → director response → archive 链路 |
| **禁止** | 实现前端 challenge UI |
| **禁止** | 自动触发副作用（不重排任务、不改计划） |
| **推荐模型** | Codex |
| **测试** | 新增 `test_user_challenge.py` targeted tests |
| **Gate 标准** | Challenge 类型覆盖核心质疑场景；targeted tests 通过 |

#### P7-G：PlanRevision / DirectorActionProposal / ProposalApproval

| 项目 | 说明 |
|------|------|
| **允许** | 设计 `PlanRevision` 域模型（计划变更草案） |
| **允许** | 设计 `DirectorActionProposal` 域模型（AI 主管行动建议，只建议不执行） |
| **允许** | 设计 `ProposalApproval` 域模型（审批工作流） |
| **允许** | 设计 proposal 与 P6 dispatch decision 的引用关系 |
| **禁止** | 实现 proposal 自动应用（apply 必须用户显式确认） |
| **禁止** | 启动外部执行器 |
| **禁止** | 实现 Git 写操作 |
| **推荐模型** | Codex（实现）→ DeepSeek（证据） |
| **测试** | 新增 `test_plan_revision.py` + `test_action_proposal.py` + `test_proposal_approval.py` targeted tests |
| **Gate 标准** | Contract 设计完整；proposal 安全边界明确；targeted tests 通过 |

#### P7-H：Conversation-to-Task / Conversation-to-Plan 影响链路

| 项目 | 说明 |
|------|------|
| **允许** | 设计 conversation → plan revision → task update 影响链路 |
| **允许** | 设计只读影响 trace（对话产生的计划变更可追溯） |
| **禁止** | 自动创建/修改任务 |
| **禁止** | 自动应用计划变更 |
| **推荐模型** | Codex |
| **测试** | 新增 `test_conversation_impact_trace.py` targeted tests |
| **Gate 标准** | 影响链路可追踪；不自动执行；targeted tests 通过 |

#### P7-I：前端工作台多会话最低可用展示

| 项目 | 说明 |
|------|------|
| **允许** | 实现多会话列表切换 |
| **允许** | 实现自由对话 UI（不再把任意文本当新 goal_text） |
| **允许** | 实现会话历史回放 |
| **允许** | 复用已有 `DirectorChatEntry.tsx`、`hooks.ts` 基础 |
| **禁止** | 新增 retry / dispatch / apply / git 操作按钮 |
| **禁止** | 调用写入型 API（除已有 session/message 端点） |
| **推荐模型** | Codex |
| **测试** | `cd apps/web && npm run build` |
| **Gate 标准** | 前端 build 通过；多会话可用；自由对话不重置 session |

#### P7-J：P7 targeted evidence + UAT seed dataset

| 项目 | 说明 |
|------|------|
| **允许** | 整理 P7 全链 targeted test 覆盖 |
| **允许** | 设计 UAT seed dataset（模拟对话 + proposal + approval 流） |
| **允许** | 回填 P7 ledger evidence |
| **禁止** | 修改业务代码 |
| **禁止** | 把 P7 总 Gate 写成 Pass（需等 P7 Final 评审） |
| **推荐模型** | DeepSeek |
| **测试** | `pytest` P7 targeted tests + `git diff --check` |
| **Gate 标准** | 全链 targeted evidence 完整 |

#### P7 Final Gate

| 项目 | 说明 |
|------|------|
| **允许** | P7-A 至 P7-J 全部完成后，做 P7 总 Gate 评审 |
| **允许** | 判断 Conversation Hub + Governance scoped gate Pass |
| **禁止** | 把 AI Project Director 总闭环写成 Pass（P7 完成后总闭环仍待评估） |
| **禁止** | 实现产品运行时 Git 写操作 |
| **推荐模型** | DeepSeek |

---

## 7. 文档治理规则

P7 阶段统一遵守以下规则（与 P4/P5/P6 ledger 一致）：

| 场景 | 策略 |
|------|------|
| P7 子阶段实现（B/C/D/E/F/G/H/I） | 证据追加到本文档 |
| 小修正 / R1 / R2 / R3 | 追加到本文档，不新建独立文件 |
| 测试结果记录 | 追加到本文档 |
| 阶段内 Gate 变更 | 更新本文档第 8 节 |
| P7 大阶段完成 | 在本文档内做 Final Gate，不新建独立 closure |
| P7 高风险新设计（需对齐多个模块） | 可新建独立设计文档 |
| 后续给 Codex / DeepSeek 的任务指令 | 必须遵守本规则，不得要求新建零散 closure 文档 |
| 旧 P7 设计/审计文档 | 不删除不覆盖，作为历史输入保留 |

---

## 8. Gate 结论

| Gate | 结论 | 证据 |
|------|------|------|
| `origin/main` HEAD 确认 | Pass | `f7736b97bc3a84d5c1e1bb9d89bd5aabbe11befd` |
| P1 Coding Session + Worktree Lifecycle | **Pass** | P1 closure |
| P2 Worker Context + Runtime Safe Execution | **Pass** | P2 closure |
| P3 Runtime Lifecycle Evidence + Event Audit | **Pass** | P3 closure |
| P4 Git Delivery + Human Approval Gate | **Pass** | P4 ledger final gate |
| P5 Failure Recovery | **Pass** | P5 ledger final gate |
| P6 Agent Orchestration | **Pass**（scoped gate） | P6 ledger final gate |
| P8/P9 roadmap | **Pass**（仅路线规划） | P8-P9 规划文档 |
| **P7-A Ledger + Stage Design** | **Pass** | 本文档 |
| **P7-B Current-state audit / contract freeze** | **Pass** | 本文档 §10；65 passed；只读审计完成 |
| **P7-C ProjectDirectorConversation / ConversationList contract** | **Pass** | 本文档 §11；contract 设计完成 |
| **P7-C1 initial ConversationList backend read-only implementation** | **Partial，需要 R1** | 本文档 §13；初版只读后端实现 + 71 passed targeted tests，但 before/kind/limit contract 缺口需 R1 修正 |
| **P7-C1-R1 ConversationList read-only API contract correction** | **Pass** | 本文档 §14；before cursor / limit=100 / kind 推导修正 + 77 passed targeted tests |
| **P7-C2 ConversationList frontend API/types/hooks** | **Pass** | 本文档 §15；只读前端类型/API/hooks 接线；`npm run build` + `git diff --check` |
| **P7-C3 Workbench ConversationList read-only UI** | **Pass** | 本文档 §16；Workbench 只读 ConversationList + 显式选择恢复；`npm run build` + `git diff --check` |
| **P7-D DirectorInbox / cross-page note & challenge intake contract** | **Pass** | 本文档 §12；contract 设计完成 |
| **P7-D1 DirectorInbox synthetic read-only API** | **Pass** | 本文档 §17；`GET /project-director/inbox` synthetic read model；84 passed targeted tests |
| P7-E ConversationRouter + DirectorContextAssembler | **Not started** | — |
| P7-F UserChallenge / Intervention | **Not started** | — |
| P7-G PlanRevision / DirectorActionProposal / ProposalApproval | **Not started** | — |
| P7-H Conversation-to-Task / Conversation-to-Plan | **Not started** | — |
| P7-I 前端工作台多会话 | **Not started** | — |
| P7-J targeted evidence + UAT seed | **Not started** | — |
| **P7 Final Gate** | **Not started** | 需 P7-A 至 P7-J 全部完成后评估 |
| P8/P9 implementation | **Not started** | — |
| 产品运行时 Git 写操作 | **Not started** | — |
| **AI Project Director 总闭环** | **Partial** | P7 完成前不得写 Pass |

---

## 9. 本轮收口声明

### 9.1 P7-A 实际文件变更记录

| 文件 | 动作 | 说明 | 是否越过产品运行时边界 |
|------|------|------|------------------------|
| `07-P7主管对话与治理/P7主管对话与治理总账与阶段设计-20260607.md` | **新增** | P7 ledger + stage design 合一文档 | 否 |
| `00-总览与索引/AI项目主管文档索引.md` | **修改** | 补充 P7-A 入口与状态 | 否 |

### 9.2 边界声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否写 AgentMessage | 否 |
| 是否实现 Conversation Hub 代码 | 否 |
| 是否实现 retry / 自动派发 / 创建任务 | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |
| 是否把 P7 总 Gate 写成 Pass | 否 |
| 是否把 AI Project Director 总闭环写成 Pass | 否 |
| 是否删除旧 P7 文档 | 否 |
| 是否只新增设计文档 | 是 |

---

## 附录 A：P7 依赖的现有代码信号参考

以下清单来自 `origin/main` = `3c72e39` 的代码审计（部分通过只读文件确认）。

### Project Director 域模型层

| 字段 / 类 | 位置 | 说明 |
|----------|------|------|
| `ProjectDirectorMessage` | `runtime/orchestrator/app/domain/project_director_message.py` | 对话消息域模型 |
| `ProjectDirectorMessageRole` | 同上 | USER / ASSISTANT / SYSTEM |
| `ProjectDirectorMessageSource` | 同上 | AI / RULE_FALLBACK / SYSTEM |
| `ProjectDirectorSession` | `runtime/orchestrator/app/domain/project_director_session.py` | session 状态机 |
| `ProjectDirectorPlanVersion` | `runtime/orchestrator/app/domain/project_director_plan_version.py` | 计划版本 |

### Project Director 服务层

| 服务 | 位置 | 说明 |
|------|------|------|
| `ProjectDirectorMessageService` | `runtime/orchestrator/app/services/project_director_message_service.py` | Stage 7-B2：消息持久化 + provider-first chat |
| `ProjectDirectorContextBuilderService` | `runtime/orchestrator/app/services/project_director_context_builder_service.py` | Stage 7-B2：只读上下文拼装 |

### Project Director API 层

| 端点（部分） | 位置 | 说明 |
|------------|------|------|
| POST `/project-director/sessions` | `runtime/orchestrator/app/api/routes/project_director.py` | 创建 session |
| POST `/sessions/{id}/messages` | 同上 | 发送消息 |
| GET `/sessions/{id}/messages` | 同上 | 获取 session 消息列表 |
| GET `/workbench/resume` | 同上 | 工作台恢复 |

### 前端

| 组件 / 文件 | 位置 | 说明 |
|------------|------|------|
| `DirectorChatEntry.tsx` | `apps/web/src/pages/workbench/components/DirectorChatEntry.tsx` | 工作台对话输入组件 |
| `hooks.ts` | `apps/web/src/features/project-director/hooks.ts` | session / plan / task creation hooks |

---

## 附录 B：历史 P7 文档参考映射

| 旧文档 | 与本文档关系 | 处理方式 |
|--------|------------|---------|
| `P7对话中枢设计-20260603.md` | Stage 7-B0 设计基线，Conversational Brain 叠加状态机设计 | 历史输入，不删除，不覆盖 |
| `P7对话中枢审计-20260603.md` | Stage 7-B 审计基线，识别核心缺口 | 历史输入，不删除，不覆盖。审计结论的 12 个缺口清单仍有效 |
| `P7主管对话与治理说明.md` | 目录说明 | 保留 |

---

## 10. P7-B Current-state audit / contract freeze（2026-06-07）

### 10.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P7-B** | Current-state audit / contract freeze |
| 代码边界 | **未越过** | 只读审计 11 个代码文件 + 2 套测试文件；未修改任何代码 |
| 是否写代码 | 否 | 未新增/修改 Python、TypeScript、测试、API、DB/migration |
| 是否进入 P7-C | 否 | P7-C 才允许设计 ProjectDirectorConversation / ConversationList contract |

### 10.2 审计读取的代码文件

| # | 文件 | 类型 | 行数（估计） |
|---|------|------|------------|
| 1 | `runtime/orchestrator/app/api/routes/project_director.py` | API 路由 | ~2400 |
| 2 | `runtime/orchestrator/app/domain/project_director_message.py` | 域模型 | 72 |
| 3 | `runtime/orchestrator/app/services/project_director_message_service.py` | 服务 | 597 |
| 4 | `runtime/orchestrator/app/services/project_director_context_builder_service.py` | 服务 | 255 |
| 5 | `runtime/orchestrator/app/repositories/project_director_message_repository.py` | Repository | ~120 |
| 6 | `apps/web/src/pages/workbench/components/DirectorChatEntry.tsx` | 前端组件 | ~1250 |
| 7 | `apps/web/src/features/project-director/hooks.ts` | 前端 hooks | ~80 |
| 8 | `apps/web/src/features/project-director/api.ts` | 前端 API | ~250 |
| 9 | `apps/web/src/features/project-director/types.ts` | 前端类型 | ~100 |
| 10 | `runtime/orchestrator/tests/test_project_director_messages.py` | 测试 | ~370 |
| 11 | `runtime/orchestrator/tests/test_project_director_sessions.py` | 测试 | ~400 |

### 10.3 后端 API Contract 审计

#### POST `/sessions/{session_id}/messages`（核心对话端点）

| 属性 | 当前状态 |
|------|---------|
| Request | `{ "content": str }`（min_length=1） |
| Response | `{ "session_id", "gate_conclusion": "Partial", "source": "rule_fallback" \| "ai", "forbidden_actions": [...], "user_message": {...}, "assistant_message": {...} }` |
| 是否只读 | **否** — 持久化 user + assistant 两条 message |
| 是否调用 Provider | **是** — 尝试调用 OpenAI provider，失败则 rule fallback |
| 是否持久化消息 | **是** — user message + assistant reply 各一条 |
| 是否改变 session status | **否** — session status 不变 |
| 是否创建 task/run/worker | **否** |
| `forbidden_actions` | 固定包含：不启动 Worker、不创建 Run、不执行 planning/apply、不执行 apply-local、不写仓库、不执行 suggested_actions |
| Provider output contract | JSON：intent、answer、related_plan_version_id、suggested_actions、requires_confirmation、risk_level、forbidden_actions_detected |

#### GET `/sessions/{session_id}/messages`

| 属性 | 当前状态 |
|------|---------|
| Response | `{ "session_id", "messages": [...], "has_more": bool }` |
| 是否只读 | **是** — 仅查询 |
| 排序 | 按 sequence_no 升序 |
| 分页 | 默认 limit=50，支持 before_message_id cursor |
| `has_more` | 当返回条数 == limit 时为 True |

#### POST `/project-director/sessions`

| 属性 | 当前状态 |
|------|---------|
| Request | `{ "goal_text", "project_id?" , "constraints?" }` |
| Response | `{ "session_id", "status": "clarifying", "clarifying_questions": [...], "source": "ai" \| "rule_fallback" }` |
| 是否只读 | **否** — 创建新 session |
| 是否调用 Provider | **是** — 尝试生成澄清问题 |
| 特征 | **任何输入都被当成新 goal_text**（历史审计结论仍有效） |

#### GET `/workbench/resume`

| 属性 | 当前状态 |
|------|---------|
| Query params | `mode`（new-project/continue）, `project_id?`, `session_id?` |
| Response | `{ "session": {...}, "plan_version": {...}, "task_creation": {...}, "recent_messages": [...] }` |
| 是否只读 | **是** — 纯查询 |
| `session_id` 传入时 | 直接恢复指定 session |
| 无 `session_id` 时 | 返回可恢复候选 session |
| recent_messages | 通过 `ProjectDirectorMessageRepository` 查询 |

#### GET `/workbench/resumable-sessions`

| 属性 | 当前状态 |
|------|---------|
| Response | `{ "sessions": [...] }` — 未完成 session 列表 |
| 是否只读 | **是** — 纯查询 |

### 10.4 后端 Domain / Service Contract 审计

#### ProjectDirectorMessage 域模型

| 字段 | 类型 | 约束 |
|------|------|------|
| `id` | UUID | 自动生成 |
| `session_id` | UUID | 必填 |
| `role` | USER / ASSISTANT / SYSTEM | 必填 |
| `content` | str | min=1, max=10000 |
| `sequence_no` | int | ge=1 |
| `intent` | str \| None | max=80 |
| `related_plan_version_id` | UUID \| None | — |
| `related_project_id` | UUID \| None | — |
| `related_task_id` | UUID \| None | — |
| `source` | AI / RULE_FALLBACK / SYSTEM | 默认 SYSTEM |
| `source_detail` | str | max=300 |
| `suggested_actions` | list[dict] | 每个 dict 含 type/label/requires_confirmation/risk_level |
| `requires_confirmation` | bool | 默认 False |
| `risk_level` | LOW / MEDIUM / HIGH \| None | 默认 None |
| `forbidden_actions_detected` | list[str] | — |
| `token_count` | int \| None | ge=0 |
| `estimated_cost` | float \| None | ge=0 |
| `created_at` | datetime | 自动 UTC now |

#### ProjectDirectorMessageService 行为

| 行为 | 当前实现 |
|------|---------|
| `list_messages` | 先 ensure_session_exists，再按 session_id 分页查询 |
| `post_user_message` | 验证 content 非空 → 持久化 user message → 构建 context → 生成 assistant reply → 持久化 assistant message → commit |
| Provider 调用链 | resolve config → check api_key → build prompt → call provider → parse JSON contract → sanitize |
| Fallback 链 | provider_config_unavailable → provider_not_configured → provider_generation_failed → provider_empty_output → provider_contract_invalid |
| `suggested_actions` 白名单 | summarize / explain / navigate / request_changes / create_formal_project / run_worker_once / none |
| `create_formal_project` / `run_worker_once` | 强制 requires_confirmation=True，risk_level 至少 medium |
| 禁止执行声明检测 | 检测"已启动 Worker"、"已创建 Run"、"已执行 planning/apply"、"已执行 apply-local"、"已写入仓库"、"已提交代码"、"git push 已完成" |
| Intent 白名单 | general_discussion / ask_about_current_context / ask_about_plan / ask_about_risks / ask_about_next_step / request_plan_change / request_action / navigation_help / restart_or_new_goal / unknown |

#### ProjectDirectorContextBuilderService 行为

| 行为 | 当前实现 |
|------|---------|
| `build_context` | session info + clarifying questions/answers + recent messages（limit 1-50）+ latest plan version + task creation readback + project snapshot + task snapshot + safety boundary |
| 是否只读 | **是** — 纯查询，无副作用 |
| Plan version readback | 包含 phases、proposed_tasks、risks、acceptance_criteria、project_scope、complexity_assessment |
| Task creation readback | 通过 plan_version_id 查询已创建项目的 task 信息 |
| Project snapshot | project name/summary/status/stage/task_stats |
| Task snapshot | 按 project_id 统计 task 状态分布 + 最近 10 条 task |

#### ProjectDirectorMessageRepository 行为

| 行为 | 当前实现 |
|------|---------|
| `create` | 写入 ProjectDirectorMessageTable 并 flush |
| `get_by_id` | 单条查询 |
| `list_by_session_id` | 按 session_id + sequence_no 排序，支持 limit + before_message_id cursor |
| `get_next_sequence_no` | COUNT + 1 幂等 |
| `commit` | session.commit() |

### 10.5 前端 Contract 审计

#### DirectorChatEntry.tsx 当前行为

| 行为 | 当前实现 | P7 缺口 |
|------|---------|--------|
| session 为 null 时输入文本 | `handleSubmit` → `createSessionMutation`（创建新 session） | **任意文本被当成新 goal_text**（历史审计 P0 缺口仍存在） |
| session 存在时输入文本 | `handleSubmit` → `postProjectDirectorSessionMessage` | 已有基础自由对话能力 |
| resumeCandidate 恢复 | 有 `session_id` 参数或 `resumeCandidate` 数据时直接恢复 | 不支持多会话并行 |
| messageTimeline | `messagesQuery.data` 更新 | 已有消息时间线 UI |
| 多会话列表 | **不存在** | P7-I 待做 |
| DirectorInbox | **不存在** | P7-D 待做 |
| Proposal / Approval UI | **不存在** | P7-G 待做 |
| 自由对话是否重置 session | session 存在时不重置 | session 为 null 时任何输入都创建新 session（需 P7-C 修复） |

#### hooks.ts 当前能力

| Hook | 功能 | 说明 |
|------|------|------|
| `useCreateProjectDirectorSession` | 创建 session mutation | — |
| `useProjectDirectorWorkbenchResume` | 工作台恢复 query | 支持 mode/projectId/sessionId 参数 |
| `useProjectDirectorWorkbenchResumableSessions` | 可恢复 session 列表 query | — |
| `useProjectDirectorSessionMessages` | session 消息列表 query | 传入 sessionId 或 null |
| `usePostProjectDirectorSessionMessage` | 发送消息 mutation | — |
| `useSubmitProjectDirectorAnswers` | 提交澄清答案 mutation | — |
| `useConfirmProjectDirectorGoal` | 确认目标 mutation | — |
| `useCreateProjectDirectorPlanVersion` | 创建计划草案 mutation | — |
| `useReviewProjectDirectorPlanVersion` | 审核草案 mutation | — |
| `useConfirmProjectDirectorPlanVersion` | 确认计划 mutation | — |
| `useCreateProjectDirectorTaskQueue` | 创建正式项目与任务 mutation | — |

#### api.ts 当前端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/project-director/sessions` | POST | 创建 session |
| `/project-director/workbench/resume` | GET | 工作台恢复 |
| `/project-director/workbench/resumable-sessions` | GET | 可恢复 session 列表 |
| `/project-director/sessions/{id}/messages` | GET | 获取消息列表 |
| `/project-director/sessions/{id}/messages` | POST | 发送消息 |
| `/project-director/sessions/{id}/answers` | POST | 提交澄清答案 |
| `/project-director/sessions/{id}/confirm` | POST | 确认目标 |
| `/project-director/sessions/{id}/plan-versions` | POST | 生成计划草案 |
| `/project-director/plan-versions/{id}/review` | POST | 审核计划草案 |
| `/project-director/plan-versions/{id}/confirm` | POST | 确认计划 |
| `/project-director/plan-versions/{id}/create-formal-project` | POST | 创建正式项目与任务 |
| `/project-director/setup-readiness` | GET | 设置就绪状态 |
| 其他 config 端点 | GET/POST | agent_team / skill_binding / repository_binding / verification |

### 10.6 测试 Contract 审计

#### test_project_director_messages.py（本轮 65 passed 之一部分）

| 测试 | 覆盖能力 |
|------|---------|
| `test_post_message_persists_user_and_rule_fallback_assistant` | 消息持久化、rule fallback contract、forbidden_actions |
| `test_list_messages_returns_session_timeline_in_sequence_order` | 消息排序、sequence_no |
| `test_list_messages_defaults_to_latest_50_and_supports_before_cursor` | 分页、cursor |
| `test_list_messages_for_empty_session_matches_contract` | 空 session contract |
| `test_post_message_rejects_blank_content_without_persisting_messages` | 空白 content 拒绝 |
| `test_post_message_rejects_nonexistent_session` | 不存在 session 报错 |
| `test_post_message_with_valid_session_persists_two_messages` | user + assistant 两条 message |
| `test_post_message_to_existing_session_increments_sequence_numbers` | sequence_no 递增 |
| `test_provider_chat_response_uses_read_only_context_and_creates_no_run` | Provider 对话不创建 run |
| `test_rule_fallback_uses_plan_risks_and_task_creation_context` | Rule fallback 使用上下文 |
| `test_context_builder_reads_recent_messages_project_and_tasks` | Context builder 读取范围 |

#### test_project_director_sessions.py（本轮 65 passed 之一部分）

| 测试 | 覆盖能力 |
|------|---------|
| `test_create_session_returns_201_with_clarifying_questions` | Session 创建 contract |
| `test_create_session_with_project_id` | 绑定 project_id |
| `test_create_session_with_constraints` | 绑定 constraints |
| `test_create_session_rejects_empty_goal` | 空 goal 拒绝 |
| `test_create_session_marks_rule_fallback_source_when_provider_absent` | Fallback 标记 |
| `test_get_existing_session` / `test_get_nonexistent_session_returns_404` | Session 读取 |
| `test_resumable_sessions_lists_unfinished_director_sessions` | Resumable 列表 |
| `test_workbench_resume_can_restore_selected_session_id` | 按 session_id 恢复 |
| `test_workbench_resume_includes_recent_messages_for_selected_session` | 恢复含 recent messages |
| `test_workbench_resume_rejects_session_context_mismatch` | 上下文不匹配拒绝 |
| 澄清答案 / 确认 / 计划草案 / 审核 / 创建任务 | 对应端点覆盖 |

#### 未覆盖的 P7 必须能力

| 缺口 | 说明 |
|------|------|
| ConversationList 多会话切换 | 无对应测试 |
| DirectorInbox 跨页面消息汇总 | 无对应测试 |
| ConversationRouter 消息路由 | 无对应测试 |
| UserChallenge / Intervention | 无对应测试 |
| PlanRevision 计划变更 | 无对应测试 |
| DirectorActionProposal 行动建议 | 无对应测试 |
| ProposalApproval 审批工作流 | 无对应测试 |
| Conversation-to-Task 影响链路 | 无对应测试 |
| 自由对话 session 不重置（session null 时） | 前端行为测试缺失 |

### 10.7 P7-B 缺口清单

| P7 能力 | 当前状态 | 已有基础 | P7 目标 |
|---------|---------|---------|--------|
| ProjectDirectorConversation | **已有基础** | message domain/service/repository/API/frontend 均已存在；支持 session-scoped 消息持久化 + provider chat + rule fallback | 需 P7-C 冻结自由对话 contract + 修复 session null 时任意文本被当成 goal_text |
| ConversationList | **缺** | `resumable-sessions` 端点可返回 session 列表但前端无多会话切换 UI | P7-C 设计 ConversationList 域模型 + P7-I 前端实现 |
| DirectorInbox | **缺** | 无跨页面积压消息汇总机制 | P7-D 设计 |
| ConversationRouter | **缺** | 无消息路由归属控制 | P7-E 设计 |
| DirectorContextAssembler | **已有基础** | `ProjectDirectorContextBuilderService` 已可拼装 session/plan/task/project 上下文 | P7-E 强化 governance hooks |
| UserChallenge / Intervention | **缺** | 无结构化质疑类型或介入机制 | P7-F 设计 |
| PlanRevision | **缺** | `plan-versions` 支持 approve/reject/request_changes 但无对话驱动的计划修订提案 | P7-G 设计 |
| DirectorActionProposal | **部分有** | `suggested_actions` 已在 message 中承载，但只是只读列表，无 proposal 审批工作流 | P7-G 设计 proposal contract |
| ProposalApproval | **缺** | `requires_confirmation` 字段存在但无独立审批模型 | P7-G 设计 |
| Conversation-to-Task 影响链路 | **缺** | `create-formal-project` 是单向操作，无 conversation → plan revision → task 闭环 | P7-H 设计 |
| Conversation-to-Plan 影响链路 | **缺** | 同上 | P7-H 设计 |

### 10.8 P7-B Contract Freeze

**本轮冻结边界**：以下子阶段边界从 P7-B 起强制执行。

| 子阶段 | 允许 | 禁止提前做 |
|--------|------|-----------|
| P7-C | 设计 ProjectDirectorConversation / ConversationList contract | 实现代码 |
| P7-D | 设计 DirectorInbox contract | 实现代码 |
| P7-E | 设计 ConversationRouter + DirectorContextAssembler governance | 实现代码 |
| P7-F | 设计 UserChallenge / Intervention contract | 实现代码 |
| P7-G | 设计 PlanRevision / DirectorActionProposal / ProposalApproval | 实现代码 |
| P7-H | 设计 Conversation-to-Task / Conversation-to-Plan 影响链路 | 实现代码 |
| P7-I | 实现前端多会话最低可用展示 | 不改后端 |
| P7-J | 整理 targeted evidence + UAT seed | 不改业务代码 |

### 10.9 测试证据

| 命令 | 结果 |
|------|------|
| `pytest tests/test_project_director_messages.py tests/test_project_director_sessions.py -q` | **65 passed, 8 warnings in 5.75s** |
| `git diff --check` | **Pass**（见 10.11） |

### 10.10 边界声明

| 声明 | 结论 |
|------|------|
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否写 AgentMessage | 否 |
| 是否实现 Conversation Hub 代码 | 否 |
| 是否进入 P7-C | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |

### 10.11 P7-B Gate

| Gate | 结论 |
|------|------|
| 后端 API contract 已审计并记录 | **Pass** |
| 后端 domain / service contract 已审计并记录 | **Pass** |
| 前端 contract 已审计并记录 | **Pass** |
| 测试 contract 已审计并记录（65 passed） | **Pass** |
| 缺口清单已输出（11 项逐项判定） | **Pass** |
| contract freeze 已建立（P7-C 至 P7-I 边界明确） | **Pass** |
| 未改代码 / 未改 API / 未改前端 / 未改 DB | **Pass** |
| P7-C/P7-D/P7-E/P7-F/P7-G/P7-H/P7-I/P7-J 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-B Gate 结论** | **Pass** |

---

## 11. P7-C ProjectDirectorConversation / ConversationList contract（2026-06-07）

### 11.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P7-C** | ProjectDirectorConversation / ConversationList contract design |
| 代码边界 | **未越过** | 只设计 contract，不写代码；未修改 Python/TypeScript/测试/API/DB/migration |
| 是否实现代码 | 否 | P7-I 才允许前端实现多会话展示 |
| 是否进入 P7-D | 否 | DirectorInbox 留给 P7-D |

### 11.2 只读读取的代码与文档文件

| # | 文件 | 读取目的 |
|---|------|---------|
| 1 | `runtime/orchestrator/app/domain/project_director_session.py` | 审计现有 session 状态机作为 conversation 基座 |
| 2 | `runtime/orchestrator/app/domain/project_director_message.py` | 审计 message ↔ session 1:N 关系 |
| 3 | `runtime/orchestrator/app/repositories/project_director_session_repository.py` | 审计 session CRUD 与 list 能力 |
| 4 | `runtime/orchestrator/app/repositories/project_director_message_repository.py` | 审计 message 排序/分页/sequence_no |
| 5 | `runtime/orchestrator/app/services/project_director_message_service.py` | 审计 post_user_message 行为 |
| 6 | `runtime/orchestrator/app/services/project_director_context_builder_service.py` | 审计 context builder readback 范围 |
| 7 | `runtime/orchestrator/app/api/routes/project_director.py` | 审计现有 resume/resumable-sessions 端点 |
| 8 | `apps/web/src/pages/workbench/components/DirectorChatEntry.tsx` | 审计 handleSubmit / resumeCandidate / messageTimeline 行为 |
| 9 | `apps/web/src/features/project-director/types.ts` | 审计前端 session/message 类型 |
| 10 | `apps/web/src/features/project-director/api.ts` | 审计前端 API 调用层 |
| 11 | `apps/web/src/features/project-director/hooks.ts` | 审计前端 resume/resumable/messages hooks |

### 11.3 ProjectDirectorConversation 定义

#### 11.3.1 与 ProjectDirectorSession 的关系

**设计原则**：ProjectDirectorConversation 是 ProjectDirectorSession 的对话层叠加，不替换现有状态机。

| 维度 | ProjectDirectorSession（已有） | ProjectDirectorConversation（P7-C 设计新增） |
|------|------------------------------|------------------------------------------|
| 职责 | 目标摄入 → 澄清 → 确认 → 计划草案 → 审核 → 创建正式项目 | 自由对话、会话管理、多会话列表 |
| 主键 | `session.id` | 复用 `session.id` 作为 `conversation_id` |
| 状态机 | DRAFT → CLARIFYING → READY_TO_CONFIRM → CONFIRMED | active / idle / awaiting_user / archived |
| 消息 | 已有 `ProjectDirectorMessage`，按 `session_id` 关联 | 复用同一 message 表，不新增 `conversation_id` 列 |
| 与 project 关系 | `session.project_id`（可为 null） | 继承 `session.project_id` |
| 与 plan 关系 | 通过 `ProjectDirectorPlanVersion` 间接关联 | 通过 `latest_plan_version_id` 直读引用 |
| 与 task 关系 | 通过 `TaskCreationRecord` 间接关联 | 通过 `latest_task_creation` 直读引用 |

**结论**：P7-C 不新增 `conversation_id`。`session.id` 即 `conversation_id`。Conversation 是 session 的对话视图，不新建数据实体。

#### 11.3.2 Conversation 状态枚举

```text
ProjectDirectorConversationStatus
├── active: 有活跃对话，最近 N 分钟内有消息
├── idle: 无活跃对话，但 session 未确认/未完成
├── awaiting_user: assistant 最后一条消息 requires_confirmation=True
├── archived: 用户主动归档
├── completed: session 状态为 CONFIRMED 且已创建正式项目
```

#### 11.3.3 Conversation 与关键对象的关联边界

| 关联对象 | 关系 | 访问方式 | 读写 |
|---------|------|---------|------|
| ProjectDirectorMessage | 1:N | 复用 `message_repository.list_by_session_id(session_id)` | 只读 |
| ProjectDirectorPlanVersion | N:1（最新） | 复用 `plan_version_repository.list_by_session_id()` 取最新 | 只读 |
| TaskCreationRecord | N:1（最新） | 复用 `task_creation_repository.get_by_plan_version_id()` | 只读 |
| P6 AgentDispatchDecision | N:1（引用） | conversation 可引用 dispatch_decision_id 只读展示 | 只读 |
| P8/P9 ExecutorLaunch | **禁止直接引用** | P7 只允许 DirectorActionProposal 建议启动，不允许真实 launch | 不读不写 |
| Run / Approval / Deliverable | N:M（通过 project_id 间接关联） | ContextBuilderService 已可拼装 | 只读 |
| Project | N:1 | `session.project_id` | 只读 |

#### 11.3.4 Conversation 与 P6 dispatch decision 的只读引用

P7 conversation 可以只读引用 P6 `AgentDispatchDecision`：
- 对话上下文中展示"AI 主管上次调度建议：建议由 Codex 修复此任务"
- 用户可在对话中讨论 dispatch suggestion
- 讨论结果可生成 DirectorActionProposal（P7-G）
- 但不自动派发，不自动创建任务

#### 11.3.5 Conversation 与 P8/P9 的关系

| 规则 | 说明 |
|------|------|
| P7 conversation 中可以讨论"要不要让 Codex 修这个 bug？" | 允许 |
| 讨论结果可生成 DirectorActionProposal | 允许（P7-G） |
| Proposal 可以引用 P6 dispatch decision | 允许 |
| Proposal 不能直接触发 executor launch | **禁止** |
| 从 Proposal 到真实 Launch 必须经过 P8/P9 guardrail | **强制** |

### 11.4 ConversationList contract

#### 11.4.1 列表项字段设计

```text
ConversationListItem
├── conversation_id: UUID              # session.id
├── project_id: UUID | None
├── title: str                         # goal_text 的前 80 字符
├── kind: ConversationKind             # project_onboarding | general_discussion | plan_review | follow_up
├── status: ConversationStatus         # active | idle | awaiting_user | archived | completed
├── session_status: str                # session.status.value（DRAFT/CLARIFYING/READY_TO_CONFIRM/CONFIRMED）
├── last_message_preview: str          # 最近一条消息的前 120 字符
├── last_message_at: datetime | None   # 最近一条消息时间
├── message_count: int                 # 消息总数
├── pending_challenge_count: int       # 待处理质疑数（P7-F 后启用）
├── pending_proposal_count: int        # 待审批提案数（P7-G 后启用）
├── requires_user_action: bool         # awaiting_user 或 pending_challenge > 0 或 pending_proposal > 0
├── owner_scope: str                   # "project" | "user"
├── created_at: datetime
└── updated_at: datetime
```

#### 11.4.2 ConversationKind 枚举

```text
ConversationKind
├── project_onboarding: 目标输入 → 澄清 → 确认 → 计划草案（当前主流）
├── general_discussion: 自由对话（P7-C 新能力）
├── plan_review: 计划审核讨论
├── follow_up: 执行后的后续讨论
```

#### 11.4.3 排序与筛选规则

| 维度 | 规则 |
|------|------|
| 默认排序 | `last_message_at DESC`（最近活跃的在前） |
| 全部 | 所有 non-archived conversation |
| 当前项目 | `project_id == selectedProjectId` |
| 未完成 | `session_status != CONFIRMED` 或 `status == active` |
| 有待确认 | `status == awaiting_user` |
| 有质疑 | `pending_challenge_count > 0`（P7-F 后） |
| 已归档 | `status == archived` |
| 空状态 | "还没有主管会话。点击「新建主管会话」开始。" |

#### 11.4.4 恢复规则

| 场景 | 行为 |
|------|------|
| 点击 ConversationList 中的 session | 恢复该 session → 加载 messageTimeline，**不创建新 session** |
| session 为 null + 用户输入自然语言文本 | **不得默默当成新 goal_text 创建 session**。必须先要求用户选择现有会话或明确新建 |
| 用户点击「新建主管会话」 | 进入 goal_text 输入流程 → POST `/project-director/sessions` |
| 用户点击「继续现有会话」 | 直接恢复 session，不触发 provider，不生成澄清问题 |

#### 11.4.5 新建规则（关键安全修正）

**这是 P7-C 最关键的设计决策之一**：修正历史审计 P0 缺口。

| 修正前（P7-B 现状） | 修正后（P7-C contract） |
|-------------------|---------------------|
| session 为 null + 任何文本输入 → `handleSubmit` 调用 `createSessionMutation` | session 为 null + 自然语言输入 → 前端提示"请先选择或新建主管会话" |
| 自由文本被当成新 goal_text | 只有用户明确进入"新建主管会话"流程才允许创建 session |
| `DirectorChatEntry` 的 `handleSubmit` 无条件创建 session | 拆分两个路径：`handleStartNewSession`（创建）vs `handleSendMessage`（发送对话消息） |

### 11.5 API contract 设计

以下为 P7-C 冻结的 API contract 设计，**不实现代码**。

#### GET `/project-director/conversations`

| 属性 | 设计 |
|------|------|
| Query | `project_id?`, `status?`, `kind?`, `limit?`, `before?` |
| Response | `{ "conversations": [ConversationListItem], "has_more": bool }` |
| 是否只读 | **是** — 纯查询 |
| 实现方式 | 基于 `ProjectDirectorSessionRepository.list_all()` + `ProjectDirectorMessageRepository` join 最近消息 |
| 禁止 | 创建 session / 触发 provider / 创建 run / task / worker / Git 写 |

#### GET `/project-director/conversations/{conversation_id}`

| 属性 | 设计 |
|------|------|
| Response | `{ "conversation": ConversationDetail, "recent_messages": [...], "latest_plan_version": {...}, "task_creation": {...} }` |
| 是否只读 | **是** — 等价于现有 `workbench/resume?session_id=X` 的增强版 |
| 禁止 | 触发 provider |

#### POST `/project-director/conversations`

| 属性 | 设计 |
|------|------|
| Request | `{ "kind": ConversationKind, "goal_text?": str, "project_id?": UUID }` |
| Response | `{ "conversation_id": UUID, "session_id": UUID, "status": "active" }` |
| 行为 | 当 `kind == project_onboarding` 时等价于现有 `POST /project-director/sessions`。当 `kind == general_discussion` 时创建 session 但不生成澄清问题 |
| 禁止 | 自动触发 provider（general_discussion 不生成澄清问题） |

#### POST `/project-director/conversations/{conversation_id}/messages`

| 属性 | 设计 |
|------|------|
| Request | `{ "content": str }` |
| Response | 复用现有 `POST /sessions/{session_id}/messages` contract |
| 是否只读 | **否** — 持久化 user + assistant 两条 message |
| 禁止 | 执行 suggested_actions、创建 task/run/worker、启动 executor、Git 写 |

#### PATCH `/project-director/conversations/{conversation_id}/archive`

| 属性 | 设计 |
|------|------|
| Request | `{ "archive": true }` |
| Response | `{ "conversation_id", "status": "archived", "archived_at": "..." }` |
| 行为 | 设置 conversation status 为 archived |
| 禁止 | **不删除历史消息**。messages 保留不变 |
| 恢复 | `{ "archive": false }` 可恢复 |

#### GET `/project-director/conversations/{conversation_id}/timeline`

| 属性 | 设计 |
|------|------|
| Response | `{ "conversation_id", "items": [ConversationTimelineItem] }` |
| 是否只读 | **是** — 聚合 session 内的关键事件时间线 |
| ConversationTimelineItem | `{ "timestamp", "kind": "message" \| "plan_draft" \| "plan_confirmed" \| "task_created" \| "approval" \| "challenge" \| "proposal" \| "executor_launch", "summary_cn": str }` |
| 禁止 | 触发任何副作用 |

### 11.6 Domain contract 设计

#### 11.6.1 核心域模型字段

**ProjectDirectorConversation**（对话视图，非独立数据实体）

```text
ProjectDirectorConversation
├── conversation_id: UUID              # = session.id
├── session_id: UUID                   # = conversation_id
├── project_id: UUID | None
├── title: str                         # 会话标题（≤ 200 chars）
├── kind: ConversationKind
├── status: ConversationStatus
├── owner_scope: str                   # "project" | "user"
├── related_plan_version_id: UUID | None
├── related_task_creation_id: UUID | None
├── related_run_id: UUID | None        # P7-H 后通过 conversation ↔ task 追溯
├── last_message_at: datetime | None
├── last_message_preview: str          # ≤ 120 chars
├── message_count: int                 # 可通过 message_repository count 得出
├── unread_count: int
├── challenge_count: int               # P7-F 后启用
├── proposal_count: int                # P7-G 后启用
├── archived_at: datetime | None
├── created_at: datetime               # = session.created_at
└── updated_at: datetime               # = max(session.updated_at, last_message_at)
```

**ConversationStatus**

```text
ConversationStatus
├── active: 最近 30 分钟内有消息
├── idle: 超过 30 分钟无消息
├── awaiting_user: 最新 assistant message requires_confirmation=True
├── archived: 用户主动归档
└── completed: session CONFIRMED 且已创建正式项目
```

**ConversationKind**

```text
ConversationKind
├── project_onboarding: 目标输入 → 澄清 → 计划草案
├── general_discussion: 自由对话
├── plan_review: 计划审核讨论
└── follow_up: 执行后跟进
```

**ConversationListItem**（列表用轻量视图）

```text
ConversationListItem
├── conversation_id: UUID
├── project_id: UUID | None
├── title: str
├── kind: ConversationKind
├── status: ConversationStatus
├── session_status: str               # 底层 session.status
├── last_message_preview: str
├── last_message_at: datetime | None
├── message_count: int
├── pending_challenge_count: int
├── pending_proposal_count: int
├── requires_user_action: bool
├── created_at: datetime
└── updated_at: datetime
```

**ConversationTimelineItem**

```text
ConversationTimelineItem
├── timestamp: datetime
├── kind: TimelineItemKind            # message | plan_draft | plan_confirmed | task_created | approval | challenge | proposal | executor_launch
├── summary_cn: str                   # ≤ 200 chars
├── related_message_id: UUID | None
├── related_plan_version_id: UUID | None
├── related_task_id: UUID | None
└── related_proposal_id: UUID | None
```

**ConversationOwnership**

```text
ConversationOwnership
├── owner_scope: str                  # "project" | "user"
├── project_id: UUID | None
├── user_visible: bool
├── transfer_allowed: bool
└── ownership_policy: str             # "single_project" | "multi_project" | "user_personal"
```

**ConversationResumePolicy**

```text
ConversationResumePolicy
├── auto_resume: bool                 # 自动恢复到最近活跃 session（默认 false，要求显式选择）
├── auto_create_on_input: bool        # session null 时自然语言输入是否自动创建 session（**P7-C 设为 false**）
├── require_explicit_new: bool        # 新建 session 是否需要用户显式确认（**P7-C 设为 true**）
├── max_active_conversations: int     # 最大活跃会话数（默认 20）
├── idle_archive_after_days: int      # 闲置多少天后自动归档（默认 30）
└── allow_parallel_sessions: bool     # 是否允许多个活跃 session 并行（**P7-C 设为 true**）
```

### 11.7 前端 contract 设计

#### 11.7.1 布局建议

| 区域 | 内容 | 说明 |
|------|------|------|
| 左侧 / 顶部导航 | ConversationList | 多会话列表：每条显示 title、kind badge、status dot、last_message_preview、requires_user_action indicator |
| 中央主区域 | DirectorChatEntry | 接收 `conversationId`（= session.id），展示 `messageTimeline` |
| 顶部操作栏 | 新建按钮 + 筛选下拉 | 明确区分「新建主管会话」和「继续现有会话」 |

#### 11.7.2 DirectorChatEntry 行为修正

| 修正点 | 修正前 | 修正后 |
|--------|--------|--------|
| `handleSubmit` 分流 | session null → `createSessionMutation` | session null → 提示"请先选择或新建主管会话" |
| 新建按钮 | 无独立按钮 | 「新建主管会话」→ 进入 goal_text 输入流程 |
| 自由对话 | 不存在（session null 时所有文本被当成 goal） | session 存在 + kind=general_discussion → `postMessage`，不重置 session |
| 消息发送 | `handleSubmit` 单一入口 | 拆分为 `handleStartNewSession` + `handleSendMessage` |

#### 11.7.3 与现有 hooks 的关系

| 现有 hook | P7-C 复用方式 | 是否变更 |
|----------|-------------|---------|
| `useProjectDirectorWorkbenchResumableSessions` | 重命名为 `useConversationList` | 否（P7-I 实现时再做） |
| `useProjectDirectorWorkbenchResume` | 拆分为 `useConversationDetail`（GET /conversations/{id}） | 否 |
| `useProjectDirectorSessionMessages` | 保持现有 contract，传入 `conversationId`（= session.id） | 否 |
| `usePostProjectDirectorSessionMessage` | 保持现有 contract | 否 |
| `useCreateProjectDirectorSession` | 增加 `kind` 参数区分 project_onboarding vs general_discussion | 否（P7-I 实现时扩展） |

### 11.8 安全边界

| # | 规则 | 说明 |
|---|------|------|
| 1 | ConversationList 只负责选择/恢复/展示会话，**不执行任务** | 纯 UI 行为 |
| 2 | ProjectDirectorConversation **不自动创建 task/run/worker** | 复用现有 message service 安全边界 |
| 3 | POST message **不执行 suggested_actions** | 复用现有 `_FORBIDDEN_MESSAGE_ACTIONS` |
| 4 | conversation restore **不触发 provider** | GET conversation detail 是只读查询 |
| 5 | conversation archive **不删除历史消息** | 仅更新 status 字段 |
| 6 | conversation title 自动生成**不调用外部执行器** | 使用 goal_text 截断或 rule-based 生成 |
| 7 | **不启动 Codex / Claude Code / DeepSeek** | P7 全线禁止执行器启动 |
| 8 | **不做 git add / commit / push / PR / merge** | 全线禁止 |
| 9 | **不把"新建会话"当成"创建正式项目"** | 新建 general_discussion conversation 不触发 plan draft |
| 10 | **不把"用户继续对话"当成"用户确认执行"** | conversation 内讨论不产生副作用 |
| 11 | `auto_create_on_input` **必须为 false** | session null 时自然语言输入不默默创建 session |

### 11.9 与后续子阶段的边界

| 子阶段 | 本轮 P7-C 边界 |
|--------|---------------|
| P7-D DirectorInbox | P7-C 不设计跨页面消息汇总、note intake、challenge intake。留给 P7-D |
| P7-E ConversationRouter + DirectorContextAssembler | P7-C 设计 conversation ownership 字段但路由规则留给 P7-E |
| P7-F UserChallenge / Intervention | P7-C 在 ConversationListItem 中预留 `pending_challenge_count`，但 challenge domain 留给 P7-F |
| P7-G PlanRevision / DirectorActionProposal / ProposalApproval | P7-C 在 ConversationListItem 中预留 `pending_proposal_count`，但 proposal domain 留给 P7-G |
| P7-H Conversation-to-Task / Conversation-to-Plan | P7-C 设计 `related_plan_version_id` / `related_task_creation_id` 引用字段，但影响链路留给 P7-H |
| P7-I 前端实现 | P7-C 只设计前端 contract，不实现代码 |
| P7-J targeted evidence + UAT seed | 留给 P7-J |

### 11.10 测试规划

本轮不新增测试。后续 P7-C 实现时应新增以下 targeted tests：

| # | 测试名称 | 覆盖能力 |
|---|---------|---------|
| 1 | `test_conversation_list_default_sort_by_last_message_desc` | 列表排序 |
| 2 | `test_conversation_restore_does_not_create_new_session` | 恢复不创建新 session |
| 3 | `test_null_session_natural_text_does_not_create_goal` | session null 时不默默创建 session |
| 4 | `test_archive_does_not_delete_messages` | archive 保留消息 |
| 5 | `test_conversation_with_mismatched_project_rejected` | conversation ↔ project 所有权校验 |
| 6 | `test_post_message_does_not_create_run_task_worker` | POST message 安全边界 |
| 7 | `test_suggested_actions_are_suggestions_not_executed` | suggested_actions 只建议不执行 |
| 8 | `test_new_general_discussion_session_not_trigger_clarification` | general_discussion 不生成澄清问题 |
| 9 | `test_conversation_list_empty_state_message` | 空列表文案 |

### 11.11 测试证据

| 命令 | 结果 |
|------|------|
| `git diff --check` | **Pass** |

### 11.12 边界声明

| 声明 | 结论 |
|------|------|
| 是否设计 ProjectDirectorConversation contract | 是 — 复用 session.id 作为 conversation_id |
| 是否设计 ConversationList contract | 是 — 含 13 个字段、4 种 kind、5 种 status、6 种筛选 |
| 是否设计 API contract | 是 — 6 个建议端点 |
| 是否设计 Domain contract | 是 — 6 个域模型 |
| 是否设计前端 contract | 是 — 布局 + handleSubmit 分流 + hooks 复用 |
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否实现代码 | 否 |
| 是否进入 P7-D | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |

### 11.13 P7-C Gate

| Gate | 结论 |
|------|------|
| ProjectDirectorConversation contract 已冻结 | **Pass** |
| ConversationList contract 已冻结 | **Pass** |
| session null 时自然语言输入不再默默创建 session（修复历史 P0 缺口） | **Pass**（contract 层冻结，实现留给 P7-I） |
| API contract 设计完成（6 端点） | **Pass** |
| Domain contract 设计完成（6 模型） | **Pass** |
| 前端 contract 设计完成 | **Pass** |
| 安全边界明确（11 项） | **Pass** |
| 与 P7-D/P7-E/P7-F/P7-G/P7-H/P7-I 边界明确 | **Pass** |
| 测试规划已完成（9 条） | **Pass** |
| 未改代码 / 未改 API / 未改前端 / 未改 DB | **Pass** |
| P7-D/P7-E/P7-F/P7-G/P7-H/P7-I/P7-J 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-C Gate 结论** | **Pass** |

---

## 12. P7-D DirectorInbox / cross-page note & challenge intake contract（2026-06-07）

### 12.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P7-D** | DirectorInbox / cross-page note & challenge intake contract design |
| 代码边界 | **未越过** | 只设计 contract，不写代码 |
| 是否实现代码 | 否 | P7-I 才允许前端实现 |
| 是否进入 P7-E | 否 | ConversationRouter + DirectorContextAssembler 留给 P7-E |

### 12.2 只读读取文件

| # | 文件 | 读取目的 |
|---|------|---------|
| 1 | `project_director_message.py` | 审计 suggested_actions / requires_confirmation / forbidden_actions 作为 inbox 信号源 |
| 2 | `project_director_message_service.py` | 审计 _FORBIDDEN_MESSAGE_ACTIONS 作为 inbox 安全基线 |
| 3 | `project_director_context_builder_service.py` | 审计 context readback 范围（plan/task/approval 状态）作为 inbox 数据源 |
| 4 | `project_director.py` (API) | 审计现有 resume/workbench 端点 |
| 5 | `DirectorChatEntry.tsx` | 审计前端无 inbox UI |
| 6 | `types.ts` / `api.ts` / `hooks.ts` (project-director) | 审计前端无 inbox/api/hooks |
| 7 | P7 ledger §1-§11 | 确保与 P7-A/B/C 已冻结 contract 一致 |

### 12.3 DirectorInbox 定义

#### 12.3.1 DirectorInbox 是什么

DirectorInbox 是 AI 项目主管的**跨页面收件箱**，汇总来自不同页面和对象的需要主管注意的事项：用户随手备注、计划质疑、调度问题、审批提醒、运行阻塞、失败回流信号、治理警告等。

| DirectorInbox 是 | DirectorInbox 不是 |
|-----------------|-------------------|
| 跨页面注意事项的汇总视图 | 任务队列（不取代 Task list） |
| 备注、质疑、待处理事项的 intake 容器 | 通知中心泛化（不覆盖所有系统事件） |
| 主管注意力的管理中心 | Worker 执行入口 |
| 结构化 challenge 的种子来源 | 自动派发入口 |

#### 12.3.2 DirectorInbox 与其他 P7 组件的关系

| 组件 | 关系 | 说明 |
|------|------|------|
| ProjectDirectorConversation | inbox item 可 link 到已有 conversation | 用户把 inbox 中的事项带到对话中讨论 |
| ConversationList | inbox 在 ConversationList 上方作为 badge/入口 | 提醒用户有待处理事项 |
| UserChallenge (P7-F) | inbox item 可 convert 为 challenge seed | P7-D 只设计 intake，完整 challenge domain 留给 P7-F |
| DirectorActionProposal (P7-G) | inbox item 可 convert 为 proposal seed | P7-D 只设计 intake，完整 proposal domain 留给 P7-G |
| P6 dispatch decision | inbox 可显示 dispatch_question 类型 | 用户质疑 dispatch suggestion → 进入 inbox |

#### 12.3.3 DirectorInbox 不是什么

| # | 不是 | 说明 |
|---|------|------|
| 1 | 不是任务队列 | 不取代 Task list / Run list |
| 2 | 不是通知中心泛化 | 不覆盖所有系统事件（token 用完、model switch 等不在此） |
| 3 | 不是 Worker 执行入口 | 不启动 Worker |
| 4 | 不是自动派发入口 | 不派发 Codex / Claude Code / DeepSeek |
| 5 | 不是 retry 入口 | run_blocker 项不触发 retry |
| 6 | 不是 approval 审批流 | approval_attention 只是提醒，审批在 P4/G 链路 |

### 12.4 InboxItem 类型设计

#### 12.4.1 核心域模型字段

```text
DirectorInboxItem
├── id: UUID
├── conversation_id: UUID | None        # 关联的 conversation (= session.id)
├── session_id: UUID | None             # 直接关联的 session
├── project_id: UUID | None
├── source_page: str                    # "workbench" | "run_detail" | "task_detail" | "approval_page" | "deliverable_page" | "governance_page" | "dispatch_suggestion" | "system"
├── source_entity_type: str             # "run" | "task" | "plan_version" | "approval" | "deliverable" | "dispatch_decision" | "note" | "system_event"
├── source_entity_id: UUID | None
├── kind: InboxItemKind
├── title: str                          # ≤ 200 chars
├── summary: str                        # ≤ 1000 chars，中文用户可见摘要
├── status: InboxItemStatus
├── priority: InboxItemPriority
├── requires_user_action: bool
├── auto_generated: bool                # system-generated vs user-created note
├── related_message_id: UUID | None
├── related_plan_version_id: UUID | None
├── related_task_id: UUID | None
├── related_run_id: UUID | None
├── related_approval_id: UUID | None
├── related_dispatch_decision_id: UUID | None
├── linked_conversation_at: datetime | None
├── converted_to_challenge_at: datetime | None   # P7-F 后启用
├── converted_to_proposal_at: datetime | None    # P7-G 后启用
├── resolved_at: datetime | None
├── archived_at: datetime | None
├── created_at: datetime
└── updated_at: datetime
```

#### 12.4.2 InboxItemKind 枚举

```text
InboxItemKind
├── note: 用户随手备注（任何页面）
├── user_challenge_seed: 用户质疑的初始种子
├── plan_question: 关于计划的疑问
├── dispatch_question: 关于调度建议的质疑
├── approval_attention: 审批待处理提醒
├── run_blocker: 运行阻塞信号
├── failure_recovery_attention: 失败回流关注
├── proposal_attention: 提案待审批提醒（P7-G 后）
├── governance_warning: 成本/角色/Skill 风险提示
└── system_notice: 系统级别通知
```

#### 12.4.3 InboxItemStatus 枚举

```text
InboxItemStatus
├── unread: 未读
├── read: 已读
├── needs_response: 需要主管回应
├── linked_to_conversation: 已关联到 conversation 讨论
├── converted_to_challenge: 已转为正式 UserChallenge（P7-F 后）
├── converted_to_proposal: 已转为正式 Proposal（P7-G 后）
├── resolved: 已处理完毕
└── archived: 已归档
```

#### 12.4.4 InboxItemPriority 枚举

```text
InboxItemPriority
├── low: 仅供参考
├── normal: 一般关注
├── high: 需要尽快处理
└── critical: 阻塞性事项
```

#### 12.4.5 InboxIntakePolicy

```text
InboxIntakePolicy
├── allow_user_note: bool               # 是否允许用户随手创建 note（默认 true）
├── auto_create_on_run_blocked: bool    # run blocked 时自动生成 inbox item（默认 true）
├── auto_create_on_failure: bool        # run failed 时自动生成 inbox item（默认 true）
├── auto_create_on_approval_pending: bool  # 审批 pending 时自动生成 inbox item（默认 true）
├── auto_create_on_dispatch_decision: bool # 有 dispatch decision 时自动生成 inbox item（默认 true）
├── max_unread_items: int               # 最大未读 item 数（默认 100）
├── auto_archive_resolved_after_days: int # 已处理项自动归档天数（默认 7）
└── allow_cross_project_items: bool     # 是否允许跨 project 收件（默认 true）
```

### 12.5 Cross-page intake 来源

| 来源页面 / 对象 | 触发条件 | kind | auto_generated | 说明 |
|---------------|---------|------|---------------|------|
| Workbench | 用户主动创建 note | note | false | "记录一条想法" |
| Workbench | 用户质疑当前计划 | plan_question / user_challenge_seed | false | 用户在对话中表达质疑 → 前端提供"提交至 Inbox"入口 |
| Run detail | run.status=FAILED / BLOCKED / CANCELLED | run_blocker / failure_recovery_attention | true | 系统自动生成 |
| Task detail | task 阻塞、验收不明确 | plan_question / run_blocker | true/false | 系统生成或用户手动 |
| Approval page | 待确认审批 | approval_attention | true | 系统自动生成 |
| Deliverable page | 交付物质疑 | note / plan_question | false | 用户手动 |
| Governance page | 成本超限、角色冲突、Skill 配置风险 | governance_warning | true | 系统自动生成 |
| P6 dispatch decision | 用户质疑调度建议 | dispatch_question | false | 用户在 dispatch suggestion 卡片上点"质疑" |
| P5 failure recovery | run failed + 有 recovery decision | failure_recovery_attention | true | 系统自动生成 |

### 12.6 API contract 设计

以下为 P7-D 冻结的 API contract 设计，**不实现代码**。

#### GET `/project-director/inbox`

| 属性 | 设计 |
|------|------|
| Query | `project_id?`, `status?`, `kind?`, `priority?`, `limit?`, `before?` |
| Response | `{ "items": [DirectorInboxItem], "has_more": bool, "unread_count": int, "needs_response_count": int }` |
| 是否只读 | **是** — 纯查询 |
| 默认排序 | `created_at DESC`（最新的在前） |
| 默认筛选 | `status != archived` |

#### POST `/project-director/inbox/items`

| 属性 | 设计 |
|------|------|
| Request | `{ "kind": InboxItemKind, "title": str, "summary": str, "source_page": str, "source_entity_type?": str, "source_entity_id?": UUID, "related_*_id?": UUID }` |
| Response | `{ "item": DirectorInboxItem }` |
| 是否只读 | **否** — 创建 inbox item（仅持久化 metadata） |
| 禁止 | 创建 task/run/worker、触发 provider、执行 suggested_actions |

#### PATCH `/project-director/inbox/items/{item_id}/read`

| 属性 | 设计 |
|------|------|
| Request | `{ "status": "read" }` |
| Response | `{ "item": DirectorInboxItem }` |
| 是否只读 | **否** — 仅更新 status |
| 禁止 | 修改 conversation / task / run / plan |

#### PATCH `/project-director/inbox/items/{item_id}/archive`

| 属性 | 设计 |
|------|------|
| Request | `{ "status": "archived" }` |
| Response | `{ "item": DirectorInboxItem }` |
| 是否只读 | **否** — 仅更新 status |
| 禁止 | 删除历史数据、删除关联 message、修改 conversation |

#### POST `/project-director/inbox/items/{item_id}/link-conversation`

| 属性 | 设计 |
|------|------|
| Request | `{ "conversation_id": UUID }` |
| Response | `{ "item": DirectorInboxItem, "conversation_id": UUID }` |
| 行为 | 设置 `item.conversation_id` + `status=linked_to_conversation` + `linked_conversation_at=now` |
| 禁止 | 自动新建 conversation（conversation 必须已存在）、移动历史消息、创建 task/run/worker |

#### POST `/project-director/inbox/items/{item_id}/convert-to-challenge`

| 属性 | 设计 |
|------|------|
| Request | `{ "challenge_type": str, "challenge_detail": str }` |
| Response | `{ "item": DirectorInboxItem, "challenge_seed_id": UUID }` |
| 行为 | 创建 challenge seed 记录（P7-F 的输入），设置 `item.status=converted_to_challenge` |
| 禁止 | 应用计划变更、修改 task/run、触发 provider |
| 说明 | convert-to-challenge 只是生成 challenge seed，完整 UserChallenge domain 留给 P7-F |

### 12.7 Conversation relation contract

| 规则 | 说明 |
|------|------|
| inbox item 可关联已有 conversation | `link-conversation` → `item.conversation_id = conversation.id` |
| inbox item **不能**自动新建 conversation | conversation 必须已存在 |
| link-conversation 只改变 item 归属 | 不移动历史消息 |
| item linked 后 conversation timeline 可显示 inbox-linked 事件 | timeline 聚合时包含 inbox item 摘要 |
| conversation archive **不删除** inbox item | inbox item 独立保留 |
| inbox item resolved/archived **不删除** conversation message | message 独立保留 |
| inbox item 可存在但不关联任何 conversation | `conversation_id = None` 是合法状态 |

### 12.8 前端 contract 设计

#### 12.8.1 推荐位置

| 位置 | 内容 | 交互 |
|------|------|------|
| ConversationList 上方 badge | 未读项数 / needs_response 项数 | 点击打开 inbox drawer |
| 右侧 inbox drawer | inbox item 列表（可筛选、可排序） | 点击 item 展开详情 |
| 顶部导航条 | inbox 图标 + 未读 badge | — |

#### 12.8.2 InboxItem 卡片显示

| 显示字段 | 说明 |
|---------|------|
| `kind` badge | note / challenge / plan_question / approval / run_blocker 等彩色标签 |
| `priority` indicator | low/normal/high/critical 色标 |
| `source_page` | 来源页面名称 |
| `title` | 标题（≤ 80 chars 截断） |
| `summary` | 摘要（≤ 200 chars 截断） |
| `requires_user_action` | 需要行动标记（红色 dot） |
| `created_at` | 相对时间或绝对时间 |

#### 12.8.3 用户可用动作

| 动作 | 说明 | 是否产生副作用 |
|------|------|-------------|
| 标记已读 | `PATCH /inbox/items/{id}/read` | 否 |
| 归档 | `PATCH /inbox/items/{id}/archive` | 否 |
| 关联到当前会话 | `POST /inbox/items/{id}/link-conversation` | 否 |
| 转为质疑 | `POST /inbox/items/{id}/convert-to-challenge` | 仅生成 seed，不修改 plan/task |
| 打开来源对象 | 前端导航到 source_entity 对应的页面 | 否 |

#### 12.8.4 严禁出现

| # | 严禁 |
|---|------|
| 1 | 「启动 Worker」按钮 |
| 2 | 「启动 Codex / Claude Code」按钮 |
| 3 | 「自动执行 / 自动派发」按钮 |
| 4 | 「Retry」按钮 |
| 5 | 「Approve」按钮（approval 在独立审批链路） |
| 6 | 任何调用写入型 API（除 inbox 自身 CRUD）的入口 |

### 12.9 安全边界

| # | 规则 | 说明 |
|---|------|------|
| 1 | DirectorInbox 只收集和归档注意事项，**不执行任务** | 纯信息聚合 |
| 2 | inbox item **不自动创建 conversation** | 用户必须显式 link-conversation |
| 3 | inbox item **不自动创建 task/run/worker** | 全线禁止 |
| 4 | inbox item **不自动触发 provider** | 无 AI 调用 |
| 5 | inbox item **不执行 suggested_actions** | 不调用任何执行逻辑 |
| 6 | convert-to-challenge **不等于应用计划变更** | 仅生成 seed |
| 7 | link-conversation **不等于用户确认** | 不触发 approval |
| 8 | **不启动 Codex / Claude Code / DeepSeek** | P7 全线禁止 |
| 9 | **不做 git add / commit / push / PR / merge** | 全线禁止 |
| 10 | **不把 run_blocker 写成 retry 指令** | 只记录注意项，不触发重试 |
| 11 | **不把 approval_attention 写成 approval_applied** | 只提醒，不审批 |
| 12 | system_notice **不得包含用户数据泄露** | 仅系统级摘要 |

### 12.10 与后续子阶段边界

| 子阶段 | 本轮 P7-D 边界 |
|--------|---------------|
| P7-E ConversationRouter + DirectorContextAssembler | P7-D 设计 source_page / source_entity 字段但路由规则留给 P7-E |
| P7-F UserChallenge / Intervention | P7-D 设计 user_challenge_seed kind 和 convert-to-challenge 入口，但完整 challenge domain 留给 P7-F |
| P7-G PlanRevision / DirectorActionProposal / ProposalApproval | P7-D 设计 proposal_attention kind，但 proposal domain 留给 P7-G |
| P7-H Conversation-to-Task / Conversation-to-Plan | P7-D 不设计 conversation ↔ task 影响链路 |
| P7-I 前端实现 | P7-D 只设计前端 contract，不实现代码 |
| P7-J targeted evidence + UAT seed | 留给 P7-J |

### 12.11 测试规划

| # | 测试名称 | 覆盖能力 |
|---|---------|---------|
| 1 | `test_inbox_list_default_filters_unread_non_archived` | 默认筛选 |
| 2 | `test_create_inbox_item_persists_metadata_only` | 仅写 metadata |
| 3 | `test_mark_read_does_not_modify_conversation` | 读操作安全 |
| 4 | `test_archive_inbox_item_does_not_delete_messages` | archive 保留 messages |
| 5 | `test_link_inbox_item_to_conversation_does_not_create_task_run_worker` | link 安全 |
| 6 | `test_convert_to_challenge_seed_does_not_apply_plan_change` | convert 安全 |
| 7 | `test_run_blocker_item_does_not_trigger_retry` | run_blocker 不触发 retry |
| 8 | `test_approval_attention_does_not_set_approval_applied` | approval 不自动审批 |
| 9 | `test_inbox_item_project_mismatch_rejected` | project 归属校验 |
| 10 | `test_system_generated_inbox_item_on_run_failed` | 自动生成条件 |

### 12.12 测试证据

| 命令 | 结果 |
|------|------|
| `git diff --check` | **Pass** |

### 12.13 边界声明

| 声明 | 结论 |
|------|------|
| 是否设计 DirectorInbox contract | 是 — 含 5 个域模型、10 种 kind、8 种 status、4 种 priority、7 个 intake policy |
| 是否设计 API contract | 是 — 6 个建议端点 |
| 是否设计前端 contract | 是 — 布局 + 卡片字段 + 用户动作 + 严禁列表 |
| 是否设计 cross-page intake 来源 | 是 — 9 个来源页面/对象 |
| 是否修改 Python 业务代码 | 否 |
| 是否修改测试代码 | 否 |
| 是否修改前端 | 否 |
| 是否修改数据库 / migration | 否 |
| 是否新增 API | 否 |
| 是否实现代码 | 否 |
| 是否进入 P7-E | 否 |
| 是否实现产品运行时 Git 写操作 | 否 |

### 12.14 P7-D Gate

| Gate | 结论 |
|------|------|
| DirectorInbox contract 已冻结 | **Pass** |
| InboxItem domain 设计完成（5 个模型） | **Pass** |
| InboxItemKind 枚举完成（10 种） | **Pass** |
| InboxItemStatus 枚举完成（8 种） | **Pass** |
| Cross-page intake 来源设计完成（9 个） | **Pass** |
| API contract 设计完成（6 端点） | **Pass** |
| Conversation relation contract 已冻结 | **Pass** |
| 前端 contract 设计完成 | **Pass** |
| 安全边界明确（12 项） | **Pass** |
| 与 P7-E/P7-F/P7-G/P7-H/P7-I 边界明确 | **Pass** |
| 测试规划已完成（10 条） | **Pass** |
| 未改代码 / 未改 API / 未改前端 / 未改 DB | **Pass** |
| P7-E/P7-F/P7-G/P7-H/P7-I/P7-J 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-D Gate 结论** | **Pass** |

---

## 13. P7-C1 ConversationList backend read-only implementation（2026-06-07）

### 13.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P7-C1** | ConversationList backend read-only implementation |
| 代码边界 | **未越过** | 只做后端只读 API + targeted tests + ledger 回填 |
| 是否修改前端 | 否 | 未修改 `apps/web` |
| 是否修改数据库 / migration | 否 | 复用 `project_director_sessions` / `project_director_messages` / plan/task creation 既有表 |
| 是否实现写入型 conversation API | 否 | 未实现 POST/PATCH conversations |
| 是否进入 P7-E | 否 | ConversationRouter + DirectorContextAssembler governance 仍 Not started |

### 13.2 只读读取文件

| # | 文件 | 读取目的 |
|---|------|---------|
| 1 | `runtime/orchestrator/app/api/routes/project_director.py` | 复用既有 session/message/workbench response 与路由风格 |
| 2 | `runtime/orchestrator/app/repositories/project_director_session_repository.py` | 复用 session readback 与状态字段 |
| 3 | `runtime/orchestrator/app/repositories/project_director_message_repository.py` | 复用 message list/sequence 排序规则 |
| 4 | `runtime/orchestrator/app/repositories/project_director_plan_version_repository.py` | 复用 latest plan version readback |
| 5 | `runtime/orchestrator/app/repositories/project_director_task_creation_repository.py` | 复用 task creation record readback |
| 6 | `runtime/orchestrator/app/core/db_tables.py` | 确认现有表字段，未新增 DB/migration |
| 7 | `runtime/orchestrator/tests/test_project_director_messages.py` | 对齐 Project Director API test fixture 风格 |
| 8 | `runtime/orchestrator/tests/test_project_director_sessions.py` | 对齐 workbench resume 只读语义 |
| 9 | `docs/产品文档/AI项目主管/07-P7主管对话与治理/P7主管对话与治理总账与阶段设计-20260607.md` §11 | 对齐 P7-C frozen contract |

### 13.3 实现内容

| 层 | 文件 | 内容 |
|----|------|------|
| Service | `runtime/orchestrator/app/services/project_director_conversation_service.py` | 新增只读 ConversationList/Detail/Timeline 聚合服务 |
| API | `runtime/orchestrator/app/api/routes/project_director.py` | 新增 3 个 GET endpoints 与 response DTO |
| Tests | `runtime/orchestrator/tests/test_project_director_conversation_readonly_api.py` | 新增 6 条 targeted API tests |
| Ledger | 本文档 | 回填 P7-C1 evidence 与 Gate |

### 13.4 新增 API

| API | 方法 | 只读 | 说明 |
|-----|------|------|------|
| `/project-director/conversations` | GET | 是 | ConversationList；支持 `project_id` / `status` / `kind` / `limit` |
| `/project-director/conversations/{conversation_id}` | GET | 是 | Conversation detail；返回 session、recent_messages、latest_plan_version、task_creation readback |
| `/project-director/conversations/{conversation_id}/timeline` | GET | 是 | Conversation timeline；聚合 message / plan draft / plan confirmed / task created 只读事件 |

### 13.5 借鉴机制

| 来源 | 借鉴机制 | 本轮落地方式 |
|------|----------|-------------|
| 现有 workbench resume API | 显式恢复 session 不创建新 session、不触发 provider | Conversation detail 仅根据 `session.id` readback |
| 现有 message repository | session-scoped message sequence + chronological replay | Timeline 使用 message sequence/created_at 回放 |
| P7-C contract | `session.id == conversation_id`，不新增 conversation 表 | Conversation 是 read model，不是新 DB 实体 |
| P7-C 安全边界 | ConversationList 只选择/恢复/展示，不执行任务 | 仅新增 GET；POST `/project-director/conversations` 仍 405 |
| 参考项目 AO 规划机制 | 多会话列表、activity/readback、只读恢复思路 | 只借鉴机制，不复制代码，不引入 TypeScript/Node 插件栈 |

### 13.6 测试证据

| 命令 | 结果 |
|------|------|
| `runtime/orchestrator/.venv/bin/pytest runtime/orchestrator/tests/test_project_director_conversation_readonly_api.py -q` | **Pass** — 6 passed, 2 warnings |
| `runtime/orchestrator/.venv/bin/pytest runtime/orchestrator/tests/test_project_director_conversation_readonly_api.py runtime/orchestrator/tests/test_project_director_messages.py runtime/orchestrator/tests/test_project_director_sessions.py -q` | **Pass** — 71 passed, 9 warnings |

### 13.7 Targeted tests 覆盖

| 测试 | 覆盖 |
|------|------|
| `test_conversation_list_default_sort_by_last_message_desc` | 默认按最近消息排序 |
| `test_conversation_list_project_and_awaiting_user_filters` | project/status 筛选与 requires_user_action |
| `test_conversation_restore_does_not_create_session_or_messages` | 恢复只读，不创建 session/message |
| `test_conversation_with_mismatched_project_rejected` | conversation ↔ project 所有权校验 |
| `test_conversation_timeline_is_read_only_message_replay` | timeline 只读消息回放 |
| `test_conversation_post_endpoint_not_implemented_for_p7_c1_readonly_scope` | 本轮未新增写入型 conversation API |

### 13.8 边界声明

| 声明 | 结论 |
|------|------|
| 是否只做 ConversationList 后端只读最小实现 | 是 |
| 是否新增前端实现 | 否 |
| 是否修改 DB / migration | 否 |
| 是否实现 ConversationRouter / DirectorContextAssembler | 否，P7-E 仍 Not started |
| 是否实现 UserChallenge / Proposal / PlanRevision | 否 |
| 是否实现 P8/P9 executor config/runtime | 否，P8/P9 implementation 仍 Not started |
| 是否实现产品运行时 git add / commit / push / PR | 否 |
| 是否触发 provider / Worker / Run / Task 创建 | 否 |
| 是否复制参考项目代码或引入参考项目技术栈 | 否 |

### 13.9 P7-C1 Initial Gate

| Gate | 结论 |
|------|------|
| ConversationList backend read-only API 已实现 | **Pass** |
| Conversation detail readback 已实现 | **Pass** |
| Conversation timeline readback 已实现 | **Pass** |
| Targeted tests 已补齐并通过 | **Pass** |
| `before` query contract 已实现 | **Fail** |
| `limit` 上限与 contract 一致 | **Fail** |
| `kind` 根据 plan/task creation 推导 | **Fail** |
| 未修改前端 | **Pass** |
| 未修改 DB / migration | **Pass** |
| 未实现写入型 conversation API | **Pass** |
| P7-E 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 Git 写操作仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-C1 initial implementation Gate 结论** | **Partial，需要 R1** |

## 14. P7-C1-R1 ConversationList read-only API contract correction（2026-06-08）

### 14.1 R1 修正原因

| 缺口 | 说明 |
|------|------|
| 上轮未读取参考项目源码 | P7-C1 初版只记录了参考机制，但没有按要求实际读取参考项目源码文件 |
| `before` query 缺失 | P7-C frozen contract 要求 `GET /project-director/conversations` 支持 `before?` cursor，初版未实现 |
| `kind` 推导不完整 | 初版固定返回 `project_onboarding`，未根据 plan/task creation 推导 `plan_review` / `follow_up` |
| `limit` contract 不一致 | 初版 clamp 为 1-50，P7-C ConversationList contract 需要本轮修正为 1-100 |

### 14.2 参考项目实际读取文件列表

本轮只读读取本地参考项目 `/Users/kk/project explore/agent-orchestrator`，未修改任何参考项目文件。

| # | 文件 |
|---|------|
| 1 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/session-manager.ts` |
| 2 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-state.ts` |
| 3 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` |
| 4 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-manager.ts` |

### 14.3 从参考项目借鉴的机制摘要

| # | 机制 | P7-C1-R1 吸收方式 |
|---|------|------------------|
| 1 | Session list / metadata readback 与 runtime 行为分离 | ConversationList 继续作为 session-derived read model，不创建 conversation 表、不触发 provider |
| 2 | Canonical lifecycle state 只表达真实状态 | `kind` 只从已有 plan/task creation 事实推导，不把建议或未来能力写成已发生 |
| 3 | Activity event 采用 source/kind/level 结构化分类并 best-effort 写入 | 本轮只借鉴“结构化、可审计、失败不扩散”的治理思想；P7-C1-R1 不新增 event 写入 |
| 4 | Lifecycle manager 以排序、轮询、状态转换为中心，副作用被明确隔离 | ConversationList cursor 在排序结果中定位，只返回 cursor 之后 items，不改变 session/message/task/run |

### 14.4 本轮代码修正点

| 文件 | 修正 |
|------|------|
| `runtime/orchestrator/app/api/routes/project_director.py` | `GET /project-director/conversations` 新增 `before: UUID | None` query；cursor 校验错误返回 422 |
| `runtime/orchestrator/app/services/project_director_conversation_service.py` | `limit` clamp 改为 1-100；按 latest activity 稳定排序后应用 `before` cursor；`kind` 改为 task creation → `follow_up`，latest plan → `plan_review`，否则 `project_onboarding` |
| `runtime/orchestrator/tests/test_project_director_conversation_readonly_api.py` | 新增 cursor、unknown cursor、limit cap、plan_review、follow_up targeted tests |

### 14.5 测试结果

| 命令 | 结果 |
|------|------|
| `pytest tests/test_project_director_conversation_readonly_api.py tests/test_project_director_messages.py tests/test_project_director_sessions.py -q` | **Pass** — 77 passed, 10 warnings |
| `git diff --check` | **Pass** |

### 14.6 P7-C1-R1 Gate

| Gate | 结论 |
|------|------|
| `before` query 已实现并测试 | **Pass** |
| unknown `before` cursor 返回预期错误 | **Pass** — 422 |
| `limit` 上限为 100 并测试 | **Pass** |
| `kind=plan_review` 基于 latest plan 推导并测试 | **Pass** |
| `kind=follow_up` 基于 task creation 推导并测试 | **Pass** |
| 未修改前端 | **Pass** |
| 未修改 DB / migration | **Pass** |
| 未实现 DirectorInbox / Proposal / 外部执行器 | **Pass** |
| 未触发 provider / Worker / Run / Task 创建 | **Pass** |
| 未复制参考项目代码、未引入参考项目技术栈 | **Pass** |
| P7-E 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 git add / commit / push / PR 仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-C1-R1 Gate 结论** | **Pass** |


## 15. P7-C2 ConversationList frontend API/types/hooks read-only wiring（2026-06-08）

### 15.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P7-C2** | ConversationList frontend API/types/hooks read-only wiring |
| 代码边界 | **未越过** | 只修改 `types.ts` / `api.ts` / `hooks.ts` 与本 ledger |
| 是否接入 UI 页面 | 否 | 未修改 `apps/web/src/pages/**`，未改 `DirectorChatEntry` |
| 是否修改后端 / DB / migration | 否 | 复用 P7-C1-R1 已实现的 read-only GET APIs |
| 是否进入 P7-E | 否 | ConversationRouter + DirectorContextAssembler 仍 Not started |

### 15.2 参考项目读取记录

本轮只读读取本地参考项目 `/Users/kk/project explore/agent-orchestrator`，未修改任何参考项目文件；未读取或实现 external agent / Claude Code / Codex adapter / launch command。

| # | 文件 | 读取目的 |
|---|------|---------|
| 1 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/session-manager.ts` | 学习 session list / metadata readback 与运行时副作用隔离 |
| 2 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-state.ts` | 学习 canonical lifecycle state 的前端可读状态表达 |
| 3 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` | 学习 observable list/timeline 事件的结构化摘要思路 |

### 15.3 借鉴机制摘要

| # | 机制 | P7-C2 吸收方式 |
|---|------|----------------|
| 1 | Session list/readback 与 launch/runtime 操作分离 | 前端只新增 GET API client 与 useQuery hooks，不新增 mutation，不触发 provider/worker/run/task |
| 2 | Lifecycle state 作为稳定 read model 暴露 | 前端新增 `ProjectDirectorConversationStatus` / `ProjectDirectorConversationKind` 对齐后端枚举 |
| 3 | Observable activity event/timeline 以结构化字段呈现 | 前端新增 `ProjectDirectorConversationTimelineItem` / `TimelineResponse`，只读承接后端 timeline 聚合 |

### 15.4 修改文件

| 文件 | 修改 |
|------|------|
| `apps/web/src/features/project-director/types.ts` | 新增 ConversationList / Detail / Timeline response 类型与 query params 类型 |
| `apps/web/src/features/project-director/api.ts` | 新增三个 read-only GET API 函数与 query string 构造 helper |
| `apps/web/src/features/project-director/hooks.ts` | 新增三个 React Query read-only hooks；detail/timeline 在 `conversationId` 为空时 disabled |
| `docs/产品文档/AI项目主管/07-P7主管对话与治理/P7主管对话与治理总账与阶段设计-20260607.md` | 回填 P7-C2 范围、证据与 Gate |

### 15.5 新增前端类型/API/hooks

| 类别 | 新增项 |
|------|--------|
| Types | `ProjectDirectorConversationStatus`；`ProjectDirectorConversationKind`；`ProjectDirectorConversationListItem`；`ProjectDirectorConversationListResponse`；`ProjectDirectorConversationDetailResponse`；`ProjectDirectorConversationTimelineItem`；`ProjectDirectorConversationTimelineResponse` |
| API | `listProjectDirectorConversations(params)`；`getProjectDirectorConversation(conversationId, params?)`；`getProjectDirectorConversationTimeline(conversationId, params?)` |
| Hooks | `useProjectDirectorConversations(params, options?)`；`useProjectDirectorConversation(conversationId, params?, options?)`；`useProjectDirectorConversationTimeline(conversationId, params?, options?)` |

> 注：P7-C1-R1 后端 `ConversationListItemResponse` 未暴露独立 `session_id` 字段。前端类型按 P7-C contract 在注释中声明 `session_id = conversation_id` 可推导，保持与后端 response 对齐且不虚构运行时字段。

### 15.6 测试结果

| 命令 | 结果 |
|------|------|
| `cd apps/web && npm run build` | **Pass** — `tsc -b && vite build` succeeded；Vite emitted existing chunk-size warning only |
| `git diff --check` | **Pass** |

### 15.7 安全边界

| 边界 | 结论 |
|------|------|
| 只增加 read-only GET API client | **Pass** |
| 不新增 POST / PATCH / DELETE | **Pass** |
| 不新增 mutation | **Pass** |
| 不改页面 UI / 不改 DirectorChatEntry | **Pass** |
| 不调用 create session / create tasks / run worker / approval / review | **Pass** |
| 不调用 provider，不启动 Codex / Claude Code / DeepSeek | **Pass** |
| 不修改后端 / DB / migration / tests / package / lock / CI | **Pass** |
| 产品运行时 git add / commit / push / PR 仍 Not started | **Pass** |
| 未复制参考项目代码、未引入参考项目技术栈 | **Pass** |

### 15.8 P7-C2 Gate

| Gate | 结论 |
|------|------|
| ConversationList frontend response types 已对齐 P7-C1-R1 后端 response | **Pass** |
| ConversationDetail / Timeline frontend response types 已新增 | **Pass** |
| 三个 read-only GET API 函数已新增 | **Pass** |
| 三个 React Query read-only hooks 已新增 | **Pass** |
| queryKey 区分 `project_id` / `status` / `kind` / `limit` / `before` | **Pass** |
| detail/timeline 在 `conversationId` 为空时 disabled | **Pass** |
| 未新增 UI / mutation / provider / worker / executor / Git 写运行时能力 | **Pass** |
| `cd apps/web && npm run build` | **Pass** |
| `git diff --check` | **Pass** |
| P7-E 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 git add / commit / push / PR 仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-C2 Gate 结论** | **Pass** |


## 16. P7-C3 Workbench ConversationList read-only UI（2026-06-08）

### 16.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P7-C3** | Workbench ConversationList read-only UI |
| 代码边界 | **未越过** | 只修改 Workbench/DirectorChatEntry 周边 UI 与本 ledger |
| 是否实现后端 / DB / migration | 否 | 复用 P7-C1-R1 后端 read-only API 与 P7-C2 hook |
| 是否实现 DirectorInbox / Challenge / Proposal | 否 | 仍留给后续阶段 |
| 是否进入 P7-E | 否 | ConversationRouter + DirectorContextAssembler 仍 Not started |

### 16.2 参考项目读取记录

本轮只读读取本地参考项目 `/Users/kk/project explore/agent-orchestrator`，未修改任何参考项目文件；未读取或实现 external agent / Claude Code / Codex adapter / launch command。

| # | 文件 | 读取目的 |
|---|------|---------|
| 1 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/session-manager.ts` | 学习 session list / metadata readback 与选择恢复思想 |
| 2 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-state.ts` | 学习 status badge / lifecycle state 的可读表达 |
| 3 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` | 学习 observable list / activity readback 的结构化摘要思想 |

### 16.3 借鉴机制摘要

| # | 机制 | P7-C3 吸收方式 |
|---|------|----------------|
| 1 | Session list 只读展示与 runtime/launch 操作隔离 | Workbench 新增 ConversationList 只读列表，点击只选择/恢复，不创建 session、不触发 Provider |
| 2 | Lifecycle/status 以 badge 形式表达 | 列表展示 `kind` / `status` badge 与 selected state / requires_user_action 标记 |
| 3 | Activity readback 以最近活动与摘要辅助选择 | 列表展示 `last_message_preview`、`last_message_at`/`updated_at`、`message_count`、owner/project 简要信息 |

### 16.4 修改文件

| 文件 | 修改 |
|------|------|
| `apps/web/src/pages/workbench/components/ProjectDirectorConversationList.tsx` | 新增轻量只读 ConversationList UI |
| `apps/web/src/pages/workbench/WorkbenchPage.tsx` | 挂载 ConversationList；点击项更新 selected session/conversation 与 URL，复用现有 resume/messages 链路 |
| `apps/web/src/pages/workbench/components/DirectorChatEntry.tsx` | 拆分普通输入与显式新建会话；无 session 普通输入只提示，不默默创建 session |
| `docs/产品文档/AI项目主管/07-P7主管对话与治理/P7主管对话与治理总账与阶段设计-20260607.md` | 回填 P7-C3 范围、证据与 Gate |

### 16.5 UI 行为说明

| 行为 | 说明 |
|------|------|
| ConversationList loading | 显示骨架 loading 状态 |
| ConversationList empty | 显示“还没有主管会话”并提示点击新建主管会话开始 |
| ConversationList error | 展示读取失败 message |
| ConversationList item | 展示 title、kind badge、status badge、last message preview、activity time、message_count、owner_scope/project_id |
| selected state | 当前 selected conversation 显示“当前选择”与高亮边框 |
| requires_user_action | 显示“需要你处理”简短提示 |
| 点击会话 | 设置 `selectedDirectorSessionId` / URL 参数；复用 `useProjectDirectorWorkbenchResume` 与 `useProjectDirectorSessionMessages` 加载 detail/messages |

### 16.6 session null 输入行为修正

| 场景 | P7-C3 行为 |
|------|------------|
| 当前无 selected session/conversation，用户在普通输入框输入自然语言并提交 | 不调用 `createSessionMutation`；显示“请先选择一个主管会话，或点击新建主管会话开始新目标。” |
| 用户点击“新建主管会话 / 开始新目标” | 进入显式新建模式，随后提交才沿用原 create session 流程 |
| 点击 ConversationList item | 退出新建模式并恢复选中会话，不创建新 session，不触发 provider |
| 已有 session 下提交 | 保持原行为：调用现有 message mutation 追加到当前 session messages |

### 16.7 测试结果

| 命令 | 结果 |
|------|------|
| `cd apps/web && npm run build` | **Pass** — `tsc -b && vite build` succeeded；Vite emitted existing chunk-size warning only |
| `git diff --check` | **Pass** |

### 16.8 安全边界

| 边界 | 结论 |
|------|------|
| 本轮只展示和选择 conversation | **Pass** |
| 不改后端 / DB / migration | **Pass** |
| 不新增 mutation | **Pass** |
| 不直接手写 fetch，使用 P7-C2 hook | **Pass** |
| 不调用 create task / run worker / approval / review | **Pass** |
| 不调用 provider，不执行 suggested_actions | **Pass** |
| 不启动 Codex / Claude Code / DeepSeek | **Pass** |
| 不新增 DirectorInbox / Challenge / Proposal / Retry / Git 操作按钮 | **Pass** |
| 产品运行时 git add / commit / push / PR 仍 Not started | **Pass** |
| 未复制参考项目代码、未引入参考项目技术栈 | **Pass** |

### 16.9 P7-C3 Gate

| Gate | 结论 |
|------|------|
| Workbench ConversationList 只读 UI 已新增 | **Pass** |
| 列表字段覆盖 title/kind/status/preview/time/message_count/requires_user_action/owner/project | **Pass** |
| loading / empty / error / selected / requires_user_action 状态文案已覆盖 | **Pass** |
| 点击列表项恢复指定 conversation/session | **Pass** |
| session null 普通输入不再默默 create session | **Pass** |
| 显式“新建主管会话 / 开始新目标”入口已提供 | **Pass** |
| 未新增执行型按钮 / mutation / provider / worker / executor / Git 写运行时能力 | **Pass** |
| `cd apps/web && npm run build` | **Pass** |
| `git diff --check` | **Pass** |
| P7-E 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 git add / commit / push / PR 仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-C3 Gate 结论** | **Pass** |


## 17. P7-D1 DirectorInbox backend synthetic read-only API（2026-06-08）

### 17.1 本轮范围

| 项目 | 结论 | 说明 |
|------|------|------|
| 子阶段 | **P7-D1** | DirectorInbox backend synthetic read-only API |
| 代码边界 | **未越过** | 只新增后端 synthetic read model、GET API、targeted tests 与本 ledger |
| 是否建表 / migration | 否 | 本轮不新增 inbox table，不改 DB schema |
| 是否改前端 | 否 | `apps/web/**` 未修改 |
| 是否实现写接口 | 否 | 不实现 create/read/archive/link/convert 写接口 |
| 是否进入 P7-E | 否 | ConversationRouter + DirectorContextAssembler 仍 Not started |

### 17.2 参考项目读取记录

本轮只读读取本地参考项目 `/Users/kk/project explore/agent-orchestrator`，未修改任何参考项目文件；未引入 tmux/runtime/plugin 架构，未实现 Claude Code / Codex adapter，未实现 agent launch。

| # | 文件 | 读取目的 |
|---|------|---------|
| 1 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/activity-events.ts` | 学习 activity event 的 source/kind/level/summary 结构化 readback 思路 |
| 2 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/lifecycle-state.ts` | 学习 lifecycle status → attention/readback 的稳定状态表达 |
| 3 | `/Users/kk/project explore/agent-orchestrator/packages/core/src/session-manager.ts` | 学习 session/activity list 与 runtime side effect 隔离 |

### 17.3 借鉴机制摘要

| # | 机制 | P7-D1 吸收方式 |
|---|------|----------------|
| 1 | Activity event 以 source/kind/summary 结构化表达 | `DirectorInboxItem` 暴露 `source_page` / `source_entity_type` / `kind` / `summary` |
| 2 | Lifecycle readback 只表达已发生事实 | synthetic items 只从现有 message/plan/task/run 状态合成，不推断未来动作 |
| 3 | Observable list 与 runtime/launch 分离 | `GET /project-director/inbox` 只读，不创建 inbox item，不触发 provider/worker/run/retry |

### 17.4 修改文件

| 文件 | 修改 |
|------|------|
| `runtime/orchestrator/app/services/project_director_inbox_service.py` | 新增 synthetic DirectorInbox read model service；定义 kind/status/priority/read result |
| `runtime/orchestrator/app/api/routes/project_director.py` | 新增 DTO 与 `GET /project-director/inbox` 只读路由 |
| `runtime/orchestrator/tests/test_project_director_inbox_readonly_api.py` | 新增 P7-D1 targeted tests |
| `docs/产品文档/AI项目主管/07-P7主管对话与治理/P7主管对话与治理总账与阶段设计-20260607.md` | 回填 P7-D1 范围、证据与 Gate |

### 17.5 新增 synthetic inbox API

| API | Query | Response | 只读边界 |
|-----|-------|----------|----------|
| `GET /project-director/inbox` | `project_id?` / `kind?` / `status?` / `priority?` / `limit?` | `{ items, has_more, source: "synthetic_project_director_inbox_read_model" }` | 不创建 inbox/session/message/task/run/worker，不触发 provider，不执行 suggested_actions |

### 17.6 已实现 synthetic sources

| Source | 实现 | kind / status / priority |
|--------|------|--------------------------|
| A. conversation requires user action | latest assistant message `requires_confirmation=true` | `plan_question` / `needs_response` / `normal` |
| B. plan pending confirmation | `ProjectDirectorPlanVersion.status=pending_confirmation` | `approval_attention` / `needs_response` / `high` |
| C1. task blocker/failure/waiting human | `Task.status in failed/blocked/waiting_human` via existing `TaskRepository.list_with_latest_run()` | failed → `failure_recovery_attention` / `critical`; blocked/waiting_human → `run_blocker` / `critical|high` |
| C2. run failure/cancelled | latest run `status in failed/cancelled` from task readback | `failure_recovery_attention` / `needs_response` / `critical` |

### 17.7 未实现 sources 与原因

| Source | 状态 | 原因 |
|--------|------|------|
| D. P6 dispatch decision → `dispatch_question` | **Not implemented，留给 R1** | 现有 dispatch decision readback 分散在 worker result/audit 链路；本轮若强接需要扩大读取和设计范围，可能触及 P6/P7-E 边界。P7-D1 保持最小 synthetic read-only slice。 |
| 用户手动 note / link / archive / convert-to-challenge | **Not implemented** | 本轮明确不持久化 inbox item，不实现写接口；留给后续 P7-D/P7-F。 |

### 17.8 测试结果

| 命令 | 结果 |
|------|------|
| `cd runtime/orchestrator && pytest tests/test_project_director_inbox_readonly_api.py tests/test_project_director_conversation_readonly_api.py tests/test_project_director_messages.py tests/test_project_director_sessions.py -q` | **Pass** — 84 passed, 10 warnings |
| `git diff --check` | **Pass** |

### 17.9 安全边界

| 边界 | 结论 |
|------|------|
| `GET /project-director/inbox` 只读 | **Pass** |
| 不创建 inbox item / conversation / session / message / task / run / worker | **Pass** |
| 不触发 provider | **Pass** |
| 不执行 suggested_actions | **Pass** |
| 不启动 Codex / Claude Code / DeepSeek | **Pass** |
| 不做 retry，不应用 approval | **Pass** |
| 不改 DB / migration | **Pass** |
| 不改前端 | **Pass** |
| 不实现 Proposal / ConversationRouter / 外部执行器 | **Pass** |
| 产品运行时 git add / commit / push / PR 仍 Not started | **Pass** |
| 未复制参考项目代码、未引入参考项目技术栈 | **Pass** |

### 17.10 P7-D1 Gate

| Gate | 结论 |
|------|------|
| DirectorInbox synthetic read-only service 已新增 | **Pass** |
| `GET /project-director/inbox` 已新增 | **Pass** |
| deterministic synthetic UUID 已实现 | **Pass** |
| project_id / kind / status / priority filter 已实现 | **Pass** |
| limit clamp 1..100 + has_more 已实现 | **Pass** |
| Source A conversation requires user action 已实现并测试 | **Pass** |
| Source B pending plan approval_attention 已实现并测试 | **Pass** |
| Source C task/run blocker/failure 已实现并测试 | **Pass** |
| Source D dispatch_question 未实现且记录 R1 原因 | **Pass** |
| targeted tests 通过 | **Pass** |
| 未新增写接口 / DB / frontend / executor / Git 写运行时能力 | **Pass** |
| P7-E 仍 Not started | **Pass** |
| P7 总 Gate 仍未 Pass | **Pass** |
| P8/P9 implementation 仍 Not started | **Pass** |
| 产品运行时 git add / commit / push / PR 仍 Not started | **Pass** |
| AI Project Director 总闭环仍 Partial | **Pass** |
| **P7-D1 Gate 结论** | **Pass** |
