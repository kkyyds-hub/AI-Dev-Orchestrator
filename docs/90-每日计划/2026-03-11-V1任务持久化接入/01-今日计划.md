# 2026-03-11 范围冻结：V1 任务持久化接入

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`SQLite / Task / Run`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-11`
- 文档性质：**日计划冻结文档**
- 目标：完成 `Task / Run` 最小持久化接入，让 Day 2 冻结的数据模型可以正式落到 `SQLite`

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是把 `Task / Run` 最小模型落到 `SQLite`，建立 Day 3 的持久化基础。
2. 今天优先解决数据库位置、连接方式、最小表结构和初始化方式。
3. 今天明确不做任务创建接口、列表接口、`Worker` 调度和复杂迁移体系。

---

## 1. 边界决策（已冻结）

1. 持久层采用 `SQLite`：**YES**
2. 先围绕 Day 2 的 `Task / Run` 最小字段建表：**YES**
3. 持久化层优先使用 `SQLAlchemy 2.x`：**YES**
4. 数据库文件放在本地 `runtime/data/` 下统一管理：**YES**
5. 今天先采用“最小初始化建表”方式，不引入完整迁移工具：**YES**
6. 今天顺手做任务创建 / 查询接口：**NO**
7. 今天顺手扩展 `Event / BudgetSnapshot / InterventionItem`：**NO**

---

## 2. 当前基线（基于仓库事实）

1. Day 1 已完成 `FastAPI` 最小后端骨架，服务入口和统一路由已具备。
2. Day 2 已完成 `Task / Run` 最小领域模型和数据模型冻结文档。
3. 当前 `runtime/orchestrator` 还没有数据库连接、表定义和初始化逻辑，因此 Day 3 的核心是把“领域模型”推进到“可持久化状态”。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P3-S1` | 数据库位置与基础设施落位 | 固定数据库文件位置、连接入口和基础目录 | 代码 / 文档 |
| `P3-S2` | `Task / Run` 表结构接入 | 让最小模型具备可建表的持久化定义 | 代码 |
| `P3-S3` | 初始化建表与落库验证 | 完成最小建表动作并验证数据库文件生成 | 代码 / 结果 |

---

## 4. Step `P3-S1`：数据库位置与基础设施落位

- 目标：统一 Day 3 的数据库位置、连接方式和初始化入口。
- 范围：
  1. 约定 `SQLite` 文件路径和 `runtime/data/` 目录结构。
  2. 增加数据库配置与连接基础代码。
  3. 预留后续仓储层和会话管理入口。
- 输出：
  1. 数据库文件位置约定
  2. 数据库连接模块
  3. 初始化入口约定
- DoD：
  1. 数据库连接入口唯一且清晰。
  2. Day 4 可以直接复用 Day 3 的持久化基础。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已补充 `runtime/orchestrator/app/core/db.py`
  2. 已补充 `runtime/orchestrator/app/core/config.py` 的数据库路径配置
  3. 已生成本地目录 `runtime/data/db/`

## 5. Step `P3-S2`：`Task / Run` 表结构接入

- 目标：让 Day 2 冻结的最小模型拥有可建表的持久化定义。
- 范围：
  1. 为 `Task` 和 `Run` 定义最小表结构。
  2. 明确主键、外键和时间字段的持久化映射。
  3. 保持字段范围与 Day 2 冻结文档一致，不随意加字段。
- 输出：
  1. `Task` 表定义
  2. `Run` 表定义
  3. `Task -> Run` 关系映射
- DoD：
  1. `Task / Run` 表结构可正常创建。
  2. 表结构与 Day 2 的模型边界保持一致。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已补充 `runtime/orchestrator/app/core/db_tables.py`
  2. 已创建 `tasks` / `runs` 表结构与 `Task -> Run` 外键关系

## 6. Step `P3-S3`：初始化建表与落库验证

- 目标：证明 Day 3 的持久化不是停留在代码层，而是已经真的能落地。
- 范围：
  1. 编写最小建表初始化逻辑。
  2. 执行建表或启动时初始化。
  3. 验证数据库文件生成以及核心表存在。
- 输出：
  1. 最小建表初始化代码
  2. 本地 `SQLite` 数据库文件
  3. 建表验证结果
- DoD：
  1. 本地数据库文件已生成。
  2. `Task / Run` 核心表已创建成功。
  3. Day 4 可以直接基于数据库继续做任务创建接口。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已通过 `init_database()` 生成 `runtime/data/db/orchestrator.db`
  2. 已验证数据库中存在 `tasks`、`runs` 两张核心表
  3. 已验证应用生命周期启动时会自动初始化数据库

---

## 7. 风险与阻塞

1. 如果 Day 2 的领域模型和 Day 3 的表结构定义不一致，后续接口层会返工。
2. 如果今天把范围扩展到迁移工具、仓储层和接口层，容易导致收尾失败。
3. `SQLite` 路径、导入路径和初始化时机如果不统一，后续启动和测试会混乱。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 仓储层与任务接口顺延到 Day 4
- 备注：
  1. 已完成 Day 3 最小持久化接入，`Task / Run` 已正式具备本地 `SQLite` 落库基础
