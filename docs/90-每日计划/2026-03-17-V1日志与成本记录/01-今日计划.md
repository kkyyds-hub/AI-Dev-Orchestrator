# 2026-03-17 范围冻结：V1 日志与成本记录
- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`Worker / Run / Logging / Cost`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-17`
- 文档性质：**日计划冻结文档**
- 目标：完成 Day 9 的最小日志落盘与成本记录闭环，让执行结果除了状态之外还能留下过程痕迹和成本数据。
---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是：把 `Worker` 的单轮执行结果扩展为“状态 + 日志 + 成本”的最小闭环。
2. 今天优先解决的问题是：Day 8 已有执行与验证结果，但缺少本地日志文件和最小成本记录。
3. 今天明确不做：日志流式推送、独立日志查询接口、真实模型计费对账、复杂预算守卫。

---

## 1. 边界决策（已冻结）
1. 运行日志落到本地 `runtime/data/logs/`：**YES**
2. 成本记录先挂到 `Run` 上，而不是扩展复杂统计表：**YES**
3. 成本先用启发式估算支撑 Day 9 / Day 10，后续再替换成真实提供商账单：**YES**

---

## 2. 当前基线（基于仓库事实）

1. 当前已经具备：`Task / Run` 持久化、`Worker` 单轮循环、`Executor` 最小执行、`Verifier` 最小验证。
2. 当前仍然缺少：运行过程日志文件、`Run` 级 token / 成本字段、旧 SQLite 数据库的增量升级。
3. 今天的工作将基于：Day 7 / Day 8 已冻结的 `Worker -> Executor -> Verifier -> 状态回写` 主链路继续扩展。

---

## 3. 今日路线图（冻结）
| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P9-S1` | Run 观测字段扩展 | 给 `Run` 增加日志与成本记录位 | 代码 / 数据结构 |
| `P9-S2` | Worker 接入日志与成本 | 让单轮执行写日志并回写成本 | 代码 / 接口 |
| `P9-S3` | 文档与验证补齐 | 沿用前几天流程补 README 与验证记录 | 文档 / 记录 |

---

## 4. Step `P9-S1`：Run 观测字段扩展

- 目标：给 Day 9 增加最小可持久化的日志路径、token 统计和成本字段。
- 范围：
  1. 扩展 `runtime/orchestrator/app/domain/run.py`
  2. 扩展 `runtime/orchestrator/app/core/db_tables.py`
  3. 为已有 SQLite 数据库补充增量列升级
- 输出：
  1. `prompt_tokens`
  2. `completion_tokens`
  3. `estimated_cost`
  4. `log_path`
- DoD：
  1. 新老数据库都能持久化 Day 9 字段
  2. `RunRepository` 可以回写并读出这些字段
- 完成状态：**已完成**
- 完成交付证据：
  1. 已更新 `runtime/orchestrator/app/domain/run.py`
  2. 已更新 `runtime/orchestrator/app/core/db_tables.py`
  3. 已更新 `runtime/orchestrator/app/core/db.py`

## 5. Step `P9-S2`：Worker 接入日志与成本

- 目标：让 `Worker` 在执行、验证和结束阶段留下结构化日志，并把成本结果写回 `Run`。
- 范围：
  1. 新增本地 JSONL 日志服务
  2. 新增启发式成本估算服务
  3. 扩展 `POST /workers/run-once` 响应
- 输出：
  1. `runtime/orchestrator/app/services/run_logging_service.py`
  2. `runtime/orchestrator/app/services/cost_estimator_service.py`
  3. `runtime/orchestrator/app/workers/task_worker.py`
  4. `runtime/orchestrator/app/api/routes/workers.py`
- DoD：
  1. 每次运行都会落一份 `jsonl` 日志
  2. Worker 成功和失败路径都会产生最终成本记录
  3. 接口调用方可以直接看到 `log_path` 和成本字段
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `TaskWorker` 串上日志初始化、执行记录、验证记录、成本记录和最终回写
  2. 已在 `WorkerRunOnceResponse` 暴露 `log_path`、`prompt_tokens`、`completion_tokens`、`estimated_cost`
  3. 已通过本地烟测验证日志文件实际落盘

## 6. Step `P9-S3`：文档与验证补齐

- 目标：沿用前几天的交付流程，把 Day 9 的 README 和每日计划一起补齐。
- 范围：
  1. 更新运行说明
  2. 记录 Day 9 完成边界与验证结果
- 输出：
  1. `runtime/orchestrator/README.md`
  2. `docs/90-每日计划/2026-03-17-V1日志与成本记录/01-今日计划.md`
- DoD：
  1. README 能说明 Day 9 新增能力
  2. 每日计划文档能作为后续 Day 10 交接依据
- 完成状态：**已完成**
- 完成交付证据：
  1. 已更新 `runtime/orchestrator/README.md`
  2. 已新增本文件记录 Day 9 范围与完成结果
  3. 已完成 `compileall` 与最小 Worker 烟测

---

## 7. 风险与阻塞
1. 当前成本仍是启发式估算，不等同于真实模型厂商账单。
2. 当前日志已经结构化落盘，但还没有独立查询或流式订阅接口。
3. 如果后续要接真实多模型路由，Day 10 之后应把成本来源从启发式切换为 provider telemetry。

---

## 8. 顺延与交接
- 今日是否全部完成：**是**
- 顺延事项：
  1. Day 10 继续把成本展示接到最小控制台首页
  2. 后续再补日志查询接口或 SSE 日志流
- 备注：
  1. Day 9 的价值不是做完整观测平台，而是先把“执行过什么、花了多少”沉淀下来
  2. 只要 Day 9 完成，Day 10 就可以直接消费 `Run` 上的日志路径和成本字段
