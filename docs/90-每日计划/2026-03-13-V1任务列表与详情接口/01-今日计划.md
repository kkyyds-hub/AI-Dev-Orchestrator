# 2026-03-13 范围冻结：V1 任务列表与详情接口

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`Task / API / Repository / Service`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-13`
- 文档性质：**日计划冻结文档**
- 目标：完成 `Task` 最小列表与详情接口接入，让后端具备查看任务状态的基础能力

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是打通 `GET /tasks` 和 `GET /tasks/{task_id}` 的最小查询闭环，而不是继续扩展完整任务中心。
2. 今天优先解决查询接口契约、仓储层查询能力、服务层封装和接口验证。
3. 今天明确不做任务筛选、分页、排序规则扩展、任务更新删除、`Run` 列表和 `Worker` 调度逻辑。

---

## 1. 边界决策（已冻结）

1. Day 5 先只提供任务列表与任务详情两个最小查询接口：**YES**
2. 查询返回继续围绕 Day 2 已冻结的 `Task` 最小字段展开：**YES**
3. 查询能力继续复用 Day 4 已建立的仓储层与服务层结构：**YES**
4. 任务详情使用 `task_id` 精确查询，未命中时返回明确错误：**YES**
5. 任务列表先返回最小集合，不提前引入分页和筛选参数：**YES**
6. 今天顺手扩展 `Run` 详情与执行历史接口：**NO**
7. 今天顺手做任务状态更新与删除接口：**NO**
8. 今天引入复杂搜索、鉴权和统一异常体系：**NO**

---

## 2. 当前基线（基于仓库事实）

1. Day 1 已完成 `FastAPI` 最小后端骨架，统一路由与健康检查已具备。
2. Day 2 已完成 `Task / Run` 最小模型冻结，`Task` 的字段、状态和优先级语义已固定。
3. Day 3 已完成 `SQLite` 持久化接入，`tasks / runs` 表和初始化逻辑已落地。
4. Day 4 已完成 `POST /tasks` 创建接口，任务仓储层与任务服务层已经有最小可用结构。
5. 当前系统已经可以创建任务，但还不能通过 API 查看任务列表和单条任务详情，因此 Day 5 的核心是把“可创建”推进到“可查看”。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P5-S1` | 查询契约与文件对齐 | 冻结列表与详情接口的返回结构和代码位置 | 文档 / 代码约定 |
| `P5-S2` | 仓储层与服务层查询能力 | 建立任务列表查询和按 ID 查询能力 | 代码 |
| `P5-S3` | 路由接入与查询验证 | 暴露查询接口并验证结果可返回 | 接口 / 结果 |

---

## 4. Step `P5-S1`：查询契约与文件对齐

- 目标：明确 Day 5 要提供哪些查询接口、返回哪些字段，以及代码继续放在哪里。
- 范围：
  1. 冻结 `GET /tasks` 和 `GET /tasks/{task_id}` 的最小响应结构。
  2. 明确 Day 5 继续在现有 `tasks.py`、任务仓储层和任务服务层中扩展。
  3. 对齐 Day 2 的 `Task` 字段边界，不在查询接口自行扩字段。
- 输出：
  1. 任务列表接口契约
  2. 任务详情接口契约
  3. Day 5 文件落位约定
- DoD：
  1. 列表接口和详情接口的字段口径与 Day 4 创建响应保持一致。
  2. 代码落位不引入新的职责混乱。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/api/routes/tasks.py` 增加 `GET /tasks` 与 `GET /tasks/{task_id}` 的响应契约
  2. 已继续沿用 `runtime/orchestrator/app/repositories/task_repository.py` 与 `runtime/orchestrator/app/services/task_service.py` 的既有落位

## 5. Step `P5-S2`：仓储层与服务层查询能力

- 目标：把任务查询逻辑收敛到可复用的最小仓储层与服务层中。
- 范围：
  1. 为任务仓储层增加任务列表查询能力。
  2. 为任务仓储层增加按 `task_id` 查询单条任务能力。
  3. 为任务服务层增加任务列表与详情封装。
- 输出：
  1. `runtime/orchestrator/app/repositories/task_repository.py`
  2. `runtime/orchestrator/app/services/task_service.py`
  3. 任务查询最小调用链
- DoD：
  1. 查询逻辑不直接散落在路由层。
  2. 任务列表和单条详情都能返回领域对象。
  3. Day 6 之后如果继续扩展状态流转，查询能力不需要返工。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/repositories/task_repository.py` 补充 `list_all()` 与 `get_by_id()`
  2. 已在 `runtime/orchestrator/app/services/task_service.py` 补充 `list_tasks()` 与 `get_task()`
  3. 已形成从路由 -> 服务 -> 仓储 -> 数据库的任务查询最小调用链

## 6. Step `P5-S3`：路由接入与查询验证

- 目标：把任务列表与详情能力正式暴露为可调用 API，并留下最小验证结果。
- 范围：
  1. 在任务路由中接入 `GET /tasks` 和 `GET /tasks/{task_id}`。
  2. 验证 Swagger 文档中出现任务列表与任务详情接口。
  3. 实际创建任务后再查询列表与详情，确认返回结果正确。
  4. 补充最小运行说明或接口说明。
- 输出：
  1. `runtime/orchestrator/app/api/routes/tasks.py`
  2. 任务列表接口
  3. 任务详情接口
  4. 创建后查询的验证结果
- DoD：
  1. `GET /tasks` 可以返回已创建任务集合。
  2. `GET /tasks/{task_id}` 可以返回指定任务详情。
  3. 未命中的 `task_id` 能返回明确错误结果。
  4. Day 6 可以在此基础上继续做 `Worker` 最小循环或任务状态推进。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/api/routes/tasks.py` 接入任务列表与详情接口
  2. 已更新 `runtime/orchestrator/README.md` 的任务列表与任务详情调用说明
  3. 已通过临时数据库验证 `POST /tasks`、`GET /tasks`、`GET /tasks/{task_id}` 和缺失任务 `404` 返回

---

## 7. 风险与阻塞

1. 如果 Day 5 查询接口字段口径和 Day 4 创建响应不一致，前端和后续控制台层会产生重复适配。
2. 如果今天把查询接口扩展到分页、筛选和复杂排序，容易导致范围失焦。
3. 如果仓储层、服务层和路由层边界再次混乱，Day 6 之后的状态推进和执行逻辑会返工。
4. 如被阻塞，优先顺延到下一日计划，不在当天强行扩项。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 分页、筛选和排序增强顺延到后续工作日
  2. `Run` 列表与详情接口顺延到后续工作日
- 备注：
  1. Day 5 的价值不是做复杂查询，而是让任务从“可创建”推进到“可查看”
  2. 只要今天完成“查询契约 + 仓储/服务查询 + 接口验证”，`V1` 就继续保持最小闭环推进节奏
