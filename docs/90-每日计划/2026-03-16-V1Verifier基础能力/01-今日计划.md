# 2026-03-16 范围冻结：V1 Verifier 基础能力

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`Verifier / Executor / Worker / Task / Run`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-16`
- 文档性质：**日计划冻结文档**
- 目标：完成 `Verifier` 最小接入，让任务在执行后具备最小验证动作，并把验证结果回写到 `Task / Run`

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是打通 `Verifier` 最小验证链路，而不是一次性做完整验证平台。
2. 今天优先解决验证输入约定、最小本地命令验证、模拟验证和验证结果回写。
3. 今天明确不做复杂测试编排、工件归档、覆盖率统计、流式日志和多阶段验证流水线。

---

## 1. 边界决策（已冻结）

1. Day 8 先提供两种最小验证模式：本地命令验证与模拟验证：**YES**
2. `Verifier` 继续复用 Day 7 的本地命令能力，不引入外部 CI 平台：**YES**
3. 当 `Task.input_summary` 中包含 `verify:` 或 `check:` 行时，按本地命令验证：**YES**
4. 当没有显式验证指令时，默认执行最小模拟验证：**YES**
5. 只有当执行成功后，才进入验证环节：**YES**
6. 如果验证失败，任务状态和运行记录都应回写为失败：**YES**
7. 今天顺手完成复杂测试矩阵、并行验证和覆盖率统计：**NO**
8. 今天顺手扩展独立 `Run` 查询接口和验证报告工件：**NO**

---

## 2. 当前基线（基于仓库事实）

1. Day 1 已完成 `FastAPI` 最小后端骨架，统一路由和健康检查已具备。
2. Day 2 已冻结 `Task / Run` 最小模型，`Run` 已具备 `result_summary` 和时间字段。
3. Day 3 已完成 `SQLite` 持久化接入，数据库和核心表已落地。
4. Day 4 / Day 5 已完成任务创建、列表和详情接口，系统可以创建和查看任务。
5. Day 6 已完成最小 `Worker` 单轮循环，系统可以取走待执行任务并创建运行记录。
6. Day 7 已完成最小 `Executor` 接入，系统已经可以执行本地命令或模拟执行，并回写执行结果。
7. 当前系统已经具备最小执行闭环，但还缺少“执行后验证”这一步，因此 Day 8 的核心是让任务在执行成功后继续经过最小验证动作。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P8-S1` | Verifier 契约与文件落位 | 冻结 Day 8 的验证模式、输入约定和代码位置 | 文档 / 代码约定 |
| `P8-S2` | 验证器与结果回写 | 实现本地命令验证 / 模拟验证并回写最终结果 | 代码 |
| `P8-S3` | Worker 接入与验证闭环 | 通过 `Worker` 触发执行后验证，验证成功和失败路径 | 接口 / 结果 |

---

## 4. Step `P8-S1`：Verifier 契约与文件落位

- 目标：明确 Day 8 的 `Verifier` 用什么输入、支持什么模式、放在哪个文件里。
- 范围：
  1. 冻结 `Task.input_summary` 中验证指令的最小写法。
  2. 明确 `Verifier` 放在 `runtime/orchestrator/app/services/` 下实现。
  3. 明确 `Worker` 只负责串联执行与验证，而验证细节由 `Verifier` 处理。
- 输出：
  1. Day 8 验证模式约定
  2. `runtime/orchestrator/app/services/verifier_service.py`
  3. `Executor -> Verifier -> 状态回写` 调用边界
- DoD：
  1. 验证模式边界清晰，不和 `Worker`、`Executor`、仓储层混淆。
  2. Day 9 可以在此基础上继续接日志与成本记录，不需要返工 Day 8 主结构。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已新增 `runtime/orchestrator/app/services/verifier_service.py`
  2. 已新增 `runtime/orchestrator/app/services/task_instruction_parser.py`
  3. 已明确 `verify:` / `check:` 作为 Day 8 的最小验证指令

## 5. Step `P8-S2`：验证器与结果回写

- 目标：让系统在执行成功后能继续做最小验证，并把验证结果与最终状态写回数据库。
- 范围：
  1. 实现本地命令验证能力。
  2. 实现默认模拟验证能力。
  3. 扩展 `Worker` 最终结果，让执行与验证结果都可见。
- 输出：
  1. `runtime/orchestrator/app/services/verifier_service.py`
  2. `runtime/orchestrator/app/services/task_instruction_parser.py`
  3. `runtime/orchestrator/app/workers/task_worker.py`
- DoD：
  1. 验证成功时，任务保持 `completed`，运行记录保持 `succeeded`。
  2. 验证失败时，任务回写为 `failed`，运行记录回写为 `failed`。
  3. `result_summary` 中能看到执行与验证的最小结果摘要。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/services/verifier_service.py` 实现本地命令验证与模拟验证
  2. 已在 `runtime/orchestrator/app/workers/task_worker.py` 补充执行后验证与结果汇总
  3. 已复用 Day 7 的本地命令执行基础能力，没有重复引入新的执行基础设施

## 6. Step `P8-S3`：Worker 接入与验证闭环

- 目标：让 Day 7 的执行链路继续经过 Day 8 的最小验证，并留下真实验证结果。
- 范围：
  1. 让 `POST /workers/run-once` 触发后在执行成功时继续做验证。
  2. 验证默认模拟验证成功路径。
  3. 验证显式本地命令验证成功路径。
  4. 验证显式本地命令验证失败路径。
  5. 补充最小运行说明。
- 输出：
  1. `runtime/orchestrator/app/api/routes/workers.py`
  2. `runtime/orchestrator/app/workers/task_worker.py`
  3. Day 8 验证闭环结果
  4. `runtime/orchestrator/README.md`
- DoD：
  1. `Worker` 单轮触发后可以得到执行 + 验证的最终结果。
  2. 成功与失败路径都能正确回写 `Task / Run` 状态。
  3. Day 9 可以在此基础上继续记录日志和成本。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/api/routes/workers.py` 返回验证模式和验证摘要
  2. 已更新 `runtime/orchestrator/README.md` 的 Day 8 运行说明
  3. 已通过临时数据库验证模拟验证成功、显式命令验证成功、显式命令验证失败和执行失败跳过验证

---

## 7. 风险与阻塞

1. 如果 Day 8 直接做复杂测试编排和工件归档，范围会迅速失控。
2. 如果验证结果没有和执行结果一起回写，后续复盘和日志记录会不一致。
3. 如果把验证细节直接塞进 `Worker` 或路由层，Day 9 接日志与成本时会返工。
4. 如被阻塞，优先顺延到下一日计划，不在当天强行扩项。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 日志与成本记录顺延到 Day 9
  2. 覆盖率、工件归档和复杂验证流水线顺延到后续工作日
- 备注：
  1. Day 8 的价值不是做复杂测试平台，而是让任务第一次具备“执行后验证”的最小能力
  2. 只要今天完成“Verifier + 状态回写 + Worker 验证闭环”，`V1` 的最小执行与验证链路就真正成立
