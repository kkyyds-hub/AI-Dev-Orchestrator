# 2026-03-24 范围冻结：V2-A 状态机规则与转移表

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V2`
- 模块：`状态机 / 任务 / 运行 / 事件`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-24`
- 实际执行日期：`2026-03-10`（按 `Day 1` 范围提前启动）
- 文档性质：**日计划冻结文档**
- 目标：把 `Task / Run / Event` 的状态语义和主要转移路径统一冻结成一张可执行的规则表。

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是：冻结 `V2-A` 的状态机规则与转移表。
2. 今天优先解决的问题是：当前状态流转已经存在，但规则散落在 `TaskService`、`TaskWorker`、`Router` 和控制台逻辑里。
3. 今天明确不做：大规模代码重构、并行执行、复杂观测面板改造。

---

## 1. 边界决策（已冻结）

1. 外部接口继续保持显式动作语义，不新增万能状态修改接口：**YES**
2. 先统一规则和文档，再进入状态机代码收敛：**YES**
3. 状态机规则优先覆盖 `Task / Run / Event` 主路径，不在今天扩展到未来多执行单元：**YES**

---

## 2. 当前基线（基于仓库事实）

1. 当前已经具备：`pending / running / completed / failed / blocked / paused / waiting_human` 等任务状态，以及运行与事件流。
2. 当前仍然缺少：一份统一可解释的状态转移矩阵和明确的“谁可以触发什么转移”规则。
3. 今天的工作将基于：`runtime/orchestrator/app/domain/task.py`、`runtime/orchestrator/app/domain/run.py`、`runtime/orchestrator/app/services/task_service.py` 继续整理。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P2A1-S1` | 状态现状盘点 | 列出当前真实存在的状态与入口 | 文档 / 记录 |
| `P2A1-S2` | 转移矩阵冻结 | 定义允许转移、触发方和失败分流规则 | 文档 / 规则 |
| `P2A1-S3` | 接口与命名收口 | 冻结状态相关文件和接口命名约定 | 文档 / 规范 |

---

## 4. Step `P2A1-S1`：状态现状盘点

- 目标：把仓库当前已经实现的状态和动作真实列出来。
- 范围：
  1. 盘点 `Task`、`Run`、`Event` 当前状态枚举
  2. 盘点 `TaskService`、`TaskWorker`、`Router` 可触发的状态变化
  3. 盘点控制台当前能看到的状态与动作
- 输出：
  1. `docs/20-V2阶段文档/22-V2-状态机与调度规则.md`
  2. `runtime/orchestrator/app/domain/task.py`
  3. `runtime/orchestrator/app/domain/run.py`
- DoD：
  1. 当前状态种类被完整列出
  2. 每个状态变化入口都有来源说明
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `docs/20-V2阶段文档/22-V2-状态机与调度规则.md` 盘点 `Task / Run / Event` 当前真实状态
  2. 已核对 `runtime/orchestrator/app/domain/task.py`
  3. 已核对 `runtime/orchestrator/app/domain/run.py`
  4. 已核对 `runtime/orchestrator/app/services/task_service.py`、`runtime/orchestrator/app/workers/task_worker.py`、`runtime/orchestrator/app/services/event_stream_service.py`

## 5. Step `P2A1-S2`：转移矩阵冻结

- 目标：形成一张后续代码改造必须遵守的状态转移矩阵。
- 范围：
  1. 定义允许转移路径
  2. 定义非法转移的处理方式
  3. 定义失败、阻塞、人工介入、重试的主分流规则
- 输出：
  1. `docs/20-V2阶段文档/22-V2-状态机与调度规则.md`
  2. 状态转移矩阵表
  3. 非法转移处理约定
- DoD：
  1. 主要路径可被表格表达
  2. 失败后的去向有统一规则
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `docs/20-V2阶段文档/22-V2-状态机与调度规则.md` 冻结 `Task` 状态转移矩阵
  2. 已在 `docs/20-V2阶段文档/22-V2-状态机与调度规则.md` 冻结 `Run` 状态转移矩阵
  3. 已统一“预算守卫阻断 -> task blocked / run cancelled”和“失败 -> task failed / run failed”的主分流口径
  4. 已明确 `pending` 任务存在“保持原状态但不可路由”的阻塞规则

## 6. Step `P2A1-S3`：接口与命名收口

- 目标：确保后续实现阶段文件和接口命名不会混乱。
- 范围：
  1. 冻结状态机服务命名
  2. 冻结就绪判断服务命名
  3. 冻结状态相关外部接口命名原则
- 输出：
  1. 计划新增 `runtime/orchestrator/app/services/task_state_machine_service.py`
  2. 计划新增 `runtime/orchestrator/app/services/task_readiness_service.py`
  3. 计划保留 `POST /tasks/{task_id}/pause`、`POST /tasks/{task_id}/resume` 等显式接口
- DoD：
  1. 文件职责一眼可识别
  2. 对外接口语义不含糊
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `docs/20-V2阶段文档/22-V2-状态机与调度规则.md` 冻结 `task_state_machine_service.py` 命名与职责
  2. 已在 `docs/20-V2阶段文档/22-V2-状态机与调度规则.md` 冻结 `task_readiness_service.py` 命名与职责
  3. 已确认继续保留 `POST /tasks/{task_id}/pause`、`POST /tasks/{task_id}/resume`、`POST /tasks/{task_id}/request-human`、`POST /tasks/{task_id}/resolve-human` 等显式动作接口

---

## 7. 风险与阻塞

1. 如果今天不先冻结规则，后续代码实现很容易出现多套口径并存。
2. 如果今天直接写大量代码而不先定矩阵，人工介入和重试规则会反复返工。
3. 如果接口命名不先统一，`V2-B` 观测接口会继续沿用模糊归属。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 无
- 备注：
  1. 本轮按排期中的 `Day 1` 范围提前执行，实际完成日期为 `2026-03-10`
  2. 目录日期 `2026-03-24` 保持不变，用于对齐 `V2` 排期目录结构
  3. Day 2 可以直接进入状态守卫与统一入口收敛
