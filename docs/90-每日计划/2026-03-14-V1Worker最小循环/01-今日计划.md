# 2026-03-14 范围冻结：V1 Worker 最小循环

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`Worker / Task / Run / API`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-14`
- 文档性质：**日计划冻结文档**
- 目标：完成单 `Worker` 的最小单轮循环，让系统可以从待执行任务中取出一条任务并推进到执行中状态

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是打通 `Worker` 最小单轮循环，而不是一次性做完整调度系统。
2. 今天优先解决待执行任务选取、任务状态推进、`Run` 记录创建和最小触发验证。
3. 今天明确不做真实 `Executor`、验证器、失败重试、后台常驻线程和复杂状态机恢复逻辑。

---

## 1. 边界决策（已冻结）

1. Day 6 先只实现单 `Worker` 的单轮处理能力：**YES**
2. `Worker` 本轮只从 `pending` 任务中取一条任务并推进为 `running`：**YES**
3. `Worker` 取到任务后，需要同步创建一条 `Run` 记录：**YES**
4. Day 6 先通过显式接口触发一次 `Worker` 运行，便于验证：**YES**
5. Day 6 暂不接入真实执行器，因此任务和运行记录可以先停留在 `running`：**YES**
6. 今天顺手完成任务完成态、失败态和重试策略：**NO**
7. 今天顺手接入后台守护线程、定时轮询和多 `Worker` 并发：**NO**
8. 今天顺手做 `Run` 列表与详情接口：**NO**

---

## 2. 当前基线（基于仓库事实）

1. Day 1 已完成 `FastAPI` 最小后端骨架，统一路由与健康检查已具备。
2. Day 2 已冻结 `Task / Run` 最小模型，任务与运行记录的状态语义已经明确。
3. Day 3 已完成 `SQLite` 持久化接入，`tasks / runs` 表和数据库初始化逻辑已落地。
4. Day 4 已完成 `POST /tasks` 创建接口，系统可以创建新任务。
5. Day 5 已完成 `GET /tasks` 和 `GET /tasks/{task_id}`，系统可以查看任务状态。
6. 当前系统已经具备“可创建、可查看”的最小闭环，但还不能把待执行任务推进到执行中，因此 Day 6 的核心是建立第一条最小状态推进链路。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P6-S1` | Worker 契约与文件落位 | 冻结单轮 Worker 的职责边界、状态口径和代码位置 | 文档 / 代码约定 |
| `P6-S2` | 取任务与建 Run 能力 | 实现待执行任务选取、任务状态推进和运行记录创建 | 代码 |
| `P6-S3` | 触发接口与单轮验证 | 暴露最小 Worker 触发入口并验证状态已推进 | 接口 / 结果 |

---

## 4. Step `P6-S1`：Worker 契约与文件落位

- 目标：明确 Day 6 的 `Worker` 到底负责什么、不负责什么，以及代码放在哪里。
- 范围：
  1. 冻结单轮 `Worker` 的最小处理语义。
  2. 明确 `Worker`、任务仓储和运行仓储的职责边界。
  3. 约定通过 `POST /workers/run-once` 触发 Day 6 的最小演示链路。
- 输出：
  1. Day 6 `Worker` 职责边界说明
  2. `runtime/orchestrator/app/workers/task_worker.py`
  3. `runtime/orchestrator/app/api/routes/workers.py`
- DoD：
  1. `Worker`、仓储层和接口层的职责不混淆。
  2. Day 7 可以在此基础上继续接入 `Executor`，不需要返工 Day 6 主结构。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已新增 `runtime/orchestrator/app/workers/task_worker.py`
  2. 已新增 `runtime/orchestrator/app/api/routes/workers.py`
  3. 已明确通过 `POST /workers/run-once` 触发 Day 6 最小 `Worker` 循环

## 5. Step `P6-S2`：取任务与建 Run 能力

- 目标：把“取待执行任务 -> 推进任务状态 -> 创建运行记录”落成第一条最小内部链路。
- 范围：
  1. 在任务仓储层增加获取并占用一条 `pending` 任务的能力。
  2. 在运行仓储层增加创建最小 `Run` 记录的能力。
  3. 在 `Worker` 中统一组织一次最小单轮处理。
- 输出：
  1. `runtime/orchestrator/app/repositories/task_repository.py`
  2. `runtime/orchestrator/app/repositories/run_repository.py`
  3. `runtime/orchestrator/app/workers/task_worker.py`
- DoD：
  1. 单轮 `Worker` 可以选到一条待执行任务。
  2. 被选中的任务会从 `pending` 推进到 `running`。
  3. 数据库中会生成与该任务关联的一条 `Run` 记录。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/repositories/task_repository.py` 增加 `claim_next_pending()`
  2. 已新增 `runtime/orchestrator/app/repositories/run_repository.py`
  3. 已在 `runtime/orchestrator/app/workers/task_worker.py` 完成“取任务 -> 推进状态 -> 创建 Run”的最小单轮处理

## 6. Step `P6-S3`：触发接口与单轮验证

- 目标：把 Day 6 的最小 `Worker` 循环作为可调用接口暴露出来，并留下验证结果。
- 范围：
  1. 增加 `POST /workers/run-once` 触发接口。
  2. 验证没有待执行任务时返回空闲结果。
  3. 验证有待执行任务时能推进 `Task` / `Run` 状态。
  4. 补充最小运行说明或接口说明。
- 输出：
  1. `runtime/orchestrator/app/api/routes/workers.py`
  2. `POST /workers/run-once` 接口
  3. Day 6 单轮执行验证结果
  4. `runtime/orchestrator/README.md`
- DoD：
  1. `POST /workers/run-once` 能返回本轮是否取到任务。
  2. 取到任务时响应中可以看到 `task_id` 和 `run_id`。
  3. 任务详情接口能看到任务状态已推进到 `running`。
  4. Day 7 可以在这个触发链路上继续接入 `Executor`。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/api/routes/workers.py` 提供 `POST /workers/run-once`
  2. 已在 `runtime/orchestrator/README.md` 补充 Worker 触发说明
  3. 已通过临时数据库验证任务创建、Worker 单轮触发、任务状态推进为 `running`、`Run` 记录生成以及空闲返回结果

---

## 7. 风险与阻塞

1. 如果 Day 6 直接把真实执行、验证和重试都塞进来，范围会迅速失控。
2. 如果任务状态推进和 `Run` 创建不在同一条处理链路里，后续状态追踪会出现不一致。
3. 如果今天跳过 `Worker` 层，直接把逻辑堆进接口路由，Day 7 接 `Executor` 时会返工。
4. 如被阻塞，优先顺延到下一日计划，不在当天强行扩项。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 真实 `Executor` 接入顺延到 Day 7
  2. `Run` 查询接口、失败处理和重试策略顺延到后续工作日
- 备注：
  1. Day 6 的价值不是“执行成功”，而是让任务第一次具备被 `Worker` 取走并推进状态的能力
  2. 只要今天完成“单轮 Worker + 状态推进 + Run 创建 + 触发验证”，`V1` 的最小调度链路就真正开始成立
