# 2026-03-15 范围冻结：V1 Executor 基础能力

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`Executor / Worker / Task / Run`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-15`
- 文档性质：**日计划冻结文档**
- 目标：完成 `Executor` 最小接入，让 `Worker` 不再只推进状态，而是能够执行本地命令或模拟执行并回写结果

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是打通 `Executor` 最小执行链路，而不是一次性做完整执行平台。
2. 今天优先解决执行模式约定、最小本地命令执行、模拟执行和执行结果回写。
3. 今天明确不做验证器、复杂沙箱、流式日志、工件归档和多步骤编排执行。

---

## 1. 边界决策（已冻结）

1. Day 7 先提供两种最小执行模式：本地命令执行与模拟执行：**YES**
2. `Task.input_summary` 暂时作为 Day 7 的执行输入来源，不新增 Day 2 之外的任务字段：**YES**
3. 当 `input_summary` 以 `shell:`、`cmd:` 或 `command:` 开头时，按本地命令执行：**YES**
4. 当 `input_summary` 不带命令前缀时，默认按模拟执行处理：**YES**
5. `Executor` 执行后需要把 `Run` 和 `Task` 状态一起回写：**YES**
6. Day 7 继续复用 `POST /workers/run-once` 作为验证入口：**YES**
7. 今天顺手完成验证器、重试策略和复杂日志系统：**NO**
8. 今天顺手扩展多命令编排、工作目录隔离和权限策略：**NO**

---

## 2. 当前基线（基于仓库事实）

1. Day 1 已完成 `FastAPI` 最小后端骨架，统一路由和健康检查已具备。
2. Day 2 已冻结 `Task / Run` 最小模型，运行记录具备 `status / started_at / finished_at / result_summary` 字段。
3. Day 3 已完成 `SQLite` 持久化接入，数据库和核心表已落地。
4. Day 4 已完成任务创建接口，系统可以创建任务。
5. Day 5 已完成任务列表与详情接口，系统可以查看任务状态。
6. Day 6 已完成最小 `Worker` 单轮循环，系统可以取走 `pending` 任务并创建 `Run` 记录。
7. 当前 `Worker` 还不能真正执行任务，因此任务和运行记录会停留在 `running`；Day 7 的核心是让任务真正经过一次最小执行并产出结果。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P7-S1` | Executor 契约与文件落位 | 冻结 Day 7 的执行模式、输入约定和代码位置 | 文档 / 代码约定 |
| `P7-S2` | 执行器与状态回写 | 实现本地命令执行 / 模拟执行并回写 `Task / Run` 状态 | 代码 |
| `P7-S3` | Worker 接入与执行验证 | 通过 `Worker` 触发 `Executor`，验证成功和失败结果 | 接口 / 结果 |

---

## 4. Step `P7-S1`：Executor 契约与文件落位

- 目标：明确 Day 7 的 `Executor` 接收什么输入、支持什么模式、放在哪个文件里。
- 范围：
  1. 冻结 `input_summary` 到执行模式的最小映射规则。
  2. 明确 `Executor` 放在 `runtime/orchestrator/app/services/` 下实现。
  3. 明确 `Worker` 只负责调度与状态流转，真正执行细节交给 `Executor`。
- 输出：
  1. Day 7 执行模式约定
  2. `runtime/orchestrator/app/services/executor_service.py`
  3. `Worker -> Executor` 调用边界
- DoD：
  1. 执行模式边界清晰，不和 `Worker`、仓储层混淆。
  2. Day 8 可以在此基础上接 `Verifier`，不需要推翻 Day 7 主结构。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已新增 `runtime/orchestrator/app/services/executor_service.py`
  2. 已明确 `Task.input_summary` 到 `shell / simulate` 的最小映射规则
  3. 已保持 `Worker` 负责调度，`Executor` 负责执行细节

## 5. Step `P7-S2`：执行器与状态回写

- 目标：让系统在最小范围内真正执行一次任务，并把执行结果写回数据库。
- 范围：
  1. 实现本地命令执行能力。
  2. 实现默认模拟执行能力。
  3. 为 `Task` 和 `Run` 增加执行完成后的状态回写能力。
- 输出：
  1. `runtime/orchestrator/app/services/executor_service.py`
  2. `runtime/orchestrator/app/repositories/task_repository.py`
  3. `runtime/orchestrator/app/repositories/run_repository.py`
- DoD：
  1. 成功执行后，任务状态可推进到 `completed`，运行记录可推进到 `succeeded`。
  2. 命令失败时，任务状态可推进到 `failed`，运行记录可推进到 `failed`。
  3. `result_summary` 中可以看到最小执行结果摘要。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/repositories/task_repository.py` 增加 `set_status()`
  2. 已在 `runtime/orchestrator/app/repositories/run_repository.py` 增加 `finish_run()`
  3. 已在 `runtime/orchestrator/app/services/executor_service.py` 实现本地命令执行与模拟执行

## 6. Step `P7-S3`：Worker 接入与执行验证

- 目标：把 `Executor` 接进 Day 6 的 `Worker` 单轮循环，并留下真实验证结果。
- 范围：
  1. 让 `POST /workers/run-once` 触发后真正执行任务。
  2. 验证模拟执行成功路径。
  3. 验证本地命令执行成功路径。
  4. 验证本地命令执行失败路径。
  5. 补充最小运行说明。
- 输出：
  1. `runtime/orchestrator/app/workers/task_worker.py`
  2. `runtime/orchestrator/app/api/routes/workers.py`
  3. 执行验证结果
  4. `runtime/orchestrator/README.md`
- DoD：
  1. `Worker` 单轮触发后可以得到最终执行结果，而不再只是进入 `running`。
  2. 成功与失败路径都能正确回写 `Task / Run` 状态。
  3. Day 8 可以在 `Executor` 结果基础上继续接 `Verifier`。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `runtime/orchestrator/app/workers/task_worker.py` 接入 `Executor`
  2. 已在 `runtime/orchestrator/app/api/routes/workers.py` 返回执行模式与结果摘要
  3. 已更新 `runtime/orchestrator/README.md` 的 Day 7 运行说明
  4. 已通过临时数据库验证模拟执行成功、本地命令执行成功、本地命令执行失败和空闲返回

---

## 7. 风险与阻塞

1. 如果 Day 7 直接做流式输出、工件管理和复杂命令编排，范围会失控。
2. 如果执行结果没有和 `Task / Run` 状态一起回写，后续复盘和验证链路会不一致。
3. 如果今天把 `Executor` 逻辑直接塞进路由层或仓储层，Day 8 接 `Verifier` 时会返工。
4. 如被阻塞，优先顺延到下一日计划，不在当天强行扩项。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. `Verifier` 接入顺延到 Day 8
  2. 流式日志、工件归档和复杂执行编排顺延到后续工作日
- 备注：
  1. Day 7 的价值不是把执行系统做复杂，而是让任务第一次真正“执行完成”或“执行失败”
  2. 只要今天完成“Executor + 状态回写 + Worker 验证”，`V1` 的最小执行闭环就开始成立
