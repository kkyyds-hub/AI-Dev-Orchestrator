# 2026-04-05 范围冻结：V2-C 单机有限并行验证

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V2`
- 模块：`有限并行 / Worker Pool / 复盘回放 / 验证`
- 文档版本：`v1.0`
- 冻结日期：`2026-04-05`
- 文档性质：**日计划冻结文档**
- 目标：实现并验证单机有限并行的最小闭环，确认失败复盘、决策回放与槽位方案已经串成同一链路，完成 `V2-C` 收口。

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是：落地单机有限并行最小实现，并完成 `V2-C` 统一验证。
2. 今天优先解决的问题是：前 3 天分别冻结了失败复盘、决策回放和槽位方案，今天必须确认它们在仓库里能串成一条可运行链路。
3. 今天明确不做：无边界并发、分布式执行、复杂性能压测平台。

---

## 1. 边界决策（已冻结）

1. 并行实现优先保证正确性，不以吞吐最大化为目标：**YES**
2. 并行验证必须覆盖复盘与回放链路，而不只验证 `Worker` 能否并发执行：**YES**
3. 如果并行实现破坏状态一致性或预算守卫，优先回退到单轮模式：**YES**

---

## 2. 当前基线（基于仓库事实）

1. 当前已经具备：`V2-C day1-day3` 对失败复盘结构、决策回放口径和有限并行槽位方案的冻结文档，以及既有单轮 `Worker`、运行日志和 `runs / workers` 接口骨架。
2. 当前仍然缺少：并行执行骨架、并行下复盘/回放链路接通、槽位占用显示和阶段收口验证。
3. 今天的工作将基于：`docs/20-V2阶段文档/24-V2-复盘与有限并行方案.md`、`runtime/orchestrator/app/workers/task_worker.py`、`runtime/orchestrator/app/api/routes/runs.py` 与 `runtime/orchestrator/app/api/routes/workers.py` 继续收口。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P2C4-S1` | 并行骨架落地 | 实现固定槽位的最小并行入口 | 代码 / Worker |
| `P2C4-S2` | 并行观测与链路接通 | 让槽位状态和并行运行轨迹可见 | 代码 / 观测 |
| `P2C4-S3` | 阶段验证收口 | 完成并行验证并冻结 `V2-C / V2` 结论 | 验证 / 文档 |

---

## 4. Step `P2C4-S1`：并行骨架落地

- 目标：实现一个受控的单机固定槽位执行骨架。
- 范围：
  1. 实现槽位服务与池化入口
  2. 接通固定配置的并行调度
  3. 保持与现有单轮 `Worker` 兼容并可回退
- 输出：
  1. `runtime/orchestrator/app/services/worker_slot_service.py`
  2. `runtime/orchestrator/app/workers/worker_pool.py`
  3. `runtime/orchestrator/app/core/config.py`
- DoD：
  1. 固定槽位可工作且数量可配置
  2. 单轮模式保留为明确回退路径
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 5. Step `P2C4-S2`：并行观测与链路接通

- 目标：让并行执行不打断前 2 天建立的复盘与回放链路。
- 范围：
  1. 展示槽位占用、等待与运行数量
  2. 让并行运行继续产生日志、复盘和决策回放所需字段
  3. 在控制台或日志视图中标识并行模式与风险提示
- 输出：
  1. `runtime/orchestrator/app/api/routes/console.py`
  2. `GET /console/worker-slots`
  3. `apps/web/src/features/run-log/RunLogPanel.tsx`
- DoD：
  1. 控制台能看见槽位状态和等待压力
  2. 并行运行后的日志与回放链路仍可被前端消费
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 6. Step `P2C4-S3`：阶段验证收口

- 目标：验证有限并行没有破坏 `V2-C` 前 3 天成果，并给出阶段结论。
- 范围：
  1. 验证状态一致性与槽位释放
  2. 验证预算守卫、失败复盘和决策回放在并行下仍生效
  3. 冻结 `V2-C` 收口结论和顺延项
- 输出：
  1. 并行烟测记录
  2. `docs/20-V2阶段文档/24-V2-复盘与有限并行方案.md`
  3. `docs/20-V2阶段文档/21-V2-实施排期与每日计划.md`
- DoD：
  1. 并行能力不会破坏状态机、预算规则和回放链路
  2. `V2-C` 与 `V2` 完成与否有明确结论
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

---

## 7. 风险与阻塞

1. 如果只验证并行吞吐，不验证复盘/回放，`V2-C` 前三天会断成碎片。
2. 如果并行状态不可观测，用户很难信任系统。
3. 如果没有明确回退策略，单机并行出问题时会放大影响。

---

## 8. 顺延与交接

- 今日是否全部完成：`否`
- 顺延事项：
  1. 如并行链路未达到稳定门槛，可回退为单轮模式并保留已冻结的复盘、回放与槽位方案
- 备注：
  1. 今天是 `V2-C` 的收口日，重点是把前 3 天成果串成“可控并行 + 可复盘回放”的闭环
