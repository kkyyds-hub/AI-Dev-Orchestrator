# 2026-04-05 范围冻结：V2-C 单机有限并行验证

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V2`
- 模块：`有限并行 / Worker Pool / 控制台观测 / 验证`
- 文档版本：`v1.0`
- 冻结日期：`2026-04-05`
- 文档性质：**日计划冻结文档**
- 目标：实现并验证单机有限并行的最小闭环，完成 `V2-C` 收口。

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是：落地单机有限并行最小实现并完成验证。
2. 今天优先解决的问题是：前一天只冻结了方案，今天必须确认它在仓库里能最小可用。
3. 今天明确不做：无边界并发、分布式执行、复杂性能压测平台。

---

## 1. 边界决策（已冻结）

1. 并行实现优先保证正确性，不以吞吐最大化为目标：**YES**
2. 并行数量使用固定配置，不做动态自动伸缩：**YES**
3. 如果并行实现破坏状态一致性，优先回退到单轮模式：**YES**

---

## 2. 当前基线（基于仓库事实）

1. 当前已经具备：有限并行方案、状态机、预算守卫、路由策略、失败复盘结构和控制台观测基础。
2. 当前仍然缺少：并行执行骨架、槽位占用显示和阶段收口验证。
3. 今天的工作将基于：Day 12 冻结的槽位模型和既有 `Worker` 主流程继续实现。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P2C4-S1` | 并行骨架落地 | 实现固定槽位的最小并行入口 | 代码 / Worker |
| `P2C4-S2` | 槽位观测接通 | 让控制台能看到并行槽位状态 | 代码 / 前端 |
| `P2C4-S3` | 阶段验证收口 | 完成并行验证与 `V2` 交接结论 | 验证 / 文档 |

---

## 4. Step `P2C4-S1`：并行骨架落地

- 目标：实现一个受控的单机固定槽位执行骨架。
- 范围：
  1. 实现槽位服务
  2. 实现并行入口或池化入口
  3. 保持与现有单轮 `Worker` 兼容
- 输出：
  1. `runtime/orchestrator/app/services/worker_slot_service.py`
  2. `runtime/orchestrator/app/workers/worker_pool.py`
  3. `runtime/orchestrator/app/core/config.py`
- DoD：
  1. 固定槽位可工作
  2. 单轮模式可保留为回退路径
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 5. Step `P2C4-S2`：槽位观测接通

- 目标：让并行状态对用户可见，而不是隐藏在后台。
- 范围：
  1. 展示槽位占用
  2. 展示等待与运行数量
  3. 展示并行模式下的预算和失败风险提示
- 输出：
  1. `runtime/orchestrator/app/api/routes/console.py`
  2. `GET /console/worker-slots`
  3. `apps/web/src/features/console-metrics/WorkerSlotPanel.tsx`
- DoD：
  1. 控制台能看见槽位状态
  2. 并行运行不会变成黑盒
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 6. Step `P2C4-S3`：阶段验证收口

- 目标：验证有限并行没有破坏 `V2` 前面的成果，并给出阶段结论。
- 范围：
  1. 验证状态一致性
  2. 验证预算守卫在并行下仍生效
  3. 冻结 `V2` 收口结论和顺延项
- 输出：
  1. 并行烟测记录
  2. `docs/20-V2阶段文档/24-V2-复盘与有限并行方案.md`
  3. `docs/20-V2阶段文档/21-V2-实施排期与每日计划.md`
- DoD：
  1. 并行能力不会破坏状态机与预算规则
  2. `V2` 阶段完成与否有明确结论
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

---

## 7. 风险与阻塞

1. 如果今天只实现并行而不验证，之前的 `V2-A / V2-B` 收益可能被破坏。
2. 如果并行状态不可观测，用户很难信任系统。
3. 如果没有明确回退策略，单机并行出问题时会放大影响。

---

## 8. 顺延与交接

- 今日是否全部完成：`否`
- 顺延事项：
  1. 如并行能力未达到稳定门槛，可回退为单轮模式并保留方案文档
- 备注：
  1. 今天是 `V2` 的收口日，重点是“可控并行”而不是“更多并行”
