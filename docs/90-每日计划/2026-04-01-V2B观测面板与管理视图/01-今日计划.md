# 2026-04-01 范围冻结：V2-B 观测面板与管理视图

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V2`
- 模块：`控制台 / 指标面板 / 管理视图 / 前端`
- 文档版本：`v1.0`
- 冻结日期：`2026-04-01`
- 文档性质：**日计划冻结文档**
- 目标：把 `V2-B` 的预算、路由和失败指标做成用户能用来决策的观测面板。

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是：落地观测面板与管理视图。
2. 今天优先解决的问题是：即便后端已有指标接口，用户仍然需要直观视图去理解系统状态。
3. 今天明确不做：复盘模板、历史回放、并发执行。

---

## 1. 边界决策（已冻结）

1. 面板先服务“理解与决策”，不追求复杂图表系统：**YES**
2. 首页展示优先保留高价值指标，不堆过多面板：**YES**
3. 管理视图优先支持成本、失败、路由三类观察，而不是做完整 BI：**YES**

---

## 2. 当前基线（基于仓库事实）

1. 当前已经具备：任务总览页面、预算信息、任务详情、运行日志和状态徽章。
2. 当前仍然缺少：统一的指标面板、失败分布视图和路由解释面板。
3. 今天的工作将基于：Day 8 新增的控制台接口和 `apps/web/src/app/App.tsx` 继续扩展。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P2B4-S1` | 面板结构设计 | 设计控制台观测布局和组件边界 | 前端 / 设计 |
| `P2B4-S2` | 指标视图落地 | 展示预算、失败、路由核心信息 | 前端 / 代码 |
| `P2B4-S3` | 管理决策视图 | 提供可行动的管理提示 | 前端 / 交互 |

---

## 4. Step `P2B4-S1`：面板结构设计

- 目标：定义 `V2-B` 控制台观测区的结构与组件职责。
- 范围：
  1. 设计指标卡片分组
  2. 设计失败分布区域
  3. 设计路由解释区域
- 输出：
  1. `apps/web/src/features/console-metrics/`
  2. `apps/web/src/features/failure-insights/`
  3. `apps/web/src/app/App.tsx`
- DoD：
  1. 组件边界清晰
  2. 面板区域职责不重叠
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 5. Step `P2B4-S2`：指标视图落地

- 目标：让用户能直接看到预算、失败和路由信息。
- 范围：
  1. 展示预算健康状态
  2. 展示失败类型分布
  3. 展示主要路由原因分布
- 输出：
  1. `apps/web/src/features/console-metrics/ConsoleMetricsPanel.tsx`
  2. `apps/web/src/features/failure-insights/FailureDistributionPanel.tsx`
  3. `apps/web/src/features/console/api.ts`
- DoD：
  1. 核心指标一屏可读
  2. 数据字段和后端接口一致
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 6. Step `P2B4-S3`：管理决策视图

- 目标：让面板不仅展示数据，还能提示用户下一步关注什么。
- 范围：
  1. 增加预算风险提示
  2. 增加失败热点提示
  3. 增加路由异常或阻塞热点提示
- 输出：
  1. `apps/web/src/features/console-metrics/DecisionHintPanel.tsx`
  2. `apps/web/src/lib/status.ts`
  3. `apps/web/src/features/events/helpers.ts`
- DoD：
  1. 面板可以支持真实管理决策
  2. 提示信息不和后台规则冲突
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

---

## 7. 风险与阻塞

1. 如果今天追求复杂图表库，会明显增加前端复杂度。
2. 如果面板只展示数字不解释含义，用户仍然难以做决策。
3. 如果字段命名和接口不稳定，后续会频繁返工。

---

## 8. 顺延与交接

- 今日是否全部完成：`否`
- 顺延事项：
  1. 如高级图表样式未完成，顺延到未来优化，不阻塞 `V2-C`
- 备注：
  1. 今天是 `V2-B` 的收口日，重点是让观测真正可用
