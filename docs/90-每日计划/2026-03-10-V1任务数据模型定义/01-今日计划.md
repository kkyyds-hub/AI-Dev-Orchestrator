# 2026-03-10 范围冻结：V1 任务数据模型定义

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`Task / Run`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-10`
- 文档性质：**日计划冻结文档**
- 目标：完成 `Task / Run` 最小数据模型的边界、字段和代码落位冻结，为 Day 3 的 `SQLite` 持久化提供统一基础

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是冻结 `Task / Run` 的最小数据模型，而不是把任务系统全部做完。
2. 今天优先解决职责边界、字段集合、状态命名和代码落位。
3. 今天明确不做数据库建表、接口开发、`Worker` 调度和复杂事件模型。

---

## 1. 边界决策（已冻结）

1. 今天只解决模型定义，不进入 `SQLite` 实现：**YES**
2. `Task` 与 `Run` 必须拆分为两个核心对象：**YES**
3. 今天只冻结最小字段，不预先设计过多高级字段：**YES**
4. `Task -> Run` 关系先按一对多理解：**YES**
5. 今天顺手做任务创建接口：**NO**
6. 今天顺手引入完整事件表或预算快照模型：**NO**

---

## 2. 当前基线（基于仓库事实）

1. Day 1 已完成 `FastAPI` 最小后端骨架，后端入口、路由和健康检查已具备。
2. `runtime/orchestrator/app/domain/` 目录已经预留，但核心业务对象尚未定义。
3. `SQLite`、任务表结构和接口层仍未接入，因此今天冻结的数据模型会直接决定 Day 3 和 Day 4 的推进效率。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P2-S1` | 模型边界与文件落位 | 固定 `Task / Run` 的职责和代码位置 | 文档 / 代码约定 |
| `P2-S2` | `Task` 模型冻结 | 冻结 `Task` 最小字段和状态语义 | 文档 |
| `P2-S3` | `Run` 模型冻结 | 冻结 `Run` 最小字段和关联语义 | 文档 |

---

## 4. Step `P2-S1`：模型边界与文件落位

- 目标：明确 `Task` 与 `Run` 各自负责什么，以及代码应该放在哪里。
- 范围：
  1. 明确 `Task` 表示“要做什么”和“当前推进状态”。
  2. 明确 `Run` 表示“某次执行尝试及其结果”。
  3. 约定 `runtime/orchestrator/app/domain/` 下的文件命名与拆分方式。
- 输出：
  1. `Task / Run` 职责边界说明
  2. `app/domain/` 模型文件落位约定
- DoD：
  1. `Task` 和 `Run` 的责任不再混淆。
  2. Day 3 可以基于统一落位直接开始做持久化。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已补充 `docs/10-V1阶段文档/13-V1-数据模型.md`
  2. 已落位 `runtime/orchestrator/app/domain/task.py`
  3. 已落位 `runtime/orchestrator/app/domain/run.py`

## 5. Step `P2-S2`：`Task` 模型冻结

- 目标：冻结 `Task` 的最小字段和第一版状态语义。
- 范围：
  1. 定义标识、标题、状态、优先级、输入摘要、创建时间、更新时间等最小字段。
  2. 说明哪些字段属于 Day 2 必要，哪些字段留待后续扩展。
- 输出：
  1. `Task` 最小字段清单
  2. `Task` 状态枚举初稿
- DoD：
  1. `Task` 的最小字段已可支持 Day 3 建表。
  2. `Task` 状态命名足以支撑 Day 4 接口开发。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `docs/10-V1阶段文档/13-V1-数据模型.md` 冻结 `Task` 字段与状态枚举
  2. 已在 `runtime/orchestrator/app/domain/task.py` 定义 `Task`、`TaskStatus`、`TaskPriority`

## 6. Step `P2-S3`：`Run` 模型冻结

- 目标：冻结 `Run` 的最小字段、状态和与 `Task` 的关系语义。
- 范围：
  1. 定义标识、关联 `task_id`、执行状态、模型信息、开始时间、结束时间、结果摘要等最小字段。
  2. 说明 `Run` 用于记录每次执行尝试，而不是替代 `Task` 的整体状态。
- 输出：
  1. `Run` 最小字段清单
  2. `Task -> Run` 关系说明
- DoD：
  1. `Run` 的最小字段已可支持 Day 3 建表。
  2. `Task / Run` 的边界已足够清晰，不需要在 Day 3 回头重构核心字段。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已在 `docs/10-V1阶段文档/13-V1-数据模型.md` 冻结 `Run` 字段、状态与关系语义
  2. 已在 `runtime/orchestrator/app/domain/run.py` 定义 `Run`、`RunStatus`

---

## 7. 风险与阻塞

1. 如果 `Task` 状态与 `Run` 状态职责划分不清，后续接口和调度实现会反复返工。
2. 如果字段设计过多，会拖慢 Day 3 和 Day 4 的节奏。
3. 如果代码落位、命名和文档口径不统一，后续实现会出现命名漂移。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 无
- 备注：
  1. 已完成 `Task / Run` 最小模型代码落位、状态冻结和数据模型文档沉淀，可直接进入 Day 3 的 `SQLite` 持久化
