# 2026-03-09 范围冻结：V1 后端骨架启动

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`runtime/orchestrator`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-09`
- 文档性质：**日计划冻结文档**
- 目标：在本地跑通 `V1` 后端最小骨架，建立一个可启动、可访问、可继续扩展的 `FastAPI` 应用起点

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是跑通 `FastAPI` 最小后端骨架。
2. 今天优先解决“后端入口、统一路由、健康检查、基础运行说明”四件事。
3. 今天不扩展数据库、任务表、`Worker`、鉴权或复杂日志系统。

---

## 1. 边界决策（已冻结）

1. `runtime/orchestrator` 作为 `V1` 后端主目录：**YES**
2. 采用 `FastAPI` 作为后端最小入口：**YES**
3. 统一保留 `api / core / domain / services / workers` 基础目录：**YES**
4. 先提供 `GET /health` 作为验证接口：**YES**
5. 今天接入数据库：**NO**
6. 今天实现任务状态机或调度循环：**NO**

---

## 2. 当前基线（基于仓库事实）

1. 仓库已经具备 `runtime/` 目录，适合承接独立后端子项目。
2. `V1` 的总体技术栈与目录方向已经确定，但后端最小服务尚未作为可访问应用落地。
3. 今天的工作将作为后续任务模型、持久化和接口开发的基础入口。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P1-S1` | 后端目录与应用入口 | 固定后端主目录并建立应用入口 | 代码 |
| `P1-S2` | 统一路由与健康接口 | 提供最小可访问 API | 代码 |
| `P1-S3` | 启动验证与运行说明 | 验证 `/health`、`/docs` 并补最小说明 | 文档 + 结果 |

---

## 4. Step `P1-S1`：后端目录与应用入口

- 目标：建立可启动的后端最小项目骨架。
- 范围：
  1. 确认 `runtime/orchestrator` 为后端主目录。
  2. 初始化最小 `Python` 项目结构和应用入口文件。
- 输出：
  1. `runtime/orchestrator/pyproject.toml`
  2. `runtime/orchestrator/app/main.py`
- DoD：
  1. 项目结构清晰可继续扩展。
  2. `app.main:app` 可作为启动入口。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已建立 `runtime/orchestrator/pyproject.toml`
  2. 已建立 `runtime/orchestrator/app/main.py`

## 5. Step `P1-S2`：统一路由与健康接口

- 目标：提供最小 API 能力，证明服务可访问。
- 范围：
  1. 创建统一路由入口。
  2. 添加 `GET /health` 接口。
  3. 预留基础模块目录。
- 输出：
  1. `runtime/orchestrator/app/api/router.py`
  2. `runtime/orchestrator/app/api/routes/health.py`
  3. `runtime/orchestrator/app/core/`
  4. `runtime/orchestrator/app/domain/`
  5. `runtime/orchestrator/app/services/`
  6. `runtime/orchestrator/app/workers/`
- DoD：
  1. 应用能统一注册 API 路由。
  2. `GET /health` 返回成功结果。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已建立 `runtime/orchestrator/app/api/router.py`
  2. 已建立 `runtime/orchestrator/app/api/routes/health.py`
  3. 已预留后续扩展目录

## 6. Step `P1-S3`：启动验证与运行说明

- 目标：验证最小骨架可运行，并留下后续可复用说明。
- 范围：
  1. 启动服务并确认文档页可访问。
  2. 补充最小启动说明。
- 输出：
  1. `runtime/orchestrator/README.md`
  2. `/docs` 可访问
  3. `/health` 可访问
- DoD：
  1. 后端服务可以正常启动。
  2. 浏览器可以打开接口文档页面。
  3. 健康检查接口返回成功结果。
- 完成状态：**已完成**
- 完成交付证据：
  1. 已补充 `runtime/orchestrator/README.md`
  2. 已验证 `/docs`
  3. 已验证 `/health`

---

## 7. 风险与阻塞

1. 本机 `Python` 环境需要可用。
2. 依赖安装过程可能出现版本或网络问题。
3. 包导入路径和运行入口如果不清晰，可能导致启动报错。
4. 今天刻意不引入数据库和调度逻辑，避免任务失焦。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 预先约定 Day 2 的 `Task / Run` 数据模型落位与字段范围
- 备注：
  1. 已完成 `FastAPI` 最小后端骨架，并验证 `/health` 与 `/docs` 可正常访问
