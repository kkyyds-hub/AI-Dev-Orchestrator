# 2026-03-28 范围冻结：V2-A 状态机验证与文档收口

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V2`
- 模块：`状态机 / 验证 / 文档 / 回归记录`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-28`
- 实际执行日期：`2026-03-10`（按 `Day 5` 范围提前收口）
- 文档性质：**日计划冻结文档**
- 目标：为 `V2-A` 补齐最小验证记录，确认状态机规则和实现口径已经一致。

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是：完成 `V2-A` 的最小验证和文档收口。
2. 今天优先解决的问题是：前四天会引入一系列规则和代码改造，必须集中验证一次。
3. 今天明确不做：进入 `V2-B` 预算观测实现、引入新的大型 UI 功能。

---

## 1. 边界决策（已冻结）

1. 验证优先覆盖关键状态路径，而不是追求全量自动化测试矩阵：**YES**
2. 文档必须回写仓库事实，不能只停留在计划层：**YES**
3. 如果发现状态机规则不一致，优先修正文档与实现，不带着问题进入 `V2-B`：**YES**

---

## 2. 当前基线（基于仓库事实）

1. 当前已经具备：状态矩阵、统一入口、就绪判断、人工恢复与重试分流设计。
2. 当前仍然缺少：一份收口后的验证记录和统一说明。
3. 今天的工作将基于：`docs/20-V2阶段文档/22-V2-状态机与调度规则.md` 与相关实现文件做统一校验。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P2A5-S1` | 状态流烟测 | 验证关键主路径与非法路径 | 验证 / 记录 |
| `P2A5-S2` | 文档回写 | 让阶段文档和接口说明与实现一致 | 文档 / 记录 |
| `P2A5-S3` | 收尾决策 | 冻结 `V2-A` 是否完成及顺延项 | 文档 / 交接 |

---

## 4. Step `P2A5-S1`：状态流烟测

- 目标：验证 `V2-A` 重点状态路径已经真正稳定。
- 范围：
  1. 验证 `pending -> running -> completed`
  2. 验证 `failed / blocked -> retry`
  3. 验证 `paused / waiting_human` 进入与恢复
- 输出：
  1. 烟测记录或脚本
  2. `runtime/orchestrator/app/services/task_service.py`
  3. `runtime/orchestrator/app/workers/task_worker.py`
- DoD：
  1. 关键主路径可验证
  2. 非法转移有明确失败结果
- 完成状态：**已完成**
- 完成交付证据：
  1. 已新增 `runtime/orchestrator/scripts/v2a_day5_state_machine_smoke.py`
  2. 已执行 `.\.venv\Scripts\python.exe scripts\v2a_day5_state_machine_smoke.py`
  3. 已生成 `docs/90-每日计划/2026-03-28-V2A状态机验证与文档收口/artifacts/v2a_day5_state_machine_smoke_report.json`，`5/5` 用例通过

## 5. Step `P2A5-S2`：文档回写

- 目标：让阶段文档真正反映仓库当前行为。
- 范围：
  1. 更新状态机规则文档
  2. 更新 `README` 中状态和动作说明
  3. 记录仍未完成的边界问题
- 输出：
  1. `docs/20-V2阶段文档/22-V2-状态机与调度规则.md`
  2. `runtime/orchestrator/README.md`
  3. 验证记录
- DoD：
  1. 文档和实现不打架
  2. 剩余问题有明确去向
- 完成状态：**已完成**
- 完成交付证据：
  1. 已更新 `docs/20-V2阶段文档/22-V2-状态机与调度规则.md`，补齐 Day 4 / Day 5 落地与收口结论
  2. 已更新 `runtime/orchestrator/README.md`，补齐状态动作接口与 `409` 冲突说明
  3. 已修正文档中一处过时入口引用（`ContextBuilderService` -> `TaskReadinessService`）

## 6. Step `P2A5-S3`：收尾决策

- 目标：正式判断 `V2-A` 是否完成，并冻结顺延项。
- 范围：
  1. 列出完成项
  2. 列出未完成项
  3. 决定哪些事项顺延到 `V2-B`
- 输出：
  1. `docs/20-V2阶段文档/21-V2-实施排期与每日计划.md`
  2. 当日 `01-今日计划.md`
  3. 阶段完成结论
- DoD：
  1. `V2-A` 完成与否有明确判断
  2. 不把模糊问题带进下一阶段
- 完成状态：**已完成**
- 完成交付证据：
  1. 已新增 `docs/90-每日计划/2026-03-28-V2A状态机验证与文档收口/02-完成记录.md`
  2. 已冻结结论：`V2-A` 完成，可进入 `V2-B`，无必须顺延阻塞项

---

## 7. 风险与阻塞

1. 如果没有集中验证，`V2-B` 会建立在不稳的状态机之上。
2. 如果文档不回写，后续很快会失去“按计划推进”的优势。
3. 如果顺延项不冻结，下一阶段很容易被返工挤占。

---

## 8. 顺延与交接

- 今日是否全部完成：`是`
- 顺延事项：
  1. 无
- 备注：
  1. 本轮按排期中的 `Day 5` 范围提前执行，实际完成日期为 `2026-03-10`
  2. 目录日期 `2026-03-28` 保持不变，用于对齐 `V2` 排期目录结构
