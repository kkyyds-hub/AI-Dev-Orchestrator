# 2026-03-12 范围冻结：V1 任务创建接口

- 项目：`AI-Dev-Orchestrator`
- 阶段：`V1`
- 模块：`Task / API / Repository / Service`
- 文档版本：`v1.0`
- 冻结日期：`2026-03-12`
- 文档性质：**日计划冻结文档**
- 目标：完成 `Task` 最小创建接口接入，让后端从“可启动、可落库”推进到“可创建任务”

---

## 0. 范围定位（冻结）

1. 今天的唯一主目标是打通 `POST /tasks` 的最小创建闭环，而不是把整个任务中心一次做完。
2. 今天优先解决接口契约、仓储层写入、服务层创建逻辑和接口验证。
3. 今天明确不做任务列表、任务详情、任务更新删除、`Run` 创建、`Worker` 调度和复杂迁移体系。

---

## 1. 边界决策（已冻结）

1. Day 4 先只提供 `POST /tasks` 最小创建接口：**YES**
2. 任务创建先围绕 Day 2 已冻结的 `Task` 最小字段展开：**YES**
3. 数据写入优先通过最小仓储层和服务层封装，而不是直接堆在路由函数里：**YES**
4. 创建请求只允许提交 `title`、`input_summary` 和可选 `priority`：**YES**
5. `status`、`created_at`、`updated_at` 由服务端统一生成：**YES**
6. 今天顺手完成任务列表与详情接口：**NO**
7. 今天顺手扩展 `Run` 创建链路或调度入口：**NO**
8. 今天引入鉴权、复杂异常体系和迁移工具：**NO**

---

## 2. 当前基线（基于仓库事实）

1. Day 1 已完成 `FastAPI` 最小后端骨架，统一路由和 `GET /health` 已具备。
2. Day 2 已完成 `Task / Run` 最小领域模型冻结，`TaskStatus`、`TaskPriority` 和最小字段边界已经明确。
3. Day 3 已完成 `SQLite` 持久化接入，数据库连接、建表初始化和 `tasks / runs` 表已落地。
4. 当前 `runtime/orchestrator/app/api/` 仍只有健康检查接口，任务接口尚未接入。
5. 当前仓库还没有独立的任务仓储层和任务创建服务，因此 Day 4 的关键是把“已有模型 + 已有数据库”串成第一条真实业务链路。

---

## 3. 今日路线图（冻结）

| Step | 名称 | 核心目标 | 产出类型 |
| --- | --- | --- | --- |
| `P4-S1` | 接口契约与文件落位 | 冻结 `POST /tasks` 的请求响应结构和代码位置 | 文档 / 代码约定 |
| `P4-S2` | 仓储层与创建服务 | 建立最小任务写入封装，打通领域对象到数据库 | 代码 |
| `P4-S3` | 路由接入与创建验证 | 暴露任务创建接口并验证可落库、可返回 | 接口 / 结果 |

---

## 4. Step `P4-S1`：接口契约与文件落位

- 目标：明确 Day 4 要提供什么接口、接收什么字段、返回什么字段，以及代码放在哪里。
- 范围：
  1. 冻结 `POST /tasks` 的最小请求 DTO 和响应 DTO。
  2. 明确路由、仓储层、服务层的文件落位。
  3. 对齐 Day 2 `Task` 字段边界，避免接口层自行扩字段。
- 输出：
  1. `POST /tasks` 接口契约
  2. `runtime/orchestrator/app/api/routes/tasks.py`
  3. `runtime/orchestrator/app/repositories/`
  4. `runtime/orchestrator/app/services/task_service.py`
- DoD：
  1. 创建接口的入参与出参边界清晰且与 `Task` 模型一致。
  2. 代码落位能支撑 Day 5 继续做列表与详情接口。
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 5. Step `P4-S2`：仓储层与创建服务

- 目标：把任务创建的数据库写入和默认值生成收敛到可复用的最小业务链路中。
- 范围：
  1. 建立 `Task` 最小仓储层，负责新增任务记录。
  2. 建立任务创建服务，统一处理默认状态、默认优先级、时间字段和返回对象。
  3. 复用 Day 3 已有数据库会话入口，不重复发明连接方式。
- 输出：
  1. `runtime/orchestrator/app/repositories/task_repository.py`
  2. `runtime/orchestrator/app/services/task_service.py`
  3. 任务创建最小调用链
- DoD：
  1. 路由层不直接拼装数据库写入细节。
  2. 创建一条任务后，`tasks` 表中可以看到对应记录。
  3. 默认值口径与 Day 2 / Day 3 文档保持一致。
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

## 6. Step `P4-S3`：路由接入与创建验证

- 目标：把任务创建能力正式暴露为可调用 API，并留下最小验证结果。
- 范围：
  1. 在统一路由中注册任务路由。
  2. 验证 Swagger 文档中出现 `POST /tasks`。
  3. 实际调用一次创建接口并确认数据库落库与响应结果。
  4. 补充最小运行说明或接口说明。
- 输出：
  1. `runtime/orchestrator/app/api/router.py`
  2. `POST /tasks` 接口
  3. 创建验证结果
  4. `runtime/orchestrator/README.md`
- DoD：
  1. `POST /tasks` 可以成功创建任务。
  2. 返回体至少包含 `id`、`title`、`status`、`priority`、`input_summary`、`created_at`、`updated_at`。
  3. `/docs` 中可以看到任务创建接口。
  4. Day 5 可以在此基础上继续做任务列表与详情接口。
- 完成状态：**未开始**
- 完成交付证据：
  1. 待补充

---

## 7. 风险与阻塞

1. 如果创建接口字段超出 Day 2 已冻结范围，后续列表、详情和调度层会出现命名漂移。
2. 如果今天把仓储、服务、路由混在一起快速硬写，Day 5 继续扩接口时容易返工。
3. 如果允许客户端直接传 `status`、`created_at` 等服务端字段，会破坏任务创建语义。
4. 如被阻塞，优先顺延到下一日计划，不在当天强行扩项。

---

## 8. 顺延与交接

- 今日是否全部完成：`否`
- 顺延事项：
  1. 任务列表与详情接口顺延到 Day 5
  2. `Run` 创建与任务执行链路顺延到后续工作日
- 备注：
  1. Day 4 的价值不是接口数量，而是建立 `Task` 第一条真实业务闭环
  2. 只要今天完成“接口契约 + 仓储/服务 + 创建验证”，`V1` 就向最小闭环再前进一步
