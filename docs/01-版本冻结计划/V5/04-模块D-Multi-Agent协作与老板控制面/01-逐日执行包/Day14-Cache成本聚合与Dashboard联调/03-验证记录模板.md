# Day14：cache、成本聚合与 dashboard 联调

- 版本：`V5`
- Phase：`Phase 4`
- 模块：`模块D：Multi-Agent 协作与老板控制面`
- 工作包：`cache-and-cost-dashboard-delivery`
- 当前状态：**已实现待验证**
- owner skill：`drive-v5-orchestrator-delivery`
- 文档定位：**验证记录模板 / 完整展开**

---

## 最低验证目标

1. 至少 1 条 cache 或成本聚合证据入口。
2. Day15 要验证的页面/接口/脚本入口已冻结。
3. fallback / 缺证说明已写入正式包。
4. 若 token accounting 仍缺失，必须明确不能写成完整成本闭环。

## 实际验证记录

- 验证日期：`2026-04-18`
- 验证人 / owner skill：`drive-v5-orchestrator-delivery`（末尾最小 verify）
- 验证环境：`Windows + FastAPI TestClient + 本地 apps/web build`

## 证据明细

- 证据 1（结构判定）：
  - `apps/web/src/features/projects/ProjectOverviewPage.tsx` 已存在多段聚合逻辑与多 section 挂载，本轮未新增复杂状态/字段映射，仅新增：
    - `import { CostDashboardSection } from "../costs/sections/CostDashboardSection";`
    - `<CostDashboardSection projectId=... projectName=... />`
  - Day14 新逻辑全部落到 `apps/web/src/features/costs/`（`types/api/hooks/sections`）。
- 证据 2（聚合字段来源 + dashboard 消费）：
  - 后端新增 `GET /projects/{project_id}/cost-dashboard`（`runtime/orchestrator/app/api/routes/projects.py`）：
    - 成本聚合：`run_count`、`total_estimated_cost_usd`、`prompt_tokens`、`completion_tokens`、`total_tokens`
    - 模式聚合：`mode_breakdown`（含 `provider_reported/heuristic/missing`）
    - 角色聚合：`role_breakdown`
    - 线程聚合：`thread_count` + `thread_breakdown[]`（Day11 agent session 维度）
    - 缓存聚合信号：`cache_summary.total_memories` + `cache_summary.memory_type_counts`
    - fallback 口径：`fallback_contract.*`
  - 前端消费：`apps/web/src/features/costs/sections/CostDashboardSection.tsx`。
- 证据 3（最小联调）：
  - API smoke（FastAPI TestClient）：
    - 创建 project + task
    - `POST /workers/run-once?project_id={project_id}`（`execution_mode=provider_mock`，返回样本为 `provider_reported`）
    - `GET /projects/{project_id}/cost-dashboard` 返回：
      - `run_count=1`
      - `thread_count=1`
      - `total_estimated_cost_usd=0.001011`
      - `mode_breakdown[0].mode=provider_reported`
      - `cache_summary.total_memories=1`
      - `fallback_contract.fallback_active=false`
  - 前端构建：
    - `apps/web`: `npm.cmd run build` 通过（tsc + vite build）。
- 证据路径 / 命令：
  - `runtime/orchestrator/tmp/v5-day14-cost-dashboard-smoke`
  - `runtime/orchestrator`：`.\.venv\Scripts\python.exe`（FastAPI TestClient 最小 smoke：project/task/run-once/cost-dashboard）
  - `apps/web`：`npm.cmd run build`
- 缺证说明：
  - 本次 smoke 样本未覆盖 `heuristic`/`missing` 模式实跑，仅验证了 `provider_reported` 路径。
  - provider 级 cache hit/miss 遥测尚未形成；当前 cache 信号口径基于 Day14 memory 聚合计数。

## 验证结论

- 结论：`部分通过`
- 是否允许交给下一日：`是（可交给 Day15 verify）`
- 需补充的验证：
  - Day15 独立 verify 线程补充 heuristic/missing 模式下 `fallback_contract.fallback_active=true` 的运行证据。
  - Day15 对 `day14-cost-dashboard` 页面入口做一次端到端页面 smoke（非本线程范围）。
