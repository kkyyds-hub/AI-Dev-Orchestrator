# Day13：team assembly 与 team control center 串联交付

- 版本：`V5`
- Phase：`Phase 4`
- 模块：`模块D：Multi-Agent 协作与老板控制面`
- 工作包：`team-assembly-and-control-center`
- 当前状态：**已规划**
- owner skill：`drive-v5-orchestrator-delivery`
- 文档定位：**验证记录模板 / 完整展开**

---

## 最低验证目标

1. 至少 1 条 team policy 保存/回显合同说明。
2. 至少 1 条 role model / budget policy 影响运行的证据入口。
3. Day14 所需聚合字段与来源已冻结。
4. 若 team / policy 仍不能影响运行，必须诚实写成缺口。

## 实际验证记录

- 验证日期：`2026-04-17`
- 验证人 / owner skill：`drive-v5-orchestrator-delivery（线程末尾按 verify-v5-runtime-and-regression 最小验证执行）`
- 验证环境：`Windows + PowerShell + runtime/orchestrator TestClient + apps/web build`

## 证据明细

- 证据 1（结构判定）：
  - `ProjectOverviewPage.tsx(469 行)`、`StrategyDecisionPanel.tsx(549 行)`、`RoleCatalogPage.tsx(320 行)` 已为高负载入口。
  - 本轮触发结构治理约束，Day13 新逻辑全部落在 `apps/web/src/features/agent-teams/`，聚合页仅最小挂载 `TeamControlCenterSection` 与入口跳转按钮。
- 证据 2（保存/回显合同）：
  - 后端新增 `GET/PUT /projects/{project_id}/team-control-center`。
  - API smoke：创建项目 -> GET 快照 -> PUT 保存（`collaboration_mode=pair-routing`, `daily_budget_usd=88.0`）-> GET 回显，字段一致。
- 证据 3（运行消费边界）：
  - role model policy 保存后写入 `/strategy/rules`（`role_model_tier_preferences`、`stage_model_tier_overrides`）。
  - 返回 `runtime_consumption_boundary`，明确 runtime 消费路径：
    - `GET /strategy/projects/{project_id}/preview`
    - `POST /workers/run-once?project_id={project_id}`
  - budget policy 已冻结 project 级保存/回显合同，并输出 Day14 聚合字段前提 `day14_prerequisites`。
- 证据 4（最小构建/联调）：
  - `apps/web` 执行 `npm run build` 通过。
  - API smoke 成功链路覆盖 Day13 新接口（见证据 2）。
- 缺证说明：
  - Day12 独立页面/API 联调证据仍建议由独立 verify 线程补证。
  - budget policy 的 project 级强制执行仍未新增，当前定位为 Day13 合同冻结与 Day14 聚合前提。

## 验证结论

- 结论：`部分通过`
- 是否允许交给下一日：`允许（可进入 Day14）`
- 需补充的验证：`独立 verify 线程补 Day12 联调证据；Day14/Day15 再完成更完整回归。`
