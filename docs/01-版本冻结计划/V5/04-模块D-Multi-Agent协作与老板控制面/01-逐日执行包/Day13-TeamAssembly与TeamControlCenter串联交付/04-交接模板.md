# Day13：team assembly 与 team control center 串联交付

- 版本：`V5`
- Phase：`Phase 4`
- 模块：`模块D：Multi-Agent 协作与老板控制面`
- 工作包：`team-assembly-and-control-center`
- 当前状态：**已规划**
- owner skill：`drive-v5-orchestrator-delivery`
- 文档定位：**交接模板 / 完整展开**

---

## 交接摘要

- 当前状态：`已实现待验证`
- 当前日包：`Day13`
- 下一日包：`Day14：cache、成本聚合与 dashboard 联调`
- 下一线程 owner：`drive-v5-orchestrator-delivery`

## 已留下的交接物

- [x] Day13 实际涉及的页面/接口/策略字段列表
- [x] Day14 需要消费的 team / policy / budget 字段
- [x] team policy 是否真正可影响运行的判断依据
- [x] 当前缺证与阻塞说明
- [x] 不要误判为 Phase 4 已整体完成的点

## Day13 实际改动清单（本轮）

- 后端：
  - `runtime/orchestrator/app/api/routes/team_control_center.py`（新增）
  - `runtime/orchestrator/app/api/router.py`（注册新路由）
  - `runtime/orchestrator/app/core/db_tables.py`（`projects` 新增 team/policy/budget JSON 列）
  - `runtime/orchestrator/app/core/db.py`（新增列升级语句）
- 前端：
  - `apps/web/src/features/agent-teams/types.ts`（新增）
  - `apps/web/src/features/agent-teams/api.ts`（新增）
  - `apps/web/src/features/agent-teams/hooks.ts`（新增）
  - `apps/web/src/features/agent-teams/components/TeamAssemblyEditor.tsx`（新增）
  - `apps/web/src/features/agent-teams/components/RoleModelPolicyEditor.tsx`（新增）
  - `apps/web/src/features/agent-teams/sections/TeamControlCenterSection.tsx`（新增）
  - `apps/web/src/features/projects/ProjectOverviewPage.tsx`（最小挂载）
  - `apps/web/src/features/roles/RoleCatalogPage.tsx`（最小入口跳转）
  - `apps/web/src/features/strategy/StrategyDecisionPanel.tsx`（最小入口跳转）

## Day14 可直接消费的数据前提

- `day14_prerequisites.team_size`
- `day14_prerequisites.enabled_role_codes`
- `day14_prerequisites.budget_policy_keys`
- `day14_prerequisites.role_preference_count`
- `day14_prerequisites.stage_override_count`
- `runtime_consumption_boundary.role_model_policy_paths`
- `runtime_consumption_boundary.budget_policy_paths`

## 运行消费边界（Day13 冻结）

- role model policy：
  - 保存路径：`PUT /projects/{project_id}/team-control-center`
  - 运行消费：`/strategy/rules` -> `GET /strategy/projects/{project_id}/preview` -> `POST /workers/run-once?project_id={project_id}`
- budget policy：
  - 保存/回显路径：`PUT/GET /projects/{project_id}/team-control-center`
  - 运行边界：当前仍由既有 budget guard 主链执行，Day13 提供 project 级合同与 Day14 聚合前提。

## 缺证与非完成定义

- Day12 独立页面/API 联调证据仍待独立 verify 线程补证。
- 不可写成「Phase 4 已通过」。
- 不可写成「Day14 dashboard 已完成」。
- 不可写成「budget policy 已实现完整 project 级强制执行」。

## 下一日开始前必须先确认

1. Day14 开始前确认已有 team / policy / budget 聚合维度。
2. 确认 Day13 没有越界进入 verify / accept 线程口径。
3. 确认现有老板入口不会与新控制中心发生路径冲突。

## 当前不能误判为完成的点

1. 只有配置表单，没有运行时消费合同。
2. 只有后端字段，没有前端入口或回显规则。
3. 把 Day13 写成 team control center 全量产品完成。
4. 提前把 Day14 dashboard 或 Day15 verify 材料写成已具备。

## 交接备注

- 启动入口：`docs/01-版本冻结计划/V5/04-模块D-Multi-Agent协作与老板控制面/01-逐日执行包/Day12-AgentThread页面与BossIntervention前端入口/04-交接模板.md`
- 备注：`Day13 跨层控制中心已最小落地，下一棒转 drive-v5-orchestrator-delivery（Day14）。`
