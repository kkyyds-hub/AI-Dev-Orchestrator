# Day11：Agent Session / Message / Review-Rework 后端主链（执行说明）

- 版本：`V5`
- Phase：`Phase 3`
- 工作包：`agent-thread-backend-core`
- 当前状态：**已实现待验证**
- owner skill：`write-v5-runtime-backend`

## 1. 执行边界

1. 仅做 Day11 纯后端主链接线。
2. 不跨入 Day12 前端实现，不吞 Day13/Day14。
3. 不做 accept 裁定，只做线程内最小验证。

## 2. 本日完成定义（执行口径）

1. `agent session / agent message / review-rework / boss note-event` 已形成后端主链。
2. Day12 可直接消费的 `sessions/timeline/interventions` API 已存在且可查询。
3. Day10 -> Day11 的“上下文可恢复性”已作为 session seed 落库并进入 timeline。

## 3. 本日非完成定义（禁止误判）

1. 仅有 domain/repository 草案、未接入 worker 主链。
2. 仅包装 run log、没有 session/message 持久化合同。
3. 仅写字段名、未提供 Day12 可消费 API/状态流。

## 4. 最低验证要求

1. `python -m compileall runtime/orchestrator/app` 通过。
2. 至少 1 条真实 API 链路证明 session/message 主链可创建与回放。
3. 至少 1 条失败路径证明 `review_required -> rework_requested -> boss_note_event` 合同落地。

## 5. 收口要求

1. 必须回填 `02-状态回填模板.md`、`03-验证记录模板.md`、`04-交接模板.md`。
2. 明确列出 Day12 消费字段（timeline/message/intervention）与状态流。
3. 状态仅可写 `已实现待验证`（除非有独立 verify+accept 证据支持升级）。

## 6. 下一棒指派

下一棒默认转 `write-v5-web-control-surface`（Day12）。
