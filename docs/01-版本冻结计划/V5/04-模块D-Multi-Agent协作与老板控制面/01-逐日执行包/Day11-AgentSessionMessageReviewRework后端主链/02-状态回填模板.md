﻿﻿# Day11：Agent session / message / review-rework 后端主链（状态回填）

- 版本：`V5`
- Phase：`Phase 3`
- 模块：`模块D：Multi-Agent 协作与老板控制面`
- 工作包：`agent-thread-backend-core`
- 当前状态：**已完成**
- owner skill：`write-v5-runtime-backend`

---

## 1. 基本信息

- 回填日期：`2026-04-18`
- 当前状态：`已完成`
- 本轮 owner skill：`write-v5-runtime-backend`
- 本轮目标：只收束 Day11 后端主链，为 Day12 提供稳定读合同；不扩到 Day12–Day14

## 2. 完成定义核对

- [x] `agent session / agent message / review-rework / boss note-event` 已形成真实持久化主链
- [x] Day10 -> Day11 上下文可恢复性已通过 `context_checkpoint_id / context_rehydrated` 进入 session 与 timeline
- [x] Day12 可直接读取的三条后端合同已存在：
  - `GET /agent-threads/projects/{project_id}/sessions`
  - `GET /agent-threads/projects/{project_id}/timeline`
  - `GET /agent-threads/projects/{project_id}/interventions`
- [x] worker 显式回传 `agent_session_id / agent_session_status / agent_review_status / agent_current_phase`
- [x] 已补 Day11 专项 smoke，覆盖成功链路与 review-rework 失败链路
- [x] 已修复 session finalize 后误清空 `latest_intervention_type / latest_note_event_type` 的后端合同缺口

## 3. 非完成定义排除

- [x] 不是只有 domain/repository 草案、没有接入 worker 主链
- [x] 不是只包装 run log、没有 session/message 落库
- [x] 不是只写字段名、没有 Day12 可消费 API
- [x] 没有把 Day13 team control center / Day14 costs/cache 提前写成本轮完成

## 4. 本轮实际后端产出

1. `runtime/orchestrator/app/repositories/agent_session_repository.py`
   - 修复 `update_status()`：未传入的 `latest_intervention_type / latest_note_event_type / summary` 不再被动清空
2. `runtime/orchestrator/scripts/v5_day11_agent_thread_contract_smoke.py`
   - 新增 Day11 agent-thread 专项 smoke
   - 覆盖成功路径：
     - `session_started`
     - `context_recovery_ready`
     - `execution_started`
     - `execution_finished`
     - `review_passed`
     - `session_finalized`
   - 覆盖失败路径：
     - `review_required`
     - `rework_requested`
     - `boss_note_event`
     - `session_finalized`
   - 额外校验失败 session 能保留：
     - `latest_intervention_type = boss_note`
     - `latest_note_event_type = quality_gate_blocked`

## 5. Day12 可直接消费的字段合同

### 5.1 sessions

- `session_id`
- `project_id`
- `task_id`
- `run_id`
- `session_status`
- `review_status`
- `current_phase`
- `owner_role_code`
- `context_checkpoint_id`
- `context_rehydrated`
- `latest_intervention_type`
- `latest_note_event_type`
- `summary`
- `started_at`
- `updated_at`
- `finished_at`

### 5.2 timeline / messages

- `message_id`
- `session_id`
- `project_id`
- `task_id`
- `run_id`
- `sequence_no`
- `role`
- `message_type`
- `event_type`
- `phase`
- `state_from`
- `state_to`
- `intervention_type`
- `note_event_type`
- `context_checkpoint_id`
- `context_rehydrated`
- `content_summary`
- `content_detail`
- `created_at`

### 5.3 interventions

- 复用 message DTO
- 当前真实返回以 `review / rework / note_event` 为主
- 当前不要把它误判成“正式人工干预写接口”

## 6. 当前风险 / 缺口 / 是否阻塞

### 已保守回填的缺口

1. `POST /workers/run-once?project_id=...` 目前只是 Day12 借道使用的“触发 worker 再执行一次”入口，**不是正式的 session 级 boss intervention 写接口**
2. `/agent-threads/.../interventions` 当前是“review/rework/note-event feed”，语义比“真正 intervention command”更窄
3. 目前完成的是 Day11 线程内最小 smoke，不替代独立 `verify-v5-runtime-and-regression` 的联合回归

### 是否阻塞 Day12

- `不阻塞 Day12 前端读取与展示`
- `阻塞 Day11 工作包级正式验收升级`

## 7. 推荐下一步

- 下一线程 owner skill：`write-v5-web-control-surface`
- 前提：Day12 只消费现有 `sessions / timeline / interventions` 三条读合同，不擅自把 `/workers/run-once` 写成正式 session 级 intervention API

## 8. 2026-04-18 independent verify (verify-v5-runtime-and-regression)

- Decision: upgrade_to_done
- Evidence level: E2
- Risk level: R3
- Evidence file:
  - `runtime/orchestrator/tmp/day09-day14-verify-20260418/day11_agent_thread_contract_smoke.json`
