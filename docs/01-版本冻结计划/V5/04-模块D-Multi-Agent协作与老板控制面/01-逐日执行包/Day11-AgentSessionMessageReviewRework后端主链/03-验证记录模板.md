# Day11：Agent session / message / review-rework 后端主链（验证记录）

- 版本：`V5`
- Phase：`Phase 3`
- 模块：`模块D：Multi-Agent 协作与老板控制面`
- 工作包：`agent-thread-backend-core`
- 当前状态：**已实现待验证**
- owner skill：`write-v5-runtime-backend`

---

## 1. 最低验证目标

1. `python -m compileall runtime/orchestrator/app` 通过
2. 至少 1 条成功路径证明 Day11 session / timeline 主链成立
3. 至少 1 条失败路径证明 `review_required -> rework_requested -> boss_note_event` 合同落地
4. 失败 session 的 `latest_intervention_type / latest_note_event_type` 在 finalize 后仍可被 Day12 读取

## 2. 实际验证记录

- 验证日期：`2026-04-18`
- 验证方式：线程内最小 smoke，非独立 verify 线程
- 验证环境：`Windows + Python + FastAPI TestClient + SQLite isolated runtime dir`

## 3. 证据明细

### 证据 1：编译检查通过

命令：

```powershell
python -m compileall runtime/orchestrator/app
```

结果：

- 通过
- `runtime/orchestrator/app/api/routes/agent_threads.py`
- `runtime/orchestrator/app/repositories/agent_session_repository.py`
- `runtime/orchestrator/app/services/agent_conversation_service.py`
- `runtime/orchestrator/app/workers/task_worker.py`
- `runtime/orchestrator/app/core/db_tables.py`
  等 Day11 相关文件均可导入编译

### 证据 2：Day11 专项 smoke 通过

命令：

```powershell
python runtime/orchestrator/scripts/v5_day11_agent_thread_contract_smoke.py
```

结果：

- 成功路径：
  - `POST /workers/run-once?project_id=...` 返回
    - `agent_session_id`
    - `agent_session_status=completed`
    - `agent_review_status=review_passed`
  - `GET /agent-threads/projects/{project_id}/timeline?session_id=...` 返回事件序列：
    - `session_started`
    - `context_recovery_ready`
    - `execution_started`
    - `execution_finished`
    - `review_passed`
    - `session_finalized`
- 失败路径：
  - 通过 `simulate: ...` + `verify: exit 1` 触发 verification fail
  - `POST /workers/run-once?project_id=...` 返回
    - `agent_session_status=failed`
    - `agent_review_status=rework_required`
  - `GET /agent-threads/projects/{project_id}/timeline?session_id=...` 返回事件序列：
    - `session_started`
    - `context_recovery_ready`
    - `execution_started`
    - `execution_finished`
    - `review_required`
    - `rework_requested`
    - `boss_note_event`
    - `session_finalized`
  - `GET /agent-threads/projects/{project_id}/interventions?session_id=...` 返回：
    - `boss_note_event`
    - `rework_requested`
    - `review_required`
- Session 字段保留校验：
  - 失败 session 保留
    - `latest_intervention_type = boss_note`
    - `latest_note_event_type = quality_gate_blocked`

### 证据 3：隔离运行时目录

- smoke 输出目录：
  - `runtime/orchestrator/tmp/v5-day11-agent-thread-smoke/`

## 4. 回归 / 风险观察

- [x] Day11 后端主链已经真正接入 worker，不是静态草案
- [x] Day12 所需的三条读合同已经稳定到可消费级别
- [x] 本轮修复了 finalize 误清空 latest session 元数据的问题
- [ ] `POST /workers/run-once?project_id=...` 仍不是正式的 session 级 boss intervention 写接口
- [ ] 本轮验证不替代独立 `verify-v5-runtime-and-regression`
- [ ] 仍缺 Day09–Day11 联合回归与 Day12 前后端联调证据

## 5. 验证结论

- 验证结论：`部分通过`
- 是否允许交给下一日：`允许交给 Day12（write-v5-web-control-surface）`
- 未关闭项：
  1. 独立 verify 线程执行 Day09–Day11 联合回归
  2. Day12 前端接入后再补 timeline / interventions 联调验证
  3. 如 Day12 需要“真正的人工干预写操作”，应回切 backend 线程设计正式 session 级 intervention API
