# Day10：MemoryGovernance 控制面与交付串联（验证记录）

- 版本：`V5`
- Phase：`Phase 2`
- 工作包：`memory-governance-cross-layer-delivery`
- 验证日期：`2026-04-18`
- 执行 owner skill：`drive-v5-orchestrator-delivery`（本线程最小验证，不替代独立 verify）

---

## 1. 验证范围

1. 治理状态接口可返回 Day10 页面消费字段。
2. manual `rehydrate / compact / reset` 动作合同可真实调用。
3. `run-once` 的 `memory_governance_*` 字段既有空项目路径，也有真实任务执行路径。
4. 前端 `apps/web` 可完成 build。

## 2. 实际执行

### 2.1 后端 smoke

命令（仓库根）：

```bash
python runtime/orchestrator/scripts/v5_day10_memory_governance_smoke.py
```

结果摘要：

1. 空项目路径：
   - `GET /projects/{project_id}/memory/governance` -> `200`
   - `POST /projects/{project_id}/memory/governance/rehydrate` -> `200`，`rehydrated=false`
   - `POST /projects/{project_id}/memory/governance/compact` -> `404`
   - `POST /projects/{project_id}/memory/governance/reset` -> `200`
2. 真实任务执行路径：
   - `POST /tasks` -> `201`
   - `POST /workers/run-once?project_id=...` -> `200`
   - 返回非空 `memory_governance_checkpoint_id`
   - 返回非空 `memory_governance_pressure_level`
   - 返回非空 `memory_governance_usage_ratio`
   - `GET /projects/{project_id}/memory/governance` 中 `latest_run_id == run_once.run_id`
   - `POST /projects/{project_id}/memory/governance/compact` -> `200`
   - `POST /projects/{project_id}/memory/governance/rehydrate?task_id=...` -> `200`，`rehydrated=true`

本次样本关键 ID（2026-04-18 实跑）：

- `project_id=4923d1d3-a7b5-4c90-ba8a-2bd5156d340e`
- `task_id=1977b9dc-df28-4a75-a752-9e31fc6c8c9d`
- `run_id=4c0a7797-6835-419a-8956-4a5dd0788d5e`
- `checkpoint_id=ckpt-ce9d4671c9ef4255890eecdbc17d9737`

### 2.2 前端 build

命令：

```bash
npm.cmd run build
```

目录：

```bash
apps/web
```

结果摘要：

1. `tsc -b` 通过
2. `vite build` 通过
3. 存在 chunk size warning，但不阻塞本轮 Day10 最小验证

## 3. 对最低验证要求的映射

1. 治理状态回显验证：已满足。
2. manual 动作入口验证：已满足。
3. 相关 API / action smoke：已满足。
4. 前端最小 build：已满足。

## 4. 缺证与限制

1. 目前没有高压坏上下文真实样本。
2. 目前没有独立 verify 线程给出的完整回归结论。
3. 本轮不据此上升为 `Phase 2 已通过` 或 `Day11 已完成前置裁定`。

## 5. 验证结论

- 结论：`部分通过（Day10 最小跨层闭环成立）`
- 状态建议：`已实现待验证`
- 说明：Day10 控制面、动作合同、run-once 治理回显、前端 build 均已形成真实证据；仍建议后续线程补独立 verify 与更强场景样本。
