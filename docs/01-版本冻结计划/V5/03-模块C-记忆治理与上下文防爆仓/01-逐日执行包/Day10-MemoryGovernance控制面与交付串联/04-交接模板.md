# Day10：MemoryGovernance 控制面与交付串联（交接）

- 版本：`V5`
- Phase：`Phase 2`
- 工作包：`memory-governance-cross-layer-delivery`
- 当前状态：`已实现待验证`
- 当前 owner skill：`drive-v5-orchestrator-delivery`
- 下一棒 owner skill：`write-v5-runtime-backend`（Day11）

---

## 1. 本棒交付摘要

1. 前端新增 `features/memory-governance`，落地最小治理控制面观察区与动作区。
2. 页面已接入 `rehydrate / compact / reset` 三个真实动作入口，并覆盖成功/失败/禁用反馈。
3. 页面新增 `run-once` 回显区，消费 `memory_governance_*` 字段族。
4. 后端补齐治理状态字段，新增可消费项：
   - `latest_compaction_reduction_ratio`
   - `latest_compaction_reason_codes`
   - `latest_rehydrate_used_checkpoint_id`
5. 已完成最小 API smoke + 前端 build 证据。

## 2. 改动文件清单（本棒）

### 后端
1. `runtime/orchestrator/app/services/project_memory_service.py`
2. `runtime/orchestrator/app/api/routes/projects.py`

### 前端
1. `apps/web/src/features/memory-governance/types.ts`
2. `apps/web/src/features/memory-governance/api.ts`
3. `apps/web/src/features/memory-governance/hooks.ts`
4. `apps/web/src/features/memory-governance/sections/MemoryGovernanceSection.tsx`
5. `apps/web/src/features/projects/ProjectOverviewPage.tsx`（仅挂载入口）

### 文档
1. `docs/01-版本冻结计划/V5/03-模块C-记忆治理与上下文防爆仓/01-逐日执行包/Day10-MemoryGovernance控制面与交付串联/02-状态回填模板.md`
2. `docs/01-版本冻结计划/V5/03-模块C-记忆治理与上下文防爆仓/01-逐日执行包/Day10-MemoryGovernance控制面与交付串联/03-验证记录模板.md`
3. `docs/01-版本冻结计划/V5/03-模块C-记忆治理与上下文防爆仓/01-逐日执行包/Day10-MemoryGovernance控制面与交付串联/04-交接模板.md`

## 3. Day11 可直接依赖的“上下文可恢复性”前提

1. Day10 页面已能观察最新 checkpoint / pressure / risk / rolling summary。
2. Day10 页面已能触发 manual rehydrate / compact / reset，并获得 API 反馈。
3. Day10 页面已有 run-once 回显消费位，可承接后续真实任务执行样本。
4. 状态字段与动作合同已固化到前后端类型/API，不依赖口头说明。

## 4. 未完成定义与风险（必须保留）

1. 本棒不是 Day11，不包含 agent session/message/thread 数据结构实现。
2. 本棒不是 Phase 2 gate，不得写为“Phase 2 已通过”。
3. 本棒未扩为完整 memory 产品中心，也未提前纳入 Day13/Day14。
4. 当前仍缺“真实任务执行产生非空 governance 回显”的页面联调样本证据。

## 5. 下一棒建议

1. Day11 线程先接后端主链（session/message/review-rework），并复用 Day10 提供的治理回显入口作为前置观测锚点。
2. 如需先补证据，可在 Day11 启动前追加一条“有待执行任务”的 run-once 联调样本，补齐 non-null governance 回显。

## 6. 正式交接结论

Day10 跨层控制面已最小落地并处于已实现待验证，下一棒转 `write-v5-runtime-backend`（Day11）。
