# Day10：记忆治理控制面与跨层交付

- 版本：`V5`
- Phase：`Phase 2`
- 模块：`模块C：记忆治理与上下文防爆仓`
- 工作包：`memory-governance-cross-layer-delivery`
- 当前状态：**已实现待验证**
- owner skill：`drive-v5-orchestrator-delivery`
- 文档定位：**执行说明 / 完整展开**

---

## 1. 本轮执行切片

1. 以前端 `MemoryGovernanceSection` 收束 Day10 控制面，不再把逻辑继续塞进旧 `ProjectMemoryPanel`。
2. 以前后端真实合同收束 `rehydrate / compact / reset / run-once echo`。
3. 必要时吸收 Day09 依赖缺口，但不扩到 Day11–Day14。
4. 在正式文档内写清真实状态、验证证据、剩余风险与下一线程 owner。

## 2. 本轮已落位的关键点

- `apps/web/src/features/memory-governance/` 已存在并可被首页消费。
- `ProjectOverviewPage.tsx` 已挂入 Day10 控制面入口。
- `runtime/orchestrator/app/services/context_builder_service.py` 已补 `run_id` 透传到治理 checkpoint。
- `runtime/orchestrator/scripts/v5_day10_memory_governance_smoke.py` 已提供稳定可复跑的后端 smoke 入口。

## 3. 最低验证清单

1. `python runtime/orchestrator/scripts/v5_day10_memory_governance_smoke.py`
2. `npm.cmd run build`（目录：`apps/web`）
3. 验证记录必须回填到 `03-验证记录模板.md`
4. 状态、风险、下一线程 owner 必须回填到 `02-状态回填模板.md`

## 4. 线程结束前必须回填的内容

1. 本轮实际改动文件列表。
2. 已完成项 / 未完成项。
3. 最小验证结果与证据命令。
4. 风险与缺口。
5. 下一线程推荐 owner skill。

## 5. 当前禁止事项

1. 不要把 Day11 agent thread / Day13 team control / Day14 cost dashboard 算进本轮完成项。
2. 不要在缺独立 verify 的情况下写成“已通过”。
3. 不要忽略 Day09 -> Day10 合同缺口的真实状态。
4. 不要跳过文档回填，只留代码不留口径。
