# Day09：记忆治理与上下文防爆仓后端主链

- 版本：`V5`
- Phase：`Phase 2`
- 模块：`模块C：记忆治理与上下文防爆仓`
- 工作包：`memory-governance-backend-core`
- 当前状态：**已实现待验证**
- owner skill：`write-v5-runtime-backend`
- 文档定位：**交接模板 / 完整展开**

---

## 1. 交接摘要

- 当前状态：`已实现待验证`
- 当前日包：`Day09`
- 下一日包：`Day10：记忆治理控制面与跨层交付`
- 下一线程 owner：`drive-v5-orchestrator-delivery`

## 2. 已留下的交接物

- [x] Day09 实际涉及的后端文件列表
- [x] checkpoint / rehydrate 场景说明
- [x] Day10 需要消费的治理状态字段
- [x] 当前缺证与风险边界
- [x] 不要误判为 Phase 2 已整体完成的点

后端文件列表：
1. `runtime/orchestrator/app/services/context_budget_service.py`（新增）
2. `runtime/orchestrator/app/services/memory_compaction_service.py`（新增）
3. `runtime/orchestrator/app/services/project_memory_service.py`
4. `runtime/orchestrator/app/services/context_builder_service.py`
5. `runtime/orchestrator/app/workers/task_worker.py`
6. `runtime/orchestrator/app/api/routes/workers.py`
7. `runtime/orchestrator/app/api/routes/projects.py`

checkpoint / rehydrate / compaction 主链入口：
1. Worker 执行时 `ContextBuilderService.build_context_package()` 自动触发：
   - budget 评估 -> compaction（按需）-> checkpoint 写入 -> rehydrate（高压按需）
2. Worker 日志新增 `memory_governance_checkpointed` 事件。
3. 手动动作 API：
   - `POST /projects/{project_id}/memory/governance/rehydrate`
   - `POST /projects/{project_id}/memory/governance/compact`
   - `POST /projects/{project_id}/memory/governance/reset`

Day10 可直接消费治理合同：
1. 状态字段：`GET /projects/{project_id}/memory/governance`
2. 动作入口：rehydrate / compact / reset 三个 POST 路由
3. 运行回显：`/workers/run-once` 的 `memory_governance_*` 字段族（checkpoint_id、pressure_level、bad_context、compaction_applied、rehydrated 等）

## 3. 下一日开始前必须先确认

1. Day10 开始前确认 Day09 是否已留下治理状态字段。
2. 确认 manual reset / rehydrate 动作入口是否有正式合同。
3. 确认 Day10 的页面观察面不会越出 Day09 的后端边界。

## 4. 当前不能误判为完成的点

1. 只有 checkpoint schema，没有主链入口。
2. 只有 summary 压缩，没有恢复路径。
3. 只写新增服务名，不写与 worker / context builder 的接线位置。
4. 把 Day09 写成上下文治理已经全面完成。

## 5. 交接备注

- 启动入口：`docs/01-版本冻结计划/V5/02-模块B-Provider、Prompt、Token与记忆主链/01-逐日执行包/Day08-Phase1Smoke回归与Gate输入/README.md`
- 备注：`Day09 后端主链已最小落地，当前状态为已实现待验证；Day10 可直接进入跨层控制面联调。`
