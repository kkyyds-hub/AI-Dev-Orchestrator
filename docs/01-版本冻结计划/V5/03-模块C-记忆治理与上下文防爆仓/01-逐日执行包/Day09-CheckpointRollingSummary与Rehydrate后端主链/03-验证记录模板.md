# Day09：记忆治理与上下文防爆仓后端主链

- 版本：`V5`
- Phase：`Phase 2`
- 模块：`模块C：记忆治理与上下文防爆仓`
- 工作包：`memory-governance-backend-core`
- 当前状态：**已实现待验证**
- owner skill：`write-v5-runtime-backend`
- 文档定位：**验证记录模板 / 完整展开**

---

## 1. 最低验证目标

1. 至少 1 条 checkpoint / rehydrate 场景证据入口。
2. context builder / worker 主链接线位置已明确。
3. Day10 需要消费的状态字段和动作入口已冻结。
4. 缺证时必须明确写成已规划 / 已实现待验证 / 进行中 / 阻塞之一。

## 2. 实际验证记录

- 验证日期：`2026-04-17`
- 验证人 / owner skill：`write-v5-runtime-backend`
- 验证环境：`Windows PowerShell / Python 3.x / runtime/orchestrator`

## 3. 证据明细

- 证据 1：`python -m compileall app` 通过；新增与改动文件均可编译导入（含 `context_budget_service.py`、`memory_compaction_service.py`、`context_builder_service.py`、`project_memory_service.py`、`task_worker.py`、`projects.py`、`workers.py`）。
- 证据 2：脚本级最短链路调用已跑通（checkpoint -> rehydrate -> compact -> governance state）：
  - `CHECKPOINT ckpt-387a0c63f9a843bc8747abb06b13a317`
  - `REHYDRATE True ckpt-387a0c63f9a843bc8747abb06b13a317`
  - `COMPACTION True 0.0`
  - `STATE 1 low True`
- 证据 3：Day10 消费入口已落代码：
  - `GET /projects/{project_id}/memory/governance`
  - `POST /projects/{project_id}/memory/governance/rehydrate`
  - `POST /projects/{project_id}/memory/governance/compact`
  - `POST /projects/{project_id}/memory/governance/reset`
  - `POST /workers/run-once` 回显 `memory_governance_*` 字段
- 缺证说明：未执行真实项目数据下的 API 联调 smoke（本轮仅完成编译与脚本级最短链路）。

## 4. 回归 / 风险观察

- [x] 压缩策略过度导致上下文误伤。（已记录为 R3 观察风险，需 Day10 联调关注）
- [x] 恢复链路不稳定，可能放大错误上下文。（已记录为 R2 高风险观察项，需后续验证）
- [x] 仅新增服务名但未接入主链，会造成 Day10 无法落地。（本轮已接线，风险关闭）

## 5. 验证结论

- 验证结论：`部分通过`
- 是否允许交给下一日：`允许（Day10 可消费）`
- 需补充的验证：
  1. Day10 线程执行 API smoke + 前端回显联调。
  2. 多任务并发下 checkpoint 文件并发安全验证。
