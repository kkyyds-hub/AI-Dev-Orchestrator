# Day05：Provider 抽象层与执行路由后端主链

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`provider-gateway-and-executor-routing-backend`
- 当前状态：**已完成**
- owner skill：`write-v5-runtime-backend`
- 文档定位：**状态回填 / 正式裁定回填**

---

## 1. 基本信息

- 回填日期：`2026-04-07`
- 当前状态：`已完成`
- 正式裁定：`Pass`
- 实际执行线程 / 依据：`Day05 既有实现 + 既有 verify 记录 + 本轮最小仓库复核`
- 回填人 / owner skill：`accept-v5-milestone-gate`

## 2. 完成定义核对

- [x] executor 已具备 provider 抽象层入口，不再只有 shell / simulate 两条主线路径。
- [x] provider 路由依据与 `strategy_engine_service.py` 产出的模型策略存在实际接线。
- [x] 旧 `shell` / `simulate` 模式仍然可用，没有被 V5 改造破坏。
- [x] 至少有 1 条 provider 路由最小调用或 mock 调用证据。
- [x] 已给 Day06 留下明确的 receipt / usage / token 契约接力点。

## 3. 非完成定义排雷

- [x] 未出现：只有 provider interface / schema 草案，没有 executor 主链接线。
- [x] 未出现：只改了策略展示，不改执行链。
- [x] 未出现：provider 路由建立后反而破坏了 shell / simulate 回退。
- [x] 未出现：没有任何 smoke / compile / 调用证据。
- [x] 未出现：把 Day06 的 prompt/token 工作提前混进来，却没有先把 Day05 自身主链收稳。

## 4. 产出与证据

- [x] provider gateway / model routing 后端主链说明与对应代码改动面清单
- [x] executor 接入 provider 抽象层的执行路径说明
- [x] shell / simulate fallback 保留说明
- [x] Day06 可直接接手的 receipt / usage / token 契约交接说明
- 证据摘要：
  1. `runtime/orchestrator/app/domain/model_policy.py`
  2. `runtime/orchestrator/app/services/model_routing_service.py`
  3. `runtime/orchestrator/app/services/mock_provider_executor_service.py`
  4. `runtime/orchestrator/app/services/executor_service.py`
  5. `runtime/orchestrator/app/services/strategy_engine_service.py`
  6. `runtime/orchestrator/app/workers/task_worker.py`
  7. `03-验证记录模板.md` 中 `2026-04-06` 的 compile / smoke / `provider_mock` / `shell_override` / `simulate_override` 记录

## 5. 风险 / 缺口 / 阻塞

- 当前风险：真实 provider adapter / registry / usage / token / receipt 闭环仍未形成。
- 当前缺口：Prompt Registry、Token Accounting 与真实 receipt / usage / token 口径留给 Day06 及后续工作包承接。
- 是否阻塞下一 Day：`否`
- 不阻断 Pass 的原因：Day05 合同只要求后端主链接线、mock provider 证据、fallback 回归与 Day06 契约交接；真实 provider 闭环不在本日完成定义内。

## 6. 下一步接力

- 下一线程 owner：`write-v5-runtime-backend（Day06）`
- 第二顺位：`verify-v5-runtime-and-regression`
- 启动前先读：
  1. `README.md`
  2. `03-验证记录模板.md`
  3. `04-交接模板.md`
  4. `docs/01-版本冻结计划/V5/02-模块B-Provider、Prompt、Token与记忆主链/01-逐日执行包/Day06-PromptRegistry与TokenAccounting后端落地/README.md`
- 当前不要误判为完成的点：
  1. 不能写成“真实 provider 已完成”。
  2. 不能写成“usage / token / receipt 已闭环”。
  3. 不能写成“Phase 1 已完成”。
  4. 不能把 mock provider 证据等同于真实外部 LLM 执行闭环。
  5. 本轮只完成 Day05 裁定与收口，不进入 Day06。
