# Day05：Provider 抽象层与执行路由后端主链

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`provider-gateway-and-executor-routing-backend`
- 当前状态：**已完成（工作包级回填）**
- owner skill：`manage-v5-plan-and-freeze-docs`
- 文档定位：**状态回填（2026-04-14 最新回填）**

---

## 1. 回填说明

- 回填日期：`2026-04-14`
- 本线程类型：文档治理回填线程
- 本线程边界：只回填状态/证据/边界，不做实现，不做验收裁定

## 2. 结论分层（按现实证据）

1. `项目内 provider 配置入口`：`已完成`
   - 证据：`runtime/orchestrator/app/core/config.py`、`runtime/orchestrator/app/services/provider_config_service.py`；运行时配置文件存在于 `runtime/data/provider-settings/openai-provider-config.json`（本回填不披露密钥内容）。
2. `OpenAI 真实 provider 最小调用闭环`：`已完成`
   - 证据：`runtime/orchestrator/app/services/openai_provider_executor_service.py` + `runtime/orchestrator/app/services/executor_service.py` 已接通真实 provider 分支。
3. `固定 model=gpt-5.4 的真实成功外呼证据`：`已完成`
   - 证据：`runtime/data/logs/task-runs/6ed62fb9-b17a-4d97-af99-187be926d61b/370c6cbb-7321-4181-9bfc-2527835c20a7.jsonl`。
4. `actual_execution_mode=provider_openai 且 fallback_applied=false`：`已完成`
   - 证据：同一成功样本 `run_id=370c6cbb-7321-4181-9bfc-2527835c20a7`。
5. `真实 provider receipt/source 成立`：`已完成`
   - 证据：同一成功样本含 `provider_receipt_source=real_provider`、`provider_receipt_id=resp_...`、`token_pricing_source=openai.chat_completions.usage`。
6. `provider_mock / simulate / shell 保留`：`已完成`
   - 证据：`executor_service.py` 显式保留 `shell:` / `simulate:`；真实 provider 失败样本 `runtime/data/logs/task-runs/d9d9a456-515a-4466-bc75-ff80e88f42a6/3f797d15-3229-44f1-b422-e0ad7abbd830.jsonl` 显示 `endpoint_forbidden -> provider_mock` 回退链仍可用。

## 3. 四类证据边界（强制区分）

1. 真实 provider 成功证据：`已完成`
   - 样本：`run_id=370c6cbb-7321-4181-9bfc-2527835c20a7`。
2. 真实 provider 失败但命中路径证据：`已完成`
   - 样本：`run_id=3f797d15-3229-44f1-b422-e0ad7abbd830`（`fallback_reason_category=endpoint_forbidden`）。
3. `provider_mock` 证据：`已完成`
   - 样本：`run_id=3f797d15-3229-44f1-b422-e0ad7abbd830`。
4. heuristic fallback（`simulate/shell`）证据：`已完成`
   - 样本：`runtime/orchestrator/tmp/day06-riskfix-fallback-smoke/.../6191c1d4-323c-4cb8-87f9-f1a850fdb868.jsonl`（`actual_execution_mode=simulate`）。

## 4. Day07 启动影响

1. 以 Day05 当前字段合同启动 Day07 接线：`已完成`
2. 以“Day05 已有真实 provider 成功样本”作为 Day07/Day08 输入：`已完成`
3. 以“外部账单对账已完成”作为 Day07/Day08 输入：`阻塞`
   - 阻塞点：仍缺正式外部账单对账材料。

## 5. 防误导声明

1. 可以写“Day05 真实 provider 最小闭环已成立”，但不得写“Phase 1 已完成”。
2. 不得把 Day05 最小真实闭环夸大成“多 provider 已完成”或“成本产品化已完成”。
3. 本文件只做状态回填，不构成验收裁定结论。
