# Day05：Provider 抽象层与执行路由后端主链

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`provider-gateway-and-executor-routing-backend`
- 当前状态：**已完成（工作包级验证回填）**
- owner skill：`verify-v5-runtime-and-regression`
- 文档定位：**验证记录（2026-04-14 最新回填）**

---

## 1. 本次回填采用的验证来源

1. 代码路径核对（限定文件）：`config.py`、`provider_config_service.py`、`executor_service.py`、`openai_provider_executor_service.py`、`mock_provider_executor_service.py`、`task_worker.py`。
2. 最新运行日志证据：
   - 成功样本：`runtime/data/logs/task-runs/6ed62fb9-b17a-4d97-af99-187be926d61b/370c6cbb-7321-4181-9bfc-2527835c20a7.jsonl`
   - 失败回退样本：`runtime/data/logs/task-runs/d9d9a456-515a-4466-bc75-ff80e88f42a6/3f797d15-3229-44f1-b422-e0ad7abbd830.jsonl`
3. 运行库证据：`runtime/data/db/orchestrator.db`（`runs` 表中 `run_id=370c6cbb...` 已落盘 `token_accounting_mode=provider_openai`、`token_pricing_source=openai.chat_completions.usage`）。

## 2. 四类证据回填

1. 真实 provider 成功证据：`已完成`
   - 事实：`model_name=gpt-5.4`、`actual_execution_mode=provider_openai`、`fallback_applied=false`、`provider_receipt_source=real_provider`、`token_pricing_source=openai.chat_completions.usage`。
2. 真实 provider 失败但命中路径证据：`已完成`
   - 事实：`fallback_reason_category=endpoint_forbidden`，`fallback_from=provider`，`fallback_to=provider_mock`。
3. `provider_mock` 证据：`已完成`
   - 事实：失败回退样本最终落到 `provider_mock`，并产生 mock receipt。
4. heuristic fallback 证据：`已完成`
   - 事实：`simulate` 路径样本中 `actual_execution_mode=simulate`，且 fallback 字段可观察。

## 3. 关键观察

1. Day05 已具备真实 provider 成功运行证据，不再属于“仅代码路径成立”。
2. Day05 同时具备真实 provider 失败回退证据，说明安全回退链在真实链路中可观察。
3. `provider_mock / simulate / shell` 与真实 provider 路径并存，边界可区分。

## 4. 结论边界

1. 可确认：`真实 provider 最小调用闭环 + 真实成功样本 + 结构化回退观测` 已成立。
2. 不可外推：`Phase 1 已完成`、`多 provider 矩阵已完成`、`成本产品化已完成`。
3. 本记录不做验收裁定，仅作为 Day05/Day06 正式回填输入。
