# Day06：Prompt Registry 与 Token Accounting 后端落地

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`prompt-registry-and-token-accounting-backend`
- 当前状态：**已完成**
- owner skill：`accept-v5-milestone-gate`
- 文档定位：**交接记录 / Day06 正式裁定回填**

---

## 1. 交接摘要

- 当前状态：`已完成`
- 正式裁定：`Pass`
- 当前日包：`Day06`
- 当前已确认事实：`Prompt Registry / Prompt Builder / Token Accounting 已接入 worker 主链；receipt 语义已统一到 provider_reported / heuristic 口径；Run 持久化与 worker 返回已留下 Day07 可消费字段。`
- 下一线程 owner：`drive-v5-orchestrator-delivery（Day07）`

## 2. 已留下的交接件

- [x] Prompt Registry / Prompt Builder 最小主链证据
- [x] Run 持久化中的 prompt / token / receipt 关键字段
- [x] `provider_mock` / `shell` / `simulate` 三路径统一语义证据
- [x] Day07 可消费字段合同：`provider_key`、`prompt_template_key`、`prompt_template_version`、`prompt_char_count`、`token_accounting_mode`、`provider_receipt_id`、`token_pricing_source`
- [ ] 真实外部 provider 成本闭环证据
- [ ] 真实 provider receipt 对账证据

## 3. Day07 开始前必须先确认

1. Day06 已 Pass，但 **只代表 Day06 工作包完成**，不代表 Phase 1 完成。
2. Day07 只能消费 Day06 已冻结的字段合同做控制面贯通，不能反写成 Day06 已完成跨层联调。
3. 后续若补真实 provider 证据，应作为更高层验证/验收输入处理，不能倒灌改写 Day06 的 Pass 边界。

## 4. Day06 未完成但不阻断 Pass 的边界

1. 真实外部 provider 成本闭环尚未完成。
2. 真实 provider receipt 对账证据尚未完成。
3. mock provider 证据不能等同于真实 provider 全闭环。
4. Day07 控制面贯通与 Day08 Phase 1 gate 不属于 Day06。
5. Phase 1 绝不能因此写成已完成。

## 5. 交接备注

- 启动入口：
  1. `docs/01-版本冻结计划/V5/02-模块B-Provider、Prompt、Token与记忆主链/01-逐日执行包/Day06-PromptRegistry与TokenAccounting后端落地/README.md`
  2. `docs/01-版本冻结计划/V5/02-模块B-Provider、Prompt、Token与记忆主链/01-逐日执行包/Day06-PromptRegistry与TokenAccounting后端落地/03-验证记录模板.md`
  3. `docs/01-版本冻结计划/V5/02-模块B-Provider、Prompt、Token与记忆主链/01-逐日执行包/Day07-RoleModelPolicy与MemoryRecall控制面贯通/README.md`
- 关键证据关键词：
  - `task_execution.default`
  - `provider_reported`
  - `heuristic.shell.char_count.v1`
  - `heuristic.simulate.char_count.v1`
  - `mock_provider.receipt.v1`
- 备注：`本轮只完成 Day06 裁定与收口，不进入 Day07。`
