# Day06：Prompt Registry 与 Token Accounting v1 后端落地

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`prompt-registry-and-token-accounting-backend`
- 当前状态：**已完成（工作包级回填）**
- owner skill：`manage-v5-plan-and-freeze-docs`
- 文档定位：**状态回填（2026-04-14 最新回填）**

---

## 1. 回填说明

- 回填日期：`2026-04-14`
- 本线程边界：只回填状态/证据/交接边界，不做实现，不做验收裁定。

## 2. 结论分层（按现实证据）

1. `真实 / mock / heuristic 三类口径分离`：`已完成`
   - 本次正式回填口径：`provider_openai / provider_mock / heuristic`。
   - 证据：
     - 真实样本：`runtime/data/db/orchestrator.db` 中 `run_id=370c6cbb...`，`token_accounting_mode=provider_openai`。
     - mock 样本：`run_id=3f797d15...`，`token_accounting_mode=provider_mock`。
     - heuristic 样本：`runtime/orchestrator/tmp/day06-riskfix-fallback-smoke/.../6191c1d4...jsonl`，`accounting_mode=heuristic`。
2. `fallback 结构化可观察`：`已完成`
   - 证据：`task_worker.py` 的 `execution_finished` / `run_finalized` 日志字段包含 `actual_execution_mode`、`fallback_applied`、`fallback_reason_category`、`fallback_from`、`fallback_to`。
3. `run/log 可区分真实 provider 与 mock/fallback`：`已完成`
   - 证据：
     - 真实成功样本：`actual_execution_mode=provider_openai`、`fallback_applied=false`。
     - 失败回退样本：`actual_execution_mode=provider_mock`、`fallback_applied=true`、`fallback_reason_category=endpoint_forbidden`。
4. `token/pricing source 真实 provider 成功证据`：`已完成`
   - 证据：真实成功样本含 `token_pricing_source=openai.chat_completions.usage`。
5. `旧口径兼容说明`：`已完成`
   - 事实：`prompt_contract.py` 与 `token_accounting_service.py` 仍可见历史口径 `provider_reported`；历史 smoke 与旧日志仍有该值。
   - 本次正式文档按“最新运行真值优先”回填，不再机械沿用旧文档词。
6. `外部账单对账`：`已实现待验证`
   - 缺证：当前仍缺正式外部账单对账材料。

## 3. 四类证据边界（强制区分）

1. 真实 provider 成功证据：`已完成`
2. 真实 provider 失败但命中路径证据：`已完成`
3. `provider_mock` 证据：`已完成`
4. heuristic 证据：`已完成`

## 4. Day07 启动影响

1. 以 Day06 字段合同推进 Day07 控制面/回显：`已完成`
2. 以“真实 provider 运行证据存在”作为 Day07/Day08 输入：`已完成`
3. 以“外部账单对账已完成”作为 Day07/Day08 输入：`阻塞`

## 5. 防误导声明

1. 不得把 Day05/Day06 局部补债写成“Phase 1 已完成”。
2. 不得把当前最小真实闭环写成“多 provider 矩阵已完成”。
3. 不得把当前证据写成“成本产品化体系已完成”。
