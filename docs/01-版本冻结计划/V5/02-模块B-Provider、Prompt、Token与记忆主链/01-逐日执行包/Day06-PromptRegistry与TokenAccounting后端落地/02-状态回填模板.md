# Day06：Prompt Registry 与 Token Accounting v1 后端落地

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`prompt-registry-and-token-accounting-backend`
- 当前状态：**已完成**
- owner skill：`write-v5-runtime-backend`
- 文档定位：**状态回填 / 正式裁定回填**

---

## 1. 基本信息

- 回填日期：`2026-04-12`
- 当前状态：`已完成`
- 正式裁定：`Pass`
- 实际执行线程 / 依据：`Day06 既有实现 + 2026-04-12 最小运行验证 + Day06 Step11 receipt 语义统一独立验证`
- 回填人 / owner skill：`accept-v5-milestone-gate`

## 2. 完成定义核对

- [x] `prompt_registry_service.py`、`prompt_builder_service.py`、`token_accounting_service.py` 已形成最小后端主链，而不是只停留在 schema。
- [x] `TokenAccountingService.build_snapshot(...)` 已成为 provider / non-provider 的统一归一化入口，能够写下 receipt 或 fallback 记录。
- [x] Run 持久化已留下 `prompt_template_key`、`prompt_template_version`、`prompt_char_count`、`token_accounting_mode`、`provider_receipt_id`、`token_pricing_source`、`prompt_tokens`、`completion_tokens`、`total_tokens`、`estimated_cost` 等最小证据。
- [x] Day07 可直接消费的字段合同已经落到 worker 返回与 run 持久化口径。

## 3. 非完成定义排雷

- [x] 未出现：只有 prompt template schema，没有 builder / render 主链。
- [x] 未出现：只有 token 表字段，没有 receipt / fallback 记录链路。
- [x] 未出现：仍完全停留在字符数估算却宣称 receipt 已完成。
- [x] 未出现：没有给 Day07 留下可回显、可观察的接口合同。
- [x] 未出现：把 mock provider 证据写成真实外部 provider 成本闭环完成。

## 4. 产出与证据

- [x] Prompt Registry / Prompt Builder / Token Accounting 合同骨架与运行主链接线
- [x] worker 日志主链与 Run 持久化字段回填
- [x] provider_reported / heuristic 两类口径统一到 receipt 语义
- [x] Day07 可消费字段合同
- 证据摘要：
  1. `runtime/orchestrator/app/services/prompt_registry_service.py`
  2. `runtime/orchestrator/app/services/prompt_builder_service.py`
  3. `runtime/orchestrator/app/services/token_accounting_service.py`
  4. `runtime/orchestrator/app/services/executor_service.py`
  5. `runtime/orchestrator/app/services/cost_estimator_service.py`
  6. `runtime/orchestrator/app/workers/task_worker.py`
  7. `runtime/orchestrator/app/api/routes/workers.py`
  8. `runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`
  9. `runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`
  10. `03-验证记录模板.md` 中 `2026-04-12` 的最小运行验证记录

## 5. 风险 / 缺口 / 阻塞

- 当前风险：Day07 以后若把 mock provider receipt 当成真实 provider 成本闭环，会误判 Phase 1 完成度。
- 当前缺口：
  1. 真实外部 provider 成本闭环未完成。
  2. 真实 provider receipt 对账证据未完成。
  3. Day07 控制面贯通与 Day08 Phase 1 gate 仍未开始正式交付。
- 是否阻塞下一 Day：`否`
- 不阻断 Pass 的原因：Day06 合同收口的是 prompt/token 后端主链、receipt/fallback 统一语义、Run 持久化与 Day07 消费字段；真实外部 provider 成本闭环与真实 receipt 对账证据不在 Day06 当前工作包完成定义内。

## 6. 下一步接力

- 下一线程 owner：`drive-v5-orchestrator-delivery（Day07）`
- 第二顺位：`verify-v5-runtime-and-regression`
- 启动前先读：
  1. `README.md`
  2. `03-验证记录模板.md`
  3. `04-交接模板.md`
  4. `docs/01-版本冻结计划/V5/02-模块B-Provider、Prompt、Token与记忆主链/01-逐日执行包/Day07-RoleModelPolicy与MemoryRecall控制面贯通/README.md`
- 当前不要误判为完成的点：
  1. 不能写成“真实外部 provider 成本闭环已完成”。
  2. 不能写成“真实 provider receipt 对账证据已完成”。
  3. 不能把 mock provider 证据等同于真实 provider 全闭环。
  4. 不能把 Phase 1 写成已完成。
  5. 本轮只完成 Day06 裁定与收口，不进入 Day07。
