# Day06：Prompt Registry 与 Token Accounting v1 后端落地

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`prompt-registry-and-token-accounting-backend`
- 当前状态：**已完成（工作包级验证回填）**
- owner skill：`verify-v5-runtime-and-regression`
- 文档定位：**验证记录（2026-04-14 最新回填）**

---

## 1. 本次回填采用的验证来源

1. 代码路径核对（限定文件）：`prompt_contract.py`、`token_accounting_service.py`、`cost_estimator_service.py`、`task_worker.py`、`executor_service.py`、`openai_provider_executor_service.py`。
2. 最新运行日志证据：
   - 真实成功样本：`runtime/data/logs/task-runs/6ed62fb9-b17a-4d97-af99-187be926d61b/370c6cbb-7321-4181-9bfc-2527835c20a7.jsonl`
   - 真实失败回退样本：`runtime/data/logs/task-runs/d9d9a456-515a-4466-bc75-ff80e88f42a6/3f797d15-3229-44f1-b422-e0ad7abbd830.jsonl`
3. 运行库证据：`runtime/data/db/orchestrator.db`（`runs` 表已落盘 `provider_openai`、`provider_mock`）。
4. 历史兼容口径证据：`runtime/orchestrator/tmp/day06-step9-provider-reported-verify/...jsonl` 仍可见 `provider_reported`。

## 2. 关键验证回填

1. `provider_openai / provider_mock / heuristic` 三类口径：`已完成`
   - 真实样本（`370c6cbb...`）为 `provider_openai`。
   - 回退样本（`3f797d15...`）为 `provider_mock`。
   - simulate 样本为 `heuristic`。
2. fallback 结构化可观察：`已完成`
   - 证据：日志字段可见 `actual_execution_mode`、`fallback_applied`、`fallback_reason_category`、`fallback_from`、`fallback_to`。
3. 真实 provider token/pricing source：`已完成`
   - 证据：真实成功样本 `token_pricing_source=openai.chat_completions.usage`。
4. 旧口径兼容存在：`已完成`
   - 证据：代码枚举/历史样本中仍存在 `provider_reported`，但本次正式口径已按最新运行真值回填。
5. 外部账单对账：`已实现待验证`
   - 缺证：未见正式账单对账记录。

## 3. 结论边界

1. 可确认：Day06 运行观测口径分离已具备真实样本支撑。
2. 不可确认：外部账单对账与更高层成本产品化完成。
3. 本记录不做验收裁定，仅作为后续线程输入。
