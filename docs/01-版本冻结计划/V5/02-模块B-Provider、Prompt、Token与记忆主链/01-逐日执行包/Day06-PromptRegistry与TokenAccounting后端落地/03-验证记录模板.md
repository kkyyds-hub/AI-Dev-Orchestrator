# Day06：Prompt Registry 与 Token Accounting v1 后端落地

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token 与记忆主链`
- 工作包：`prompt-registry-and-token-accounting-backend`
- 当前状态：**已完成**
- owner skill：`verify-v5-runtime-and-regression`
- 文档定位：**验证记录 / Day06 Pass 依据之一**

---

## 1. 本轮验证范围

1. 验证 prompt registry / builder 主链是否真实进入 worker 与 run 持久化。
2. 验证 `TokenAccountingService.build_snapshot(...)` 是否已统一 provider / non-provider 的 receipt 语义。
3. 验证 Day06 改动没有破坏 Day05 的 `provider_mock`、`shell`、`simulate` 三条最小路径。
4. 验证 Day07 需要消费的关键字段是否已冻结到 worker 返回与 run 持久化口径。

## 2. 实际执行记录

- 验证日期：`2026-04-12`
- 验证人 / owner skill：`verify-v5-runtime-and-regression`
- 验证环境：`Windows + runtime/orchestrator/.venv`

## 3. 证据明细

- 证据 1：执行  
  `runtime/orchestrator/.venv/Scripts/python.exe runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`  
  结果：通过。输出包含  
  `prompt_template_key=task_execution.default`、`prompt_template_version=day06.step1`、`prompt_char_count=664`、`token_accounting_mode=provider_reported`、`provider_receipt_id=mock-receipt-*`、`token_pricing_source=mock_provider.receipt.v1`，且 shell / simulate 路径仍可运行。
- 证据 2：执行  
  `runtime/orchestrator/.venv/Scripts/python.exe runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`  
  结果：通过。输出确认三种路径当前口径为：  
  `provider_mock -> provider_reported / openai / mock-receipt-* / mock_provider.receipt.v1`  
  `shell -> heuristic / null / heuristic.shell.char_count.v1`  
  `simulate -> heuristic / null / heuristic.simulate.char_count.v1`
- 证据 3：`runtime/orchestrator/app/api/routes/workers.py` 已暴露  
  `provider_key`、`prompt_template_key`、`prompt_template_version`、`prompt_char_count`、`token_accounting_mode`、`provider_receipt_id`、`token_pricing_source`  
  等字段，可作为 Day07 页面回显合同入口。
- 证据 4：运行产物已落到  
  `runtime/orchestrator/tmp/day06-step6-run-persistence/` 与  
  `runtime/orchestrator/tmp/day06-step10-receipt-semantics/`，其中 SQLite `runs` 记录与 JSONL 日志均可复核。
- 缺证说明：当前只验证到 Day06 的 receipt 语义统一、Run 持久化与 mock provider / heuristic 路径；没有真实外部 provider 成本闭环，也没有真实 provider receipt 对账证据。

## 4. 回归 / 风险观察

- 已确认：Day05 的 `provider_mock` 默认路径未被 Day06 改动破坏。
- 已确认：`shell:` / `simulate:` 显式前缀回归成立。
- 已确认：Run 持久化字段已从“仅 token 数值”提升为“prompt 合同 + token 语义 + receipt/fallback 来源”组合证据。
- 仍有限制：mock provider receipt 不能等同于真实 provider 全闭环；Phase 1 仍不能写成已完成。

## 5. 验证结论

- 验证结论：`通过（限 Day06 工作包边界）`
- 是否允许继续作为 Day06 正式裁定输入：`允许`
- 需补充的验证：`真实外部 provider 成本闭环、真实 provider receipt 对账证据、Day07/Day08 跨层与 gate 级验证`
- 裁定引用边界：`本记录支撑 Day06 Pass；不代表真实 provider 成本闭环已完成，也不代表 Phase 1 已完成。`
