# Day07：Role Model Policy、Memory Recall 与控制面贯通

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token与记忆主链`
- 工作包：`phase1-cross-layer-delivery`
- 当前状态：**进行中**
- owner skill：`drive-v5-orchestrator-delivery`
- 文档定位：**状态回填 / 正式回填**

---

## 1. 基本信息

- 回填日期：`2026-04-13`
- 回填线程：`Day07-Step11`
- 回填 owner skill：`manage-v5-plan-and-freeze-docs`
- 当前正式口径：`Day07-Step2/Step3/Step5/Step6/Step8/Step9/Step10 已有阶段性事实；当前只是 Day07 持续回填，不代表 Day07 已完成；当前仍在 Day07，不进入 Day08。`

## 2. 当前真实状态

- Day07-Step2 已完成：worker 默认执行主链已显式接入 `include_project_memory=True`。
- Day07-Step3 已完成：老板首页“最近一次手动执行”区域已新增 memory recall 最小回显接点。
- Day07-Step5 已完成：`/workers/run-once` 已形成 Provider / Prompt / Token 的后端 DTO 字段合同，并已映射到前端类型。
- Day07-Step6 已完成：首页“最近一次手动执行”区域已新增 `Provider / Prompt / Token` 最小回显卡片，真实消费 Step5 字段。
- Day07-Step8 新增验证事实：Day06 Step2 smoke 通过；Day06 Step6 / Step10 smoke 在当前环境失败，暂归因为环境问题。
- Day07-Step9 新增验证事实：Step8 失败项口径已统一为“环境阻塞优先”，当前不判定为代码回归。
- Day07-Step10 新增验证事实：Step8/9 事实已纳入 Day07 官方回填口径，但不改变 Day07 仍未完成结论。
- 当前仍不能宣告 Day07 完成：Role Model Policy 运行时闭环与更强联调证据尚未收口。

## 3. 已满足 / 未满足

### 已满足
- [x] worker 默认 memory recall 已成为正式执行主链的一部分。
- [x] 页面已有 memory recall 最小回显接点，不再只停留在后端字段。
- [x] Provider / Prompt / Token 字段合同已贯通到前端类型与最小展示卡片。
- [x] 已有编译级证据（`tsc -b` 通过）可证明 Step6 接线无类型错误。
- [x] Step8/9/10 的新增事实已完成统一口径冻结并回填到 Day07 官方文档。

### 仍未满足
- [ ] Role Model Policy 还未形成可宣告完成的运行时闭环。
- [ ] provider / prompt / token 还未完成完整老板入口控制面。
- [ ] Day07 仍缺浏览器联调与更强回归证据，不能上升为 Day08，也不能做 Pass / Partial / Blocked 裁定。

## 4. 本轮正式固化的证据

- Step2 运行证据：
  - `project_memory_enabled = true`
  - `project_memory_item_count = 0`
  - `project_memory_context_summary` 已真实返回降级摘要
- Step3 页面消费证据：
  - 首页“最近一次手动执行”区域新增 `Project Memory Recall` 卡片
  - 已接通字段：
    - `project_memory_enabled`
    - `project_memory_query_text`
    - `project_memory_item_count`
    - `project_memory_context_summary`
- Step5 合同证据：
  - `workers` DTO 与映射已包含
    - `provider_key`、`prompt_template_key`、`prompt_template_version`、`prompt_char_count`
    - `token_accounting_mode`、`provider_receipt_id`、`token_pricing_source`
    - `prompt_tokens`、`completion_tokens`、`total_tokens`
  - 前端 `WorkerRunOnceResponse` 类型已同步上述字段
- Step6 前端接点证据：
  - 首页“最近一次手动执行”区域新增 `Provider / Prompt / Token` 卡片
  - `tsc -b` 通过（编译级证据）
- Step8/9/10 回填证据：
  - `runtime/orchestrator/tmp/day07-step8-smoke-20260413-111529-356544/`
  - `runtime/orchestrator/tmp/day07-step8-io-probe/probe.txt`（`ok`）
  - `runtime/orchestrator/scripts/v5_day06_step2_prompt_token_smoke.py`
  - `runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`
  - `runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`

## 5. 阻塞与缺口（统一口径）

- 当前闭环仍是“最小回显闭环”，不是 Day07 全量联调闭环。
- 当前环境下 `apps/web` 的 `vite build` 存在阻塞：
  - `npm run build`：PowerShell 执行策略拦截 `npm.ps1`
  - `npm.cmd run build`：`vite/esbuild spawn EPERM`
- Step8 新增口径：Day06 Step6 / Step10 smoke 当前环境失败，先按环境问题跟踪；未形成代码回归定性。
- 因此当前以“合同接线 + 编译级证据 + 环境归因回填”为主，浏览器级证据仍待补强。

## 6. 下一棒交接

- 下一线程第一顺位：`drive-v5-orchestrator-delivery`
- 下一线程第二顺位：`verify-v5-runtime-and-regression`
- 下一棒目标：继续补足 Day07 剩余缺口或补强验证，不进入 Day08。
- 下一棒最小建议：
  1. 补 Role Model Policy 的运行时闭环证据。
  2. 补 provider / prompt / token 的页面联调证据（含浏览器可见性）。
  3. 处理 Step6 的 `vite build` 环境阻塞并回填验证结论。
  4. 复核 Step8 中 Day06 Step6/Step10 的环境阻塞是否已消除。

## 7. 明确不这样写

- 不把 Day07 写成已完成。
- 不把 Step2 / Step3 / Step5 / Step6 / Step8 / Step9 / Step10 的局部证据外推成 Day07 全量完成。
- 不把当前线程写成已进入 Day08。
- 不在本轮文档里做 Pass / Partial / Blocked 裁定。
