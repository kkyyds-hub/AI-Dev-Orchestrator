# Day07：Role Model Policy、Memory Recall 与控制面贯通

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token与记忆主链`
- 工作包：`phase1-cross-layer-delivery`
- 当前状态：**进行中**
- owner skill：`drive-v5-orchestrator-delivery`
- 文档定位：**验证记录 / 正式回填**

---

## 1. 验证基线

- 验证日期：`2026-04-13`
- 回填线程：`Day07-Step11`
- 本次固化范围：`在 Step7 事实基础上，补录 Step8 / Step9 / Step10 新事实与口径一致性`
- 当前正式口径：`当前只是 Day07 的继续回填，不代表 Day07 已完成；当前仍在 Day07，不进入 Day08。`

## 2. 已固化的验证结果

### Step2：worker 默认 recall 主链
- `py_compile` 已通过。
- 最小 smoke 已证明 worker 默认执行主链真实尝试 recall。
- 已记录降级路径证据：
  - `project_memory_enabled = true`
  - `project_memory_item_count = 0`
  - `project_memory_context_summary` 返回降级摘要

### Step3：memory recall 最小前端回显接点
- 后端 `/workers/run-once` 已真实返回以下字段：
  - `project_memory_enabled`
  - `project_memory_query_text`
  - `project_memory_item_count`
  - `project_memory_context_summary`
- 前端首页“最近一次手动执行”区域已真实消费这些字段，并展示 `Project Memory Recall` 卡片。

### Step5：Provider / Prompt / Token 合同接线
- `/workers/run-once` DTO 与 `from_result` 映射已包含：
  - `provider_key`
  - `prompt_template_key`
  - `prompt_template_version`
  - `prompt_char_count`
  - `token_accounting_mode`
  - `provider_receipt_id`
  - `token_pricing_source`
  - `prompt_tokens` / `completion_tokens` / `total_tokens`
- 前端 `WorkerRunOnceResponse` 类型已同步上述字段。
- 运行级上游证据沿用 Day06：`v5_day06_step6_run_persistence_smoke.py`。

### Step6：Provider / Prompt / Token 最小前端回显接点
- 首页“最近一次手动执行”区域新增 `Provider / Prompt / Token` 卡片，已消费 Step5 字段。
- 已执行：`node .\node_modules\typescript\bin\tsc -b`，结果通过。
- 构建阻塞（环境口径）：
  - `npm run build` 失败：PowerShell 执行策略拦截 `npm.ps1`
  - `npm.cmd run build` 失败：`vite/esbuild spawn EPERM`
- 结论：当前为“编译级证据已成立，完整 vite build 受环境阻塞”。

### Step8：新增 smoke 事实
- Day06 Step2 smoke：通过。
- Day06 Step6 / Step10 smoke：在当前环境失败。
- 当前归因：优先按环境问题记录，暂不直接定性为代码回归。

### Step9：归因口径冻结
- 已将 Step8 中失败项统一归并为“环境阻塞待消除”。
- 与 Day06 历史通过记录不冲突：本次是 `2026-04-13` 当前环境复验口径，仍需后续消阻后复测。

### Step10：回填口径冻结
- Step8/9 新事实已纳入 Day07 官方文档口径。
- 该动作只属于 Day07 持续回填，不构成 Day07 完成，也不触发进入 Day08。

## 3. 证据清单

- 后端主链接线文件：
  - `runtime/orchestrator/app/workers/task_worker.py`
  - `runtime/orchestrator/app/services/context_builder_service.py`
  - `runtime/orchestrator/app/api/routes/workers.py`
- 前端消费文件：
  - `apps/web/src/features/task-actions/types.ts`
  - `apps/web/src/features/task-actions/WorkerMemoryRecallCard.tsx`
  - `apps/web/src/features/task-actions/WorkerProviderPromptTokenCard.tsx`
  - `apps/web/src/app/App.tsx`
- 验证入口与脚本：
  - `POST /workers/run-once`
  - `apps/web -> node .\node_modules\typescript\bin\tsc -b`
  - `runtime/orchestrator/scripts/v5_day06_step2_prompt_token_smoke.py`
  - `runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`
  - `runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`
- Step8 现场产物：
  - `runtime/orchestrator/tmp/day07-step8-smoke-20260413-111529-356544/`
  - `runtime/orchestrator/tmp/day07-step8-io-probe/probe.txt`

## 4. 当前缺证 / 阻塞说明（统一口径）

- 浏览器截图级联调证据本轮未固化进正式文档。
- Step6 的完整 `vite build` 在当前环境受阻（执行策略 + `spawn EPERM`）。
- Step8 已补 Day06 Step2/6/10 复验事实，但 Step6/10 仍处于环境阻塞状态。
- 当前已具备 API 合同、页面接线、TypeScript 编译三类证据；但这仍只是 Day07 局部证据，不是 Day07 全量完成证据。

## 5. 验证结论

- 结论：`部分通过`
- 允许交给下一线程：`是，但仅限继续推进 Day07 剩余缺口或补强验证`
- 不允许据此宣告：
  - `Day07 已完成`
  - `已进入 Day08`
  - `Phase 1 已完成`
