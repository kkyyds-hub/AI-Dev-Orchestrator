# Day07：Role Model Policy、Memory Recall 与控制面贯通

- 版本：`V5`
- Phase：`Phase 1`
- 模块：`模块B：Provider、Prompt、Token与记忆主链`
- 工作包：`phase1-cross-layer-delivery`
- 当前状态：**进行中**
- owner skill：`drive-v5-orchestrator-delivery`
- 文档定位：**验证记录 / 正式回填**

---

## 1. 验证基线

- 验证日期：`2026-04-14`
- 回填线程：`Day07-持续推进-RolePolicyRuntime闭环与控制面补强`
- 本次固化范围：`补 workers project scope 控制面、补 tasks/{id}/runs 一致性断言、补 no-routable 浏览器录屏样本、补 degraded 读模型空运行断言、复核并固化构建与 Day06/Day07 smoke`
- 当前正式口径：`当前仍在 Day07，不进入 Day08。`

## 2. 已执行验证与结果

### A. 前端构建复核
- 命令：`apps/web -> node .\node_modules\typescript\bin\tsc -b`
- 结果：通过
- 命令：`apps/web -> npm.cmd run build`
- 结果：通过（Vite 构建完成，产物输出到 `dist/`）

### B. Day06 Step6 / Step10 smoke 复核
- 命令：`python runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`
- 结果：通过
- 关键证据：
  - `provider_key=openai`
  - `prompt_template_key=task_execution.default`
  - `token_accounting_mode=provider_reported`
  - `token_pricing_source=mock_provider.receipt.v1`
- 命令：`python runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`
- 结果：通过
- 关键证据：provider / shell / simulate 三路径 receipt/accounting 语义符合预期

### C. 策略主链回归
- 命令：`python runtime/orchestrator/scripts/v3d_day15_strategy_engine_smoke.py`
- 结果：通过
- 关键证据：
  - `strategy_reasons` 含 `role_model_policy_runtime`
  - override 与预算压力切换路径保持可观测
  - worker 路径可持久化 `strategy_code/model_tier`

### D. Day07 既有闭环 smoke
- 命令：`python runtime/orchestrator/scripts/v5_day07_role_model_policy_control_surface_smoke.py`
- 结果：通过
- 校验点：
  - `/strategy/projects/{id}/preview` 返回 `role_model_policy_runtime`
  - `/workers/run-once`（含可选 `project_id` 作用域）返回 role policy runtime + provider/prompt/token 字段
  - `/console/project-overview` 的 `latest_task` 返回增强字段（含 `prompt_char_count/estimated_cost/created_at/finished_at`）
  - worker `run_created_at/run_finished_at` 与 console `latest_run_created_at/latest_run_finished_at` 映射一致
  - preview 与 worker 的 `selected_task_id / role_model_policy_source / role_model_policy_final_tier` 保持一致
  - `/tasks/console` 与 `/tasks/{id}/detail` 的 latest run 与 worker run 保持一致（`run_id/status/created_at/finished_at`），且 `strategy_decision.role_model_policy_source` 一致
  - `/tasks/{id}/runs` 的 latest run 与 worker run 保持一致（`run_id/status/created_at/finished_at/provider/prompt/token/role policy`）
  - `/tasks/console` 与 `/tasks/{id}/detail` 的 latest run 新增字段映射一致：
    - `provider_key / prompt_template_key / token_accounting_mode / total_tokens`
    - 顶层 `role_model_policy_source / role_model_policy_final_tier`
- 命令：`python runtime/orchestrator/scripts/v5_day07_role_model_policy_boundary_smoke.py`
- 结果：通过
- 校验点：
  - `stage_override` / `role_preference` / `budget_fallback` / `critical` 四类路径稳定
  - preview -> worker -> console 的 runtime source 一致
- 本线程补充实测（`2026-04-14`，仅补 Day07 边界回归口径）：
  - `stage_override`：`source=stage_override`，`desired/adjusted/final=premium/premium/premium`，`stage_override_applied=true`
  - `role_preference`：`source=role_preference`，`desired/adjusted/final=balanced/balanced/balanced`，`stage_override_applied=false`
  - `budget_fallback`：preview `source=budget_fallback`，worker `source=budget_fallback` 且 `final_tier=balanced`，console latest run `source=budget_fallback`
  - `critical`：`budget_pressure_level=critical` 下保持 `source=stage_override`，且 `desired/adjusted/final=premium/economy/economy`（临界预算降档成立）

### E. 本轮新增 Day07 degraded smoke
- 命令：`python runtime/orchestrator/scripts/v5_day07_control_surface_degraded_smoke.py`
- 结果：通过
- 校验点：
  - 无项目：`/console/project-overview` 返回 `total_projects=0`，`/workers/run-once` 稳定返回 no-pending；
  - 无可路由任务：preview/worker 返回 `none are currently routable`；
  - latest run 空字段：`latest_run_id/prompt_char_count/estimated_cost` 保持 `null`；
  - no-run/no-routable 场景下 `/tasks/console`、`/tasks/{id}/detail`、`/tasks/{id}/runs` 保持空运行记录一致；
  - 无 override / 无 role_preference / 非 critical 预算：`role_model_policy_runtime.source=budget_fallback`，且 preview -> worker -> console 一致。
- 本线程补充实测（`2026-04-14`，仅补 Day07 degraded/no-run/no-routable 正式回填）：
  - `no_project`：`total_projects=0`，worker 消息为 `No pending tasks available for routing.`
  - `no_run`：`latest_run_id/latest_run_prompt_char_count/latest_run_estimated_cost` 均为 `null`，且 `tasks_console.latest_run=null`、`task_detail.latest_run=null`、`tasks_runs_count=0`
  - `no_routable`：preview 与 worker 消息一致（均含 `none are currently routable`），且 `tasks_console.latest_run=null`、`task_detail.latest_run=null`、`tasks_runs_count=0`
  - `degraded(budget_fallback)`：preview `source=budget_fallback`（`desired/adjusted/final=balanced/balanced/balanced`），worker `source=budget_fallback`，console latest run `source=budget_fallback`

### F. 浏览器链路与截图证据
- 本机可复用浏览器路径检查：
  - `HKLM\\...\\App Paths\\msedge.exe` 指向 `C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe`（存在）。
- Playwright 安装链路复核（2026-04-14）：
  - 命令：`apps/web -> npm.cmd exec -- playwright install chromium`
  - 结果：通过
  - 命令：`apps/web -> npm.cmd exec -- playwright install chromium-headless-shell`
  - 结果：通过
  - 说明：先前 `ECONNRESET/timeout` 在本轮未复现。
- 本轮截图命令（均使用本机 `msedge` channel）：
  - `playwright screenshot --browser=chromium --channel=msedge --full-page http://127.0.0.1:5173/ ...01-home-manual-entry.png`
  - `playwright screenshot --browser=chromium --channel=msedge --full-page http://127.0.0.1:5173/#project-detail ...02-project-strategy-preview.png`
  - `playwright screenshot --browser=chromium --channel=msedge --full-page http://127.0.0.1:5173/#project-detail ...03-project-latest-run-control-surface.png`
  - `npx.cmd --prefix apps/web playwright test apps/web/scripts/day07_manual_run_card_evidence.spec.mjs --workers=1`
    - 运行前在本机临时拉起：
      - `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`（`runtime/orchestrator`）
      - 前端复用已在 `127.0.0.1:5173` 运行的 dev 服务（如未运行，可用 `npm.cmd run dev -- --host 127.0.0.1 --port 5173` 拉起）
    - 断言首页 `/` “最近一次手动执行”卡片内 `Run ID / 创建时间 / 结束时间 / Provider / Prompt / Token / Role Model Policy Runtime` 可见；
    - 断言首页任务列表 latest run 行内 `provider / prompt / accounting / role policy` 摘要可见；
    - 自动产出同场景时间序列证据：点击前 -> 过渡帧 -> 卡片可见 -> 稳定帧；
    - 自动产出同场景录屏：`10-home-manual-run-click-flow.webm`。
    - 新增 `budget_fallback` 任务类型样本（首页点击执行到结果卡片稳定）：
      - 断言 `Role Model Policy Runtime` 包含 `budget_fallback`；
      - 自动产出 `12~18` 号截图/录屏证据。
    - 新增 `no-routable` 任务类型样本（首页点击执行到结果卡片稳定）：
      - 断言结果卡片回显 `none are currently routable` 且状态为“未领取任务”；
      - 断言任务行保持“尚未运行”；
      - 自动产出 `19~25` 号截图/录屏证据。
- 截图产物：
  - `runtime/orchestrator/tmp/day07-browser-evidence/01-home-manual-entry.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/02-project-strategy-preview.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/03-project-latest-run-control-surface.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/04-home-manual-run-before-trigger.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/05-home-manual-run-result-card-after-trigger.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/06-home-manual-run-result-card-focus.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/07-home-manual-run-transition-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/08-home-manual-run-visible-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/09-home-manual-run-stable-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/10-home-manual-run-click-flow.webm`
  - `runtime/orchestrator/tmp/day07-browser-evidence/11-home-task-table-latest-run-contract.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/12-home-budget-fallback-before-trigger.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/13-home-budget-fallback-transition-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/14-home-budget-fallback-visible-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/15-home-budget-fallback-result-card-after-trigger.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/16-home-budget-fallback-stable-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/17-home-budget-fallback-task-row-contract.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/18-home-budget-fallback-click-flow.webm`
  - `runtime/orchestrator/tmp/day07-browser-evidence/19-home-no-routable-before-trigger.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/20-home-no-routable-transition-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/21-home-no-routable-visible-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/22-home-no-routable-result-card-after-trigger.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/23-home-no-routable-stable-frame.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/24-home-no-routable-task-row-no-run.png`
  - `runtime/orchestrator/tmp/day07-browser-evidence/25-home-no-routable-click-flow.webm`

## 3. 证据清单

- 运行时主链与策略闭环：
  - `runtime/orchestrator/app/domain/model_policy.py`
  - `runtime/orchestrator/app/services/model_routing_service.py`
  - `runtime/orchestrator/app/services/prompt_registry_service.py`
  - `runtime/orchestrator/app/services/prompt_builder_service.py`
  - `runtime/orchestrator/app/services/strategy_engine_service.py`
  - `runtime/orchestrator/app/domain/run.py`
  - `runtime/orchestrator/app/api/routes/strategy.py`
  - `runtime/orchestrator/app/api/routes/workers.py`
  - `runtime/orchestrator/app/workers/task_worker.py`
  - `runtime/orchestrator/app/api/routes/console.py`
  - `runtime/orchestrator/app/api/routes/tasks.py`
  - `runtime/orchestrator/app/repositories/task_repository.py`
- 前端控制面增强：
  - `apps/web/vite.config.ts`
  - `apps/web/src/features/task-actions/WorkerRoleModelPolicyCard.tsx`
  - `apps/web/src/features/projects/ProjectLatestRunControlSurface.tsx`
  - `apps/web/src/app/App.tsx`
  - `apps/web/src/features/projects/ProjectOverviewPage.tsx`
  - `apps/web/src/features/strategy/StrategyDecisionPanel.tsx`
  - `apps/web/src/features/console/types.ts`
  - `apps/web/src/features/strategy/types.ts`
  - `apps/web/src/features/task-actions/types.ts`
  - `apps/web/src/features/projects/types.ts`
  - `apps/web/scripts/day07_manual_run_card_evidence.spec.mjs`
- smoke 与构建验证入口：
  - `runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`
  - `runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`
  - `runtime/orchestrator/scripts/v3d_day15_strategy_engine_smoke.py`
  - `runtime/orchestrator/scripts/v5_day07_role_model_policy_control_surface_smoke.py`
  - `runtime/orchestrator/scripts/v5_day07_role_model_policy_boundary_smoke.py`
  - `runtime/orchestrator/scripts/v5_day07_control_surface_degraded_smoke.py`
  - `apps/web -> node .\node_modules\typescript\bin\tsc -b`
  - `apps/web -> npm.cmd run build`

## 4. 当前缺证 / 仍需补强

- 已有截图级证据，但 Day07 仍需继续扩展到更强的多入口控制面治理与持续回归覆盖。
- 当前结论仍是：Day07 持续回填中，不进入 Day08。

## 5. 本轮验证口径小结

- 已确认：前端构建通过、Day06/Day07 既有 smoke 通过、本轮新增 degraded 读模型断言通过、浏览器截图链路与截图产物已形成（含 no-routable 样本）。
- 同时保留：Day07 仍未收口为“可进入 Day08”的状态，后续继续补 Day07 范围内控制面深度与回归强度。

## 6. 2026-04-14 Day07 增量回填（双入口字段一致性最小切片）

- 范围：首页“最近一次手动执行”卡片 vs 项目详情页 `Latest Run Control Surface`。
- 单一缺口：同一组 `role_model_policy_*` 字段在两入口展示口径不一致（首页是 5 个独立字段，详情页是摘要拼接）。
- 代码变更：`apps/web/src/features/projects/ProjectLatestRunControlSurface.tsx`
  - 将 `Role Model Policy Runtime` 区块改为与首页一致的字段命名：`Source / Desired Tier / Adjusted Tier / Final Tier / Stage Override`。
  - 移除上方独立 `Stage Override` 卡片，避免同字段在不同区块重复且命名口径分散。
- 验证：`apps/web -> npm.cmd run build` 通过。
- 口径说明：本次只补 Day7 单一缺口，不进入 Day08。
## 7. 2026-04-14 Day07 增量回填（latest run 成本空值口径统一最小切片）

- 范围：首页“最近一次手动执行”卡片 vs 项目详情 `Latest Run Control Surface`，仅对 `estimated_cost` 做单点对齐。
- 单一缺口：首页在 `estimated_cost=null` 时显示 `$0`，项目详情显示 `n/a`，语义不一致（“未知/未返回”被误读为“真实为 0”）。
- 代码变更：`apps/web/src/app/App.tsx`
  - 将首页卡片 `estimated_cost` 展示从 `formatCurrencyUsd(estimated_cost ?? 0)` 调整为：`null -> n/a`，非空才格式化金额。
- 验证：`apps/web -> npm.cmd run build` 通过。
- 口径说明：本次只补 Day7 单一缺口，不进入 Day08。