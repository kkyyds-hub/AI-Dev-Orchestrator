# Day08：Phase 1 Smoke、回归与 Gate 输入（交接）

- 版本：`V5`
- Phase：`Phase 1`
- 工作包：`phase1-smoke-and-gate-input`
- 当前状态：`已完成`（Day08 工作包级）
- 证据等级：`E3`
- gate 输入包：`已闭合`
- 本线程边界：verify 收口与文档回填，不代做 accept 裁定
- 补充说明：`2026-04-17` 已追加 accept 正式裁定，见第 6 节

## 1. 交接摘要（2026-04-17）
1. Day08 固定顺序 8 个核心入口已全部复跑通过。
2. 之前已解的代码级断言保持通过，provider path `provider_reported` 与 shell/simulate `heuristic` 语义保持稳定。
3. Day08 三件套已完成闭环回填，材料可供下游 accept 线程直接消费。
4. 本线程不宣告 `Phase 1 已通过`，仅宣告 Day08 verify 材料闭合。

## 2. 本轮新增 verify 事实
1. `python runtime/orchestrator/scripts/v5_day06_step2_prompt_token_smoke.py`：通过。
2. `python runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`：通过。
3. `python runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`：通过。
4. `python runtime/orchestrator/scripts/v5_day07_role_model_policy_control_surface_smoke.py`：通过。
5. `python runtime/orchestrator/scripts/v5_day07_role_model_policy_boundary_smoke.py`：通过。
6. `python runtime/orchestrator/scripts/v5_day07_control_surface_degraded_smoke.py`：通过。
7. `$env:PYTHONUTF8='1'; python runtime/orchestrator/scripts/v5_day07_minimal_continuous_regression_pack.py --mode runtime`：通过。
8. `npm.cmd run build`（`apps/web`）：通过。

## 3. 沿用 Day07 既有证据（已复核继续消费）
1. same-sample / alias 对齐事实。
2. latest 索引、latest 批次与跨视图一致性事实。
3. Day07 role/memory/control-surface 最小贯通前提。

## 4. 风险与缺证
1. `R1 阻塞级`：无。
2. `R2 高风险级`：无。
3. `R3 观察级`：`npm.ps1` 可能阻塞默认 `npm run build`，但不阻断本轮代码事实（`npm.cmd run build` 已通过）。
4. 缺证说明：本轮无新增关键缺证项。

## 5. 下一棒 owner（verify 线程历史记录）
1. 第一顺位：`accept-v5-milestone-gate`
   - 输入：Day08 三件套 + Day05-Day07 正式材料 + 本轮复跑结果。
   - 任务：执行 Pass / Partial / Blocked 正式裁定，不得复用口头结论。
2. 第二顺位（仅当 accept 识别新增缺证或阻塞）：`verify-v5-runtime-and-regression`。

## 正式交接结论（verify）

Day08 verify 已收口到 E3，gate 输入闭合，下一棒转 `accept-v5-milestone-gate`。

## 6. 2026-04-17 正式 accept 裁定补充（`accept-v5-milestone-gate`）

### 6.1 验收归属与裁定层级
1. 验收归属：`Phase 1 / 模块B / Day08 / phase1-smoke-and-gate-input`。
2. 本轮裁定层级：仅 `Day08 工作包级`。
3. 本轮明确不裁定：`Phase 1 是否整体通过`、`Day09 是否完成`、`Day16 最终收口`。

### 6.2 依据材料与证据等级
1. Day03 统一口径：`Day08 输入边界 / E3 定义 / R1-R3 分级 / accept 消费边界`（Day03 README）。
2. Day08 正式材料：`README`、`02-状态回填模板.md`、`03-验证记录模板.md`、本文件。
3. 根口径文档：`00-V5正式冻结执行计划.md`、`00-V5总纲.md`、`00-V5总览.md`。
4. 最小抽查：`runtime/orchestrator/app/services/token_accounting_service.py`、`runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`，未见与 verify 结论冲突。
5. 证据等级：`E3`；风险分级：`R1=无`、`R2=无`、`R3=npm.ps1 策略观察项`。

### 6.3 正式裁定结论
1. `Day08 工作包：Pass`。
2. Day08 对 Phase 1 gate 的影响：`gate 输入已闭合，已具备正式消费条件`。
3. 非本轮结论：以上不自动推出 `Phase 1 已通过`、`V5 已通过` 或 `Day16 可直接 Pass`。

### 6.4 范围确认
1. 能确认完成：
   - Day08 固定顺序 8 个核心入口复跑全部通过。
   - Day08 三件套已形成可消费的 E3 材料包（smoke、回归、风险、缺证说明）。
   - provider path `provider_reported` 与 shell/simulate `heuristic` 语义一致性保持。
2. 不能确认完成：
   - Phase 1 的阶段级最终通过。
   - Day09 及后续工作包的实现与验证完成度。
   - Day16 级最终裁定与 V5 最终收口。

### 6.5 遗留项 / 观察项 / 缺证项
1. 遗留项：进入 Day09（`memory-governance-backend-core`）后续主链实现与验证。
2. 观察项（`R3`）：
   - `npm.ps1` 执行策略可能影响默认 `npm run build`，当前以 `npm.cmd run build` 作为有效事实入口。
   - Day08 `README/01-执行说明` 仍保留规划态措辞，不影响 Day08 三件套与根文档的正式消费边界，建议后续文档治理线程择机统一。
3. 缺证项：本轮未识别会阻断 Day08 工作包裁定的新缺证项。

### 6.6 下一棒 owner
1. 第一顺位：`write-v5-runtime-backend`（启动 Day09：`memory-governance-backend-core`）。
2. 若 Day09 前出现新增证据冲突：回退 `verify-v5-runtime-and-regression` 做最小补证，不回滚本轮 Day08 裁定结论。

## 正式裁定交接结论（accept）

Day08 工作包 Pass；Phase 1 gate 输入已具备正式消费条件；下一棒转 `write-v5-runtime-backend`（Day09）。
