# Day08：Phase 1 Smoke、回归与 Gate 输入（状态回填）

- 回填日期：`2026-04-17`
- 当前状态：`已完成`
- owner skill：`verify-v5-runtime-and-regression`（本线程：Day08 verify 收口）
- 下一棒建议：`accept-v5-milestone-gate`
- 证据等级：`E3`（闭环证据）

## 本轮边界
1. 本轮仅做 Day08 verify 收口与文档回填，不做实现补丁，不做 accept 裁定。
2. 输入边界继承 Day03：仅消费 Day05-Day07 正式 README、状态回填、验证记录、交接件与真实 smoke/回归证据。
3. `npm run build` 的 `npm.ps1` 策略阻塞仅记环境备注；代码事实以 `npm.cmd run build` 为准。

## Day08 固定顺序核心复跑（2026-04-17）

| 序号 | 命令 | 结果 | 与上一棒相比 | 失败类型 |
| --- | --- | --- | --- | --- |
| 1 | `python runtime/orchestrator/scripts/v5_day06_step2_prompt_token_smoke.py` | 通过 | 保持通过 | - |
| 2 | `python runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py` | 通过 | 保持通过 | - |
| 3 | `python runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py` | 通过 | 保持通过 | - |
| 4 | `python runtime/orchestrator/scripts/v5_day07_role_model_policy_control_surface_smoke.py` | 通过 | 保持通过 | - |
| 5 | `python runtime/orchestrator/scripts/v5_day07_role_model_policy_boundary_smoke.py` | 通过 | 无变化（上一棒未在最小复跑中单列） | - |
| 6 | `python runtime/orchestrator/scripts/v5_day07_control_surface_degraded_smoke.py` | 通过 | 无变化（上一棒未在最小复跑中单列） | - |
| 7 | `$env:PYTHONUTF8='1'; python runtime/orchestrator/scripts/v5_day07_minimal_continuous_regression_pack.py --mode runtime` | 通过 | 保持通过 | - |
| 8 | `npm.cmd run build`（`apps/web`） | 通过 | 无变化（上一棒未在 Day08 最小复跑中单列） | - |

## 代码级断言保持检查
1. Step6 断言 `token_accounting_mode was not upgraded to provider_reported` 保持通过。
2. Step10 断言 `provider path should persist provider_reported accounting mode` 保持通过。
3. Day07 control-surface 断言 `worker response should expose token accounting mode` 保持通过。
4. shell / simulate 语义保持 `heuristic`，未见误伤。

## E / R 分级（继承 Day03 口径）
- `E3 闭环证据`：8 个核心入口全部通过，且已形成 smoke 结果、回归清单、缺证说明、风险分级、可供下游消费的 gate 输入说明。
- `R1 阻塞级`：无（关键入口无失败）。
- `R2 高风险级`：无（当前无阻断 accept 消费的高风险缺证项）。
- `R3 观察级`：保留环境策略备注，`npm run build` 可能受 `npm.ps1` 策略影响；本轮有效事实入口 `npm.cmd run build` 已通过。

## Gate 输入包闭合状态
- smoke 日志/结果：已具备。
- 回归清单：已具备。
- 缺证说明：已具备（本轮无新增缺证，且沿用 Day07 既有 same-sample/alias 证据继续可消费）。
- 风险分级：已具备（R1/R2 清零，R3 保留环境策略备注）。
- 可供下游消费说明：已具备（可直接供 `accept-v5-milestone-gate` 消费）。
- 闭合结论：`已闭合`。

## 证据来源拆分（Day07 沿用 vs Day08 新增）
1. 沿用 Day07 既有证据（已复核可继续消费）：same-sample/alias 对齐事实、Day07 latest 索引与批次链路事实。
2. Day08 本轮新增 verify 事实：固定顺序 1-8 核心复跑全部通过；`npm.cmd run build` 通过；Day08 三件套完成 E3 闭环回填。

## 下一棒 owner 路由
1. 第一顺位：`accept-v5-milestone-gate`（消费 Day08 gate 输入包，执行 Pass / Partial / Blocked 裁定）。
2. 第二顺位（仅当 accept 识别新增缺证或阻塞）：`verify-v5-runtime-and-regression`。

## 正式结论

Day08 verify 已收口到 E3，gate 输入闭合，下一棒转 `accept-v5-milestone-gate`。
