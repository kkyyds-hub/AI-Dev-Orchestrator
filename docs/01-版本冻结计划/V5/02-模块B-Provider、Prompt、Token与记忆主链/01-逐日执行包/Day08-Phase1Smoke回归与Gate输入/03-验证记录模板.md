# Day08：Phase 1 Smoke、回归与 Gate 输入（验证记录）

- 验证日期：`2026-04-17`
- 验证线程：`V5 Day08 verify 收口线程`
- owner skill：`verify-v5-runtime-and-regression`
- 当前状态：`已完成`（Day08 工作包级）
- 结论边界：本记录用于 Day08 verify 与 gate 输入闭合，不代替 accept 做 Pass / Partial / Blocked 裁定

## 1. 验证范围与口径
1. 本轮按 Day08 固定顺序执行 8 个核心入口，覆盖后端 smoke、回归 pack 与前端 build 事实入口。
2. 本轮不补实现，不重开 Day07 长历史，不把 Day07 既有证据改写为 Day08 新证据。
3. Day03 口径绑定：
   - Day08 要写 `已完成` 必须达到 `E3`；
   - 风险按 `R1/R2/R3` 分级；
   - gate 输入仅消费 Day05-Day07 正式材料与真实运行证据。

## 2. 固定顺序复跑结果

| 序号 | 命令 | 结果 | 与上一棒相比 | 失败类型 | 关键事实 |
| --- | --- | --- | --- | --- | --- |
| 1 | `python runtime/orchestrator/scripts/v5_day06_step2_prompt_token_smoke.py` | 通过 | 保持通过 | - | `token_snapshot.accounting_mode=heuristic` |
| 2 | `python runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py` | 通过 | 保持通过 | - | provider run record 持久化 `token_accounting_mode=provider_reported` |
| 3 | `python runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py` | 通过 | 保持通过 | - | provider=`provider_reported`；shell/simulate=`heuristic` |
| 4 | `python runtime/orchestrator/scripts/v5_day07_role_model_policy_control_surface_smoke.py` | 通过 | 保持通过 | - | worker / latest_run / task_views 一致回显 `provider_reported` |
| 5 | `python runtime/orchestrator/scripts/v5_day07_role_model_policy_boundary_smoke.py` | 通过 | 无变化（上一棒未单列） | - | `stage_override`、`role_preference`、`budget_fallback`、`critical_budget` 四路径均通过 |
| 6 | `python runtime/orchestrator/scripts/v5_day07_control_surface_degraded_smoke.py` | 通过 | 无变化（上一棒未单列） | - | no project / no run / no routable / budget fallback 退化路径均通过 |
| 7 | `$env:PYTHONUTF8='1'; python runtime/orchestrator/scripts/v5_day07_minimal_continuous_regression_pack.py --mode runtime` | 通过 | 保持通过 | - | pack 三项 smoke 全部 passed，`overall_status=passed` |
| 8 | `npm.cmd run build`（`apps/web`） | 通过 | 无变化（上一棒未单列） | - | `tsc -b && vite build` 成功，产物生成完成 |

## 3. 已解断言保持核对
1. Step6 历史断言失败点已保持通过：`token_accounting_mode was not upgraded to provider_reported`。
2. Step10 历史断言失败点已保持通过：`provider path should persist provider_reported accounting mode`。
3. Day07 control-surface 历史断言失败点已保持通过：`worker response should expose token accounting mode`。
4. runtime minimal pack 首项历史失败点 `role_model_policy_control_surface_mainline` 保持通过。

## 4. 失败/阻塞记录
1. 本轮 8 个核心入口均通过。
2. 无代码级断言失败、无脚本入口失败、无环境权限/路径阻塞。
3. `npm.ps1` 策略问题仅保留为环境策略备注，不作为代码回归依据；本轮 build 事实入口是 `npm.cmd run build`（已通过）。

## 5. E / R 分级与 gate 输入闭合判定
1. `E3`：满足 Day08 闭环证据要求（smoke 结果、回归清单、缺证说明、风险分级、可消费 gate 输入说明齐全）。
2. `R1`：无。
3. `R2`：无。
4. `R3`：环境策略观察项（`npm.ps1` 可能阻塞默认命令），不阻断当前 gate 输入消费。
5. gate 输入包：`已闭合`。

## 6. Day07 沿用证据与 Day08 新增事实
1. Day07 沿用证据（已复核、继续消费）：
   - same-sample / alias 对齐事实；
   - latest 索引、latest 批次与跨视图一致性事实。
2. Day08 新增 verify 事实：
   - 固定顺序 8 个核心入口复跑全部通过；
   - build 事实入口 `npm.cmd run build` 通过；
   - Day08 三件套回填到可供下游 accept 消费的闭合状态。

## 7. 下一棒建议
1. 第一顺位：`accept-v5-milestone-gate`（消费 Day08 gate 输入包并做正式裁定）。
2. 若 accept 发现新增缺证或阻塞，再回退 `verify-v5-runtime-and-regression` 或按事实分流到对应实现 owner。
