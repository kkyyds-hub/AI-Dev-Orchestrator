# Day15：V5 E2E、回归与风险汇总

- 版本：`V5`
- Phase：`Phase 5`
- 模块：`模块E：优化验收与正式收口`
- 工作包：`v5-e2e-regression-and-risk-rollup`
- 当前状态：**已完成**
- owner skill：`verify-v5-runtime-and-regression`
- 文档定位：**验证记录 / Day15 实际执行记录（2026-04-17）**

---

## 验证环境

- 验证日期：`2026-04-17`
- 验证人 / owner skill：`verify-v5-runtime-and-regression`
- 工作区：`E:/new-AI-Dev-Orchestrator-push`
- shell：`powershell`
- Python：`Python 3.13`
- 前端构建入口：`npm.cmd run build`

## Day03 口径标注

- 证据等级：`E3`
- 风险分级：`R2`（1项）+ `R3`（2项）
- 验证结论类型：`部分通过（可交 Day16，建议 Partial 输入）`

## 实际验证记录

### 1) Phase 1 回归验证（Day08 稳定入口）

- `python runtime/orchestrator/scripts/v5_day06_step2_prompt_token_smoke.py`：`通过`
- `python runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`：`通过`
- `python runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`：`通过`
- `$env:PYTHONUTF8='1'; python runtime/orchestrator/scripts/v5_day07_minimal_continuous_regression_pack.py --mode runtime`：`通过`
- 关键事实：provider/prompt/token/role-policy/control-surface 回归链路未见回退。

### 2) Phase 2 验证（Day09/Day10 最小链路）

- 最终通过链路：
  - `GET /projects/{project_id}/memory/governance` -> `200`
  - `POST /projects/{project_id}/memory/governance/rehydrate` -> `200`
  - `POST /projects/{project_id}/memory/governance/compact`（`{"target_chars":900}`）-> `200`
  - `POST /projects/{project_id}/memory/governance/reset` -> `200`
- 关键事实：checkpoint/rehydrate/compact/governance state 均有实跑证据。

### 3) Phase 3 验证（Day11/Day12 最小链路）

- 最终通过链路：
  - `POST /workers/run-once?project_id={project_id}` -> `200`
  - `GET /agent-threads/projects/{project_id}/sessions` -> `200`（`sessions_count=1`）
  - `GET /agent-threads/projects/{project_id}/timeline` -> `200`（`total_messages=6`）
  - `GET /agent-threads/projects/{project_id}/interventions` -> `200`（`interventions_count=1`）
- 关键事实：Day12 缺失的独立联调证据已在本轮补齐（API 联调链路可复跑）。

### 4) Phase 4 验证（Day13/Day14 最小链路）

- 最终通过链路：
  - `GET /projects/{project_id}/team-control-center` -> `200`
  - `PUT /projects/{project_id}/team-control-center`（完整合同字段）-> `200`
  - `GET /projects/{project_id}/cost-dashboard` -> `200`
- 关键事实：
  - `dashboard_run_count=1`
  - `fallback_active=false`（仅代表当前样本为 provider_reported）
  - `mode_breakdown` 存在 provider_reported 聚合

### 5) 前端构建与结构阻塞判定

- `apps/web: npm.cmd run build`：`通过`
- `apps/web: npm run build`：`失败（ExecutionPolicy 阻塞，npm.ps1）`
- 结论：代码构建入口可用，`npm run build` 失败归类为环境策略观察项，不是前端代码失败。

## 失败与分类（必须保留）

1. `sqlite unable to open database file`（初次联调脚本）
- 分类：`环境路径/执行方式问题`
- 原因：脚本未使用 `with TestClient(...)` 触发生命周期初始化数据库。
- 处置：改为 `with TestClient(...)` 后链路通过。

2. `POST .../memory/governance/compact -> 422`（初次调用）
- 分类：`请求合同不匹配（非代码缺陷）`
- 原因：缺少请求体 `target_chars`。
- 处置：补 `{"target_chars":900}` 后通过（`200`）。

3. `PUT .../team-control-center -> 422`（初次调用）
- 分类：`请求合同不匹配（非代码缺陷）`
- 原因：缺 `team_name/team_mission/team_policy` 等必填字段。
- 处置：按 GET 回显合同完整回传后通过（`200`）。

4. `npm run build` -> PowerShell `npm.ps1` 阻塞
- 分类：`环境策略阻塞`
- 处置：`npm.cmd run build` 作为有效事实入口继续通过。

## 汇总清单（Day16 直接消费）

### 已验证通过项

- Day08 Phase1 固定回归入口 4 条全通过。
- Day09/Day10 governance 最小动作链路（rehydrate/compact/reset）通过。
- Day11/Day12 session/timeline/interventions + run-once 联调证据通过。
- Day13/Day14 team-control-center 保存回显 + cost-dashboard API 链路通过。
- `npm.cmd run build` 通过。

### 部分通过项

- fallback 路径仅沿用上游口径，未新增 `heuristic/missing` 实跑样本。

### 阻塞项

- 无 `R1` 阻塞项。

### 观察项

- `npm run build` 被 ExecutionPolicy 阻塞（环境策略）。
- Vite 产物 chunk 警告（>500kB），当前不阻塞 Day15。

### 仅沿用上游证据项

- Day14 fallback 解释边界（`fallback_active=false` 的解释口径）。
- Day13 runtime consumption boundary 说明（role model / budget policy 路径约束）。

### 缺证项

- 未构造 `heuristic/missing` 模式实跑样本，fallback 分支证据仍不完整。

## 结论

- Day15 verify 结论：`部分通过（E3, 可交 Day16）`
- Day16 输入建议：`Partial 输入消费，不可直接判 Pass`

## 2026-04-17 R2 fallback 最小补证附记

### 追加目的

- 记录 `R2 fallback` 缺证的后续最小补证索引。
- 保留 Day15 历史 verify 结论不变：`E3，可交 Day16，但建议 Partial 输入`。
- 为 Day16 后续 accept 消费提供可追溯附加证据。

### 追加证据

1. 总证据包
   - `runtime/orchestrator/tmp/day16-r2-fallback-evidence/day16_r2_fallback_evidence.json`
2. `heuristic` 日志样本
   - `runtime/orchestrator/tmp/day16-r2-fallback-evidence/runtime-data/logs/task-runs/6c909ded-36fc-4951-96ba-92ccaecdbf3f/fb385995-3b9c-40c8-9d7f-49680949a96d.jsonl`
3. `missing` 日志样本
   - `runtime/orchestrator/tmp/day16-r2-fallback-evidence/runtime-data/logs/task-runs/07b98720-9385-4861-8fd4-73c88e81e161/25dedf94-f198-4a9c-bc1e-615ed8dc0e5b.jsonl`

### 追加事实

- `heuristic`
  - `run_id=fb385995-3b9c-40c8-9d7f-49680949a96d`
  - `token_accounting_ready.data.accounting_mode=heuristic`
  - `cost-dashboard.fallback_contract.heuristic_run_count=1`
  - `cost-dashboard.fallback_contract.fallback_active=true`
- `missing`
  - `run_id=25dedf94-f198-4a9c-bc1e-615ed8dc0e5b`
  - 通过对已落盘 run 做 `token_accounting_mode=null` replay，补齐 `missing` 分支消费事实
  - `mode_breakdown[0].mode=missing`
  - `fallback_contract.missing_mode_run_count=1`
  - `fallback_contract.fallback_active=true`

### 对 Day15 历史验证结论的影响

- Day15 历史 verify 结论仍保持：`部分通过（E3，可交 Day16）`。
- 该附记只说明 `heuristic/missing` 的后续补证已具备可追溯证据。
- 后续 accept 若消费这些附加证据，应在 Day15/Day16 对应 accept 文档中声明正式结论更新。

## 2026-04-18 当前工作树复验追加记录

### 追加环境

- 验证日期：`2026-04-18`
- Python：`runtime/orchestrator/.venv -> Python 3.11.9`
- 工作区：`E:/new-AI-Dev-Orchestrator-push`
- 隔离证据根目录：`runtime/orchestrator/tmp/day15-verify-20260418/`
- 当前复验边界：`只补 verify 事实，不做 accept 裁定`

### 追加执行命令

1. Phase1 复跑：
   - `runtime/orchestrator/.venv/Scripts/python.exe runtime/orchestrator/scripts/v5_day06_step2_prompt_token_smoke.py`
   - `runtime/orchestrator/.venv/Scripts/python.exe runtime/orchestrator/scripts/v5_day06_step6_run_persistence_smoke.py`
   - `runtime/orchestrator/.venv/Scripts/python.exe runtime/orchestrator/scripts/v5_day06_step10_receipt_semantics_smoke.py`
   - `$env:PYTHONUTF8='1'; runtime/orchestrator/.venv/Scripts/python.exe runtime/orchestrator/scripts/v5_day07_minimal_continuous_regression_pack.py --mode runtime`
2. Day09~Day14 当前工作树 API 复验：
   - 使用 `FastAPI TestClient` + 隔离 `RUNTIME_DATA_DIR=runtime/orchestrator/tmp/day15-verify-20260418/runtime-data`
   - 生成证据包：`runtime/orchestrator/tmp/day15-verify-20260418/day15_verify_evidence.json`
3. 前端构建复核：
   - `apps/web: npm run build`
   - `apps/web: npm.cmd run build`

### 追加结果事实

#### A. 当前工作树 API 闭环

- 创建项目：
  - `project_id=b758cd31-4162-4288-b6f7-98dca1dfdfbe`
- Team control center：
  - `GET /projects/{project_id}/team-control-center` -> `200`
  - `PUT /projects/{project_id}/team-control-center` -> `200`
  - `day14_prerequisites.team_size=2`
- Worker 三次 claim 结果：
  - `provider sample A` -> `provider_mock` / `provider_reported` / `succeeded`
  - `provider sample B` -> `provider_mock` / `provider_reported` / `succeeded`
  - `heuristic failure sample` -> `simulate` / `heuristic` / `failed`
- Memory governance：
  - `GET /projects/{project_id}/memory/governance` -> `200`
  - `rehydrate` -> `200`
  - `compact` -> `200`
  - `reset` -> `200`
  - `checkpoint_id=ckpt-7355da8c31994b23a76b17e40635e9c7`
- Agent threads：
  - `sessions=3`
  - `timeline.total_messages=20`
  - `interventions.total_items=5`
- Cost dashboard：
  - 初始 `run_count=3`
  - `fallback_contract.provider_reported_run_count=2`
  - `fallback_contract.heuristic_run_count=1`
  - `fallback_contract.fallback_active=true`
- Missing replay 后：
  - `fallback_contract.missing_mode_run_count=1`
  - `mode_breakdown` 同时出现 `provider_reported / heuristic / missing`
- Day15 repository flow：
  - `GET /projects/{project_id}/day15-repository-flow` -> `200`
  - `overall_status=blocked`
  - `blocked_step_count=2`
  - 该结果对应 fresh project 缺少仓库闭环前置条件，不是接口失败

#### B. 当前工作树证据路径

- 总证据包：
  - `runtime/orchestrator/tmp/day15-verify-20260418/day15_verify_evidence.json`
- 三条 run 日志：
  - `runtime/orchestrator/tmp/day15-verify-20260418/runtime-data/logs/task-runs/eac86c49-2628-4288-9633-e29fdd89dfea/7f57df06-e847-4f85-998d-11502970eb08.jsonl`
  - `runtime/orchestrator/tmp/day15-verify-20260418/runtime-data/logs/task-runs/aee4f86b-85a0-49bc-b153-781af35ccbf1/51d00f26-a87a-4078-9d37-c307f981cbcc.jsonl`
  - `runtime/orchestrator/tmp/day15-verify-20260418/runtime-data/logs/task-runs/0bc4de52-589c-40aa-bc9d-9677fc2d59fa/0bbcc138-0030-4988-b0a2-14efbc26d0dc.jsonl`

#### C. 当前工作树构建事实

- `apps/web: npm.cmd run build`：`通过`
- `apps/web: npm run build`：`失败（PowerShell ExecutionPolicy / npm.ps1）`
- Vite 仍有 `>500kB chunk` 警告，但不构成 Day15 verify 阻塞

### 追加结论

- 当前工作树下，Day15 verify 所需的 **Phase1 回归 + Day09~Day14 API 闭环 + fallback_contract 当前消费事实 + 前端构建入口** 已重新取证。
- 当前仍不应越权下 `accept Pass` 结论。
- `missing` 分支证据本轮仍是 **隔离 replay 消费事实**，不是自然 worker 主链自动产出事实。
