# V5 总纲

- 项目：`AI-Dev-Orchestrator`
- 版本：`V5`
- 更新时间：`2026-04-07`
- 当前状态：**进行中**
- 文档类型：**正式冻结基线 / Day01-Day05 已回填**
- 母本绑定：`C:/Users/Administrator/Desktop/三省六部文件/AI-Dev-Orchestrator-V5-Plan.md`

---

## 当前冻结边界

1. V5 正式冻结文档仍只落在 `docs/01-版本冻结计划/V5/`。
2. 当前已完成的是 **Day01：基线与现实核对冻结**、**Day02：工作包目录与合同冻结**、**Day03：验证验收与风险口径冻结**、**Day04：正式发布与 Day05 启动包冻结**、**Day05：Provider 抽象层与执行路由后端主链**；Day06-Day16 仍未执行。
3. Day01-Day04 完成的是文档冻结、现实对齐、合同固化、统一口径与正式入口发布；Day05 完成的是工作包级实现、最小验证与正式裁定闭合，但**不代表 Phase 1 完成**。
4. Day05 之后所有工作包必须继续以 Day01 基线、Day02 合同、Day03 统一口径、Day04 正式入口与仓库现实为上限，不得重新发明范围或把母本愿景直接写成已存在能力。

## Day01-Day05 冻结 / 裁定结论

1. Day01 已确认 V5 五条主线仍成立：真实 Provider / 模型执行层、记忆进入执行主链、Prompt 产品化、token / 成本治理、多 Agent 协作与老板控制面。
2. Day02 已把 `Phase / 模块 / Day / owner skill / 主要改动面 / 完成定义 / 非完成定义 / 最低验证 / 交接路线` 固定到 `00-V5正式冻结执行计划.md` 与对应 Day README。
3. Day03 已把 Day05-Day16 共用的状态回填规则、证据口径、风险分级与 Day08 / Day15 / Day16 输入边界冻结到正式文档。
4. Day04 已把 V5 正式入口、模块入口、逐日执行入口与 Day05 启动顺序冻结到正式文档。
5. Day05 已完成工作包级 Pass 裁定：完成范围是 provider routing 主链接线、mock provider 最小执行证据、fallback 回归与 Day06 契约交接；未把真实 provider / usage / token / receipt 闭环写成已完成。
6. 自 Day06 起，后续线程应直接以对应 Day README、执行说明、Day03 统一口径、Day04 启动包、Day05 交接与执行计划合同表接手，不再反复解释口径。

## V5 范围内

1. Provider 抽象、执行路由与真实模型调用主链的正式冻结与后续实现。
2. Prompt Registry、Prompt 版本治理、Token Accounting / 成本治理的正式冻结与后续实现。
3. Project Memory Recall 默认进入 Worker 主链，以及后续记忆治理、防爆仓与上下文压缩能力。
4. 多 Agent 协作线程、review / rework、boss intervention、team assembly 与 control center。
5. V5 的端到端验证、风险汇总、Pass / Partial / Blocked 裁定与正式收口。

## V5 明确范围外

1. Day01-Day04 不做任何业务代码、测试代码、脚本、配置修改。
2. Day01-Day04 不把 `strategy_engine_service.py` 里的模型选择预览写成“已经具备真实 LLM Provider 执行”。
3. Day01-Day04 不把现有记忆检索、成本估算、项目总览页面写成“多 Agent 主链、精确 token 记账、老板控制中心已经完成”。
4. Day01-Day04 不扩写母本之外的新版本主题，也不提前进入 Day05 真实实现、Day08 verify 或 Day16 裁定。

## 当前现实差距

1. Day05 已让 `strategy_engine_service.py` 的模型选择接入 `executor_service.py` 与 `task_worker.py`，并形成 `provider_mock` 最小执行路径；但真实 Provider adapter / registry / usage / token / receipt 闭环仍未落地。
2. `runtime/orchestrator/app/services/project_memory_service.py` 与 `context_builder_service.py` 已有任务记忆召回能力，但 `runtime/orchestrator/app/workers/task_worker.py` 当前调用 `build_context_package(task=task)`，没有默认打开 `include_project_memory=True`，记忆尚未并入默认执行主链。
3. `runtime/orchestrator/app/services/cost_estimator_service.py` 仍使用“字符数约等于 token + 固定单价”的启发式估算，不是 Provider 真实回传账单。
4. `runtime/orchestrator/app/services/` 当前未见 `provider`、`prompt`、`token`、`agent`、`team`、`cache` 命名的现成服务文件；`apps/web/src/features/` 当前未见 `agents`、`agent-teams`、`memory-governance`、`costs` 等对应目录。
5. 因此 Day06-Day16 里凡是以“计划新增”标注的目录 / 文件 / 页面，都仍应按新增合同推进，不能按已存在能力回填。

## 当前状态词依据

- V5 总体状态写为 `进行中`：因为只完成了 Day01-Day05，Day06-Day16 尚未执行，且尚未完成 Phase 级闭合。
- Day01 状态写为 `已完成`：因为基线、现实差距、状态回填与交接材料已回填。
- Day02 状态写为 `已完成`：因为工作包目录、owner skill、主要改动面、完成定义、非完成定义、最低验证与交接路线已正式冻结。
- Day03 状态写为 `已完成`：因为最低验证矩阵、证据口径、状态回填规则、风险分级与验收输入边界已正式冻结并回填。
- Day04 状态写为 `已完成`：因为正式入口、模块入口、逐日入口与 Day05 启动包已正式冻结并回填。
- Day05 状态写为 `已完成`：因为 Day05 合同范围内的主链接线、mock provider 证据、fallback 回归与 Day06 契约交接已具备，并已完成正式裁定。
- Day06-Day16 仍写为 `已规划`：因为本轮只完成 Day05 裁定与收口，不进入 Day06 及后续线程。

## 放行检查入口

- 文档层正式放行结论见：`docs/01-版本冻结计划/V5/00-V5-16天总规划正式放行检查.md`
- 该结论只约束规划文档是否可放行，不代表 V5 已实现完成。

